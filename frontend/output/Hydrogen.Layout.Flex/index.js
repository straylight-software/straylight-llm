// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Semiring from "../Data.Semiring/index.js";
var append = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var add1 = /* #__PURE__ */ Data_Semiring.add(Data_Semiring.semiringNumber);
var JustifyStart = /* #__PURE__ */ (function () {
    function JustifyStart() {

    };
    JustifyStart.value = new JustifyStart();
    return JustifyStart;
})();
var JustifyCenter = /* #__PURE__ */ (function () {
    function JustifyCenter() {

    };
    JustifyCenter.value = new JustifyCenter();
    return JustifyCenter;
})();
var JustifyEnd = /* #__PURE__ */ (function () {
    function JustifyEnd() {

    };
    JustifyEnd.value = new JustifyEnd();
    return JustifyEnd;
})();
var JustifySpaceBetween = /* #__PURE__ */ (function () {
    function JustifySpaceBetween() {

    };
    JustifySpaceBetween.value = new JustifySpaceBetween();
    return JustifySpaceBetween;
})();
var JustifySpaceAround = /* #__PURE__ */ (function () {
    function JustifySpaceAround() {

    };
    JustifySpaceAround.value = new JustifySpaceAround();
    return JustifySpaceAround;
})();
var JustifySpaceEvenly = /* #__PURE__ */ (function () {
    function JustifySpaceEvenly() {

    };
    JustifySpaceEvenly.value = new JustifySpaceEvenly();
    return JustifySpaceEvenly;
})();
var Row = /* #__PURE__ */ (function () {
    function Row() {

    };
    Row.value = new Row();
    return Row;
})();
var RowReverse = /* #__PURE__ */ (function () {
    function RowReverse() {

    };
    RowReverse.value = new RowReverse();
    return RowReverse;
})();
var Column = /* #__PURE__ */ (function () {
    function Column() {

    };
    Column.value = new Column();
    return Column;
})();
var ColumnReverse = /* #__PURE__ */ (function () {
    function ColumnReverse() {

    };
    ColumnReverse.value = new ColumnReverse();
    return ColumnReverse;
})();
var AlignStart = /* #__PURE__ */ (function () {
    function AlignStart() {

    };
    AlignStart.value = new AlignStart();
    return AlignStart;
})();
var AlignCenter = /* #__PURE__ */ (function () {
    function AlignCenter() {

    };
    AlignCenter.value = new AlignCenter();
    return AlignCenter;
})();
var AlignEnd = /* #__PURE__ */ (function () {
    function AlignEnd() {

    };
    AlignEnd.value = new AlignEnd();
    return AlignEnd;
})();
var AlignStretch = /* #__PURE__ */ (function () {
    function AlignStretch() {

    };
    AlignStretch.value = new AlignStretch();
    return AlignStretch;
})();
var zipWithIndexGo = function ($copy_f) {
    return function ($copy_arr) {
        return function ($copy_i) {
            return function ($copy_acc) {
                var $tco_var_f = $copy_f;
                var $tco_var_arr = $copy_arr;
                var $tco_var_i = $copy_i;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(f, arr, i, acc) {
                    var v = Data_Array.take(1)(arr);
                    if (v.length === 0) {
                        $tco_done = true;
                        return acc;
                    };
                    if (v.length === 1) {
                        $tco_var_f = f;
                        $tco_var_arr = Data_Array.drop(1)(arr);
                        $tco_var_i = i + 1 | 0;
                        $copy_acc = append(acc)([ f(i)(v[0]) ]);
                        return;
                    };
                    $tco_done = true;
                    return acc;
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_f, $tco_var_arr, $tco_var_i, $copy_acc);
                };
                return $tco_result;
            };
        };
    };
};
var zipWithIndex = function (f) {
    return function (arr) {
        return zipWithIndexGo(f)(arr)(0)([  ]);
    };
};
var toNumberGo = function ($copy_i) {
    return function ($copy_acc) {
        var $tco_var_i = $copy_i;
        var $tco_done = false;
        var $tco_result;
        function $tco_loop(i, acc) {
            if (i <= 0) {
                $tco_done = true;
                return acc;
            };
            if (Data_Boolean.otherwise) {
                $tco_var_i = i - 1 | 0;
                $copy_acc = acc + 1.0;
                return;
            };
            throw new Error("Failed pattern match at Hydrogen.Layout.Flex (line 379, column 1 - line 379, column 38): " + [ i.constructor.name, acc.constructor.name ]);
        };
        while (!$tco_done) {
            $tco_result = $tco_loop($tco_var_i, $copy_acc);
        };
        return $tco_result;
    };
};
var toNumber = function (i) {
    return toNumberGo(i)(0.0);
};
var scanlGo = function ($copy_f) {
    return function ($copy_acc) {
        return function ($copy_arr) {
            return function ($copy_result) {
                var $tco_var_f = $copy_f;
                var $tco_var_acc = $copy_acc;
                var $tco_var_arr = $copy_arr;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(f, acc, arr, result) {
                    var v = Data_Array.take(1)(arr);
                    if (v.length === 0) {
                        $tco_done = true;
                        return result;
                    };
                    if (v.length === 1) {
                        $tco_var_f = f;
                        $tco_var_acc = f(acc)(v[0]);
                        $tco_var_arr = Data_Array.drop(1)(arr);
                        $copy_result = append(result)([ acc ]);
                        return;
                    };
                    $tco_done = true;
                    return result;
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_f, $tco_var_acc, $tco_var_arr, $copy_result);
                };
                return $tco_result;
            };
        };
    };
};
var scanl = function (f) {
    return function (init) {
        return function (arr) {
            return scanlGo(f)(init)(arr)([  ]);
        };
    };
};
var maxInt = function (a) {
    return function (b) {
        var $31 = a > b;
        if ($31) {
            return a;
        };
        return b;
    };
};
var isRow = function (v) {
    if (v instanceof Row) {
        return true;
    };
    if (v instanceof RowReverse) {
        return true;
    };
    return false;
};
var mainAxisSize = function (c) {
    var $33 = isRow(c.direction);
    if ($33) {
        return c.width;
    };
    return c.height;
};
var isReversed = function (v) {
    if (v instanceof RowReverse) {
        return true;
    };
    if (v instanceof ColumnReverse) {
        return true;
    };
    return false;
};
var getAt = function (i) {
    return function (arr) {
        var v = Data_Array.take(1)(Data_Array.drop(i)(arr));
        if (v.length === 1) {
            return v[0];
        };
        return 0.0;
    };
};
var foldlArray = function ($copy_f) {
    return function ($copy_init) {
        return function ($copy_arr) {
            var $tco_var_f = $copy_f;
            var $tco_var_init = $copy_init;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(f, init, arr) {
                var v = Data_Array.take(1)(arr);
                if (v.length === 0) {
                    $tco_done = true;
                    return init;
                };
                if (v.length === 1) {
                    $tco_var_f = f;
                    $tco_var_init = f(init)(v[0]);
                    $copy_arr = Data_Array.drop(1)(arr);
                    return;
                };
                $tco_done = true;
                return init;
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_f, $tco_var_init, $copy_arr);
            };
            return $tco_result;
        };
    };
};
var mapArray = function (f) {
    return foldlArray(function (acc) {
        return function (x) {
            return append(acc)([ f(x) ]);
        };
    })([  ]);
};
var sumArray = /* #__PURE__ */ foldlArray(add1)(0.0);
var flexItem = function (basis) {
    return function (size) {
        return {
            minWidth: 0.0,
            minHeight: 0.0,
            maxWidth: 99999.0,
            maxHeight: 99999.0,
            basis: basis,
            grow: 0.0,
            shrink: 1.0,
            alignSelf: AlignStretch.value
        };
    };
};
var flexContainer = function (width) {
    return function (height) {
        return {
            width: width,
            height: height,
            direction: Row.value,
            justify: JustifyStart.value,
            align: AlignStretch.value,
            gap: 0.0,
            padding: 0.0
        };
    };
};
var eqFlexJustify = {
    eq: function (x) {
        return function (y) {
            if (x instanceof JustifyStart && y instanceof JustifyStart) {
                return true;
            };
            if (x instanceof JustifyCenter && y instanceof JustifyCenter) {
                return true;
            };
            if (x instanceof JustifyEnd && y instanceof JustifyEnd) {
                return true;
            };
            if (x instanceof JustifySpaceBetween && y instanceof JustifySpaceBetween) {
                return true;
            };
            if (x instanceof JustifySpaceAround && y instanceof JustifySpaceAround) {
                return true;
            };
            if (x instanceof JustifySpaceEvenly && y instanceof JustifySpaceEvenly) {
                return true;
            };
            return false;
        };
    }
};
var eqFlexDirection = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Row && y instanceof Row) {
                return true;
            };
            if (x instanceof RowReverse && y instanceof RowReverse) {
                return true;
            };
            if (x instanceof Column && y instanceof Column) {
                return true;
            };
            if (x instanceof ColumnReverse && y instanceof ColumnReverse) {
                return true;
            };
            return false;
        };
    }
};
var eqFlexAlign = {
    eq: function (x) {
        return function (y) {
            if (x instanceof AlignStart && y instanceof AlignStart) {
                return true;
            };
            if (x instanceof AlignCenter && y instanceof AlignCenter) {
                return true;
            };
            if (x instanceof AlignEnd && y instanceof AlignEnd) {
                return true;
            };
            if (x instanceof AlignStretch && y instanceof AlignStretch) {
                return true;
            };
            return false;
        };
    }
};
var crossAxisSize = function (c) {
    var $45 = isRow(c.direction);
    if ($45) {
        return c.height;
    };
    return c.width;
};
var computeStartPositions = function (gap) {
    return function (sizes) {
        return scanl(function (pos) {
            return function (size) {
                return pos + size + gap;
            };
        })(0.0)(sizes);
    };
};
var computeSpaceEvenlyPositions = function (available) {
    return function (sizes) {
        var totalSize = sumArray(sizes);
        var n = Data_Array.length(sizes);
        var gap = (available - totalSize) / toNumber(n + 1 | 0);
        return mapArray(function (p) {
            return p + gap;
        })(computeStartPositions(gap)(sizes));
    };
};
var computeSpaceBetweenPositions = function (available) {
    return function (sizes) {
        var totalSize = sumArray(sizes);
        var n = Data_Array.length(sizes);
        var gap = (function () {
            var $46 = n > 1;
            if ($46) {
                return (available - totalSize) / toNumber(n - 1 | 0);
            };
            return 0.0;
        })();
        return computeStartPositions(gap)(sizes);
    };
};
var computeSpaceAroundPositions = function (available) {
    return function (sizes) {
        var totalSize = sumArray(sizes);
        var n = Data_Array.length(sizes);
        var gap = (available - totalSize) / toNumber(n * 2 | 0);
        return mapArray(function (p) {
            return p + gap;
        })(computeStartPositions(gap * 2.0)(sizes));
    };
};
var computeMainSizes = function (_dir) {
    return function (available) {
        return function (items) {
            var totalGrow = sumArray(mapArray(function (i) {
                return i.grow;
            })(items));
            var totalBasis = sumArray(mapArray(function (i) {
                return i.basis;
            })(items));
            var remaining = available - totalBasis;
            var n = Data_Array.length(items);
            var $47 = remaining > 0.0 && totalGrow > 0.0;
            if ($47) {
                return mapArray(function (i) {
                    return i.basis + (remaining * i.grow) / totalGrow;
                })(items);
            };
            return mapArray(function (i) {
                return i.basis;
            })(items);
        };
    };
};
var computeEndPositions = function (offset) {
    return function (gap) {
        return function (sizes) {
            return mapArray(function (p) {
                return p + offset;
            })(computeStartPositions(gap)(sizes));
        };
    };
};
var computeCrossPosition = function (align) {
    return function (available) {
        return function (itemSize) {
            if (align instanceof AlignStart) {
                return 0.0;
            };
            if (align instanceof AlignCenter) {
                return (available - itemSize) / 2.0;
            };
            if (align instanceof AlignEnd) {
                return available - itemSize;
            };
            if (align instanceof AlignStretch) {
                return 0.0;
            };
            throw new Error("Failed pattern match at Hydrogen.Layout.Flex (line 317, column 49 - line 321, column 22): " + [ align.constructor.name ]);
        };
    };
};
var computeCenterPositions = function (offset) {
    return function (gap) {
        return function (sizes) {
            return mapArray(function (p) {
                return p + offset;
            })(computeStartPositions(gap)(sizes));
        };
    };
};
var computePositions = function (container) {
    return function (sizes) {
        var totalSize = sumArray(sizes);
        var n = Data_Array.length(sizes);
        var totalGap = container.gap * toNumber(maxInt(0)(n - 1 | 0));
        var used = totalSize + totalGap;
        var available = mainAxisSize(container) - container.padding * 2.0;
        var freeSpace = available - used;
        if (container.justify instanceof JustifyStart) {
            return computeStartPositions(container.gap)(sizes);
        };
        if (container.justify instanceof JustifyEnd) {
            return computeEndPositions(freeSpace)(container.gap)(sizes);
        };
        if (container.justify instanceof JustifyCenter) {
            return computeCenterPositions(freeSpace / 2.0)(container.gap)(sizes);
        };
        if (container.justify instanceof JustifySpaceBetween) {
            return computeSpaceBetweenPositions(available)(sizes);
        };
        if (container.justify instanceof JustifySpaceAround) {
            return computeSpaceAroundPositions(available)(sizes);
        };
        if (container.justify instanceof JustifySpaceEvenly) {
            return computeSpaceEvenlyPositions(available)(sizes);
        };
        throw new Error("Failed pattern match at Hydrogen.Layout.Flex (line 257, column 5 - line 263, column 72): " + [ container.justify.constructor.name ]);
    };
};
var clamp = function (minVal) {
    return function (maxVal) {
        return function (val) {
            if (val < minVal) {
                return minVal;
            };
            if (val > maxVal) {
                return maxVal;
            };
            if (Data_Boolean.otherwise) {
                return val;
            };
            throw new Error("Failed pattern match at Hydrogen.Layout.Flex (line 369, column 1 - line 369, column 46): " + [ minVal.constructor.name, maxVal.constructor.name, val.constructor.name ]);
        };
    };
};
var computeCrossSize = function (align) {
    return function (available) {
        return function (item) {
            if (align instanceof AlignStretch) {
                return clamp(item.minHeight)(item.maxHeight)(available);
            };
            return item.basis;
        };
    };
};
var computeLayout = function (container) {
    return function (items) {
        var n = Data_Array.length(items);
        var totalGap = container.gap * toNumber(maxInt(0)(n - 1 | 0));
        var availableMain = mainAxisSize(container) - container.padding * 2.0;
        var spaceForItems = availableMain - totalGap;
        var mainSizes = computeMainSizes(container.direction)(spaceForItems)(items);
        var positions = computePositions(container)(mainSizes);
        var availableCross = crossAxisSize(container) - container.padding * 2.0;
        return zipWithIndex(function (i) {
            return function (item) {
                var pos = getAt(i)(positions);
                var mainSize = getAt(i)(mainSizes);
                var crossSize = computeCrossSize(container.align)(availableCross)(item);
                var $54 = isRow(container.direction);
                if ($54) {
                    return {
                        x: pos + container.padding,
                        y: computeCrossPosition(container.align)(availableCross)(crossSize) + container.padding,
                        width: mainSize,
                        height: crossSize
                    };
                };
                return {
                    x: computeCrossPosition(container.align)(availableCross)(crossSize) + container.padding,
                    y: pos + container.padding,
                    width: crossSize,
                    height: mainSize
                };
            };
        })(items);
    };
};
export {
    flexContainer,
    flexItem,
    Row,
    RowReverse,
    Column,
    ColumnReverse,
    AlignStart,
    AlignCenter,
    AlignEnd,
    AlignStretch,
    JustifyStart,
    JustifyCenter,
    JustifyEnd,
    JustifySpaceBetween,
    JustifySpaceAround,
    JustifySpaceEvenly,
    computeLayout,
    eqFlexDirection,
    eqFlexAlign,
    eqFlexJustify
};
//# sourceMappingURL=index.js.map
