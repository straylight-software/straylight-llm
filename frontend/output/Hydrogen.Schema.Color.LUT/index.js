// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Bounded from "../Hydrogen.Schema.Bounded/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var div1 = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var eq = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Eq.eqArray(Data_Eq.eqNumber));
var eq1 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Data_Eq.eqArray(Data_Eq.eqString));
var Size1K = /* #__PURE__ */ (function () {
    function Size1K() {

    };
    Size1K.value = new Size1K();
    return Size1K;
})();
var Size4K = /* #__PURE__ */ (function () {
    function Size4K() {

    };
    Size4K.value = new Size4K();
    return Size4K;
})();
var Size16K = /* #__PURE__ */ (function () {
    function Size16K() {

    };
    Size16K.value = new Size16K();
    return Size16K;
})();
var Size65K = /* #__PURE__ */ (function () {
    function Size65K() {

    };
    Size65K.value = new Size65K();
    return Size65K;
})();
var CubeFormat = /* #__PURE__ */ (function () {
    function CubeFormat() {

    };
    CubeFormat.value = new CubeFormat();
    return CubeFormat;
})();
var ThreeDLFormat = /* #__PURE__ */ (function () {
    function ThreeDLFormat() {

    };
    ThreeDLFormat.value = new ThreeDLFormat();
    return ThreeDLFormat;
})();
var CLFFormat = /* #__PURE__ */ (function () {
    function CLFFormat() {

    };
    CLFFormat.value = new CLFFormat();
    return CLFFormat;
})();
var LOOKFormat = /* #__PURE__ */ (function () {
    function LOOKFormat() {

    };
    LOOKFormat.value = new LOOKFormat();
    return LOOKFormat;
})();
var CTLFormat = /* #__PURE__ */ (function () {
    function CTLFormat() {

    };
    CTLFormat.value = new CTLFormat();
    return CTLFormat;
})();
var UnknownFormat = /* #__PURE__ */ (function () {
    function UnknownFormat() {

    };
    UnknownFormat.value = new UnknownFormat();
    return UnknownFormat;
})();
var Cube17 = /* #__PURE__ */ (function () {
    function Cube17() {

    };
    Cube17.value = new Cube17();
    return Cube17;
})();
var Cube33 = /* #__PURE__ */ (function () {
    function Cube33() {

    };
    Cube33.value = new Cube33();
    return Cube33;
})();
var Cube65 = /* #__PURE__ */ (function () {
    function Cube65() {

    };
    Cube65.value = new Cube65();
    return Cube65;
})();
var LUT3D = function (x) {
    return x;
};
var LUT1D = function (x) {
    return x;
};
var showLUTSize = {
    show: function (v) {
        if (v instanceof Size1K) {
            return "1024";
        };
        if (v instanceof Size4K) {
            return "4096";
        };
        if (v instanceof Size16K) {
            return "16384";
        };
        if (v instanceof Size65K) {
            return "65536";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 91, column 1 - line 95, column 25): " + [ v.constructor.name ]);
    }
};
var showLUTFormat = {
    show: function (v) {
        if (v instanceof CubeFormat) {
            return ".cube";
        };
        if (v instanceof ThreeDLFormat) {
            return ".3dl";
        };
        if (v instanceof CLFFormat) {
            return ".clf";
        };
        if (v instanceof LOOKFormat) {
            return ".look";
        };
        if (v instanceof CTLFormat) {
            return ".ctl";
        };
        if (v instanceof UnknownFormat) {
            return "unknown";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 407, column 1 - line 413, column 33): " + [ v.constructor.name ]);
    }
};
var showLUT3DSize = {
    show: function (v) {
        if (v instanceof Cube17) {
            return "17x17x17";
        };
        if (v instanceof Cube33) {
            return "33x33x33";
        };
        if (v instanceof Cube65) {
            return "65x65x65";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 112, column 1 - line 115, column 27): " + [ v.constructor.name ]);
    }
};
var showLUT3D = {
    show: function (v) {
        return "LUT3D[" + (v.metadata.title + (", " + (show(v.size) + ("x" + (show(v.size) + ("x" + (show(v.size) + "]")))))));
    }
};
var showLUT1D = {
    show: function (v) {
        return "LUT1D[" + (v.metadata.title + (", " + (show(Data_Array.length(v.red)) + " entries]")));
    }
};
var lutSizeToInt = function (v) {
    if (v instanceof Size1K) {
        return 1024;
    };
    if (v instanceof Size4K) {
        return 4096;
    };
    if (v instanceof Size16K) {
        return 16384;
    };
    if (v instanceof Size65K) {
        return 65536;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 98, column 1 - line 98, column 31): " + [ v.constructor.name ]);
};
var lutMetadata = function (title) {
    return function (inputCS) {
        return function (outputCS) {
            return {
                title: title,
                comments: [  ],
                inputColorSpace: inputCS,
                outputColorSpace: outputCS,
                inputMin: 0.0,
                inputMax: 1.0,
                outputMin: 0.0,
                outputMax: 1.0
            };
        };
    };
};
var lut3DSizeToInt = function (v) {
    if (v instanceof Cube17) {
        return 17;
    };
    if (v instanceof Cube33) {
        return 33;
    };
    if (v instanceof Cube65) {
        return 65;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 118, column 1 - line 118, column 35): " + [ v.constructor.name ]);
};
var lut3DSize = function (v) {
    return v.size;
};
var lut1DSize = function (v) {
    return Data_Array.length(v.red);
};
var lerp = function (a) {
    return function (b) {
        return function (t) {
            return a + (b - a) * t;
        };
    };
};
var lerpRGB = function (a) {
    return function (b) {
        return function (t) {
            return {
                r: lerp(a.r)(b.r)(t),
                g: lerp(a.g)(b.g)(t),
                b: lerp(a.b)(b.b)(t)
            };
        };
    };
};
var invertLUT1D = function (v) {
    var invertChannel = function (arr) {
        return Data_Array.reverse(arr);
    };
    return {
        red: invertChannel(v.red),
        green: invertChannel(v.green),
        blue: invertChannel(v.blue),
        metadata: {
            comments: v.metadata.comments,
            inputColorSpace: v.metadata.inputColorSpace,
            outputColorSpace: v.metadata.outputColorSpace,
            inputMin: v.metadata.inputMin,
            inputMax: v.metadata.inputMax,
            outputMin: v.metadata.outputMin,
            outputMax: v.metadata.outputMax,
            title: v.metadata.title + " (Inverted)"
        }
    };
};
var getVoxel = function (n) {
    return function (lutData) {
        return function (ri) {
            return function (gi) {
                return function (bi) {
                    var idx = ((((bi * n | 0) * n | 0) + (gi * n | 0) | 0) + ri | 0) * 3 | 0;
                    return {
                        r: Data_Maybe.fromMaybe(0.0)(Data_Array.index(lutData)(idx)),
                        g: Data_Maybe.fromMaybe(0.0)(Data_Array.index(lutData)(idx + 1 | 0)),
                        b: Data_Maybe.fromMaybe(0.0)(Data_Array.index(lutData)(idx + 2 | 0))
                    };
                };
            };
        };
    };
};
var generateRamp = function (n) {
    return Data_Array.mapWithIndex(function (i) {
        return function (v) {
            return Data_Int.toNumber(i) / Data_Int.toNumber(n - 1 | 0);
        };
    })(Data_Array.replicate(n)(0.0));
};
var generateIdentityCube = function (n) {
    var mod = function (a) {
        return function (b) {
            return a - (div1(a)(b) * b | 0) | 0;
        };
    };
    var foldlArrayGo = function ($copy_f) {
        return function ($copy_acc) {
            return function ($copy_arr) {
                return function ($copy_idx) {
                    var $tco_var_f = $copy_f;
                    var $tco_var_acc = $copy_acc;
                    var $tco_var_arr = $copy_arr;
                    var $tco_done = false;
                    var $tco_result;
                    function $tco_loop(f, acc, arr, idx) {
                        var v = Data_Array.index(arr)(idx);
                        if (v instanceof Data_Maybe.Nothing) {
                            $tco_done = true;
                            return acc;
                        };
                        if (v instanceof Data_Maybe.Just) {
                            $tco_var_f = f;
                            $tco_var_acc = f(acc)(v.value0);
                            $tco_var_arr = arr;
                            $copy_idx = idx + 1 | 0;
                            return;
                        };
                        throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 465, column 32 - line 467, column 57): " + [ v.constructor.name ]);
                    };
                    while (!$tco_done) {
                        $tco_result = $tco_loop($tco_var_f, $tco_var_acc, $tco_var_arr, $copy_idx);
                    };
                    return $tco_result;
                };
            };
        };
    };
    var foldlArray = function (f) {
        return function (init) {
            return function (arr) {
                var v = Data_Array.index(arr)(0);
                if (v instanceof Data_Maybe.Nothing) {
                    return init;
                };
                if (v instanceof Data_Maybe.Just) {
                    return foldlArrayGo(f)(f(init)(v.value0))(arr)(1);
                };
                throw new Error("Failed pattern match at Hydrogen.Schema.Color.LUT (line 462, column 27 - line 464, column 54): " + [ v.constructor.name ]);
            };
        };
    };
    var join = function (arrs) {
        return foldlArray(function (acc) {
            return function (arr) {
                return append1(acc)(arr);
            };
        })([  ])(arrs);
    };
    var maxIdx = Data_Int.toNumber(n - 1 | 0);
    var indices = Data_Array.mapWithIndex(function (i) {
        return function (v) {
            return i;
        };
    })(Data_Array.replicate((n * n | 0) * n | 0)(0));
    var generateEntry = function (idx) {
        var r = mod(idx)(n);
        var g = mod(div1(idx)(n))(n);
        var b = div1(idx)(n * n | 0);
        return [ Data_Int.toNumber(r) / maxIdx, Data_Int.toNumber(g) / maxIdx, Data_Int.toNumber(b) / maxIdx ];
    };
    return join(map(generateEntry)(indices));
};
var eqLUTSize = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Size1K && y instanceof Size1K) {
                return true;
            };
            if (x instanceof Size4K && y instanceof Size4K) {
                return true;
            };
            if (x instanceof Size16K && y instanceof Size16K) {
                return true;
            };
            if (x instanceof Size65K && y instanceof Size65K) {
                return true;
            };
            return false;
        };
    }
};
var eqLUTFormat = {
    eq: function (x) {
        return function (y) {
            if (x instanceof CubeFormat && y instanceof CubeFormat) {
                return true;
            };
            if (x instanceof ThreeDLFormat && y instanceof ThreeDLFormat) {
                return true;
            };
            if (x instanceof CLFFormat && y instanceof CLFFormat) {
                return true;
            };
            if (x instanceof LOOKFormat && y instanceof LOOKFormat) {
                return true;
            };
            if (x instanceof CTLFormat && y instanceof CTLFormat) {
                return true;
            };
            if (x instanceof UnknownFormat && y instanceof UnknownFormat) {
                return true;
            };
            return false;
        };
    }
};
var eqLUT3DSize = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Cube17 && y instanceof Cube17) {
                return true;
            };
            if (x instanceof Cube33 && y instanceof Cube33) {
                return true;
            };
            if (x instanceof Cube65 && y instanceof Cube65) {
                return true;
            };
            return false;
        };
    }
};
var eqLUT3D = {
    eq: function (x) {
        return function (y) {
            return eq(x.data)(y.data) && (eq1(x.metadata.comments)(y.metadata.comments) && x.metadata.inputColorSpace === y.metadata.inputColorSpace && x.metadata.inputMax === y.metadata.inputMax && x.metadata.inputMin === y.metadata.inputMin && x.metadata.outputColorSpace === y.metadata.outputColorSpace && x.metadata.outputMax === y.metadata.outputMax && x.metadata.outputMin === y.metadata.outputMin && x.metadata.title === y.metadata.title) && x.size === y.size;
        };
    }
};
var eqLUT1D = {
    eq: function (x) {
        return function (y) {
            return eq(x.blue)(y.blue) && eq(x.green)(y.green) && (eq1(x.metadata.comments)(y.metadata.comments) && x.metadata.inputColorSpace === y.metadata.inputColorSpace && x.metadata.inputMax === y.metadata.inputMax && x.metadata.inputMin === y.metadata.inputMin && x.metadata.outputColorSpace === y.metadata.outputColorSpace && x.metadata.outputMax === y.metadata.outputMax && x.metadata.outputMin === y.metadata.outputMin && x.metadata.title === y.metadata.title) && eq(x.red)(y.red);
        };
    }
};
var defaultMetadata = {
    title: "Untitled LUT",
    comments: [  ],
    inputColorSpace: "Linear",
    outputColorSpace: "Linear",
    inputMin: 0.0,
    inputMax: 1.0,
    outputMin: 0.0,
    outputMax: 1.0
};
var lut1DIdentity = function (size) {
    var n = lutSizeToInt(size);
    var entries = generateRamp(n);
    return {
        red: entries,
        green: entries,
        blue: entries,
        metadata: {
            comments: defaultMetadata.comments,
            inputColorSpace: defaultMetadata.inputColorSpace,
            outputColorSpace: defaultMetadata.outputColorSpace,
            inputMin: defaultMetadata.inputMin,
            inputMax: defaultMetadata.inputMax,
            outputMin: defaultMetadata.outputMin,
            outputMax: defaultMetadata.outputMax,
            title: "Identity"
        }
    };
};
var lut3DIdentity = function (size) {
    var n = lut3DSizeToInt(size);
    var lutData = generateIdentityCube(n);
    return {
        data: lutData,
        size: n,
        metadata: {
            comments: defaultMetadata.comments,
            inputColorSpace: defaultMetadata.inputColorSpace,
            outputColorSpace: defaultMetadata.outputColorSpace,
            inputMin: defaultMetadata.inputMin,
            inputMax: defaultMetadata.inputMax,
            outputMin: defaultMetadata.outputMin,
            outputMax: defaultMetadata.outputMax,
            title: "Identity 3D"
        }
    };
};
var clamp01 = /* #__PURE__ */ Hydrogen_Schema_Bounded.clampNumber(0.0)(1.0);
var lut1D = function (r) {
    return function (g) {
        return function (b) {
            return function (meta) {
                return {
                    red: map(clamp01)(r),
                    green: map(clamp01)(g),
                    blue: map(clamp01)(b),
                    metadata: meta
                };
            };
        };
    };
};
var lut1DFromCurves = function (size) {
    return function (redFn) {
        return function (greenFn) {
            return function (blueFn) {
                var n = lutSizeToInt(size);
                var inputs = generateRamp(n);
                var redOut = map(function (x) {
                    return clamp01(redFn(x));
                })(inputs);
                var greenOut = map(function (x) {
                    return clamp01(greenFn(x));
                })(inputs);
                var blueOut = map(function (x) {
                    return clamp01(blueFn(x));
                })(inputs);
                return {
                    red: redOut,
                    green: greenOut,
                    blue: blueOut,
                    metadata: {
                        comments: defaultMetadata.comments,
                        inputColorSpace: defaultMetadata.inputColorSpace,
                        outputColorSpace: defaultMetadata.outputColorSpace,
                        inputMin: defaultMetadata.inputMin,
                        inputMax: defaultMetadata.inputMax,
                        outputMin: defaultMetadata.outputMin,
                        outputMax: defaultMetadata.outputMax,
                        title: "Generated Curves"
                    }
                };
            };
        };
    };
};
var lut3D = function (size) {
    return function (lutData) {
        return function (meta) {
            return {
                data: map(clamp01)(lutData),
                size: size,
                metadata: meta
            };
        };
    };
};
var sample1DChannel = function (channelData) {
    return function (inputVal) {
        return function (v) {
            var n = Data_Array.length(channelData);
            var maxIdx = n - 1 | 0;
            var clamped = clamp01(inputVal);
            var scaledPos = clamped * Data_Int.toNumber(maxIdx);
            var lowIdx = Data_Int.floor(scaledPos);
            var frac = scaledPos - Data_Int.toNumber(lowIdx);
            var highIdx = (function () {
                var $83 = lowIdx >= maxIdx;
                if ($83) {
                    return maxIdx;
                };
                return lowIdx + 1 | 0;
            })();
            var highVal = Data_Maybe.fromMaybe(0.0)(Data_Array.index(channelData)(highIdx));
            var lowVal = Data_Maybe.fromMaybe(0.0)(Data_Array.index(channelData)(lowIdx));
            return lerp(lowVal)(highVal)(frac);
        };
    };
};
var composeLUT1D = function (v) {
    return function (lut2) {
        var composeChannel = function (arr) {
            return map(function (v1) {
                return sample1DChannel(arr)(v1)(lut2);
            })(generateRamp(Data_Array.length(arr)));
        };
        return {
            red: composeChannel(v.red),
            green: composeChannel(v.green),
            blue: composeChannel(v.blue),
            metadata: {
                comments: defaultMetadata.comments,
                inputColorSpace: defaultMetadata.inputColorSpace,
                outputColorSpace: defaultMetadata.outputColorSpace,
                inputMin: defaultMetadata.inputMin,
                inputMax: defaultMetadata.inputMax,
                outputMin: defaultMetadata.outputMin,
                outputMax: defaultMetadata.outputMax,
                title: v.metadata.title + (" + " + lut2.metadata.title)
            }
        };
    };
};
var resizeLUT1D = function (newSize) {
    return function (v) {
        var n = lutSizeToInt(newSize);
        var inputs = generateRamp(n);
        var resizeChannel = function (oldArr) {
            return map(function (v1) {
                return sample1DChannel(oldArr)(v1)(v);
            })(inputs);
        };
        return {
            red: resizeChannel(v.red),
            green: resizeChannel(v.green),
            blue: resizeChannel(v.blue),
            metadata: v.metadata
        };
    };
};
var sample1D = function (v) {
    return function (r) {
        return function (g) {
            return function (b) {
                return {
                    r: sample1DChannel(v.red)(r)(v),
                    g: sample1DChannel(v.green)(g)(v),
                    b: sample1DChannel(v.blue)(b)(v)
                };
            };
        };
    };
};
var sample3D = function (v) {
    return function (inR) {
        return function (inG) {
            return function (inB) {
                var maxIdx = v.size - 1 | 0;
                var rScaled = clamp01(inR) * Data_Int.toNumber(maxIdx);
                var r0 = Data_Int.floor(rScaled);
                var r1 = (function () {
                    var $97 = r0 >= maxIdx;
                    if ($97) {
                        return maxIdx;
                    };
                    return r0 + 1 | 0;
                })();
                var rFrac = rScaled - Data_Int.toNumber(r0);
                var gScaled = clamp01(inG) * Data_Int.toNumber(maxIdx);
                var g0 = Data_Int.floor(gScaled);
                var g1 = (function () {
                    var $98 = g0 >= maxIdx;
                    if ($98) {
                        return maxIdx;
                    };
                    return g0 + 1 | 0;
                })();
                var gFrac = gScaled - Data_Int.toNumber(g0);
                var bScaled = clamp01(inB) * Data_Int.toNumber(maxIdx);
                var b0 = Data_Int.floor(bScaled);
                var b1 = (function () {
                    var $99 = b0 >= maxIdx;
                    if ($99) {
                        return maxIdx;
                    };
                    return b0 + 1 | 0;
                })();
                var c001 = getVoxel(v.size)(v.data)(r0)(g0)(b1);
                var c011 = getVoxel(v.size)(v.data)(r0)(g1)(b1);
                var c101 = getVoxel(v.size)(v.data)(r1)(g0)(b1);
                var c01 = lerpRGB(c001)(c101)(rFrac);
                var c111 = getVoxel(v.size)(v.data)(r1)(g1)(b1);
                var c11 = lerpRGB(c011)(c111)(rFrac);
                var c1 = lerpRGB(c01)(c11)(gFrac);
                var bFrac = bScaled - Data_Int.toNumber(b0);
                var c000 = getVoxel(v.size)(v.data)(r0)(g0)(b0);
                var c010 = getVoxel(v.size)(v.data)(r0)(g1)(b0);
                var c100 = getVoxel(v.size)(v.data)(r1)(g0)(b0);
                var c00 = lerpRGB(c000)(c100)(rFrac);
                var c110 = getVoxel(v.size)(v.data)(r1)(g1)(b0);
                var c10 = lerpRGB(c010)(c110)(rFrac);
                var c0 = lerpRGB(c00)(c10)(gFrac);
                return lerpRGB(c0)(c1)(bFrac);
            };
        };
    };
};
export {
    Size1K,
    Size4K,
    Size16K,
    Size65K,
    Cube17,
    Cube33,
    Cube65,
    lut1D,
    lut1DIdentity,
    lut1DFromCurves,
    lut1DSize,
    sample1D,
    sample1DChannel,
    lut3D,
    lut3DIdentity,
    lut3DSize,
    sample3D,
    lutMetadata,
    defaultMetadata,
    invertLUT1D,
    composeLUT1D,
    resizeLUT1D,
    CubeFormat,
    ThreeDLFormat,
    CLFFormat,
    LOOKFormat,
    CTLFormat,
    UnknownFormat,
    eqLUTSize,
    showLUTSize,
    eqLUT3DSize,
    showLUT3DSize,
    eqLUT1D,
    showLUT1D,
    eqLUT3D,
    showLUT3D,
    eqLUTFormat,
    showLUTFormat
};
//# sourceMappingURL=index.js.map
