// Generated by purs version 0.15.15
import * as Data_Maybe from "../Data.Maybe/index.js";
var value = function (v) {
    return function (props) {
        return {
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            value: v
        };
    };
};
var transitionDuration = function (d) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            transitionDuration: new Data_Maybe.Just(d)
        };
    };
};
var trackHeight = function (h) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            trackHeight: new Data_Maybe.Just(h)
        };
    };
};
var trackColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            trackColor: new Data_Maybe.Just(c)
        };
    };
};
var tooltipTextColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            tooltipTextColor: new Data_Maybe.Just(c)
        };
    };
};
var tooltipBackgroundColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            tooltipBackgroundColor: new Data_Maybe.Just(c)
        };
    };
};
var tickColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            tickColor: new Data_Maybe.Just(c)
        };
    };
};
var thumbSize = function (s) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            thumbSize: new Data_Maybe.Just(s)
        };
    };
};
var thumbColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            thumbColor: new Data_Maybe.Just(c)
        };
    };
};
var thumbBorderWidth = function (w) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            thumbBorderWidth: new Data_Maybe.Just(w)
        };
    };
};
var thumbBorderColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            thumbBorderColor: new Data_Maybe.Just(c)
        };
    };
};
var step = function (v) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            step: v
        };
    };
};
var sliderDisabled = function (d) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            disabled: d
        };
    };
};
var sliderAriaLabel = function (l) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: l
        };
    };
};
var showTooltip = function (s) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            showTooltip: s
        };
    };
};
var showTicks = function (s) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            showTicks: s
        };
    };
};
var rangeValue = function (v) {
    return function (props) {
        return {
            value: props.value,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            rangeVal: v
        };
    };
};
var rangeColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            rangeColor: new Data_Maybe.Just(c)
        };
    };
};
var orientation = function (o) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            orientation: o
        };
    };
};
var onRangeChange = function (handler) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            ariaLabel: props.ariaLabel,
            onRangeChange: new Data_Maybe.Just(handler)
        };
    };
};
var onChange = function (handler) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            onChange: new Data_Maybe.Just(handler)
        };
    };
};
var minValue = function (v) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            minVal: v
        };
    };
};
var maxValue = function (v) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            maxVal: v
        };
    };
};
var focusRingColor = function (c) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            borderRadius: props.borderRadius,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            focusRingColor: new Data_Maybe.Just(c)
        };
    };
};
var borderRadius = function (r) {
    return function (props) {
        return {
            value: props.value,
            rangeVal: props.rangeVal,
            minVal: props.minVal,
            maxVal: props.maxVal,
            step: props.step,
            disabled: props.disabled,
            orientation: props.orientation,
            showTicks: props.showTicks,
            showTooltip: props.showTooltip,
            trackColor: props.trackColor,
            rangeColor: props.rangeColor,
            thumbColor: props.thumbColor,
            thumbBorderColor: props.thumbBorderColor,
            focusRingColor: props.focusRingColor,
            tickColor: props.tickColor,
            tooltipBackgroundColor: props.tooltipBackgroundColor,
            tooltipTextColor: props.tooltipTextColor,
            trackHeight: props.trackHeight,
            thumbSize: props.thumbSize,
            thumbBorderWidth: props.thumbBorderWidth,
            transitionDuration: props.transitionDuration,
            onChange: props.onChange,
            onRangeChange: props.onRangeChange,
            ariaLabel: props.ariaLabel,
            borderRadius: new Data_Maybe.Just(r)
        };
    };
};
export {
    value,
    rangeValue,
    minValue,
    maxValue,
    step,
    sliderDisabled,
    orientation,
    showTicks,
    showTooltip,
    trackColor,
    rangeColor,
    thumbColor,
    thumbBorderColor,
    focusRingColor,
    tickColor,
    tooltipBackgroundColor,
    tooltipTextColor,
    trackHeight,
    thumbSize,
    thumbBorderWidth,
    borderRadius,
    transitionDuration,
    onChange,
    onRangeChange,
    sliderAriaLabel
};
//# sourceMappingURL=index.js.map
