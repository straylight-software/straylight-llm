// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Math_Core_Arithmetic from "../Hydrogen.Math.Core.Arithmetic/index.js";
import * as Hydrogen_Math_Core_Trigonometry from "../Hydrogen.Math.Core.Trigonometry/index.js";
import * as Hydrogen_Schema_Bounded from "../Hydrogen.Schema.Bounded/index.js";
import * as Hydrogen_Schema_Gestural_Pointer from "../Hydrogen.Schema.Gestural.Pointer/index.js";
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var min1 = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordInt);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var TouchCount = function (x) {
    return x;
};
var ThreeFingerUp = /* #__PURE__ */ (function () {
    function ThreeFingerUp() {

    };
    ThreeFingerUp.value = new ThreeFingerUp();
    return ThreeFingerUp;
})();
var ThreeFingerDown = /* #__PURE__ */ (function () {
    function ThreeFingerDown() {

    };
    ThreeFingerDown.value = new ThreeFingerDown();
    return ThreeFingerDown;
})();
var ThreeFingerLeft = /* #__PURE__ */ (function () {
    function ThreeFingerLeft() {

    };
    ThreeFingerLeft.value = new ThreeFingerLeft();
    return ThreeFingerLeft;
})();
var ThreeFingerRight = /* #__PURE__ */ (function () {
    function ThreeFingerRight() {

    };
    ThreeFingerRight.value = new ThreeFingerRight();
    return ThreeFingerRight;
})();
var EdgeTop = /* #__PURE__ */ (function () {
    function EdgeTop() {

    };
    EdgeTop.value = new EdgeTop();
    return EdgeTop;
})();
var EdgeBottom = /* #__PURE__ */ (function () {
    function EdgeBottom() {

    };
    EdgeBottom.value = new EdgeBottom();
    return EdgeBottom;
})();
var EdgeLeft = /* #__PURE__ */ (function () {
    function EdgeLeft() {

    };
    EdgeLeft.value = new EdgeLeft();
    return EdgeLeft;
})();
var EdgeRight = /* #__PURE__ */ (function () {
    function EdgeRight() {

    };
    EdgeRight.value = new EdgeRight();
    return EdgeRight;
})();
var updateEdgeSwipe = function (newDistance) {
    return function (newVelocity) {
        return function (es) {
            return {
                edge: es.edge,
                threshold: es.threshold,
                active: es.active,
                distance: newDistance,
                progress: (function () {
                    var $52 = es.threshold > 0.0;
                    if ($52) {
                        return min(1.0)(newDistance / es.threshold);
                    };
                    return 0.0;
                })(),
                velocity: newVelocity
            };
        };
    };
};
var unwrapTouchCount = function (v) {
    return v;
};
var touchSize = function (tp) {
    return tp.size;
};
var touchPressure = function (tp) {
    return tp.pressure;
};
var touchPosition = function (tp) {
    return tp.position;
};
var touchPointCount = function (ts) {
    return unwrapTouchCount(ts.count);
};
var touchPoint = function (id) {
    return function (pos) {
        return function (pres) {
            return function (sz) {
                return function (ts) {
                    return {
                        id: id,
                        position: pos,
                        pressure: pres,
                        size: sz,
                        timestamp: ts
                    };
                };
            };
        };
    };
};
var touchId = function (tp) {
    return tp.id;
};
var touchCountBounds = /* #__PURE__ */ (function () {
    return Hydrogen_Schema_Bounded.intBounds(0)(10)(Hydrogen_Schema_Bounded.Clamps.value)("touchCount")("Number of active touches (0-10)");
})();
var touchCount = function (n) {
    return max(0)(min1(10)(n));
};
var showTouchCount = {
    show: function (v) {
        return show(v) + " touches";
    }
};
var showThreeFingerDirection = {
    show: function (v) {
        if (v instanceof ThreeFingerUp) {
            return "three-finger-up";
        };
        if (v instanceof ThreeFingerDown) {
            return "three-finger-down";
        };
        if (v instanceof ThreeFingerLeft) {
            return "three-finger-left";
        };
        if (v instanceof ThreeFingerRight) {
            return "three-finger-right";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 512, column 1 - line 516, column 47): " + [ v.constructor.name ]);
    }
};
var showScreenEdge = {
    show: function (v) {
        if (v instanceof EdgeTop) {
            return "top";
        };
        if (v instanceof EdgeBottom) {
            return "bottom";
        };
        if (v instanceof EdgeLeft) {
            return "left";
        };
        if (v instanceof EdgeRight) {
            return "right";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 441, column 1 - line 445, column 27): " + [ v.constructor.name ]);
    }
};
var rotationDelta = function (rs) {
    return rs.delta;
};
var rotationCenter = function (rs) {
    return {
        x: rs.centerX,
        y: rs.centerY
    };
};
var rotationAngle = function (rs) {
    return rs.angle;
};
var rotateState = function (initAngle) {
    return function (currAngle) {
        return function (delta) {
            return function (cx) {
                return function (cy) {
                    return {
                        angle: currAngle,
                        initialAngle: initAngle,
                        delta: delta,
                        centerX: cx,
                        centerY: cy,
                        active: true
                    };
                };
            };
        };
    };
};
var recognizeTwoFingerTap = function (centerX) {
    return function (centerY) {
        return function (timestamp) {
            return {
                recognized: true,
                centerX: centerX,
                centerY: centerY,
                timestamp: timestamp
            };
        };
    };
};
var primaryTouch = function (ts) {
    return Data_Array.head(ts.touches);
};
var pinchState = function (initDist) {
    return function (currDist) {
        return function (cx) {
            return function (cy) {
                return function (scale$prime) {
                    return {
                        scale: scale$prime,
                        initialDistance: max1(1.0)(initDist),
                        currentDistance: max1(1.0)(currDist),
                        centerX: cx,
                        centerY: cy,
                        active: true
                    };
                };
            };
        };
    };
};
var pinchScale = function (ps) {
    return ps.scale;
};
var pinchDistance = function (ps) {
    return ps.currentDistance;
};
var pinchCenter = function (ps) {
    return {
        x: ps.centerX,
        y: ps.centerY
    };
};
var noTwoFingerTap = {
    recognized: false,
    centerX: 0.0,
    centerY: 0.0,
    timestamp: 0.0
};
var noThreeFingerSwipe = /* #__PURE__ */ (function () {
    return {
        direction: Data_Maybe.Nothing.value,
        distance: 0.0,
        velocity: 0.0,
        touchCount: touchCount(0),
        active: false,
        centerX: 0.0,
        centerY: 0.0
    };
})();
var noRotation = {
    angle: 0.0,
    initialAngle: 0.0,
    delta: 0.0,
    centerX: 0.0,
    centerY: 0.0,
    active: false
};
var noPinch = {
    scale: 1.0,
    initialDistance: 0.0,
    currentDistance: 0.0,
    centerX: 0.0,
    centerY: 0.0,
    active: false
};
var maxTouches = 10;
var isThreeFingerSwipeRecognized = function (tfs) {
    return tfs.active && (tfs.distance > 50.0 && (function () {
        if (tfs.direction instanceof Data_Maybe.Just) {
            return true;
        };
        if (tfs.direction instanceof Data_Maybe.Nothing) {
            return false;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 586, column 6 - line 588, column 24): " + [ tfs.direction.constructor.name ]);
    })());
};
var isSingleTouch = function (v) {
    return v === 1;
};
var isRotating = function (rs) {
    return rs.active;
};
var isPinchingOut = function (ps) {
    return ps.active && ps.scale > 1.05;
};
var isPinchingIn = function (ps) {
    return ps.active && ps.scale < 0.95;
};
var isPinching = function (ps) {
    return ps.active;
};
var isMultiTouch = function (v) {
    return v >= 2;
};
var isEdgeSwipePastThreshold = function (es) {
    return es.progress >= 1.0;
};
var eqTouchCount = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTouchCount = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqTouchCount;
    }
};
var eqThreeFingerDirection = {
    eq: function (x) {
        return function (y) {
            if (x instanceof ThreeFingerUp && y instanceof ThreeFingerUp) {
                return true;
            };
            if (x instanceof ThreeFingerDown && y instanceof ThreeFingerDown) {
                return true;
            };
            if (x instanceof ThreeFingerLeft && y instanceof ThreeFingerLeft) {
                return true;
            };
            if (x instanceof ThreeFingerRight && y instanceof ThreeFingerRight) {
                return true;
            };
            return false;
        };
    }
};
var ordThreeFingerDirection = {
    compare: function (x) {
        return function (y) {
            if (x instanceof ThreeFingerUp && y instanceof ThreeFingerUp) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof ThreeFingerUp) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof ThreeFingerUp) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof ThreeFingerDown && y instanceof ThreeFingerDown) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof ThreeFingerDown) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof ThreeFingerDown) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof ThreeFingerLeft && y instanceof ThreeFingerLeft) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof ThreeFingerLeft) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof ThreeFingerLeft) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof ThreeFingerRight && y instanceof ThreeFingerRight) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqThreeFingerDirection;
    }
};
var eqScreenEdge = {
    eq: function (x) {
        return function (y) {
            if (x instanceof EdgeTop && y instanceof EdgeTop) {
                return true;
            };
            if (x instanceof EdgeBottom && y instanceof EdgeBottom) {
                return true;
            };
            if (x instanceof EdgeLeft && y instanceof EdgeLeft) {
                return true;
            };
            if (x instanceof EdgeRight && y instanceof EdgeRight) {
                return true;
            };
            return false;
        };
    }
};
var ordScreenEdge = {
    compare: function (x) {
        return function (y) {
            if (x instanceof EdgeTop && y instanceof EdgeTop) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof EdgeTop) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof EdgeTop) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof EdgeBottom && y instanceof EdgeBottom) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof EdgeBottom) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof EdgeBottom) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof EdgeLeft && y instanceof EdgeLeft) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof EdgeLeft) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof EdgeLeft) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof EdgeRight && y instanceof EdgeRight) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqScreenEdge;
    }
};
var edgeSwipe = function (edge) {
    return function (threshold) {
        return {
            edge: edge,
            distance: 0.0,
            progress: 0.0,
            threshold: threshold,
            velocity: 0.0,
            active: false
        };
    };
};
var noEdgeSwipe = /* #__PURE__ */ (function () {
    return edgeSwipe(EdgeLeft.value)(50.0);
})();
var computeRotation = function (tps) {
    if (Data_Array.length(tps) < 2) {
        return noRotation;
    };
    if (Data_Boolean.otherwise) {
        var v = Data_Array.head(tps);
        if (v instanceof Data_Maybe.Nothing) {
            return noRotation;
        };
        if (v instanceof Data_Maybe.Just) {
            var v1 = Data_Array.head(Data_Array.filter(function (t) {
                return t.id !== v.value0.id;
            })(tps));
            if (v1 instanceof Data_Maybe.Nothing) {
                return noRotation;
            };
            if (v1 instanceof Data_Maybe.Just) {
                var y2 = Hydrogen_Schema_Gestural_Pointer.clientY(v1.value0.position);
                var y1 = Hydrogen_Schema_Gestural_Pointer.clientY(v.value0.position);
                var x2 = Hydrogen_Schema_Gestural_Pointer.clientX(v1.value0.position);
                var x1 = Hydrogen_Schema_Gestural_Pointer.clientX(v.value0.position);
                var dy = y2 - y1;
                var dx = x2 - x1;
                var cy = (y1 + y2) / 2.0;
                var cx = (x1 + x2) / 2.0;
                var angle = Hydrogen_Math_Core_Trigonometry.atan2(dy)(dx);
                return rotateState(angle)(angle)(0.0)(cx)(cy);
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 402, column 18 - line 414, column 47): " + [ v1.constructor.name ]);
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 400, column 17 - line 414, column 47): " + [ v.constructor.name ]);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 397, column 1 - line 397, column 51): " + [ tps.constructor.name ]);
};
var computePinch = function (tps) {
    if (Data_Array.length(tps) < 2) {
        return noPinch;
    };
    if (Data_Boolean.otherwise) {
        var v = Data_Array.head(tps);
        if (v instanceof Data_Maybe.Nothing) {
            return noPinch;
        };
        if (v instanceof Data_Maybe.Just) {
            var v1 = Data_Array.head(Data_Array.filter(function (t) {
                return t.id !== v.value0.id;
            })(tps));
            if (v1 instanceof Data_Maybe.Nothing) {
                return noPinch;
            };
            if (v1 instanceof Data_Maybe.Just) {
                var y2 = Hydrogen_Schema_Gestural_Pointer.clientY(v1.value0.position);
                var y1 = Hydrogen_Schema_Gestural_Pointer.clientY(v.value0.position);
                var x2 = Hydrogen_Schema_Gestural_Pointer.clientX(v1.value0.position);
                var x1 = Hydrogen_Schema_Gestural_Pointer.clientX(v.value0.position);
                var dy = y2 - y1;
                var dx = x2 - x1;
                var dist = Hydrogen_Math_Core_Arithmetic.sqrt(dx * dx + dy * dy);
                var cy = (y1 + y2) / 2.0;
                var cx = (x1 + x2) / 2.0;
                return pinchState(dist)(dist)(cx)(cy)(1.0);
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 382, column 18 - line 394, column 44): " + [ v1.constructor.name ]);
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 380, column 17 - line 394, column 44): " + [ v.constructor.name ]);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 377, column 1 - line 377, column 47): " + [ tps.constructor.name ]);
};
var touchState = function (tps) {
    return function (ts) {
        var rotate$prime = computeRotation(tps);
        var pinch$prime = computePinch(tps);
        var cnt = touchCount(Data_Array.length(tps));
        return {
            touches: tps,
            count: cnt,
            pinch: pinch$prime,
            rotate: rotate$prime,
            timestamp: ts
        };
    };
};
var noTouches = /* #__PURE__ */ touchState([  ])(0.0);
var removeTouch = function (id) {
    return function (ts) {
        return touchState(Data_Array.filter(function (t) {
            return t.id !== id;
        })(ts.touches))(ts.timestamp);
    };
};
var singleTouchState = function (tp) {
    return touchState([ tp ])(tp.timestamp);
};
var twoFingerState = function (tp1) {
    return function (tp2) {
        return touchState([ tp1, tp2 ])(max1(tp1.timestamp)(tp2.timestamp));
    };
};
var updateTouch = function (tp) {
    return function (ts) {
        var updated = map(function (t) {
            var $83 = t.id === tp.id;
            if ($83) {
                return tp;
            };
            return t;
        })(ts.touches);
        return touchState(updated)(tp.timestamp);
    };
};
var cancelThreeFingerSwipe = function (tfs) {
    return {
        direction: tfs.direction,
        distance: tfs.distance,
        velocity: tfs.velocity,
        touchCount: tfs.touchCount,
        centerX: tfs.centerX,
        centerY: tfs.centerY,
        active: false
    };
};
var cancelEdgeSwipe = function (es) {
    return {
        edge: es.edge,
        threshold: es.threshold,
        velocity: es.velocity,
        active: false,
        distance: 0.0,
        progress: 0.0
    };
};
var beginThreeFingerSwipe = function (centerX) {
    return function (centerY) {
        return {
            direction: Data_Maybe.Nothing.value,
            distance: 0.0,
            velocity: 0.0,
            touchCount: touchCount(3),
            active: true,
            centerX: centerX,
            centerY: centerY
        };
    };
};
var beginEdgeSwipe = function (edge) {
    return function (edgeDistance) {
        return function (threshold) {
            return function (es) {
                return {
                    velocity: es.velocity,
                    edge: edge,
                    distance: edgeDistance,
                    progress: (function () {
                        var $84 = threshold > 0.0;
                        if ($84) {
                            return min(1.0)(edgeDistance / threshold);
                        };
                        return 0.0;
                    })(),
                    threshold: threshold,
                    active: true
                };
            };
        };
    };
};
var allTouches = function (ts) {
    return ts.touches;
};
var addTouch = function (tp) {
    return function (ts) {
        return touchState(Data_Array.snoc(ts.touches)(tp))(tp.timestamp);
    };
};
var abs = function (n) {
    var $85 = n < 0.0;
    if ($85) {
        return -n;
    };
    return n;
};
var detectThreeFingerDirection = function (dx) {
    return function (dy) {
        if (abs(dx) < 10.0 && abs(dy) < 10.0) {
            return Data_Maybe.Nothing.value;
        };
        if (abs(dy) > abs(dx) && dy < 0.0) {
            return new Data_Maybe.Just(ThreeFingerUp.value);
        };
        if (abs(dy) > abs(dx) && dy > 0.0) {
            return new Data_Maybe.Just(ThreeFingerDown.value);
        };
        if (abs(dx) > abs(dy) && dx < 0.0) {
            return new Data_Maybe.Just(ThreeFingerLeft.value);
        };
        if (abs(dx) > abs(dy) && dx > 0.0) {
            return new Data_Maybe.Just(ThreeFingerRight.value);
        };
        if (Data_Boolean.otherwise) {
            return Data_Maybe.Nothing.value;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Gestural.Touch (line 572, column 1 - line 572, column 77): " + [ dx.constructor.name, dy.constructor.name ]);
    };
};
var updateThreeFingerSwipe = function (deltaX) {
    return function (deltaY) {
        return function (velocity) {
            return function (tfs) {
                var newDistance = Hydrogen_Math_Core_Arithmetic.sqrt(deltaX * deltaX + deltaY * deltaY);
                var dir = detectThreeFingerDirection(deltaX)(deltaY);
                return {
                    touchCount: tfs.touchCount,
                    active: tfs.active,
                    direction: dir,
                    distance: tfs.distance + newDistance,
                    velocity: velocity,
                    centerX: tfs.centerX + deltaX,
                    centerY: tfs.centerY + deltaY
                };
            };
        };
    };
};
export {
    touchPoint,
    touchId,
    touchPosition,
    touchPressure,
    touchSize,
    TouchCount,
    touchCount,
    maxTouches,
    unwrapTouchCount,
    isSingleTouch,
    isMultiTouch,
    pinchState,
    noPinch,
    pinchScale,
    pinchCenter,
    pinchDistance,
    isPinching,
    isPinchingIn,
    isPinchingOut,
    rotateState,
    noRotation,
    rotationAngle,
    rotationDelta,
    rotationCenter,
    isRotating,
    touchState,
    noTouches,
    singleTouchState,
    twoFingerState,
    addTouch,
    removeTouch,
    updateTouch,
    touchPointCount,
    primaryTouch,
    allTouches,
    EdgeTop,
    EdgeBottom,
    EdgeLeft,
    EdgeRight,
    edgeSwipe,
    noEdgeSwipe,
    beginEdgeSwipe,
    updateEdgeSwipe,
    isEdgeSwipePastThreshold,
    cancelEdgeSwipe,
    ThreeFingerUp,
    ThreeFingerDown,
    ThreeFingerLeft,
    ThreeFingerRight,
    noThreeFingerSwipe,
    beginThreeFingerSwipe,
    updateThreeFingerSwipe,
    isThreeFingerSwipeRecognized,
    cancelThreeFingerSwipe,
    noTwoFingerTap,
    recognizeTwoFingerTap,
    touchCountBounds,
    eqTouchCount,
    ordTouchCount,
    showTouchCount,
    eqScreenEdge,
    ordScreenEdge,
    showScreenEdge,
    eqThreeFingerDirection,
    ordThreeFingerDirection,
    showThreeFingerDirection
};
//# sourceMappingURL=index.js.map
