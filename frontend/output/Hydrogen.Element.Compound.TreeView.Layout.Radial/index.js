// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Map_Internal from "../Data.Map.Internal/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Tuple from "../Data.Tuple/index.js";
import * as Hydrogen_Element_Compound_TreeView_Node from "../Hydrogen.Element.Compound.TreeView.Node/index.js";
import * as Hydrogen_Element_Compound_TreeView_State from "../Hydrogen.Element.Compound.TreeView.State/index.js";
import * as Hydrogen_Element_Compound_TreeView_Types from "../Hydrogen.Element.Compound.TreeView.Types/index.js";
import * as Hydrogen_Schema_Graph_Layout_Constraints from "../Hydrogen.Schema.Graph.Layout.Constraints/index.js";
import * as Hydrogen_Schema_Graph_Layout_Params from "../Hydrogen.Schema.Graph.Layout.Params/index.js";
import * as Hydrogen_Schema_Graph_Layout_Types from "../Hydrogen.Schema.Graph.Layout.Types/index.js";
import * as Hydrogen_Schema_Graph_Viewport_Types from "../Hydrogen.Schema.Graph.Viewport.Types/index.js";
var insert = /* #__PURE__ */ Data_Map_Internal.insert(Hydrogen_Element_Compound_TreeView_Types.ordNodeId);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var eq = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Element_Compound_TreeView_Types.eqNodeId);
var nodeSizeWidth = function (v) {
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FixedSize) {
        return v.value0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FitContent) {
        return v.value0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.Proportional) {
        return 100.0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.AspectRatio) {
        return 100.0 * v.value0;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.Radial (line 368, column 1 - line 368, column 45): " + [ v.constructor.name ]);
};
var negateNum = function (n) {
    return 0.0 - n;
};
var layoutResult = function (positions) {
    return function (bounds) {
        return function (nodeCount) {
            return {
                positions: positions,
                bounds: bounds,
                nodeCount: nodeCount
            };
        };
    };
};
var floor = Data_Int.floor;
var defaultRadialParams = {
    startAngle: 0.0,
    endAngle: 6.28318,
    innerRadius: 50.0,
    outerRadius: 300.0,
    clockwise: true
};
var cos = function (angle) {
    var tau = 2.0 * 3.14159265359;
    var normalized = angle - Data_Int.toNumber(floor(angle / tau)) * tau;
    var x2 = normalized * normalized;
    var x4 = x2 * x2;
    var x6 = x4 * x2;
    return ((1.0 - x2 / 2.0) + x4 / 24.0) - x6 / 720.0;
};
var sin = function (angle) {
    return cos(angle - 3.14159265359 / 2.0);
};
var layoutCirclePackNode = function (config) {
    return function (tree) {
        return function (expanded) {
            return function (state) {
                return function (node) {
                    return function (cx) {
                        return function (cy) {
                            return function (radius) {
                                var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(node);
                                var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                                var diameter = radius * 2.0;
                                var pos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(cx)(cy)(diameter)(diameter);
                                var newPositions = insert(nid)(pos)(state.positions);
                                var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(node);
                                var $21 = isExp && Data_Array.length(children) > 0;
                                if ($21) {
                                    var stateWithNode = {
                                        positions: newPositions
                                    };
                                    var childRadius = radius * 0.4;
                                    var childNodes = Data_Array.mapMaybe(function (cid) {
                                        return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                                    })(children);
                                    var indexed = Data_Array.mapWithIndex(Data_Tuple.Tuple.create)(childNodes);
                                    var n = Data_Array.length(childNodes);
                                    return foldl(function (s) {
                                        return function (v) {
                                            var angle = (2.0 * 3.14159 * Data_Int.toNumber(v.value0)) / Data_Int.toNumber(n);
                                            var childCx = cx + (radius - childRadius) * cos(angle);
                                            var childCy = cy + (radius - childRadius) * sin(angle);
                                            return layoutCirclePackNode(config)(tree)(expanded)(s)(v.value1)(childCx)(childCy)(childRadius);
                                        };
                                    })(stateWithNode)(indexed);
                                };
                                return {
                                    positions: newPositions
                                };
                            };
                        };
                    };
                };
            };
        };
    };
};
var layoutCirclePack = function (config) {
    return function (tree) {
        return function (expanded) {
            var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
            var initialState = {
                positions: Data_Map_Internal.empty
            };
            var finalState = (function () {
                var v = Data_Array.head(roots);
                if (v instanceof Data_Maybe.Nothing) {
                    return initialState;
                };
                if (v instanceof Data_Maybe.Just) {
                    return layoutCirclePackNode(config)(tree)(expanded)(initialState)(v.value0)(0.0)(0.0)(200.0);
                };
                throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.Radial (line 263, column 18 - line 266, column 86): " + [ v.constructor.name ]);
            })();
            var bounds = Hydrogen_Schema_Graph_Viewport_Types.viewportBounds(negateNum(200.0))(negateNum(200.0))(200.0)(200.0);
            return layoutResult(finalState.positions)(bounds)(Data_Map_Internal.size(finalState.positions));
        };
    };
};
var layoutRadialLevel = function (config) {
    return function (tree) {
        return function (expanded) {
            return function (state) {
                return function (nodes) {
                    return function (radius) {
                        return function (angleStep) {
                            return function (startAngle) {
                                return function (level) {
                                    var nodeSize = nodeSizeWidth(config.sizing);
                                    var indexed = Data_Array.mapWithIndex(Data_Tuple.Tuple.create)(nodes);
                                    var stateAfterLevel = foldl(function (s) {
                                        return function (v) {
                                            var angle = startAngle + Data_Int.toNumber(v.value0) * angleStep;
                                            var x = radius * cos(angle);
                                            var y = radius * sin(angle);
                                            var pos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(x)(y)(nodeSize)(nodeSize);
                                            return {
                                                positions: insert(Hydrogen_Element_Compound_TreeView_Node.nodeId(v.value1))(pos)(s.positions)
                                            };
                                        };
                                    })(state)(indexed);
                                    var stateAfterChildren = foldl(function (s) {
                                        return function (node) {
                                            var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(node);
                                            var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                                            var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(node);
                                            var $30 = isExp && Data_Array.length(children) > 0;
                                            if ($30) {
                                                var parentIdx = Data_Maybe.fromMaybe(0)(Data_Array.findIndex(function (n) {
                                                    return eq(Hydrogen_Element_Compound_TreeView_Node.nodeId(n))(nid);
                                                })(nodes));
                                                var parentAngle = startAngle + Data_Int.toNumber(parentIdx) * angleStep;
                                                var childStartAngle = parentAngle - angleStep / 2.0;
                                                var childNodes = Data_Array.mapMaybe(function (cid) {
                                                    return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                                                })(children);
                                                var childAngleStep = angleStep / Data_Int.toNumber(Data_Array.length(childNodes));
                                                return layoutRadialLevel(config)(tree)(expanded)(s)(childNodes)(radius + 60.0)(childAngleStep)(childStartAngle)(level + 1 | 0);
                                            };
                                            return s;
                                        };
                                    })(stateAfterLevel)(nodes);
                                    return stateAfterChildren;
                                };
                            };
                        };
                    };
                };
            };
        };
    };
};
var layoutRadial = function (config) {
    return function (tree) {
        return function (expanded) {
            var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
            var radialParams = Data_Maybe.fromMaybe(defaultRadialParams)(config.radialParams);
            var start = Hydrogen_Schema_Graph_Layout_Params.startAngle(radialParams);
            var outer = Hydrogen_Schema_Graph_Layout_Params.outerRadius(radialParams);
            var nodeSize = nodeSizeWidth(config.sizing);
            var inner = Hydrogen_Schema_Graph_Layout_Params.innerRadius(radialParams);
            var initialState = {
                positions: Data_Map_Internal.empty
            };
            var end = Hydrogen_Schema_Graph_Layout_Params.endAngle(radialParams);
            var finalState = (function () {
                var v = Data_Array.head(roots);
                if (v instanceof Data_Maybe.Nothing) {
                    return initialState;
                };
                if (v instanceof Data_Maybe.Just) {
                    var rootPos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(0.0)(0.0)(nodeSize)(nodeSize);
                    var stateWithRoot = {
                        positions: insert(Hydrogen_Element_Compound_TreeView_Node.nodeId(v.value0))(rootPos)(Data_Map_Internal.empty)
                    };
                    var childIds = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(v.value0);
                    var childNodes = Data_Array.mapMaybe(function (cid) {
                        return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                    })(childIds);
                    var angleStep = (end - start) / Data_Int.toNumber(Data_Array.length(childNodes));
                    return layoutRadialLevel(config)(tree)(expanded)(stateWithRoot)(childNodes)(inner)(angleStep)(start)(1);
                };
                throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.Radial (line 142, column 18 - line 152, column 98): " + [ v.constructor.name ]);
            })();
            var bounds = Hydrogen_Schema_Graph_Viewport_Types.viewportBounds(negateNum(outer))(negateNum(outer))(outer)(outer);
            return layoutResult(finalState.positions)(bounds)(Data_Map_Internal.size(finalState.positions));
        };
    };
};
var layoutSunburst = function (config) {
    return function (tree) {
        return function (expanded) {
            return layoutRadial(config)(tree)(expanded);
        };
    };
};
export {
    layoutRadial,
    layoutSunburst,
    layoutCirclePack,
    cos,
    sin
};
//# sourceMappingURL=index.js.map
