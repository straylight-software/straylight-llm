// Generated by purs version 0.15.15
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var ViewportZoom = function (x) {
    return x;
};
var zoomStep = 1.2;
var zoomPercentage = function (v) {
    return v * 100.0;
};
var zoomLevel = function (v) {
    return v;
};
var zoomFit = 1.0;
var zoom50 = 0.5;
var zoom400 = 4.0;
var zoom25 = 0.25;
var zoom200 = 2.0;
var zoom100 = 1.0;
var viewportZoom = function (z) {
    return max(1.0e-2)(min(100.0)(z));
};
var zoomIn = function (v) {
    return viewportZoom(v * zoomStep);
};
var zoomOut = function (v) {
    return viewportZoom(v / zoomStep);
};
var zoomTo = viewportZoom;
var zoomToFit = function (contentWidth) {
    return function (contentHeight) {
        return function (viewportWidth) {
            return function (viewportHeight) {
                var zoomY = viewportHeight / contentHeight;
                var zoomX = viewportWidth / contentWidth;
                return viewportZoom(min(zoomX)(zoomY));
            };
        };
    };
};
var viewportPosition = function (x) {
    return function (y) {
        return {
            x: x,
            y: y
        };
    };
};
var viewportBounds = function (left) {
    return function (top) {
        return function (right) {
            return function (bottom) {
                return {
                    left: min(left)(right),
                    top: min(top)(bottom),
                    right: max(left)(right),
                    bottom: max(top)(bottom)
                };
            };
        };
    };
};
var showViewportZoom = {
    show: function (v) {
        return show(v * 100.0) + "%";
    }
};
var panY = function (p) {
    return p.y;
};
var panX = function (p) {
    return p.x;
};
var panTo = viewportPosition;
var panBy = function (dx) {
    return function (dy) {
        return function (p) {
            return {
                x: p.x + dx,
                y: p.y + dy
            };
        };
    };
};
var origin = {
    x: 0.0,
    y: 0.0
};
var minZoom = 1.0e-2;
var maxZoom = 100.0;
var isMinZoom = function (v) {
    return v <= 1.0e-2;
};
var isMaxZoom = function (v) {
    return v >= 100.0;
};
var expandBounds = function (margin) {
    return function (b) {
        return {
            left: b.left - margin,
            top: b.top - margin,
            right: b.right + margin,
            bottom: b.bottom + margin
        };
    };
};
var eqViewportZoom = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordViewportZoom = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqViewportZoom;
    }
};
var defaultZoom = 1.0;
var containsPoint = function (x) {
    return function (y) {
        return function (b) {
            return x >= b.left && (x <= b.right && (y >= b.top && y <= b.bottom));
        };
    };
};
var centerOn = function (targetX) {
    return function (targetY) {
        return function (viewportWidth) {
            return function (viewportHeight) {
                return {
                    x: targetX - viewportWidth / 2.0,
                    y: targetY - viewportHeight / 2.0
                };
            };
        };
    };
};
var boundsWidth = function (b) {
    return b.right - b.left;
};
var boundsTop = function (b) {
    return b.top;
};
var boundsRight = function (b) {
    return b.right;
};
var boundsLeft = function (b) {
    return b.left;
};
var boundsIntersect = function (a) {
    return function (b) {
        return a.left <= b.right && (a.right >= b.left && (a.top <= b.bottom && a.bottom >= b.top));
    };
};
var boundsHeight = function (b) {
    return b.bottom - b.top;
};
var boundsFromCenter = function (centerX) {
    return function (centerY) {
        return function (width) {
            return function (height) {
                return {
                    left: centerX - width / 2.0,
                    top: centerY - height / 2.0,
                    right: centerX + width / 2.0,
                    bottom: centerY + height / 2.0
                };
            };
        };
    };
};
var boundsCenter = function (b) {
    return {
        x: (b.left + b.right) / 2.0,
        y: (b.top + b.bottom) / 2.0
    };
};
var boundsBottom = function (b) {
    return b.bottom;
};
export {
    ViewportZoom,
    viewportZoom,
    defaultZoom,
    zoomIn,
    zoomOut,
    zoomTo,
    zoomToFit,
    zoomLevel,
    zoomPercentage,
    minZoom,
    maxZoom,
    isMinZoom,
    isMaxZoom,
    viewportPosition,
    origin,
    panX,
    panY,
    panBy,
    panTo,
    centerOn,
    viewportBounds,
    boundsFromCenter,
    boundsLeft,
    boundsTop,
    boundsRight,
    boundsBottom,
    boundsWidth,
    boundsHeight,
    boundsCenter,
    containsPoint,
    boundsIntersect,
    expandBounds,
    zoomFit,
    zoom25,
    zoom50,
    zoom100,
    zoom200,
    zoom400,
    eqViewportZoom,
    ordViewportZoom,
    showViewportZoom
};
//# sourceMappingURL=index.js.map
