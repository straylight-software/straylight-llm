// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Hydrogen_Element_Compound_DataGrid_Types from "../Hydrogen.Element.Compound.DataGrid.Types/index.js";
var sortable = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        filterable: col.filterable,
        resizable: col.resizable,
        fixed: col.fixed,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        sortable: true
    };
};
var resizable = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        fixed: col.fixed,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        resizable: true
    };
};
var notResizable = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        fixed: col.fixed,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        resizable: false
    };
};
var hidden = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        resizable: col.resizable,
        fixed: col.fixed,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        hidden: true
    };
};
var headerRenderer = function (r) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            width: col.width,
            minWidth: col.minWidth,
            maxWidth: col.maxWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellType: col.cellType,
            cellRenderer: col.cellRenderer,
            editable: col.editable,
            headerRenderer: new Data_Maybe.Just(r)
        };
    };
};
var fixedRight = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        resizable: col.resizable,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        fixed: Hydrogen_Element_Compound_DataGrid_Types.FixedRight.value
    };
};
var fixedLeft = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        resizable: col.resizable,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        fixed: Hydrogen_Element_Compound_DataGrid_Types.FixedLeft.value
    };
};
var filterable = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        resizable: col.resizable,
        fixed: col.fixed,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: col.editable,
        filterable: true
    };
};
var editable = function (col) {
    return {
        key: col.key,
        header: col.header,
        width: col.width,
        minWidth: col.minWidth,
        maxWidth: col.maxWidth,
        sortable: col.sortable,
        filterable: col.filterable,
        resizable: col.resizable,
        fixed: col.fixed,
        hidden: col.hidden,
        cellType: col.cellType,
        cellRenderer: col.cellRenderer,
        headerRenderer: col.headerRenderer,
        editable: true
    };
};
var column = function (key) {
    return function (header) {
        return function (props) {
            return Data_Array.foldl(function (c) {
                return function (f) {
                    return f(c);
                };
            })(Hydrogen_Element_Compound_DataGrid_Types.defaultColumnDef(key)(header))(props);
        };
    };
};
var colWidth = function (w) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            minWidth: col.minWidth,
            maxWidth: col.maxWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellType: col.cellType,
            cellRenderer: col.cellRenderer,
            headerRenderer: col.headerRenderer,
            editable: col.editable,
            width: new Data_Maybe.Just(w)
        };
    };
};
var colMinWidth = function (w) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            width: col.width,
            maxWidth: col.maxWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellType: col.cellType,
            cellRenderer: col.cellRenderer,
            headerRenderer: col.headerRenderer,
            editable: col.editable,
            minWidth: new Data_Maybe.Just(w)
        };
    };
};
var colMaxWidth = function (w) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            width: col.width,
            minWidth: col.minWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellType: col.cellType,
            cellRenderer: col.cellRenderer,
            headerRenderer: col.headerRenderer,
            editable: col.editable,
            maxWidth: new Data_Maybe.Just(w)
        };
    };
};
var cellType = function (t) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            width: col.width,
            minWidth: col.minWidth,
            maxWidth: col.maxWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellRenderer: col.cellRenderer,
            headerRenderer: col.headerRenderer,
            editable: col.editable,
            cellType: t
        };
    };
};
var cellRenderer = function (r) {
    return function (col) {
        return {
            key: col.key,
            header: col.header,
            width: col.width,
            minWidth: col.minWidth,
            maxWidth: col.maxWidth,
            sortable: col.sortable,
            filterable: col.filterable,
            resizable: col.resizable,
            fixed: col.fixed,
            hidden: col.hidden,
            cellType: col.cellType,
            headerRenderer: col.headerRenderer,
            editable: col.editable,
            cellRenderer: new Data_Maybe.Just(r)
        };
    };
};
export {
    column,
    colWidth,
    colMinWidth,
    colMaxWidth,
    sortable,
    filterable,
    resizable,
    notResizable,
    editable,
    hidden,
    fixedLeft,
    fixedRight,
    cellType,
    cellRenderer,
    headerRenderer
};
//# sourceMappingURL=index.js.map
