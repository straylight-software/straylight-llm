// Generated by purs version 0.15.15
import * as Hydrogen_Element_Compound_Canvas_Selection_Types from "../Hydrogen.Element.Compound.Canvas.Selection.Types/index.js";
var updateHandleDrag = function (point) {
    return function (state) {
        return {
            handle: state.handle,
            startPoint: state.startPoint,
            originalBounds: state.originalBounds,
            currentPoint: point
        };
    };
};
var startHandleDrag = function (handle) {
    return function (point) {
        return function (bounds) {
            return {
                handle: handle,
                startPoint: point,
                currentPoint: point,
                originalBounds: bounds
            };
        };
    };
};
var endHandleDrag = function (state) {
    return state;
};
var computeTransformFromDrag = function (state) {
    var dy = state.currentPoint.y - state.startPoint.y;
    var dx = state.currentPoint.x - state.startPoint.x;
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleTopLeft) {
        return {
            x: state.originalBounds.x + dx,
            y: state.originalBounds.y + dy,
            width: state.originalBounds.width - dx,
            height: state.originalBounds.height - dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleTopCenter) {
        return {
            x: state.originalBounds.x,
            y: state.originalBounds.y + dy,
            width: state.originalBounds.width,
            height: state.originalBounds.height - dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleTopRight) {
        return {
            x: state.originalBounds.x,
            y: state.originalBounds.y + dy,
            width: state.originalBounds.width + dx,
            height: state.originalBounds.height - dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleMiddleLeft) {
        return {
            x: state.originalBounds.x + dx,
            y: state.originalBounds.y,
            width: state.originalBounds.width - dx,
            height: state.originalBounds.height
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleMiddleRight) {
        return {
            x: state.originalBounds.x,
            y: state.originalBounds.y,
            width: state.originalBounds.width + dx,
            height: state.originalBounds.height
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleBottomLeft) {
        return {
            x: state.originalBounds.x + dx,
            y: state.originalBounds.y,
            width: state.originalBounds.width - dx,
            height: state.originalBounds.height + dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleBottomCenter) {
        return {
            x: state.originalBounds.x,
            y: state.originalBounds.y,
            width: state.originalBounds.width,
            height: state.originalBounds.height + dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleBottomRight) {
        return {
            x: state.originalBounds.x,
            y: state.originalBounds.y,
            width: state.originalBounds.width + dx,
            height: state.originalBounds.height + dy
        };
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleRotation) {
        return state.originalBounds;
    };
    if (state.handle instanceof Hydrogen_Element_Compound_Canvas_Selection_Types.HandleCenter) {
        return {
            x: state.originalBounds.x + dx,
            y: state.originalBounds.y + dy,
            width: state.originalBounds.width,
            height: state.originalBounds.height
        };
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.Canvas.Selection.Drag (line 100, column 6 - line 158, column 8): " + [ state.handle.constructor.name ]);
};
export {
    startHandleDrag,
    updateHandleDrag,
    endHandleDrag,
    computeTransformFromDrag
};
//# sourceMappingURL=index.js.map
