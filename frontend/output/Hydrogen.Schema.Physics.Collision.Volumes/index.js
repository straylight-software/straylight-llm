// Generated by purs version 0.15.15
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Physics_Collision_Internal from "../Hydrogen.Schema.Physics.Collision.Internal/index.js";
import * as Hydrogen_Schema_Physics_Collision_Point from "../Hydrogen.Schema.Physics.Collision.Point/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var eq = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Physics_Collision_Point.eqPoint2D);
var compare = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Physics_Collision_Point.ordPoint2D);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var OBB = function (x) {
    return x;
};
var BoundingCircle = function (x) {
    return x;
};
var BoundingCapsule = function (x) {
    return x;
};
var AABB = function (x) {
    return x;
};
var showOBB = {
    show: function (v) {
        return "OBB(" + (show(v.halfWidth) + ("x" + (show(v.halfHeight) + (" @" + (show(v.rotation) + ")")))));
    }
};
var showBoundingCircle = {
    show: function (v) {
        return "BoundingCircle(r=" + (show(v.radius) + ")");
    }
};
var showBoundingCapsule = {
    show: function (v) {
        return "BoundingCapsule(r=" + (show(v.radius) + ")");
    }
};
var showAABB = {
    show: function (v) {
        return "AABB(" + (show(v.minX) + ("," + (show(v.minY) + (" -> " + (show(v.maxX) + ("," + (show(v.maxY) + ")")))))));
    }
};
var pointToSegmentDistance = function (v) {
    return function (v1) {
        return function (v2) {
            var apy = v.y - v1.y;
            var apx = v.x - v1.x;
            var aby = v2.y - v1.y;
            var abx = v2.x - v1.x;
            var abSquared = abx * abx + aby * aby;
            var t = (function () {
                var $88 = abSquared < 1.0e-4;
                if ($88) {
                    return 0.0;
                };
                return Hydrogen_Schema_Physics_Collision_Internal.clampUnit(1.0)((apx * abx + apy * aby) / abSquared);
            })();
            var closestX = v1.x + t * abx;
            var dx = v.x - closestX;
            var closestY = v1.y + t * aby;
            var dy = v.y - closestY;
            return Data_Number.sqrt(dx * dx + dy * dy);
        };
    };
};
var obbCorners = function (v) {
    var sinR = Data_Number.sin(v.rotation);
    var cosR = Data_Number.cos(v.rotation);
    var rotatePoint = function (lx) {
        return function (ly) {
            return {
                x: (v.center.x + lx * cosR) - ly * sinR,
                y: v.center.y + lx * sinR + ly * cosR
            };
        };
    };
    return {
        tl: rotatePoint(-v.halfWidth)(-v.halfHeight),
        tr: rotatePoint(v.halfWidth)(-v.halfHeight),
        bl: rotatePoint(-v.halfWidth)(v.halfHeight),
        br: rotatePoint(v.halfWidth)(v.halfHeight)
    };
};
var obbContains = function (v) {
    return function (v1) {
        var sinR = Data_Number.sin(-v.rotation);
        var dy = v1.y - v.center.y;
        var dx = v1.x - v.center.x;
        var cosR = Data_Number.cos(-v.rotation);
        var localX = dx * cosR - dy * sinR;
        var localY = dx * sinR + dy * cosR;
        return Hydrogen_Schema_Physics_Collision_Internal.absNum(localX) <= v.halfWidth && Hydrogen_Schema_Physics_Collision_Internal.absNum(localY) <= v.halfHeight;
    };
};
var obb = function (center) {
    return function (halfWidth) {
        return function (halfHeight) {
            return function (rotation) {
                return {
                    center: center,
                    halfWidth: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(halfWidth),
                    halfHeight: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(halfHeight),
                    rotation: rotation
                };
            };
        };
    };
};
var eqOBB = {
    eq: function (x) {
        return function (y) {
            return eq(x.center)(y.center) && x.halfHeight === y.halfHeight && x.halfWidth === y.halfWidth && x.rotation === y.rotation;
        };
    }
};
var ordOBB = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare1(x.halfHeight)(y.halfHeight);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare1(x.halfWidth)(y.halfWidth);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.rotation)(y.rotation);
        };
    },
    Eq0: function () {
        return eqOBB;
    }
};
var eqBoundingCircle = {
    eq: function (x) {
        return function (y) {
            return eq(x.center)(y.center) && x.radius === y.radius;
        };
    }
};
var ordBoundingCircle = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.radius)(y.radius);
        };
    },
    Eq0: function () {
        return eqBoundingCircle;
    }
};
var eqBoundingCapsule = {
    eq: function (x) {
        return function (y) {
            return eq(x.pointA)(y.pointA) && eq(x.pointB)(y.pointB) && x.radius === y.radius;
        };
    }
};
var ordBoundingCapsule = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.pointA)(y.pointA);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare(x.pointB)(y.pointB);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.radius)(y.radius);
        };
    },
    Eq0: function () {
        return eqBoundingCapsule;
    }
};
var eqAABB = {
    eq: function (x) {
        return function (y) {
            return x.maxX === y.maxX && x.maxY === y.maxY && x.minX === y.minX && x.minY === y.minY;
        };
    }
};
var ordAABB = {
    compare: function (x) {
        return function (y) {
            var v = compare1(x.maxX)(y.maxX);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare1(x.maxY)(y.maxY);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare1(x.minX)(y.minX);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.minY)(y.minY);
        };
    },
    Eq0: function () {
        return eqAABB;
    }
};
var circleMerge = function (v) {
    return function (v1) {
        var d = Hydrogen_Schema_Physics_Collision_Point.distance(v.center)(v1.center);
        var newRadius = (d + v.radius + v1.radius) / 2.0;
        var t = (function () {
            var $121 = d < 1.0e-4;
            if ($121) {
                return 0.5;
            };
            return (newRadius - v.radius) / d;
        })();
        var newCenter = {
            x: v.center.x + t * (v1.center.x - v.center.x),
            y: v.center.y + t * (v1.center.y - v.center.y)
        };
        return {
            center: newCenter,
            radius: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(newRadius)
        };
    };
};
var circleExpand = function (delta) {
    return function (v) {
        return {
            center: v.center,
            radius: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(v.radius + delta)
        };
    };
};
var circleContains = function (v) {
    return function (p) {
        return Hydrogen_Schema_Physics_Collision_Point.distanceSquared(v.center)(p) <= v.radius * v.radius;
    };
};
var capsuleContains = function (v) {
    return function (p) {
        var dist = pointToSegmentDistance(p)(v.pointA)(v.pointB);
        return dist <= v.radius;
    };
};
var boundingCircle = function (center) {
    return function (radius) {
        return {
            center: center,
            radius: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(radius)
        };
    };
};
var boundingCapsule = function (pointA) {
    return function (pointB) {
        return function (radius) {
            return {
                pointA: pointA,
                pointB: pointB,
                radius: Hydrogen_Schema_Physics_Collision_Internal.clampPositive(100000.0)(radius)
            };
        };
    };
};
var aabbSize = function (v) {
    return {
        width: v.maxX - v.minX,
        height: v.maxY - v.minY
    };
};
var aabbMerge = function (v) {
    return function (v1) {
        return {
            minX: Hydrogen_Schema_Physics_Collision_Internal.minNum(v.minX)(v1.minX),
            minY: Hydrogen_Schema_Physics_Collision_Internal.minNum(v.minY)(v1.minY),
            maxX: Hydrogen_Schema_Physics_Collision_Internal.maxNum(v.maxX)(v1.maxX),
            maxY: Hydrogen_Schema_Physics_Collision_Internal.maxNum(v.maxY)(v1.maxY)
        };
    };
};
var aabbHalfSize = function (v) {
    return {
        hx: (v.maxX - v.minX) / 2.0,
        hy: (v.maxY - v.minY) / 2.0
    };
};
var aabbExpand = function (margin) {
    return function (v) {
        return {
            minX: v.minX - margin,
            minY: v.minY - margin,
            maxX: v.maxX + margin,
            maxY: v.maxY + margin
        };
    };
};
var aabbContains = function (v) {
    return function (v1) {
        return v1.x >= v.minX && (v1.x <= v.maxX && (v1.y >= v.minY && v1.y <= v.maxY));
    };
};
var aabbCenter = function (v) {
    return {
        x: (v.minX + v.maxX) / 2.0,
        y: (v.minY + v.maxY) / 2.0
    };
};
var aabb = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                return {
                    minX: Hydrogen_Schema_Physics_Collision_Internal.minNum(x1)(x2),
                    minY: Hydrogen_Schema_Physics_Collision_Internal.minNum(y1)(y2),
                    maxX: Hydrogen_Schema_Physics_Collision_Internal.maxNum(x1)(x2),
                    maxY: Hydrogen_Schema_Physics_Collision_Internal.maxNum(y1)(y2)
                };
            };
        };
    };
};
var aabbFromPoints = function (v) {
    return function (v1) {
        return aabb(v.x)(v.y)(v1.x)(v1.y);
    };
};
export {
    AABB,
    aabb,
    aabbFromPoints,
    aabbCenter,
    aabbSize,
    aabbHalfSize,
    aabbContains,
    aabbExpand,
    aabbMerge,
    BoundingCircle,
    boundingCircle,
    circleContains,
    circleExpand,
    circleMerge,
    BoundingCapsule,
    boundingCapsule,
    capsuleContains,
    pointToSegmentDistance,
    OBB,
    obb,
    obbCorners,
    obbContains,
    eqAABB,
    ordAABB,
    showAABB,
    eqBoundingCircle,
    ordBoundingCircle,
    showBoundingCircle,
    eqBoundingCapsule,
    ordBoundingCapsule,
    showBoundingCapsule,
    eqOBB,
    ordOBB,
    showOBB
};
//# sourceMappingURL=index.js.map
