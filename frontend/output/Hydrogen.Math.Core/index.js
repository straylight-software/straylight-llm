// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Int from "../Data.Int/index.js";
var tau = 6.283185307179586;
var sqrt2 = 1.4142135623730951;
var sqrt1_2 = 0.7071067811865476;
var sign = function (x) {
    if (x > 0.0) {
        return 1.0;
    };
    if (x < 0.0) {
        return -1.0;
    };
    if (Data_Boolean.otherwise) {
        return 0.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 153, column 1 - line 153, column 25): " + [ x.constructor.name ]);
};
var reduceForLog = function ($copy_x) {
    return function ($copy_n) {
        var $tco_var_x = $copy_x;
        var $tco_done = false;
        var $tco_result;
        function $tco_loop(x, n) {
            if (x >= 2.0) {
                $tco_var_x = x / 2.0;
                $copy_n = n + 1 | 0;
                return;
            };
            if (x < 0.5) {
                $tco_var_x = x * 2.0;
                $copy_n = n - 1 | 0;
                return;
            };
            if (Data_Boolean.otherwise) {
                $tco_done = true;
                return {
                    value: x,
                    exponent: n
                };
            };
            throw new Error("Failed pattern match at Hydrogen.Math.Core (line 396, column 1 - line 396, column 70): " + [ x.constructor.name, n.constructor.name ]);
        };
        while (!$tco_done) {
            $tco_result = $tco_loop($tco_var_x, $copy_n);
        };
        return $tco_result;
    };
};
var pi = 3.141592653589793;
var radiansToDegrees = function (rad) {
    return (rad * 180.0) / pi;
};
var negativeInfinity = /* #__PURE__ */ (function () {
    return -1.0 / 0.0;
})();
var min = function (a) {
    return function (b) {
        var $22 = a <= b;
        if ($22) {
            return a;
        };
        return b;
    };
};
var max = function (a) {
    return function (b) {
        var $23 = a >= b;
        if ($23) {
            return a;
        };
        return b;
    };
};
var maxArray = function (arr) {
    return Data_Array.foldl(max)(negativeInfinity)(arr);
};
var log2e = 1.4426950408889634;
var log10e = 0.4342944819032518;
var ln2 = 0.6931471805599453;
var ln10 = 2.302585092994046;
var lerp = function (a) {
    return function (b) {
        return function (t) {
            return a + (b - a) * t;
        };
    };
};
var $$isNaN = function (x) {
    return x !== x;
};
var inverseLerp = function (a) {
    return function (b) {
        return function (v) {
            return (v - a) / (b - a);
        };
    };
};
var remap = function (inMin) {
    return function (inMax) {
        return function (outMin) {
            return function (outMax) {
                return function (v) {
                    var t = inverseLerp(inMin)(inMax)(v);
                    return lerp(outMin)(outMax)(t);
                };
            };
        };
    };
};
var initialGuess = function (x) {
    var $24 = x >= 1.0;
    if ($24) {
        return x / 2.0;
    };
    return x * 2.0;
};
var infinity = /* #__PURE__ */ (function () {
    return 1.0 / 0.0;
})();
var $$isFinite = function (x) {
    return !$$isNaN(x) && (x !== infinity && x !== negativeInfinity);
};
var minArray = function (arr) {
    return Data_Array.foldl(min)(infinity)(arr);
};
var fround = function (x) {
    return x;
};
var floor = function (x) {
    var toNumber = function (n) {
        return Data_Int.toNumber(n);
    };
    var i = Data_Int.floor(x);
    return toNumber(i);
};
var isInteger = function (x) {
    return $$isFinite(x) && floor(x) === x;
};
var isOdd = function (x) {
    var half = x / 2.0;
    return floor(half) !== half;
};
var round = function (x) {
    var fl = floor(x);
    var diff = x - fl;
    var $25 = diff >= 0.5;
    if ($25) {
        return fl + 1.0;
    };
    return fl;
};
var reduceAngle = function (x) {
    var reduced = x - tau * round(x / tau);
    return reduced;
};
var e = 2.718281828459045;
var degreesToRadians = function (deg) {
    return (deg * pi) / 180.0;
};
var clamp = function (minVal) {
    return function (maxVal) {
        return function (x) {
            return min(maxVal)(max(minVal)(x));
        };
    };
};
var smootherstep = function (edge0) {
    return function (edge1) {
        return function (x) {
            var t = clamp(0.0)(1.0)((x - edge0) / (edge1 - edge0));
            return t * t * t * (t * (t * 6.0 - 15.0) + 10.0);
        };
    };
};
var smoothstep = function (edge0) {
    return function (edge1) {
        return function (x) {
            var t = clamp(0.0)(1.0)((x - edge0) / (edge1 - edge0));
            return t * t * (3.0 - 2.0 * t);
        };
    };
};
var ceil = function (x) {
    var fl = floor(x);
    var $26 = x === fl;
    if ($26) {
        return fl;
    };
    return fl + 1.0;
};
var trunc = function (x) {
    var $27 = x >= 0.0;
    if ($27) {
        return floor(x);
    };
    return ceil(x);
};
var abs = function (x) {
    var $28 = x < 0.0;
    if ($28) {
        return -x;
    };
    return x;
};
var asinGo = function ($copy_sum) {
    return function ($copy_term) {
        return function ($copy_x2) {
            return function ($copy_n) {
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_var_x2 = $copy_x2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(sum, term, x2, n) {
                    if (n > 50) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var k = (2 * n | 0) - 1 | 0;
                        var newTerm = (term * x2 * toNumber(k) * toNumber(k)) / (toNumber(2 * n | 0) * toNumber((2 * n | 0) + 1 | 0));
                        var newSum = sum + newTerm;
                        var $33 = abs(newTerm) < 1.0e-16 * abs(newSum);
                        if ($33) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $tco_var_x2 = x2;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 551, column 1 - line 551, column 54): " + [ sum.constructor.name, term.constructor.name, x2.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_sum, $tco_var_term, $tco_var_x2, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var asinTaylor = function (x) {
    return asinGo(x)(x)(x * x)(1);
};
var atanGo = function ($copy_sum) {
    return function ($copy_term) {
        return function ($copy_x2) {
            return function ($copy_n) {
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_var_x2 = $copy_x2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(sum, term, x2, n) {
                    if (n > 50) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var newTerm = -term * x2;
                        var k = (2 * n | 0) + 1 | 0;
                        var contrib = newTerm / toNumber(k);
                        var newSum = sum + contrib;
                        var $38 = abs(contrib) < 1.0e-16 * abs(newSum);
                        if ($38) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $tco_var_x2 = x2;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 588, column 1 - line 588, column 54): " + [ sum.constructor.name, term.constructor.name, x2.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_sum, $tco_var_term, $tco_var_x2, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var atanSmall = function (x) {
    return atanGo(x)(x)(x * x)(1);
};
var atan = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (x === infinity) {
        return pi / 2.0;
    };
    if (x === negativeInfinity) {
        return -pi / 2.0;
    };
    if (abs(x) > 1.0) {
        var $40 = x > 0.0;
        if ($40) {
            return pi / 2.0 - atanSmall(1.0 / x);
        };
        return -pi / 2.0 - atanSmall(1.0 / x);
    };
    if (Data_Boolean.otherwise) {
        return atanSmall(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 573, column 1 - line 573, column 25): " + [ x.constructor.name ]);
};
var atan2 = function (y) {
    return function (x) {
        if ($$isNaN(x) || $$isNaN(y)) {
            return 0.0 / 0.0;
        };
        if (x > 0.0) {
            return atan(y / x);
        };
        if (x < 0.0 && y >= 0.0) {
            return atan(y / x) + pi;
        };
        if (x < 0.0 && y < 0.0) {
            return atan(y / x) - pi;
        };
        if (x === 0.0 && y > 0.0) {
            return pi / 2.0;
        };
        if (x === 0.0 && y < 0.0) {
            return -pi / 2.0;
        };
        if (Data_Boolean.otherwise) {
            return 0.0;
        };
        throw new Error("Failed pattern match at Hydrogen.Math.Core (line 604, column 1 - line 604, column 36): " + [ y.constructor.name, x.constructor.name ]);
    };
};
var atanhSeriesGo = function ($copy_sum) {
    return function ($copy_term) {
        return function ($copy_y2) {
            return function ($copy_n) {
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_var_y2 = $copy_y2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(sum, term, y2, n) {
                    if (n > 50) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var newTerm = term * y2;
                        var k = (2 * n | 0) + 1 | 0;
                        var contrib = newTerm / toNumber(k);
                        var newSum = sum + contrib;
                        var $47 = abs(contrib) < 1.0e-16 * abs(newSum);
                        if ($47) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $tco_var_y2 = y2;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 413, column 1 - line 413, column 61): " + [ sum.constructor.name, term.constructor.name, y2.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_sum, $tco_var_term, $tco_var_y2, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var atanhSeries = function (y) {
    return atanhSeriesGo(y)(y)(y * y)(1);
};
var logNear1 = function (x) {
    var y = (x - 1.0) / (x + 1.0);
    return 2.0 * atanhSeries(y);
};
var log = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (x < 0.0) {
        return 0.0 / 0.0;
    };
    if (x === 0.0) {
        return negativeInfinity;
    };
    if (x === infinity) {
        return infinity;
    };
    if (x === 1.0) {
        return 0.0;
    };
    if (Data_Boolean.otherwise) {
        var toNumber = function (i) {
            return Data_Int.toNumber(i);
        };
        var reduced = reduceForLog(x)(0);
        return logNear1(reduced.value) + toNumber(reduced.exponent) * ln2;
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 380, column 1 - line 380, column 24): " + [ x.constructor.name ]);
};
var atanh = function (x) {
    if (abs(x) >= 1.0) {
        var $50 = x > 0.0;
        if ($50) {
            return infinity;
        };
        return negativeInfinity;
    };
    if (Data_Boolean.otherwise) {
        return 0.5 * log((1.0 + x) / (1.0 - x));
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 646, column 1 - line 646, column 26): " + [ x.constructor.name ]);
};
var log10 = function (x) {
    return log(x) / ln10;
};
var log2 = function (x) {
    return log(x) / ln2;
};
var log1p = function (x) {
    if (abs(x) < 1.0e-5) {
        return x - (x * x) / 2.0;
    };
    if (Data_Boolean.otherwise) {
        return log(1.0 + x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 437, column 1 - line 437, column 26): " + [ x.constructor.name ]);
};
var newtonCbrt = function ($copy_x) {
    return function ($copy_guess) {
        return function ($copy_iter) {
            var $tco_var_x = $copy_x;
            var $tco_var_guess = $copy_guess;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(x, guess, iter) {
                if (iter >= 100) {
                    $tco_done = true;
                    return guess;
                };
                if (Data_Boolean.otherwise) {
                    var next = (2.0 * guess + x / (guess * guess)) / 3.0;
                    var $55 = abs(next - guess) < 1.0e-15 * abs(guess);
                    if ($55) {
                        $tco_done = true;
                        return next;
                    };
                    $tco_var_x = x;
                    $tco_var_guess = next;
                    $copy_iter = iter + 1 | 0;
                    return;
                };
                throw new Error("Failed pattern match at Hydrogen.Math.Core (line 278, column 1 - line 278, column 48): " + [ x.constructor.name, guess.constructor.name, iter.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_x, $tco_var_guess, $copy_iter);
            };
            return $tco_result;
        };
    };
};
var cbrtPositive = function (x) {
    return newtonCbrt(x)(x / 3.0)(0);
};
var cbrt = function (x) {
    if (x === 0.0) {
        return 0.0;
    };
    if (x < 0.0) {
        return -cbrtPositive(-x);
    };
    if (Data_Boolean.otherwise) {
        return cbrtPositive(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 267, column 1 - line 267, column 25): " + [ x.constructor.name ]);
};
var newtonSqrt = function ($copy_x) {
    return function ($copy_guess) {
        return function ($copy_iter) {
            var $tco_var_x = $copy_x;
            var $tco_var_guess = $copy_guess;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(x, guess, iter) {
                if (iter >= 100) {
                    $tco_done = true;
                    return guess;
                };
                if (Data_Boolean.otherwise) {
                    var next = (guess + x / guess) / 2.0;
                    var $60 = abs(next - guess) < 1.0e-15 * abs(guess);
                    if ($60) {
                        $tco_done = true;
                        return next;
                    };
                    $tco_var_x = x;
                    $tco_var_guess = next;
                    $copy_iter = iter + 1 | 0;
                    return;
                };
                throw new Error("Failed pattern match at Hydrogen.Math.Core (line 257, column 1 - line 257, column 48): " + [ x.constructor.name, guess.constructor.name, iter.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_x, $tco_var_guess, $copy_iter);
            };
            return $tco_result;
        };
    };
};
var sqrt = function (x) {
    if (x < 0.0) {
        return 0.0 / 0.0;
    };
    if (x === 0.0) {
        return 0.0;
    };
    if (x === infinity) {
        return infinity;
    };
    if (Data_Boolean.otherwise) {
        return newtonSqrt(x)(initialGuess(x))(0);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 241, column 1 - line 241, column 25): " + [ x.constructor.name ]);
};
var acosh = function (x) {
    if (x < 1.0) {
        return 0.0 / 0.0;
    };
    if (Data_Boolean.otherwise) {
        return log(x + sqrt(x * x - 1.0));
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 640, column 1 - line 640, column 26): " + [ x.constructor.name ]);
};
var asin = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (x < -1.0 || x > 1.0) {
        return 0.0 / 0.0;
    };
    if (x === 1.0) {
        return pi / 2.0;
    };
    if (x === -1.0) {
        return -pi / 2.0;
    };
    if (abs(x) <= 0.5) {
        return asinTaylor(x);
    };
    if (x > 0.5) {
        return pi / 2.0 - 2.0 * asinTaylor(sqrt((1.0 - x) / 2.0));
    };
    if (Data_Boolean.otherwise) {
        return -(pi / 2.0) + 2.0 * asinTaylor(sqrt((1.0 + x) / 2.0));
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 537, column 1 - line 537, column 25): " + [ x.constructor.name ]);
};
var acos = function (x) {
    return pi / 2.0 - asin(x);
};
var asinh = function (x) {
    return log(x + sqrt(x * x + 1.0));
};
var hypot = function (x) {
    return function (y) {
        var ay = abs(y);
        var ax = abs(x);
        var maxVal = max(ax)(ay);
        var $64 = maxVal === 0.0;
        if ($64) {
            return 0.0;
        };
        return maxVal * sqrt((ax / maxVal) * (ax / maxVal) + (ay / maxVal) * (ay / maxVal));
    };
};
var hypot3 = function (x) {
    return function (y) {
        return function (z) {
            var az = abs(z);
            var ay = abs(y);
            var ax = abs(x);
            var maxVal = max(max(ax)(ay))(az);
            var $65 = maxVal === 0.0;
            if ($65) {
                return 0.0;
            };
            return maxVal * sqrt((ax / maxVal) * (ax / maxVal) + (ay / maxVal) * (ay / maxVal) + (az / maxVal) * (az / maxVal));
        };
    };
};
var taylorCos = function ($copy_sum) {
    return function ($copy_term) {
        return function ($copy_x2) {
            return function ($copy_n) {
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_var_x2 = $copy_x2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(sum, term, x2, n) {
                    if (n > 20) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var k = (2 * n | 0) - 1 | 0;
                        var newTerm = (-term * x2) / (toNumber(k) * toNumber(k + 1 | 0));
                        var newSum = sum + newTerm;
                        var $70 = abs(newTerm) < 1.0e-16 * abs(newSum);
                        if ($70) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $tco_var_x2 = x2;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 515, column 1 - line 515, column 57): " + [ sum.constructor.name, term.constructor.name, x2.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_sum, $tco_var_term, $tco_var_x2, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var cosTaylor = function (x) {
    var x2 = x * x;
    return taylorCos(1.0)(1.0)(x2)(1);
};
var cosReduced = function (x) {
    if (x > pi / 2.0) {
        return -cosTaylor(pi - x);
    };
    if (x < -pi / 2.0) {
        return -cosTaylor(pi + x);
    };
    if (Data_Boolean.otherwise) {
        return cosTaylor(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 503, column 1 - line 503, column 31): " + [ x.constructor.name ]);
};
var cos = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (!$$isFinite(x)) {
        return 0.0 / 0.0;
    };
    if (Data_Boolean.otherwise) {
        var reduced = reduceAngle(x);
        return cosReduced(reduced);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 494, column 1 - line 494, column 24): " + [ x.constructor.name ]);
};
var taylorExp = function ($copy_x) {
    return function ($copy_sum) {
        return function ($copy_term) {
            return function ($copy_n) {
                var $tco_var_x = $copy_x;
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(x, sum, term, n) {
                    if (n > 30) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var newTerm = (term * x) / toNumber(n);
                        var newSum = sum + newTerm;
                        var $77 = abs(newTerm) < 1.0e-16 * abs(newSum);
                        if ($77) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_x = x;
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 357, column 1 - line 357, column 57): " + [ x.constructor.name, sum.constructor.name, term.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_x, $tco_var_sum, $tco_var_term, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var expTaylor = function (x) {
    return taylorExp(x)(1.0)(1.0)(1);
};
var exp = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (x === infinity) {
        return infinity;
    };
    if (x === negativeInfinity) {
        return 0.0;
    };
    if (x === 0.0) {
        return 1.0;
    };
    if (x > 709.0) {
        return infinity;
    };
    if (x < -745.0) {
        return 0.0;
    };
    if (abs(x) > 1.0) {
        var half = exp(x / 2.0);
        return half * half;
    };
    if (Data_Boolean.otherwise) {
        return expTaylor(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 339, column 1 - line 339, column 24): " + [ x.constructor.name ]);
};
var cosh = function (x) {
    return (exp(x) + exp(-x)) / 2.0;
};
var expm1 = function (x) {
    if (abs(x) < 1.0e-5) {
        return x + (x * x) / 2.0;
    };
    if (Data_Boolean.otherwise) {
        return exp(x) - 1.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 371, column 1 - line 371, column 26): " + [ x.constructor.name ]);
};
var pow = function (base) {
    return function (exponent) {
        if (exponent === 0.0) {
            return 1.0;
        };
        if (base === 0.0) {
            var $82 = exponent > 0.0;
            if ($82) {
                return 0.0;
            };
            return infinity;
        };
        if (base === 1.0) {
            return 1.0;
        };
        if (exponent === 1.0) {
            return base;
        };
        if (base < 0.0 && !isInteger(exponent)) {
            return 0.0 / 0.0;
        };
        if (base < 0.0) {
            var result = exp(exponent * log(-base));
            var $83 = isOdd(trunc(exponent));
            if ($83) {
                return -result;
            };
            return result;
        };
        if (Data_Boolean.otherwise) {
            return exp(exponent * log(base));
        };
        throw new Error("Failed pattern match at Hydrogen.Math.Core (line 313, column 1 - line 313, column 34): " + [ base.constructor.name, exponent.constructor.name ]);
    };
};
var sinh = function (x) {
    if (abs(x) < 1.0e-5) {
        return x;
    };
    if (Data_Boolean.otherwise) {
        return (exp(x) - exp(-x)) / 2.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 619, column 1 - line 619, column 25): " + [ x.constructor.name ]);
};
var tanh = function (x) {
    if (x > 20.0) {
        return 1.0;
    };
    if (x < -20.0) {
        return -1.0;
    };
    if (Data_Boolean.otherwise) {
        return sinh(x) / cosh(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 629, column 1 - line 629, column 25): " + [ x.constructor.name ]);
};
var taylorSin = function ($copy_sum) {
    return function ($copy_term) {
        return function ($copy_x2) {
            return function ($copy_n) {
                var $tco_var_sum = $copy_sum;
                var $tco_var_term = $copy_term;
                var $tco_var_x2 = $copy_x2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(sum, term, x2, n) {
                    if (n > 20) {
                        $tco_done = true;
                        return sum;
                    };
                    if (Data_Boolean.otherwise) {
                        var toNumber = function (i) {
                            return Data_Int.toNumber(i);
                        };
                        var k = 2 * n | 0;
                        var newTerm = (-term * x2) / (toNumber(k) * toNumber(k + 1 | 0));
                        var newSum = sum + newTerm;
                        var $90 = abs(newTerm) < 1.0e-16 * abs(newSum);
                        if ($90) {
                            $tco_done = true;
                            return newSum;
                        };
                        $tco_var_sum = newSum;
                        $tco_var_term = newTerm;
                        $tco_var_x2 = x2;
                        $copy_n = n + 1 | 0;
                        return;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 477, column 1 - line 477, column 57): " + [ sum.constructor.name, term.constructor.name, x2.constructor.name, n.constructor.name ]);
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_sum, $tco_var_term, $tco_var_x2, $copy_n);
                };
                return $tco_result;
            };
        };
    };
};
var sinTaylor = function (x) {
    return taylorSin(x)(x)(x * x)(1);
};
var sinReduced = function (x) {
    if (x > pi / 2.0) {
        return sinTaylor(pi - x);
    };
    if (x < -pi / 2.0) {
        return -sinTaylor(pi + x);
    };
    if (Data_Boolean.otherwise) {
        return sinTaylor(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 467, column 1 - line 467, column 31): " + [ x.constructor.name ]);
};
var sin = function (x) {
    if ($$isNaN(x)) {
        return x;
    };
    if (!$$isFinite(x)) {
        return 0.0 / 0.0;
    };
    if (Data_Boolean.otherwise) {
        var reduced = reduceAngle(x);
        return sinReduced(reduced);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core (line 451, column 1 - line 451, column 24): " + [ x.constructor.name ]);
};
var tan = function (x) {
    return sin(x) / cos(x);
};
export {
    pi,
    e,
    tau,
    sqrt2,
    sqrt1_2,
    ln2,
    ln10,
    log2e,
    log10e,
    pow,
    sqrt,
    cbrt,
    hypot,
    hypot3,
    sin,
    cos,
    tan,
    asin,
    acos,
    atan,
    atan2,
    sinh,
    cosh,
    tanh,
    asinh,
    acosh,
    atanh,
    exp,
    expm1,
    log,
    log10,
    log2,
    log1p,
    floor,
    ceil,
    round,
    trunc,
    fround,
    abs,
    sign,
    min,
    max,
    minArray,
    maxArray,
    clamp,
    $$isNaN as isNaN,
    $$isFinite as isFinite,
    isInteger,
    infinity,
    negativeInfinity,
    lerp,
    inverseLerp,
    remap,
    smoothstep,
    smootherstep,
    degreesToRadians,
    radiansToDegrees
};
//# sourceMappingURL=index.js.map
