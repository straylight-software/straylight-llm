// Generated by purs version 0.15.15
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Geometry_Angle from "../Hydrogen.Schema.Geometry.Angle/index.js";
import * as Hydrogen_Schema_Geometry_Point from "../Hydrogen.Schema.Geometry.Point/index.js";
var show = /* #__PURE__ */ Data_Show.show(Hydrogen_Schema_Geometry_Point.showPoint2D);
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var show2 = /* #__PURE__ */ Data_Show.show(Hydrogen_Schema_Geometry_Angle.showDegrees);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var eq = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Geometry_Point.eqPoint2D);
var eq1 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Geometry_Angle.eqDegrees);
var compare = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Geometry_Point.ordPoint2D);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Geometry_Angle.ordDegrees);
var compare2 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var toRadians = /* #__PURE__ */ Hydrogen_Schema_Geometry_Angle.toRadians(Hydrogen_Schema_Geometry_Angle.angularUnitDegrees);
var Clockwise = /* #__PURE__ */ (function () {
    function Clockwise() {

    };
    Clockwise.value = new Clockwise();
    return Clockwise;
})();
var CounterClockwise = /* #__PURE__ */ (function () {
    function CounterClockwise() {

    };
    CounterClockwise.value = new CounterClockwise();
    return CounterClockwise;
})();
var Ring = function (x) {
    return x;
};
var Arc = function (x) {
    return x;
};
var Sector = function (x) {
    return x;
};
var translateArc = function (dx) {
    return function (dy) {
        return function (v) {
            return {
                center: Hydrogen_Schema_Geometry_Point.translate2D(dx)(dy)(v.center),
                radius: v.radius,
                startAngle: v.startAngle,
                endAngle: v.endAngle,
                direction: v.direction
            };
        };
    };
};
var sweepAngle = function (v) {
    var startDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v.startAngle);
    var endDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v.endAngle);
    var diff = (function () {
        if (v.direction instanceof CounterClockwise) {
            var $105 = endDeg >= startDeg;
            if ($105) {
                return endDeg - startDeg;
            };
            return (360.0 - startDeg) + endDeg;
        };
        if (v.direction instanceof Clockwise) {
            var $106 = startDeg >= endDeg;
            if ($106) {
                return startDeg - endDeg;
            };
            return (360.0 - endDeg) + startDeg;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 218, column 14 - line 226, column 41): " + [ v.direction.constructor.name ]);
    })();
    return diff;
};
var showRing = {
    show: function (v) {
        return "(Ring " + (show(v.center) + (" inner=" + (show1(v.innerRadius) + (" outer=" + (show1(v.outerRadius) + ")")))));
    }
};
var showArcDirection = {
    show: function (v) {
        if (v instanceof Clockwise) {
            return "Clockwise";
        };
        if (v instanceof CounterClockwise) {
            return "CounterClockwise";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 144, column 1 - line 146, column 45): " + [ v.constructor.name ]);
    }
};
var show3 = /* #__PURE__ */ Data_Show.show(showArcDirection);
var showArcInstance = {
    show: function (v) {
        return "(Arc " + (show(v.center) + (" r=" + (show1(v.radius) + (" " + (show2(v.startAngle) + ("->" + (show2(v.endAngle) + (" " + (show3(v.direction) + ")")))))))));
    }
};
var show4 = /* #__PURE__ */ Data_Show.show(showArcInstance);
var showSector = {
    show: function (v) {
        return "(Sector " + (show4(v) + ")");
    }
};
var showArc = function (v) {
    return "Arc { center: " + (show(v.center) + (", radius: " + (show1(v.radius) + (", " + (show1(v.startAngle) + ("\xb0 -> " + (show1(v.endAngle) + ("\xb0" + (", " + (show3(v.direction) + " }"))))))))));
};
var scaleArc = function (factor) {
    return function (v) {
        var $116 = factor > 0.0;
        if ($116) {
            return new Data_Maybe.Just({
                center: v.center,
                radius: v.radius * factor,
                startAngle: v.startAngle,
                endAngle: v.endAngle,
                direction: v.direction
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var ringArea = function (v) {
    var startDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v.startAngle);
    var endDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v.endAngle);
    var sweep = (function () {
        if (v.direction instanceof CounterClockwise) {
            var $119 = endDeg >= startDeg;
            if ($119) {
                return endDeg - startDeg;
            };
            return (360.0 - startDeg) + endDeg;
        };
        if (v.direction instanceof Clockwise) {
            var $120 = startDeg >= endDeg;
            if ($120) {
                return startDeg - endDeg;
            };
            return (360.0 - endDeg) + startDeg;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 367, column 15 - line 375, column 41): " + [ v.direction.constructor.name ]);
    })();
    var sweepRad = (sweep * Data_Number.pi) / 180.0;
    return 0.5 * (v.outerRadius * v.outerRadius - v.innerRadius * v.innerRadius) * sweepRad;
};
var reverseArc = function (v) {
    var flipDirection = function (v1) {
        if (v1 instanceof Clockwise) {
            return CounterClockwise.value;
        };
        if (v1 instanceof CounterClockwise) {
            return Clockwise.value;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 420, column 5 - line 420, column 50): " + [ v1.constructor.name ]);
    };
    return {
        center: v.center,
        radius: v.radius,
        startAngle: v.endAngle,
        endAngle: v.startAngle,
        direction: flipDirection(v.direction)
    };
};
var reflectArcCenter = function (v) {
    return {
        center: Hydrogen_Schema_Geometry_Point.point2D(-v.center.x)(-v.center.y),
        radius: v.radius,
        startAngle: v.startAngle,
        endAngle: v.endAngle,
        direction: v.direction
    };
};
var pointAtParameter = function (t) {
    return function (v) {
        var startDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v.startAngle);
        var v1 = sweepAngle(v);
        var angleDeg = (function () {
            if (v.direction instanceof CounterClockwise) {
                return startDeg + t * v1;
            };
            if (v.direction instanceof Clockwise) {
                return startDeg - t * v1;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 259, column 18 - line 261, column 42): " + [ v.direction.constructor.name ]);
        })();
        var angleRad = (angleDeg * Data_Number.pi) / 180.0;
        var y = v.center.y + v.radius * Data_Number.sin(angleRad);
        var x = v.center.x + v.radius * Data_Number.cos(angleRad);
        return Hydrogen_Schema_Geometry_Point.point2D(x)(y);
    };
};
var samplePoints = function (n) {
    return function (arc) {
        var snoc = function (arr) {
            return function (x) {
                return append1(arr)([ x ]);
            };
        };
        var rangeHelper = function ($copy_cur) {
            return function ($copy_end) {
                return function ($copy_acc) {
                    var $tco_var_cur = $copy_cur;
                    var $tco_var_end = $copy_end;
                    var $tco_done = false;
                    var $tco_result;
                    function $tco_loop(cur, end, acc) {
                        var $130 = cur > end;
                        if ($130) {
                            $tco_done = true;
                            return acc;
                        };
                        $tco_var_cur = cur + 1 | 0;
                        $tco_var_end = end;
                        $copy_acc = snoc(acc)(cur);
                        return;
                    };
                    while (!$tco_done) {
                        $tco_result = $tco_loop($tco_var_cur, $tco_var_end, $copy_acc);
                    };
                    return $tco_result;
                };
            };
        };
        var range = function (start) {
            return function (end) {
                return rangeHelper(start)(end)([  ]);
            };
        };
        var $131 = n <= 1;
        if ($131) {
            return [ pointAtParameter(0.5)(arc) ];
        };
        return map(function (i) {
            return pointAtParameter(Data_Int.toNumber(i) / Data_Int.toNumber(n - 1 | 0))(arc);
        })(range(0)(n - 1 | 0));
    };
};
var startPoint = /* #__PURE__ */ pointAtParameter(0.0);
var mkRing = function (center) {
    return function (innerRadius) {
        return function (outerRadius) {
            return function (startAngle) {
                return function (endAngle) {
                    return function (direction) {
                        var $132 = innerRadius > 0.0 && outerRadius > innerRadius;
                        if ($132) {
                            return new Data_Maybe.Just({
                                center: center,
                                innerRadius: innerRadius,
                                outerRadius: outerRadius,
                                startAngle: startAngle,
                                endAngle: endAngle,
                                direction: direction
                            });
                        };
                        return Data_Maybe.Nothing.value;
                    };
                };
            };
        };
    };
};
var mkArcUnsafe = function (center) {
    return function (radius) {
        return function (startAngle) {
            return function (endAngle) {
                return function (direction) {
                    return {
                        center: center,
                        radius: radius,
                        startAngle: startAngle,
                        endAngle: endAngle,
                        direction: direction
                    };
                };
            };
        };
    };
};
var mkArc = function (center) {
    return function (radius) {
        return function (startAngle) {
            return function (endAngle) {
                return function (direction) {
                    var $133 = radius > 0.0;
                    if ($133) {
                        return new Data_Maybe.Just({
                            center: center,
                            radius: radius,
                            startAngle: startAngle,
                            endAngle: endAngle,
                            direction: direction
                        });
                    };
                    return Data_Maybe.Nothing.value;
                };
            };
        };
    };
};
var mkSector = function (center) {
    return function (radius) {
        return function (startAngle) {
            return function (endAngle) {
                return function (direction) {
                    var v = mkArc(center)(radius)(startAngle)(endAngle)(direction);
                    if (v instanceof Data_Maybe.Just) {
                        return new Data_Maybe.Just(v.value0);
                    };
                    if (v instanceof Data_Maybe.Nothing) {
                        return Data_Maybe.Nothing.value;
                    };
                    throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 322, column 3 - line 324, column 23): " + [ v.constructor.name ]);
                };
            };
        };
    };
};
var midPoint = /* #__PURE__ */ pointAtParameter(0.5);
var eqArcDirection = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Clockwise && y instanceof Clockwise) {
                return true;
            };
            if (x instanceof CounterClockwise && y instanceof CounterClockwise) {
                return true;
            };
            return false;
        };
    }
};
var eq3 = /* #__PURE__ */ Data_Eq.eq(eqArcDirection);
var eqRing = {
    eq: function (x) {
        return function (y) {
            return eq(x.center)(y.center) && eq3(x.direction)(y.direction) && eq1(x.endAngle)(y.endAngle) && x.innerRadius === y.innerRadius && x.outerRadius === y.outerRadius && eq1(x.startAngle)(y.startAngle);
        };
    }
};
var ordArcDirection = {
    compare: function (x) {
        return function (y) {
            if (x instanceof Clockwise && y instanceof Clockwise) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Clockwise) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Clockwise) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof CounterClockwise && y instanceof CounterClockwise) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqArcDirection;
    }
};
var compare3 = /* #__PURE__ */ Data_Ord.compare(ordArcDirection);
var ordRing = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare3(x.direction)(y.direction);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare1(x.endAngle)(y.endAngle);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v3 = compare2(x.innerRadius)(y.innerRadius);
            if (v3 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v3 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v4 = compare2(x.outerRadius)(y.outerRadius);
            if (v4 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v4 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.startAngle)(y.startAngle);
        };
    },
    Eq0: function () {
        return eqRing;
    }
};
var eqArc = {
    eq: function (x) {
        return function (y) {
            return eq(x.center)(y.center) && eq3(x.direction)(y.direction) && eq1(x.endAngle)(y.endAngle) && x.radius === y.radius && eq1(x.startAngle)(y.startAngle);
        };
    }
};
var eq4 = /* #__PURE__ */ Data_Eq.eq(eqArc);
var eqSector = {
    eq: function (x) {
        return function (y) {
            return eq4(x)(y);
        };
    }
};
var ordArc = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare3(x.direction)(y.direction);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare1(x.endAngle)(y.endAngle);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v3 = compare2(x.radius)(y.radius);
            if (v3 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v3 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.startAngle)(y.startAngle);
        };
    },
    Eq0: function () {
        return eqArc;
    }
};
var compare4 = /* #__PURE__ */ Data_Ord.compare(ordArc);
var ordSector = {
    compare: function (x) {
        return function (y) {
            return compare4(x)(y);
        };
    },
    Eq0: function () {
        return eqSector;
    }
};
var endPoint = /* #__PURE__ */ pointAtParameter(1.0);
var containsAngle = function (v) {
    return function (v1) {
        var normalizeAngle = function (deg) {
            var wrapped = deg - 360.0 * Data_Int.toNumber(Data_Int.floor(deg / 360.0));
            var $163 = wrapped < 0.0;
            if ($163) {
                return wrapped + 360.0;
            };
            return wrapped;
        };
        var startDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v1.startAngle);
        var normStart = normalizeAngle(startDeg);
        var normAngle = normalizeAngle(v);
        var endDeg = Hydrogen_Schema_Geometry_Angle.unwrapDegrees(v1.endAngle);
        var normEnd = normalizeAngle(endDeg);
        if (v1.direction instanceof CounterClockwise) {
            var $165 = normEnd >= normStart;
            if ($165) {
                return normAngle >= normStart && normAngle <= normEnd;
            };
            return normAngle >= normStart || normAngle <= normEnd;
        };
        if (v1.direction instanceof Clockwise) {
            var $166 = normStart >= normEnd;
            if ($166) {
                return normAngle <= normStart && normAngle >= normEnd;
            };
            return normAngle <= normStart || normAngle >= normEnd;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Arc (line 289, column 6 - line 297, column 58): " + [ v1.direction.constructor.name ]);
    };
};
var arcStartAngle = function (v) {
    return v.startAngle;
};
var arcRadius = function (v) {
    return v.radius;
};
var arcLengthRadians = function (v) {
    var v1 = sweepAngle(v);
    var sweepRad = Hydrogen_Schema_Geometry_Angle.unwrapRadians(toRadians(v1));
    return v.radius * sweepRad;
};
var arcLength = function (v) {
    var v1 = sweepAngle(v);
    var sweepRad = (v1 * Data_Number.pi) / 180.0;
    return v.radius * sweepRad;
};
var compareByLength = function (a1) {
    return function (a2) {
        return compare2(arcLength(a1))(arcLength(a2));
    };
};
var arcEndAngle = function (v) {
    return v.endAngle;
};
var arcDirection = function (v) {
    return v.direction;
};
var arcCenter = function (v) {
    return v.center;
};
var arcArea = function (v) {
    var v1 = sweepAngle(v);
    var sweepRad = (v1 * Data_Number.pi) / 180.0;
    return 0.5 * v.radius * v.radius * sweepRad;
};
var sectorArea = function (v) {
    return arcArea(v);
};
var angularDistance = function (a) {
    return function (b) {
        var diff = Hydrogen_Schema_Geometry_Angle.subtractAngle(a)(b);
        var normalized = Data_Number.abs(diff);
        var $180 = normalized > 180.0;
        if ($180) {
            return 360.0 - normalized;
        };
        return normalized;
    };
};
export {
    Arc,
    mkArc,
    mkArcUnsafe,
    arcCenter,
    arcRadius,
    arcStartAngle,
    arcEndAngle,
    arcDirection,
    Clockwise,
    CounterClockwise,
    arcLength,
    sweepAngle,
    arcArea,
    pointAtParameter,
    startPoint,
    endPoint,
    midPoint,
    containsAngle,
    Sector,
    mkSector,
    sectorArea,
    Ring,
    mkRing,
    ringArea,
    translateArc,
    scaleArc,
    reverseArc,
    compareByLength,
    showArc,
    angularDistance,
    arcLengthRadians,
    reflectArcCenter,
    samplePoints,
    eqArcDirection,
    ordArcDirection,
    showArcDirection,
    eqArc,
    ordArc,
    showArcInstance,
    eqSector,
    ordSector,
    showSector,
    eqRing,
    ordRing,
    showRing
};
//# sourceMappingURL=index.js.map
