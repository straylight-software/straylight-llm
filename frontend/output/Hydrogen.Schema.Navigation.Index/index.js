// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Bounded from "../Hydrogen.Schema.Bounded/index.js";
var mod = /* #__PURE__ */ Data_EuclideanRing.mod(Data_EuclideanRing.euclideanRingInt);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var Index = function (x) {
    return x;
};
var Count = function (x) {
    return x;
};
var Clamp = /* #__PURE__ */ (function () {
    function Clamp() {

    };
    Clamp.value = new Clamp();
    return Clamp;
})();
var Wrap = /* #__PURE__ */ (function () {
    function Wrap() {

    };
    Wrap.value = new Wrap();
    return Wrap;
})();
var wrapIndex = function (idx) {
    return function (cnt) {
        if (cnt <= 0) {
            return 0;
        };
        if (Data_Boolean.otherwise) {
            var m = mod(idx)(cnt);
            var $47 = m < 0;
            if ($47) {
                return m + cnt | 0;
            };
            return m;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 381, column 1 - line 381, column 31): " + [ idx.constructor.name, cnt.constructor.name ]);
    };
};
var unwrapIndex = function (v) {
    return v;
};
var unwrapCount = function (v) {
    return v;
};
var unsafeIndex = Index;
var total = function (ip) {
    return unwrapCount(ip.count);
};
var showIndex = {
    show: function (v) {
        return "index " + show(v);
    }
};
var showCount = {
    show: function (v) {
        return show(v) + " items";
    }
};
var showBoundaryBehavior = {
    show: function (v) {
        if (v instanceof Clamp) {
            return "clamp";
        };
        if (v instanceof Wrap) {
            return "wrap";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 128, column 1 - line 130, column 21): " + [ v.constructor.name ]);
    }
};
var position = function (ip) {
    return unwrapIndex(ip.index);
};
var progressRatio = function (ip) {
    var cnt = total(ip);
    var $53 = cnt <= 1;
    if ($53) {
        return 0.0;
    };
    return Data_Int.toNumber(position(ip)) / Data_Int.toNumber(cnt - 1 | 0);
};
var remaining = function (ip) {
    var idx = position(ip);
    var cnt = total(ip);
    var $54 = cnt === 0;
    if ($54) {
        return 0;
    };
    return (cnt - idx | 0) - 1 | 0;
};
var isWrap = function (v) {
    if (v instanceof Wrap) {
        return true;
    };
    if (v instanceof Clamp) {
        return false;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 138, column 1 - line 138, column 38): " + [ v.constructor.name ]);
};
var isSingleton = function (v) {
    return v === 1;
};
var isLast = function (v) {
    return function (v1) {
        return v1 > 0 && v === (v1 - 1 | 0);
    };
};
var isFirst = function (v) {
    return v === 0;
};
var isEmpty = function (v) {
    return v === 0;
};
var isClamp = function (v) {
    if (v instanceof Clamp) {
        return true;
    };
    if (v instanceof Wrap) {
        return false;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 133, column 1 - line 133, column 39): " + [ v.constructor.name ]);
};
var indexedPosition = function (idx) {
    return function (cnt) {
        return function (bhv) {
            var validCount = Hydrogen_Schema_Bounded.clampInt(0)(10000)(cnt);
            var validIndex = (function () {
                var $62 = validCount === 0;
                if ($62) {
                    return 0;
                };
                if (bhv instanceof Clamp) {
                    return Hydrogen_Schema_Bounded.clampInt(0)(validCount - 1 | 0)(idx);
                };
                if (bhv instanceof Wrap) {
                    return wrapIndex(idx)(validCount);
                };
                throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 252, column 14 - line 254, column 43): " + [ bhv.constructor.name ]);
            })();
            return {
                index: validIndex,
                count: validCount,
                behavior: bhv
            };
        };
    };
};
var indexBounds = /* #__PURE__ */ Hydrogen_Schema_Bounded.intBounds(0)(9999)("index")("Zero-based position in sequence (0-9999)");
var index = function (n) {
    return Hydrogen_Schema_Bounded.clampInt(0)(9999)(n);
};
var goTo = function (targetIdx) {
    return function (ip) {
        return indexedPosition(targetIdx)(total(ip))(ip.behavior);
    };
};
var goToFirst = function (ip) {
    return goTo(0)(ip);
};
var goToLast = function (ip) {
    return goTo(total(ip) - 1 | 0)(ip);
};
var eqIndex = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordIndex = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqIndex;
    }
};
var eqCount = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordCount = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqCount;
    }
};
var eqBoundaryBehavior = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Clamp && y instanceof Clamp) {
                return true;
            };
            if (x instanceof Wrap && y instanceof Wrap) {
                return true;
            };
            return false;
        };
    }
};
var ordBoundaryBehavior = {
    compare: function (x) {
        return function (y) {
            if (x instanceof Clamp && y instanceof Clamp) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Clamp) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Clamp) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Wrap && y instanceof Wrap) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqBoundaryBehavior;
    }
};
var distanceToStart = position;
var distanceToEnd = remaining;
var countBounds = /* #__PURE__ */ Hydrogen_Schema_Bounded.intBounds(0)(10000)("count")("Number of items in sequence (0-10000)");
var count = function (n) {
    return Hydrogen_Schema_Bounded.clampInt(0)(10000)(n);
};
var canGoPrev = function (ip) {
    if (ip.behavior instanceof Wrap) {
        return total(ip) > 0;
    };
    if (ip.behavior instanceof Clamp) {
        return position(ip) > 0;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 297, column 16 - line 299, column 27): " + [ ip.behavior.constructor.name ]);
};
var canGoNext = function (ip) {
    if (ip.behavior instanceof Wrap) {
        return total(ip) > 0;
    };
    if (ip.behavior instanceof Clamp) {
        return total(ip) > 0 && position(ip) < (total(ip) - 1 | 0);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Navigation.Index (line 288, column 16 - line 290, column 54): " + [ ip.behavior.constructor.name ]);
};
var behavior = function (ip) {
    return ip.behavior;
};
var atLast = function (ip) {
    var cnt = total(ip);
    return cnt > 0 && position(ip) === (cnt - 1 | 0);
};
var atFirst = function (ip) {
    return position(ip) === 0;
};
var advance = function (n) {
    return function (ip) {
        var current = position(ip);
        var cnt = total(ip);
        var $78 = cnt === 0;
        if ($78) {
            return ip;
        };
        return indexedPosition(current + n | 0)(cnt)(ip.behavior);
    };
};
var next = function (ip) {
    return advance(1)(ip);
};
var retreat = function (n) {
    return advance(-n | 0);
};
var prev = function (ip) {
    return retreat(1)(ip);
};
export {
    Clamp,
    Wrap,
    isClamp,
    isWrap,
    Count,
    count,
    unwrapCount,
    countBounds,
    isEmpty,
    isSingleton,
    Index,
    index,
    unsafeIndex,
    unwrapIndex,
    indexBounds,
    isFirst,
    isLast,
    indexedPosition,
    position,
    total,
    behavior,
    atFirst,
    atLast,
    canGoNext,
    canGoPrev,
    next,
    prev,
    goTo,
    goToFirst,
    goToLast,
    advance,
    retreat,
    progressRatio,
    remaining,
    distanceToEnd,
    distanceToStart,
    eqBoundaryBehavior,
    ordBoundaryBehavior,
    showBoundaryBehavior,
    eqCount,
    ordCount,
    showCount,
    eqIndex,
    ordIndex,
    showIndex
};
//# sourceMappingURL=index.js.map
