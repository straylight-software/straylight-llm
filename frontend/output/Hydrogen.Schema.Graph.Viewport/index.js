// Generated by purs version 0.15.15
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var ViewportZoom = function (x) {
    return x;
};
var Instant = /* #__PURE__ */ (function () {
    function Instant() {

    };
    Instant.value = new Instant();
    return Instant;
})();
var Linear = /* #__PURE__ */ (function () {
    function Linear(value0) {
        this.value0 = value0;
    };
    Linear.create = function (value0) {
        return new Linear(value0);
    };
    return Linear;
})();
var EaseInOut = /* #__PURE__ */ (function () {
    function EaseInOut(value0) {
        this.value0 = value0;
    };
    EaseInOut.create = function (value0) {
        return new EaseInOut(value0);
    };
    return EaseInOut;
})();
var Spring = /* #__PURE__ */ (function () {
    function Spring(value0, value1) {
        this.value0 = value0;
        this.value1 = value1;
    };
    Spring.create = function (value0) {
        return function (value1) {
            return new Spring(value0, value1);
        };
    };
    return Spring;
})();
var Immediate = /* #__PURE__ */ (function () {
    function Immediate() {

    };
    Immediate.value = new Immediate();
    return Immediate;
})();
var High = /* #__PURE__ */ (function () {
    function High() {

    };
    High.value = new High();
    return High;
})();
var Normal = /* #__PURE__ */ (function () {
    function Normal() {

    };
    Normal.value = new Normal();
    return Normal;
})();
var Low = /* #__PURE__ */ (function () {
    function Low() {

    };
    Low.value = new Low();
    return Low;
})();
var Deferred = /* #__PURE__ */ (function () {
    function Deferred() {

    };
    Deferred.value = new Deferred();
    return Deferred;
})();
var LOD_Full = /* #__PURE__ */ (function () {
    function LOD_Full() {

    };
    LOD_Full.value = new LOD_Full();
    return LOD_Full;
})();
var LOD_Simplified = /* #__PURE__ */ (function () {
    function LOD_Simplified() {

    };
    LOD_Simplified.value = new LOD_Simplified();
    return LOD_Simplified;
})();
var LOD_Minimal = /* #__PURE__ */ (function () {
    function LOD_Minimal() {

    };
    LOD_Minimal.value = new LOD_Minimal();
    return LOD_Minimal;
})();
var LOD_Dot = /* #__PURE__ */ (function () {
    function LOD_Dot() {

    };
    LOD_Dot.value = new LOD_Dot();
    return LOD_Dot;
})();
var LOD_Hidden = /* #__PURE__ */ (function () {
    function LOD_Hidden() {

    };
    LOD_Hidden.value = new LOD_Hidden();
    return LOD_Hidden;
})();
var Visible = /* #__PURE__ */ (function () {
    function Visible() {

    };
    Visible.value = new Visible();
    return Visible;
})();
var PartiallyVisible = /* #__PURE__ */ (function () {
    function PartiallyVisible() {

    };
    PartiallyVisible.value = new PartiallyVisible();
    return PartiallyVisible;
})();
var Culled = /* #__PURE__ */ (function () {
    function Culled() {

    };
    Culled.value = new Culled();
    return Culled;
})();
var zoomStep = 1.2;
var zoomPercentage = function (v) {
    return v * 100.0;
};
var zoomLevel = function (v) {
    return v;
};
var zoomFit = 1.0;
var zoom50 = 0.5;
var zoom400 = 4.0;
var zoom25 = 0.25;
var zoom200 = 2.0;
var zoom100 = 1.0;
var withPanBounds = function (b) {
    return function (c) {
        return {
            minZoom: c.minZoom,
            maxZoom: c.maxZoom,
            panBounds: new Data_Maybe.Just(b)
        };
    };
};
var withMinZoom = function (z) {
    return function (c) {
        return {
            maxZoom: c.maxZoom,
            panBounds: c.panBounds,
            minZoom: z
        };
    };
};
var withMaxZoom = function (z) {
    return function (c) {
        return {
            minZoom: c.minZoom,
            panBounds: c.panBounds,
            maxZoom: z
        };
    };
};
var windowOverscan = function (w) {
    return w.overscan;
};
var windowNodes = function (w) {
    return w.nodeCount;
};
var windowConnections = function (w) {
    return w.connectionCount;
};
var viewportZoomLevel = function (v) {
    return v.zoom;
};
var viewportZoom = function (z) {
    return max(1.0e-2)(min(100.0)(z));
};
var zoomIn = function (v) {
    return viewportZoom(v * zoomStep);
};
var zoomOut = function (v) {
    return viewportZoom(v / zoomStep);
};
var zoomTo = viewportZoom;
var zoomToFit = function (contentWidth) {
    return function (contentHeight) {
        return function (viewportWidth) {
            return function (viewportHeight) {
                var zoomY = viewportHeight / contentHeight;
                var zoomX = viewportWidth / contentWidth;
                return viewportZoom(min(zoomX)(zoomY));
            };
        };
    };
};
var viewportTransition = function (t) {
    return t;
};
var viewportState = function (zoom) {
    return function (pan) {
        return function (screenWidth) {
            return function (screenHeight) {
                return {
                    zoom: zoom,
                    pan: pan,
                    screenWidth: screenWidth,
                    screenHeight: screenHeight
                };
            };
        };
    };
};
var viewportPosition = function (x) {
    return function (y) {
        return {
            x: x,
            y: y
        };
    };
};
var viewportPan = function (v) {
    return v.pan;
};
var viewportConstraints = function (minZ) {
    return function (maxZ) {
        return function (panB) {
            return {
                minZoom: minZ,
                maxZoom: maxZ,
                panBounds: panB
            };
        };
    };
};
var viewportBoundsAt = function (v) {
    var z = zoomLevel(v.zoom);
    var visibleWidth = v.screenWidth / z;
    var visibleHeight = v.screenHeight / z;
    return {
        left: v.pan.x,
        top: v.pan.y,
        right: v.pan.x + visibleWidth,
        bottom: v.pan.y + visibleHeight
    };
};
var viewportBounds = function (left) {
    return function (top) {
        return function (right) {
            return function (bottom) {
                return {
                    left: min(left)(right),
                    top: min(top)(bottom),
                    right: max(left)(right),
                    bottom: max(top)(bottom)
                };
            };
        };
    };
};
var transitionDuration = function (v) {
    if (v instanceof Instant) {
        return 0.0;
    };
    if (v instanceof Linear) {
        return v.value0;
    };
    if (v instanceof EaseInOut) {
        return v.value0;
    };
    if (v instanceof Spring) {
        return 600.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 742, column 1 - line 742, column 51): " + [ v.constructor.name ]);
};
var springTransition = function (tension) {
    return function (friction) {
        return new Spring(tension, friction);
    };
};
var smoothTransition = function (duration) {
    return new EaseInOut(duration);
};
var showViewportZoom = {
    show: function (v) {
        return show(v * 100.0) + "%";
    }
};
var showViewportTransition = {
    show: function (v) {
        if (v instanceof Instant) {
            return "instant";
        };
        if (v instanceof Linear) {
            return "linear(" + (show(v.value0) + "ms)");
        };
        if (v instanceof EaseInOut) {
            return "ease-in-out(" + (show(v.value0) + "ms)");
        };
        if (v instanceof Spring) {
            return "spring(" + (show(v.value0) + ("," + (show(v.value1) + ")")));
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 719, column 1 - line 723, column 66): " + [ v.constructor.name ]);
    }
};
var showLoadingPriority = {
    show: function (v) {
        if (v instanceof Immediate) {
            return "immediate";
        };
        if (v instanceof High) {
            return "high";
        };
        if (v instanceof Normal) {
            return "normal";
        };
        if (v instanceof Low) {
            return "low";
        };
        if (v instanceof Deferred) {
            return "deferred";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 644, column 1 - line 649, column 29): " + [ v.constructor.name ]);
    }
};
var showLevelOfDetail = {
    show: function (v) {
        if (v instanceof LOD_Full) {
            return "full";
        };
        if (v instanceof LOD_Simplified) {
            return "simplified";
        };
        if (v instanceof LOD_Minimal) {
            return "minimal";
        };
        if (v instanceof LOD_Dot) {
            return "dot";
        };
        if (v instanceof LOD_Hidden) {
            return "hidden";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 497, column 1 - line 502, column 29): " + [ v.constructor.name ]);
    }
};
var showCullResult = {
    show: function (v) {
        if (v instanceof Visible) {
            return "visible";
        };
        if (v instanceof PartiallyVisible) {
            return "partial";
        };
        if (v instanceof Culled) {
            return "culled";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 545, column 1 - line 548, column 25): " + [ v.constructor.name ]);
    }
};
var shouldRenderNode = function (v) {
    if (v instanceof LOD_Hidden) {
        return false;
    };
    return true;
};
var shouldRenderLabel = function (v) {
    if (v instanceof LOD_Full) {
        return true;
    };
    if (v instanceof LOD_Simplified) {
        return true;
    };
    return false;
};
var shouldRenderConnection = function (v) {
    if (v instanceof LOD_Hidden) {
        return false;
    };
    if (v instanceof LOD_Dot) {
        return false;
    };
    return true;
};
var setZoom = function (zoom) {
    return function (v) {
        return {
            pan: v.pan,
            screenWidth: v.screenWidth,
            screenHeight: v.screenHeight,
            zoom: zoom
        };
    };
};
var setPan = function (pan) {
    return function (v) {
        return {
            zoom: v.zoom,
            screenWidth: v.screenWidth,
            screenHeight: v.screenHeight,
            pan: pan
        };
    };
};
var regionPriority = function (r) {
    return r.priority;
};
var regionBounds = function (r) {
    return r.bounds;
};
var panY = function (p) {
    return p.y;
};
var panX = function (p) {
    return p.x;
};
var panTo = viewportPosition;
var panBy = function (dx) {
    return function (dy) {
        return function (p) {
            return {
                x: p.x + dx,
                y: p.y + dy
            };
        };
    };
};
var origin = {
    x: 0.0,
    y: 0.0
};
var minZoom = 1.0e-2;
var maxZoom = 100.0;
var unconstrainedViewport = /* #__PURE__ */ (function () {
    return {
        minZoom: minZoom,
        maxZoom: maxZoom,
        panBounds: Data_Maybe.Nothing.value
    };
})();
var lodForZoom = function (v) {
    return function (nodeSize) {
        var screenSize = v * nodeSize;
        var $93 = screenSize >= 50.0;
        if ($93) {
            return LOD_Full.value;
        };
        var $94 = screenSize >= 20.0;
        if ($94) {
            return LOD_Simplified.value;
        };
        var $95 = screenSize >= 8.0;
        if ($95) {
            return LOD_Minimal.value;
        };
        var $96 = screenSize >= 2.0;
        if ($96) {
            return LOD_Dot.value;
        };
        return LOD_Hidden.value;
    };
};
var loadingRegion = function (bounds) {
    return function (priority) {
        return {
            bounds: bounds,
            priority: priority
        };
    };
};
var isMinZoom = function (v) {
    return v <= 1.0e-2;
};
var isMaxZoom = function (v) {
    return v >= 100.0;
};
var isAnimating = function (v) {
    if (v instanceof Instant) {
        return false;
    };
    return true;
};
var instantTransition = /* #__PURE__ */ (function () {
    return Instant.value;
})();
var expandBounds = function (margin) {
    return function (b) {
        return {
            left: b.left - margin,
            top: b.top - margin,
            right: b.right + margin,
            bottom: b.bottom + margin
        };
    };
};
var expandWindow = function (extra) {
    return function (w) {
        return {
            nodeCount: w.nodeCount,
            connectionCount: w.connectionCount,
            bounds: expandBounds(extra)(w.bounds),
            overscan: w.overscan + extra
        };
    };
};
var virtualWindow = function (v) {
    return function (overscan) {
        var bounds = viewportBoundsAt(v);
        var expandedBounds = expandBounds(overscan)(bounds);
        return {
            bounds: expandedBounds,
            overscan: overscan,
            nodeCount: 0,
            connectionCount: 0
        };
    };
};
var eqViewportZoom = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordViewportZoom = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqViewportZoom;
    }
};
var eqViewportTransition = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Instant && y instanceof Instant) {
                return true;
            };
            if (x instanceof Linear && y instanceof Linear) {
                return x.value0 === y.value0;
            };
            if (x instanceof EaseInOut && y instanceof EaseInOut) {
                return x.value0 === y.value0;
            };
            if (x instanceof Spring && y instanceof Spring) {
                return x.value0 === y.value0 && x.value1 === y.value1;
            };
            return false;
        };
    }
};
var eqLoadingPriority = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Immediate && y instanceof Immediate) {
                return true;
            };
            if (x instanceof High && y instanceof High) {
                return true;
            };
            if (x instanceof Normal && y instanceof Normal) {
                return true;
            };
            if (x instanceof Low && y instanceof Low) {
                return true;
            };
            if (x instanceof Deferred && y instanceof Deferred) {
                return true;
            };
            return false;
        };
    }
};
var ordLoadingPriority = {
    compare: function (x) {
        return function (y) {
            if (x instanceof Immediate && y instanceof Immediate) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Immediate) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Immediate) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof High && y instanceof High) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof High) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof High) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Normal && y instanceof Normal) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Normal) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Normal) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Low && y instanceof Low) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Low) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Low) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Deferred && y instanceof Deferred) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqLoadingPriority;
    }
};
var eqLevelOfDetail = {
    eq: function (x) {
        return function (y) {
            if (x instanceof LOD_Full && y instanceof LOD_Full) {
                return true;
            };
            if (x instanceof LOD_Simplified && y instanceof LOD_Simplified) {
                return true;
            };
            if (x instanceof LOD_Minimal && y instanceof LOD_Minimal) {
                return true;
            };
            if (x instanceof LOD_Dot && y instanceof LOD_Dot) {
                return true;
            };
            if (x instanceof LOD_Hidden && y instanceof LOD_Hidden) {
                return true;
            };
            return false;
        };
    }
};
var ordLevelOfDetail = {
    compare: function (x) {
        return function (y) {
            if (x instanceof LOD_Full && y instanceof LOD_Full) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof LOD_Full) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof LOD_Full) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof LOD_Simplified && y instanceof LOD_Simplified) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof LOD_Simplified) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof LOD_Simplified) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof LOD_Minimal && y instanceof LOD_Minimal) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof LOD_Minimal) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof LOD_Minimal) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof LOD_Dot && y instanceof LOD_Dot) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof LOD_Dot) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof LOD_Dot) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof LOD_Hidden && y instanceof LOD_Hidden) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqLevelOfDetail;
    }
};
var eqCullResult = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Visible && y instanceof Visible) {
                return true;
            };
            if (x instanceof PartiallyVisible && y instanceof PartiallyVisible) {
                return true;
            };
            if (x instanceof Culled && y instanceof Culled) {
                return true;
            };
            return false;
        };
    }
};
var defaultZoom = 1.0;
var initialViewport = function (width) {
    return function (height) {
        return {
            zoom: defaultZoom,
            pan: origin,
            screenWidth: width,
            screenHeight: height
        };
    };
};
var containsPoint = function (x) {
    return function (y) {
        return function (b) {
            return x >= b.left && (x <= b.right && (y >= b.top && y <= b.bottom));
        };
    };
};
var isInWindow = function (x) {
    return function (y) {
        return function (w) {
            return containsPoint(x)(y)(w.bounds);
        };
    };
};
var constrainZoom = function (c) {
    return function (v) {
        return max(c.minZoom)(min(c.maxZoom)(v));
    };
};
var centerOn = function (targetX) {
    return function (targetY) {
        return function (viewportWidth) {
            return function (viewportHeight) {
                return {
                    x: targetX - viewportWidth / 2.0,
                    y: targetY - viewportHeight / 2.0
                };
            };
        };
    };
};
var boundsWidth = function (b) {
    return b.right - b.left;
};
var boundsTop = function (b) {
    return b.top;
};
var boundsRight = function (b) {
    return b.right;
};
var boundsLeft = function (b) {
    return b.left;
};
var boundsIntersect = function (a) {
    return function (b) {
        return a.left <= b.right && (a.right >= b.left && (a.top <= b.bottom && a.bottom >= b.top));
    };
};
var cullConnection = function (vp) {
    return function (x1) {
        return function (y1) {
            return function (x2) {
                return function (y2) {
                    var lineBounds = {
                        left: min(x1)(x2),
                        top: min(y1)(y2),
                        right: max(x1)(x2),
                        bottom: max(y1)(y2)
                    };
                    var $128 = !boundsIntersect(vp)(lineBounds);
                    if ($128) {
                        return Culled.value;
                    };
                    return PartiallyVisible.value;
                };
            };
        };
    };
};
var cullNode = function (vp) {
    return function (nodeX) {
        return function (nodeY) {
            return function (nodeWidth) {
                return function (nodeHeight) {
                    var nodeBounds = {
                        left: nodeX - nodeWidth / 2.0,
                        top: nodeY - nodeHeight / 2.0,
                        right: nodeX + nodeWidth / 2.0,
                        bottom: nodeY + nodeHeight / 2.0
                    };
                    var $129 = !boundsIntersect(vp)(nodeBounds);
                    if ($129) {
                        return Culled.value;
                    };
                    var $130 = containsPoint(nodeBounds.left)(nodeBounds.top)(vp) && containsPoint(nodeBounds.right)(nodeBounds.bottom)(vp);
                    if ($130) {
                        return Visible.value;
                    };
                    return PartiallyVisible.value;
                };
            };
        };
    };
};
var boundsHeight = function (b) {
    return b.bottom - b.top;
};
var constrainPan = function (c) {
    return function (v) {
        return function (pos) {
            if (c.panBounds instanceof Data_Maybe.Nothing) {
                return pos;
            };
            if (c.panBounds instanceof Data_Maybe.Just) {
                var visible = viewportBoundsAt(v);
                var visibleH = boundsHeight(visible);
                var visibleW = boundsWidth(visible);
                var maxY = c.panBounds.value0.bottom - visibleH;
                var maxX = c.panBounds.value0.right - visibleW;
                return {
                    x: max(c.panBounds.value0.left)(min(maxX)(pos.x)),
                    y: max(c.panBounds.value0.top)(min(maxY)(pos.y))
                };
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Graph.Viewport (line 792, column 3 - line 807, column 10): " + [ c.panBounds.constructor.name ]);
        };
    };
};
var boundsFromCenter = function (centerX) {
    return function (centerY) {
        return function (width) {
            return function (height) {
                return {
                    left: centerX - width / 2.0,
                    top: centerY - height / 2.0,
                    right: centerX + width / 2.0,
                    bottom: centerY + height / 2.0
                };
            };
        };
    };
};
var boundsCenter = function (b) {
    return {
        x: (b.left + b.right) / 2.0,
        y: (b.top + b.bottom) / 2.0
    };
};
var fitContent = function (contentBounds) {
    return function (v) {
        var contentWidth = boundsWidth(contentBounds);
        var contentHeight = boundsHeight(contentBounds);
        var newZoom = zoomToFit(contentWidth)(contentHeight)(v.screenWidth)(v.screenHeight);
        var newVisibleHeight = v.screenHeight / zoomLevel(newZoom);
        var newVisibleWidth = v.screenWidth / zoomLevel(newZoom);
        var center = boundsCenter(contentBounds);
        var newPan = {
            x: center.x - newVisibleWidth / 2.0,
            y: center.y - newVisibleHeight / 2.0
        };
        return {
            screenWidth: v.screenWidth,
            screenHeight: v.screenHeight,
            zoom: newZoom,
            pan: newPan
        };
    };
};
var loadingPriority = function (v) {
    return function (x) {
        return function (y) {
            var bounds = viewportBoundsAt(v);
            var center = boundsCenter(bounds);
            var distX = x - center.x;
            var distY = y - center.y;
            var dist = distX * distX + distY * distY;
            var halfDiag = (boundsWidth(bounds) * boundsWidth(bounds) + boundsHeight(bounds) * boundsHeight(bounds)) / 4.0;
            var $133 = dist < halfDiag * 0.25;
            if ($133) {
                return Immediate.value;
            };
            var $134 = containsPoint(x)(y)(bounds);
            if ($134) {
                return High.value;
            };
            var $135 = containsPoint(x)(y)(expandBounds(200.0)(bounds));
            if ($135) {
                return Normal.value;
            };
            var $136 = containsPoint(x)(y)(expandBounds(500.0)(bounds));
            if ($136) {
                return Low.value;
            };
            return Deferred.value;
        };
    };
};
var priorityRegion = function (v) {
    var bounds = viewportBoundsAt(v);
    var center = boundsCenter(bounds);
    var size = min(boundsWidth(bounds))(boundsHeight(bounds)) / 2.0;
    return {
        bounds: boundsFromCenter(center.x)(center.y)(size)(size),
        priority: Immediate.value
    };
};
var boundsBottom = function (b) {
    return b.bottom;
};
var backgroundRegion = function (v) {
    return function (expansion) {
        return {
            bounds: expandBounds(expansion)(viewportBoundsAt(v)),
            priority: Low.value
        };
    };
};
var applyZoom = function (newZoom) {
    return function (v) {
        var oldZ = zoomLevel(v.zoom);
        var oldVisibleWidth = v.screenWidth / oldZ;
        var oldVisibleHeight = v.screenHeight / oldZ;
        var newZ = zoomLevel(newZoom);
        var newVisibleWidth = v.screenWidth / newZ;
        var newVisibleHeight = v.screenHeight / newZ;
        var centerY = v.pan.y + oldVisibleHeight / 2.0;
        var centerX = v.pan.x + oldVisibleWidth / 2.0;
        var newPan = {
            x: centerX - newVisibleWidth / 2.0,
            y: centerY - newVisibleHeight / 2.0
        };
        return {
            screenWidth: v.screenWidth,
            screenHeight: v.screenHeight,
            zoom: newZoom,
            pan: newPan
        };
    };
};
var applyPan = function (dx) {
    return function (dy) {
        return function (v) {
            var z = zoomLevel(v.zoom);
            var graphDy = dy / z;
            var graphDx = dx / z;
            return {
                zoom: v.zoom,
                screenWidth: v.screenWidth,
                screenHeight: v.screenHeight,
                pan: panBy(graphDx)(graphDy)(v.pan)
            };
        };
    };
};
export {
    viewportZoom,
    defaultZoom,
    zoomIn,
    zoomOut,
    zoomTo,
    zoomToFit,
    zoomLevel,
    zoomPercentage,
    minZoom,
    maxZoom,
    isMinZoom,
    isMaxZoom,
    viewportPosition,
    origin,
    panX,
    panY,
    panBy,
    panTo,
    centerOn,
    viewportBounds,
    boundsFromCenter,
    boundsLeft,
    boundsTop,
    boundsRight,
    boundsBottom,
    boundsWidth,
    boundsHeight,
    boundsCenter,
    containsPoint,
    boundsIntersect,
    expandBounds,
    viewportState,
    initialViewport,
    viewportZoomLevel,
    viewportPan,
    viewportBoundsAt,
    setZoom,
    setPan,
    applyZoom,
    applyPan,
    fitContent,
    LOD_Full,
    LOD_Simplified,
    LOD_Minimal,
    LOD_Dot,
    LOD_Hidden,
    lodForZoom,
    shouldRenderNode,
    shouldRenderLabel,
    shouldRenderConnection,
    Visible,
    PartiallyVisible,
    Culled,
    cullNode,
    cullConnection,
    virtualWindow,
    windowNodes,
    windowConnections,
    windowOverscan,
    expandWindow,
    isInWindow,
    Immediate,
    High,
    Normal,
    Low,
    Deferred,
    loadingPriority,
    loadingRegion,
    priorityRegion,
    backgroundRegion,
    regionPriority,
    regionBounds,
    viewportTransition,
    instantTransition,
    smoothTransition,
    springTransition,
    transitionDuration,
    isAnimating,
    viewportConstraints,
    unconstrainedViewport,
    constrainZoom,
    constrainPan,
    withMinZoom,
    withMaxZoom,
    withPanBounds,
    zoomFit,
    zoom25,
    zoom50,
    zoom100,
    zoom200,
    zoom400,
    eqViewportZoom,
    ordViewportZoom,
    showViewportZoom,
    eqLevelOfDetail,
    ordLevelOfDetail,
    showLevelOfDetail,
    eqCullResult,
    showCullResult,
    eqLoadingPriority,
    ordLoadingPriority,
    showLoadingPriority,
    eqViewportTransition,
    showViewportTransition
};
//# sourceMappingURL=index.js.map
