// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Map_Internal from "../Data.Map.Internal/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Tuple from "../Data.Tuple/index.js";
import * as Hydrogen_Element_Compound_TreeView_Node from "../Hydrogen.Element.Compound.TreeView.Node/index.js";
import * as Hydrogen_Element_Compound_TreeView_State from "../Hydrogen.Element.Compound.TreeView.State/index.js";
import * as Hydrogen_Element_Compound_TreeView_Types from "../Hydrogen.Element.Compound.TreeView.Types/index.js";
import * as Hydrogen_Schema_Graph_Layout_Constraints from "../Hydrogen.Schema.Graph.Layout.Constraints/index.js";
import * as Hydrogen_Schema_Graph_Layout_Spacing from "../Hydrogen.Schema.Graph.Layout.Spacing/index.js";
import * as Hydrogen_Schema_Graph_Layout_Types from "../Hydrogen.Schema.Graph.Layout.Types/index.js";
import * as Hydrogen_Schema_Graph_Viewport_Types from "../Hydrogen.Schema.Graph.Viewport.Types/index.js";
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var union = /* #__PURE__ */ Data_Map_Internal.union(Hydrogen_Element_Compound_TreeView_Types.ordNodeId);
var insert = /* #__PURE__ */ Data_Map_Internal.insert(Hydrogen_Element_Compound_TreeView_Types.ordNodeId);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var div1 = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var nodeSizeWidth = function (v) {
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FixedSize) {
        return v.value0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FitContent) {
        return v.value0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.Proportional) {
        return 100.0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.AspectRatio) {
        return 100.0 * v.value0;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.NodeLink (line 413, column 1 - line 413, column 45): " + [ v.constructor.name ]);
};
var nodeSizeHeight = function (v) {
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FixedSize) {
        return v.value1;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.FitContent) {
        return v.value1;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.Proportional) {
        return 100.0;
    };
    if (v instanceof Hydrogen_Schema_Graph_Layout_Types.AspectRatio) {
        return 100.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.NodeLink (line 420, column 1 - line 420, column 46): " + [ v.constructor.name ]);
};
var negateNum = function (n) {
    return 0.0 - n;
};
var layoutTidySubtree = function (config) {
    return function (tree) {
        return function (expanded) {
            return function (node) {
                return function (xOffset) {
                    return function (level) {
                        var sibGap = Hydrogen_Schema_Graph_Layout_Spacing.siblingGap(config.spacing);
                        var nodeWidth = nodeSizeWidth(config.sizing);
                        var nodeHeight = nodeSizeHeight(config.sizing);
                        var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(node);
                        var levelGapVal = Hydrogen_Schema_Graph_Layout_Spacing.levelGap(config.spacing);
                        var y = Data_Int.toNumber(level) * (nodeHeight + levelGapVal);
                        var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                        var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(node);
                        var $29 = isExp && Data_Array.length(children) > 0;
                        if ($29) {
                            var childNodes = Data_Array.mapMaybe(function (cid) {
                                return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                            })(children);
                            var childResults = layoutTidyChildren(config)(tree)(expanded)(childNodes)(xOffset)(level + 1 | 0);
                            var maxChildX = foldl(function (acc) {
                                return function (r) {
                                    return max(acc)(r.maxX);
                                };
                            })(0.0)(childResults);
                            var maxChildY = foldl(function (acc) {
                                return function (r) {
                                    return max(acc)(r.maxY);
                                };
                            })(0.0)(childResults);
                            var totalChildWidth = foldl(function (acc) {
                                return function (r) {
                                    return acc + r.width + sibGap;
                                };
                            })(negateNum(sibGap))(childResults);
                            var centerX = xOffset + totalChildWidth / 2.0;
                            var pos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(centerX)(y)(nodeWidth)(nodeHeight);
                            var allPositions = foldl(function (acc) {
                                return function (r) {
                                    return union(acc)(r.positions);
                                };
                            })(Data_Map_Internal.singleton(nid)(pos))(childResults);
                            return {
                                positions: allPositions,
                                width: max(nodeWidth)(totalChildWidth),
                                x: centerX,
                                maxX: max(centerX)(maxChildX),
                                maxY: max(y + nodeHeight)(maxChildY)
                            };
                        };
                        var pos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(xOffset)(y)(nodeWidth)(nodeHeight);
                        return {
                            positions: Data_Map_Internal.singleton(nid)(pos),
                            width: nodeWidth,
                            x: xOffset,
                            maxX: xOffset + nodeWidth,
                            maxY: y + nodeHeight
                        };
                    };
                };
            };
        };
    };
};
var layoutTidyChildren = function (config) {
    return function (tree) {
        return function (expanded) {
            return function (nodes) {
                return function (startX) {
                    return function (level) {
                        var sibGap = Hydrogen_Schema_Graph_Layout_Spacing.siblingGap(config.spacing);
                        var result = foldl(function (acc) {
                            return function (node) {
                                var childResult = layoutTidySubtree(config)(tree)(expanded)(node)(acc.currentX)(level);
                                var newX = acc.currentX + childResult.width + sibGap;
                                return {
                                    currentX: newX,
                                    results: Data_Array.snoc(acc.results)(childResult)
                                };
                            };
                        })({
                            currentX: startX,
                            results: [  ]
                        })(nodes);
                        return result.results;
                    };
                };
            };
        };
    };
};
var layoutResult = function (positions) {
    return function (bounds) {
        return function (nodeCount) {
            return {
                positions: positions,
                bounds: bounds,
                nodeCount: nodeCount
            };
        };
    };
};
var layoutTidy = function (config) {
    return function (tree) {
        return function (expanded) {
            var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
            var initialState = {
                positions: Data_Map_Internal.empty,
                maxX: 0.0,
                maxY: 0.0
            };
            var finalState = foldl(function (s) {
                return function (rootNode) {
                    var result = layoutTidySubtree(config)(tree)(expanded)(rootNode)(0.0)(0);
                    var newMaxY = max(s.maxY)(result.maxY);
                    var newMaxX = max(s.maxX)(result.maxX);
                    var mergedPositions = union(s.positions)(result.positions);
                    return {
                        positions: mergedPositions,
                        maxX: newMaxX,
                        maxY: newMaxY
                    };
                };
            })(initialState)(roots);
            var bounds = Hydrogen_Schema_Graph_Viewport_Types.viewportBounds(0.0)(0.0)(finalState.maxX)(finalState.maxY);
            return layoutResult(finalState.positions)(bounds)(Data_Map_Internal.size(finalState.positions));
        };
    };
};
var layoutOrgChart = function (config) {
    return function (tree) {
        return function (expanded) {
            return layoutTidy(config)(tree)(expanded);
        };
    };
};
var layoutMindMapSide = function (config) {
    return function (tree) {
        return function (expanded) {
            return function (state) {
                return function (nodes) {
                    return function (direction) {
                        return function (level) {
                            var sibGap = Hydrogen_Schema_Graph_Layout_Spacing.siblingGap(config.spacing);
                            var nodeWidth = nodeSizeWidth(config.sizing);
                            var nodeHeight = nodeSizeHeight(config.sizing);
                            var levelGapVal = Hydrogen_Schema_Graph_Layout_Spacing.levelGap(config.spacing);
                            var xBase = direction * Data_Int.toNumber(level + 1 | 0) * (nodeWidth + levelGapVal);
                            var indexed = Data_Array.mapWithIndex(Data_Tuple.Tuple.create)(nodes);
                            var stateAfterNodes = foldl(function (s) {
                                return function (v) {
                                    var y = (Data_Int.toNumber(v.value0) - Data_Int.toNumber(Data_Array.length(nodes)) / 2.0) * (nodeHeight + sibGap);
                                    var pos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(xBase)(y)(nodeWidth)(nodeHeight);
                                    var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(v.value1);
                                    var newPositions = insert(nid)(pos)(s.positions);
                                    var newMinX = min(s.minX)(xBase);
                                    var newMaxY = max(s.maxY)(y + nodeHeight);
                                    var newMaxX = max(s.maxX)(xBase + nodeWidth);
                                    var stateWithNode = {
                                        positions: newPositions,
                                        minX: newMinX,
                                        maxX: newMaxX,
                                        maxY: newMaxY
                                    };
                                    var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                                    var childrenIds = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(v.value1);
                                    var $31 = isExp && Data_Array.length(childrenIds) > 0;
                                    if ($31) {
                                        var childNodes = Data_Array.mapMaybe(function (cid) {
                                            return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                                        })(childrenIds);
                                        return layoutMindMapSide(config)(tree)(expanded)(stateWithNode)(childNodes)(direction)(level + 1 | 0);
                                    };
                                    return stateWithNode;
                                };
                            })(state)(indexed);
                            return stateAfterNodes;
                        };
                    };
                };
            };
        };
    };
};
var layoutMindMap = function (config) {
    return function (tree) {
        return function (expanded) {
            var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
            var nodeSize = nodeSizeWidth(config.sizing);
            var initialState = {
                positions: Data_Map_Internal.empty,
                minX: 0.0,
                maxX: 0.0,
                maxY: 0.0
            };
            var finalState = (function () {
                var v = Data_Array.head(roots);
                if (v instanceof Data_Maybe.Nothing) {
                    return initialState;
                };
                if (v instanceof Data_Maybe.Just) {
                    var rootPos = Hydrogen_Schema_Graph_Layout_Constraints.nodePosition(0.0)(0.0)(nodeSize)(nodeSize);
                    var stateWithRoot = {
                        positions: Data_Map_Internal.singleton(Hydrogen_Element_Compound_TreeView_Node.nodeId(v.value0))(rootPos),
                        minX: 0.0,
                        maxX: 0.0,
                        maxY: 0.0
                    };
                    var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(v.value0);
                    var childNodes = Data_Array.mapMaybe(function (cid) {
                        return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                    })(children);
                    var halfIdx = div1(Data_Array.length(childNodes))(2);
                    var leftChildren = Data_Array.take(halfIdx)(childNodes);
                    var stateAfterLeft = layoutMindMapSide(config)(tree)(expanded)(stateWithRoot)(leftChildren)(negateNum(1.0))(0);
                    var rightChildren = Data_Array.drop(halfIdx)(childNodes);
                    var stateAfterRight = layoutMindMapSide(config)(tree)(expanded)(stateAfterLeft)(rightChildren)(1.0)(0);
                    return stateAfterRight;
                };
                throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Layout.NodeLink (line 309, column 18 - line 332, column 26): " + [ v.constructor.name ]);
            })();
            var bounds = Hydrogen_Schema_Graph_Viewport_Types.viewportBounds(finalState.minX)(0.0)(finalState.maxX)(finalState.maxY);
            return layoutResult(finalState.positions)(bounds)(Data_Map_Internal.size(finalState.positions));
        };
    };
};
var layoutDendrogram = function (config) {
    return function (tree) {
        return function (expanded) {
            return layoutTidy(config)(tree)(expanded);
        };
    };
};
export {
    layoutTidy,
    layoutDendrogram,
    layoutOrgChart,
    layoutMindMap
};
//# sourceMappingURL=index.js.map
