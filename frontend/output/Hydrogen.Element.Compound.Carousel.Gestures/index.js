// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Dimension_Temporal from "../Hydrogen.Schema.Dimension.Temporal/index.js";
import * as Hydrogen_Schema_Gestural_Gesture_Swipe from "../Hydrogen.Schema.Gestural.Gesture.Swipe/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var VoiceNext = /* #__PURE__ */ (function () {
    function VoiceNext() {

    };
    VoiceNext.value = new VoiceNext();
    return VoiceNext;
})();
var VoicePrevious = /* #__PURE__ */ (function () {
    function VoicePrevious() {

    };
    VoicePrevious.value = new VoicePrevious();
    return VoicePrevious;
})();
var VoiceFirst = /* #__PURE__ */ (function () {
    function VoiceFirst() {

    };
    VoiceFirst.value = new VoiceFirst();
    return VoiceFirst;
})();
var VoiceLast = /* #__PURE__ */ (function () {
    function VoiceLast() {

    };
    VoiceLast.value = new VoiceLast();
    return VoiceLast;
})();
var VoiceGoTo = /* #__PURE__ */ (function () {
    function VoiceGoTo(value0) {
        this.value0 = value0;
    };
    VoiceGoTo.create = function (value0) {
        return new VoiceGoTo(value0);
    };
    return VoiceGoTo;
})();
var VoiceStop = /* #__PURE__ */ (function () {
    function VoiceStop() {

    };
    VoiceStop.value = new VoiceStop();
    return VoiceStop;
})();
var VoicePlay = /* #__PURE__ */ (function () {
    function VoicePlay() {

    };
    VoicePlay.value = new VoicePlay();
    return VoicePlay;
})();
var GestureSwipe = /* #__PURE__ */ (function () {
    function GestureSwipe() {

    };
    GestureSwipe.value = new GestureSwipe();
    return GestureSwipe;
})();
var GestureDrag = /* #__PURE__ */ (function () {
    function GestureDrag() {

    };
    GestureDrag.value = new GestureDrag();
    return GestureDrag;
})();
var GesturePinch = /* #__PURE__ */ (function () {
    function GesturePinch() {

    };
    GesturePinch.value = new GesturePinch();
    return GesturePinch;
})();
var GestureScroll = /* #__PURE__ */ (function () {
    function GestureScroll() {

    };
    GestureScroll.value = new GestureScroll();
    return GestureScroll;
})();
var GestureRetinal = /* #__PURE__ */ (function () {
    function GestureRetinal() {

    };
    GestureRetinal.value = new GestureRetinal();
    return GestureRetinal;
})();
var GestureVoice = /* #__PURE__ */ (function () {
    function GestureVoice() {

    };
    GestureVoice.value = new GestureVoice();
    return GestureVoice;
})();
var GestureNone = /* #__PURE__ */ (function () {
    function GestureNone() {

    };
    GestureNone.value = new GestureNone();
    return GestureNone;
})();
var voiceToSlideDirection = function (cmd) {
    if (cmd instanceof VoiceNext) {
        return 1;
    };
    if (cmd instanceof VoicePrevious) {
        return -1 | 0;
    };
    if (cmd instanceof VoiceFirst) {
        return 0;
    };
    if (cmd instanceof VoiceLast) {
        return 0;
    };
    if (cmd instanceof VoiceGoTo) {
        return 0;
    };
    if (cmd instanceof VoiceStop) {
        return 0;
    };
    if (cmd instanceof VoicePlay) {
        return 0;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 566, column 29 - line 573, column 17): " + [ cmd.constructor.name ]);
};
var swipeToSlideDirection = function (g) {
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeLeft) {
        return 1;
    };
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeRight) {
        return -1 | 0;
    };
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeUp) {
        return 1;
    };
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeDown) {
        return -1 | 0;
    };
    if (g.direction instanceof Data_Maybe.Nothing) {
        return 0;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 230, column 27 - line 235, column 15): " + [ g.direction.constructor.name ]);
};
var swipeProgress = function (g) {
    return g.progress;
};
var swipeDirection = function (g) {
    return g.direction;
};
var showVoiceCommand = {
    show: function (v) {
        if (v instanceof VoiceNext) {
            return "next";
        };
        if (v instanceof VoicePrevious) {
            return "previous";
        };
        if (v instanceof VoiceFirst) {
            return "first";
        };
        if (v instanceof VoiceLast) {
            return "last";
        };
        if (v instanceof VoiceGoTo) {
            return "goto:" + show(v.value0);
        };
        if (v instanceof VoiceStop) {
            return "stop";
        };
        if (v instanceof VoicePlay) {
            return "play";
        };
        throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 515, column 1 - line 522, column 26): " + [ v.constructor.name ]);
    }
};
var showGestureKind = {
    show: function (v) {
        if (v instanceof GestureSwipe) {
            return "swipe";
        };
        if (v instanceof GestureDrag) {
            return "drag";
        };
        if (v instanceof GesturePinch) {
            return "pinch";
        };
        if (v instanceof GestureScroll) {
            return "scroll";
        };
        if (v instanceof GestureRetinal) {
            return "retinal";
        };
        if (v instanceof GestureVoice) {
            return "voice";
        };
        if (v instanceof GestureNone) {
            return "none";
        };
        throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 638, column 1 - line 645, column 28): " + [ v.constructor.name ]);
    }
};
var scrollMagnitude = function (g) {
    var negate1 = function (n) {
        return 0.0 - n;
    };
    var absNum = function (n) {
        var $41 = n < 0.0;
        if ($41) {
            return negate1(n);
        };
        return n;
    };
    var dy = absNum(g.deltaY);
    var dx = absNum(g.deltaX);
    return max(dx)(dy);
};
var scrollGesture = function (params) {
    return {
        deltaX: params.deltaX,
        deltaY: params.deltaY,
        timestamp: params.timestamp
    };
};
var scrollDelta = function (g) {
    return {
        x: g.deltaX,
        y: g.deltaY
    };
};
var retinalState = function (params) {
    return {
        tracking: true,
        gazeX: params.gazeX,
        gazeY: params.gazeY,
        dwellTime: params.dwellTime,
        focusThreshold: params.focusThreshold
    };
};
var pinchScale = function (g) {
    return g.scale;
};
var pinchGesture = function (params) {
    return {
        active: true,
        scale: max(0.1)(params.scale),
        centerX: params.centerX,
        centerY: params.centerY
    };
};
var noVoiceState = /* #__PURE__ */ (function () {
    return {
        listening: false,
        command: Data_Maybe.Nothing.value,
        confidence: 0.0
    };
})();
var noSwipeGesture = /* #__PURE__ */ (function () {
    return {
        active: false,
        direction: Data_Maybe.Nothing.value,
        progress: 0.0,
        startX: 0.0,
        startY: 0.0,
        currentX: 0.0,
        currentY: 0.0
    };
})();
var noScrollGesture = {
    deltaX: 0.0,
    deltaY: 0.0,
    timestamp: /* #__PURE__ */ Hydrogen_Schema_Dimension_Temporal.ms(0.0)
};
var noRetinalState = {
    tracking: false,
    gazeX: 0.0,
    gazeY: 0.0,
    dwellTime: /* #__PURE__ */ Hydrogen_Schema_Dimension_Temporal.milliseconds(0.0),
    focusThreshold: /* #__PURE__ */ Hydrogen_Schema_Dimension_Temporal.milliseconds(1000.0)
};
var noPinchGesture = {
    active: false,
    scale: 1.0,
    centerX: 0.0,
    centerY: 0.0
};
var noDragGesture = {
    active: false,
    offsetX: 0.0,
    offsetY: 0.0,
    velocityX: 0.0,
    velocityY: 0.0
};
var lastCommand = function (s) {
    return s.command;
};
var isVerticalSwipe = function (g) {
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeUp) {
        return true;
    };
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeDown) {
        return true;
    };
    return false;
};
var isSwipeActive = function (g) {
    return g.active;
};
var isScrollVertical = function (g) {
    var negate1 = function (n) {
        return 0.0 - n;
    };
    var absNum = function (n) {
        var $45 = n < 0.0;
        if ($45) {
            return negate1(n);
        };
        return n;
    };
    return absNum(g.deltaY) > absNum(g.deltaX);
};
var isScrollHorizontal = function (g) {
    var negate1 = function (n) {
        return 0.0 - n;
    };
    var absNum = function (n) {
        var $46 = n < 0.0;
        if ($46) {
            return negate1(n);
        };
        return n;
    };
    return absNum(g.deltaX) > absNum(g.deltaY);
};
var isRetinalTracking = function (s) {
    return s.tracking;
};
var isPinchZoomOut = function (g) {
    return g.active && g.scale < 1.0;
};
var isPinchZoomIn = function (g) {
    return g.active && g.scale > 1.0;
};
var isPinchActive = function (g) {
    return g.active;
};
var isListening = function (s) {
    return s.listening;
};
var isHorizontalSwipe = function (g) {
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeLeft) {
        return true;
    };
    if (g.direction instanceof Data_Maybe.Just && g.direction.value0 instanceof Hydrogen_Schema_Gestural_Gesture_Swipe.SwipeRight) {
        return true;
    };
    return false;
};
var isHighConfidence = function (s) {
    return s.listening && s.confidence > 0.7;
};
var isGazeFocused = function (s) {
    return s.tracking && Hydrogen_Schema_Dimension_Temporal.unwrapMilliseconds(s.dwellTime) >= Hydrogen_Schema_Dimension_Temporal.unwrapMilliseconds(s.focusThreshold);
};
var isDragActive = function (g) {
    return g.active;
};
var isAnyGestureActive = function (state) {
    return isSwipeActive(state.swipe) || (isDragActive(state.drag) || (isPinchActive(state.pinch) || (isRetinalTracking(state.retinal) || isListening(state.voice))));
};
var initialGestureState = {
    swipe: noSwipeGesture,
    drag: noDragGesture,
    pinch: noPinchGesture,
    scroll: noScrollGesture,
    retinal: noRetinalState,
    voice: noVoiceState
};
var hasSwipeDirection = function (g) {
    return Data_Maybe.isJust(g.direction);
};
var gazeProgress = function (s) {
    var $50 = !s.tracking;
    if ($50) {
        return 0.0;
    };
    var threshold = Hydrogen_Schema_Dimension_Temporal.unwrapMilliseconds(s.focusThreshold);
    var dwell = Hydrogen_Schema_Dimension_Temporal.unwrapMilliseconds(s.dwellTime);
    var $51 = threshold === 0.0;
    if ($51) {
        return 1.0;
    };
    return min(1.0)(dwell / threshold);
};
var gazePosition = function (s) {
    return {
        x: s.gazeX,
        y: s.gazeY
    };
};
var gazeDwellTime = function (s) {
    return s.dwellTime;
};
var eqVoiceCommand = {
    eq: function (x) {
        return function (y) {
            if (x instanceof VoiceNext && y instanceof VoiceNext) {
                return true;
            };
            if (x instanceof VoicePrevious && y instanceof VoicePrevious) {
                return true;
            };
            if (x instanceof VoiceFirst && y instanceof VoiceFirst) {
                return true;
            };
            if (x instanceof VoiceLast && y instanceof VoiceLast) {
                return true;
            };
            if (x instanceof VoiceGoTo && y instanceof VoiceGoTo) {
                return x.value0 === y.value0;
            };
            if (x instanceof VoiceStop && y instanceof VoiceStop) {
                return true;
            };
            if (x instanceof VoicePlay && y instanceof VoicePlay) {
                return true;
            };
            return false;
        };
    }
};
var eqGestureKind = {
    eq: function (x) {
        return function (y) {
            if (x instanceof GestureSwipe && y instanceof GestureSwipe) {
                return true;
            };
            if (x instanceof GestureDrag && y instanceof GestureDrag) {
                return true;
            };
            if (x instanceof GesturePinch && y instanceof GesturePinch) {
                return true;
            };
            if (x instanceof GestureScroll && y instanceof GestureScroll) {
                return true;
            };
            if (x instanceof GestureRetinal && y instanceof GestureRetinal) {
                return true;
            };
            if (x instanceof GestureVoice && y instanceof GestureVoice) {
                return true;
            };
            if (x instanceof GestureNone && y instanceof GestureNone) {
                return true;
            };
            return false;
        };
    }
};
var ordGestureKind = {
    compare: function (x) {
        return function (y) {
            if (x instanceof GestureSwipe && y instanceof GestureSwipe) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GestureSwipe) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GestureSwipe) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GestureDrag && y instanceof GestureDrag) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GestureDrag) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GestureDrag) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GesturePinch && y instanceof GesturePinch) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GesturePinch) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GesturePinch) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GestureScroll && y instanceof GestureScroll) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GestureScroll) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GestureScroll) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GestureRetinal && y instanceof GestureRetinal) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GestureRetinal) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GestureRetinal) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GestureVoice && y instanceof GestureVoice) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof GestureVoice) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof GestureVoice) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof GestureNone && y instanceof GestureNone) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqGestureKind;
    }
};
var dragVelocity = function (g) {
    return {
        x: g.velocityX,
        y: g.velocityY
    };
};
var dragOffset = function (g) {
    return {
        x: g.offsetX,
        y: g.offsetY
    };
};
var dragMagnitude = function (g) {
    var jsUnsafeSqrt = function (x) {
        var refine = function (g$prime) {
            return (g$prime + x / g$prime) / 2.0;
        };
        var guess = x / 2.0;
        var r1 = refine(guess);
        var r2 = refine(r1);
        var r3 = refine(r2);
        var r4 = refine(r3);
        return r4;
    };
    var sqrt = function (n) {
        var $60 = n > 0.0;
        if ($60) {
            return jsUnsafeSqrt(n);
        };
        return 0.0;
    };
    return sqrt(g.offsetX * g.offsetX + g.offsetY * g.offsetY);
};
var isDragFast = function (threshold) {
    return function (g) {
        var mag = dragMagnitude(g);
        return mag > threshold;
    };
};
var dragGesture = function (params) {
    return {
        active: true,
        offsetX: params.offsetX,
        offsetY: params.offsetY,
        velocityX: params.velocityX,
        velocityY: params.velocityY
    };
};
var dominantGesture = function (state) {
    if (isPinchActive(state.pinch)) {
        return GesturePinch.value;
    };
    if (isSwipeActive(state.swipe)) {
        return GestureSwipe.value;
    };
    if (isDragActive(state.drag)) {
        return GestureDrag.value;
    };
    if (scrollMagnitude(state.scroll) > 0.0) {
        return GestureScroll.value;
    };
    if (isRetinalTracking(state.retinal)) {
        return GestureRetinal.value;
    };
    if (isListening(state.voice)) {
        return GestureVoice.value;
    };
    if (Data_Boolean.otherwise) {
        return GestureNone.value;
    };
    throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 649, column 1 - line 649, column 47): " + [ state.constructor.name ]);
};
var compareGesturePriority = function (a) {
    return function (b) {
        var gesturePriorityValue = function (v) {
            if (v instanceof GesturePinch) {
                return 6;
            };
            if (v instanceof GestureSwipe) {
                return 5;
            };
            if (v instanceof GestureDrag) {
                return 4;
            };
            if (v instanceof GestureScroll) {
                return 3;
            };
            if (v instanceof GestureRetinal) {
                return 2;
            };
            if (v instanceof GestureVoice) {
                return 1;
            };
            if (v instanceof GestureNone) {
                return 0;
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 666, column 5 - line 666, column 47): " + [ v.constructor.name ]);
        };
        return compare(gesturePriorityValue(a))(gesturePriorityValue(b));
    };
};
var clampProgress = function (n) {
    return max(0.0)(min(1.0)(n));
};
var swipeGesture = function (params) {
    return {
        active: true,
        direction: new Data_Maybe.Just(params.direction),
        progress: clampProgress(params.progress),
        startX: params.startX,
        startY: params.startY,
        currentX: params.currentX,
        currentY: params.currentY
    };
};
var voiceState = function (params) {
    return {
        listening: true,
        command: new Data_Maybe.Just(params.command),
        confidence: clampProgress(params.confidence)
    };
};
var activeGestureCount = function (state) {
    var boolToInt = function (v) {
        if (v) {
            return 1;
        };
        if (!v) {
            return 0;
        };
        throw new Error("Failed pattern match at Hydrogen.Element.Compound.Carousel.Gestures (line 621, column 5 - line 621, column 32): " + [ v.constructor.name ]);
    };
    return (((boolToInt(isSwipeActive(state.swipe)) + boolToInt(isDragActive(state.drag)) | 0) + boolToInt(isPinchActive(state.pinch)) | 0) + boolToInt(isRetinalTracking(state.retinal)) | 0) + boolToInt(isListening(state.voice)) | 0;
};
export {
    swipeGesture,
    noSwipeGesture,
    isSwipeActive,
    swipeProgress,
    swipeDirection,
    hasSwipeDirection,
    isHorizontalSwipe,
    isVerticalSwipe,
    swipeToSlideDirection,
    dragGesture,
    noDragGesture,
    isDragActive,
    dragOffset,
    dragVelocity,
    isDragFast,
    dragMagnitude,
    pinchGesture,
    noPinchGesture,
    isPinchActive,
    pinchScale,
    isPinchZoomIn,
    isPinchZoomOut,
    scrollGesture,
    noScrollGesture,
    scrollDelta,
    scrollMagnitude,
    isScrollHorizontal,
    isScrollVertical,
    retinalState,
    noRetinalState,
    isRetinalTracking,
    gazePosition,
    gazeDwellTime,
    isGazeFocused,
    gazeProgress,
    VoiceNext,
    VoicePrevious,
    VoiceFirst,
    VoiceLast,
    VoiceGoTo,
    VoiceStop,
    VoicePlay,
    voiceState,
    noVoiceState,
    isListening,
    lastCommand,
    isHighConfidence,
    voiceToSlideDirection,
    initialGestureState,
    isAnyGestureActive,
    activeGestureCount,
    dominantGesture,
    compareGesturePriority,
    GestureSwipe,
    GestureDrag,
    GesturePinch,
    GestureScroll,
    GestureRetinal,
    GestureVoice,
    GestureNone,
    eqVoiceCommand,
    showVoiceCommand,
    eqGestureKind,
    ordGestureKind,
    showGestureKind
};
//# sourceMappingURL=index.js.map
