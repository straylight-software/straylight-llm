// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Geometry_Angle from "../Hydrogen.Schema.Geometry.Angle/index.js";
import * as Hydrogen_Schema_Geometry_Point from "../Hydrogen.Schema.Geometry.Point/index.js";
var toRadians = /* #__PURE__ */ Hydrogen_Schema_Geometry_Angle.toRadians(Hydrogen_Schema_Geometry_Angle.angularUnitDegrees);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var show1 = /* #__PURE__ */ Data_Show.show(Hydrogen_Schema_Geometry_Angle.showDegrees);
var eq1 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Geometry_Angle.eqDegrees);
var compare = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Geometry_Angle.ordDegrees);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var PolarPoint = function (x) {
    return x;
};
var translateRadial = function (delta) {
    return function (v) {
        var newRadius = v.radius + delta;
        return {
            radius: (function () {
                var $45 = newRadius < 0.0;
                if ($45) {
                    return 0.0;
                };
                return newRadius;
            })(),
            angle: v.angle
        };
    };
};
var toCartesian = function (v) {
    var rad = Hydrogen_Schema_Geometry_Angle.unwrapRadians(toRadians(v.angle));
    var x = v.radius * Data_Number.cos(rad);
    var y = v.radius * Data_Number.sin(rad);
    return Hydrogen_Schema_Geometry_Point.point2D(x)(y);
};
var toCartesianAt = function (center) {
    return function (polar) {
        var cartesian = toCartesian(polar);
        return Hydrogen_Schema_Geometry_Point.point2D(Hydrogen_Schema_Geometry_Point.getX(cartesian) + center.x)(Hydrogen_Schema_Geometry_Point.getY(cartesian) + center.y);
    };
};
var showPolarPointInstance = {
    show: function (v) {
        return "(PolarPoint r=" + (show(v.radius) + (" \u03b8=" + (show1(v.angle) + ")")));
    }
};
var showPolar = function (v) {
    return "Polar { r: " + (show(v.radius) + (", \u03b8: " + (show(v.angle) + "\xb0 }")));
};
var scalePolar = function (factor) {
    return function (v) {
        if (factor >= 0.0) {
            return {
                radius: v.radius * factor,
                angle: v.angle
            };
        };
        if (Data_Boolean.otherwise) {
            return {
                radius: v.radius * -factor,
                angle: Hydrogen_Schema_Geometry_Angle.addAngle(v.angle)(180.0)
            };
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Geometry.Polar (line 245, column 1 - line 245, column 49): " + [ factor.constructor.name, v.constructor.name ]);
    };
};
var sameRadius = function (v) {
    return function (v1) {
        return v.radius === v1.radius;
    };
};
var sameAngle = function (v) {
    return function (v1) {
        return eq1(v.angle)(v1.angle);
    };
};
var rotatePolar = function (delta) {
    return function (v) {
        return {
            radius: v.radius,
            angle: Hydrogen_Schema_Geometry_Angle.addAngle(v.angle)(delta)
        };
    };
};
var polarUp = {
    radius: 1.0,
    angle: 90.0
};
var polarRight = {
    radius: 1.0,
    angle: 0.0
};
var polarRadius = function (v) {
    return v.radius;
};
var polarOrigin = {
    radius: 0.0,
    angle: 0.0
};
var polarLeft = {
    radius: 1.0,
    angle: 180.0
};
var polarDown = {
    radius: 1.0,
    angle: 270.0
};
var polarDistanceSquared = function (p1) {
    return function (p2) {
        var c2 = toCartesian(p2);
        var c1 = toCartesian(p1);
        var dx = Hydrogen_Schema_Geometry_Point.getX(c2) - Hydrogen_Schema_Geometry_Point.getX(c1);
        var dy = Hydrogen_Schema_Geometry_Point.getY(c2) - Hydrogen_Schema_Geometry_Point.getY(c1);
        return dx * dx + dy * dy;
    };
};
var polarDistance = function (p1) {
    return function (p2) {
        return Data_Number.sqrt(polarDistanceSquared(p1)(p2));
    };
};
var polarAngle = function (v) {
    return v.angle;
};
var mkPolarPointUnsafe = function (r) {
    return function (angle) {
        return {
            radius: r,
            angle: angle
        };
    };
};
var mkPolarPoint = function (r) {
    return function (angle) {
        var truncatePos = function (n) {
            return n - fmod(n)(1.0);
        };
        var truncate = function (n) {
            var $61 = n >= 0.0;
            if ($61) {
                return truncatePos(n);
            };
            return -truncatePos(-n);
        };
        var fmod = function (a) {
            return function (b) {
                return a - b * floor(a / b);
            };
        };
        var floorPos = function (n) {
            return n - (n - truncate(n));
        };
        var floor = function (n) {
            var $62 = n >= 0.0;
            if ($62) {
                return floorPos(n);
            };
            return -floorPos(-n) - 1.0;
        };
        var normalizeAngle = function (v) {
            var wrapped = v - 360.0 * floor(v / 360.0);
            var $64 = wrapped < 0.0;
            if ($64) {
                return wrapped + 360.0;
            };
            return wrapped;
        };
        var $65 = r >= 0.0;
        if ($65) {
            return new Data_Maybe.Just({
                radius: r,
                angle: normalizeAngle(angle)
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var mirrorPolar = function (v) {
    var mirrored = (function () {
        var $68 = v.angle === 0.0;
        if ($68) {
            return 0.0;
        };
        return 360.0 - v.angle;
    })();
    return {
        radius: v.radius,
        angle: mirrored
    };
};
var lerpPolarShortest = function (t) {
    return function (v) {
        return function (v1) {
            var r = v.radius + t * (v1.radius - v.radius);
            var delta = Hydrogen_Schema_Geometry_Angle.shortestPath(v.angle)(v1.angle);
            var angle = v.angle + t * delta;
            return {
                radius: r,
                angle: angle
            };
        };
    };
};
var lerpPolar = function (t) {
    return function (v) {
        return function (v1) {
            var r = v.radius + t * (v1.radius - v.radius);
            var angle = Hydrogen_Schema_Geometry_Angle.lerpAngle(t)(v.angle)(v1.angle);
            return {
                radius: r,
                angle: angle
            };
        };
    };
};
var fromCartesian = function (pt) {
    var y = Hydrogen_Schema_Geometry_Point.getY(pt);
    var x = Hydrogen_Schema_Geometry_Point.getX(pt);
    var r = Data_Number.sqrt(x * x + y * y);
    var angleDeg = (Data_Number.atan2(y)(x) * 180.0) / Data_Number.pi;
    var normalized = (function () {
        var $77 = angleDeg < 0.0;
        if ($77) {
            return angleDeg + 360.0;
        };
        return angleDeg;
    })();
    return {
        radius: r,
        angle: normalized
    };
};
var fromCartesianAt = function (center) {
    return function (pt) {
        var relative = Hydrogen_Schema_Geometry_Point.point2D(Hydrogen_Schema_Geometry_Point.getX(pt) - center.x)(Hydrogen_Schema_Geometry_Point.getY(pt) - center.y);
        return fromCartesian(relative);
    };
};
var eqPolarPoint = {
    eq: function (x) {
        return function (y) {
            return eq1(x.angle)(y.angle) && x.radius === y.radius;
        };
    }
};
var ordPolarPoint = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.angle)(y.angle);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.radius)(y.radius);
        };
    },
    Eq0: function () {
        return eqPolarPoint;
    }
};
export {
    PolarPoint,
    mkPolarPoint,
    mkPolarPointUnsafe,
    polarRadius,
    polarAngle,
    toCartesian,
    fromCartesian,
    toCartesianAt,
    fromCartesianAt,
    rotatePolar,
    scalePolar,
    translateRadial,
    mirrorPolar,
    polarOrigin,
    polarRight,
    polarUp,
    polarLeft,
    polarDown,
    lerpPolar,
    lerpPolarShortest,
    polarDistanceSquared,
    polarDistance,
    sameRadius,
    sameAngle,
    showPolar,
    eqPolarPoint,
    ordPolarPoint,
    showPolarPointInstance
};
//# sourceMappingURL=index.js.map
