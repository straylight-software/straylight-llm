// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Hydrogen_Element_Compound_TreeView_Layout_Core from "../Hydrogen.Element.Compound.TreeView.Layout.Core/index.js";
import * as Hydrogen_Element_Compound_TreeView_Node from "../Hydrogen.Element.Compound.TreeView.Node/index.js";
import * as Hydrogen_Element_Compound_TreeView_State from "../Hydrogen.Element.Compound.TreeView.State/index.js";
import * as Hydrogen_Element_Compound_TreeView_Types from "../Hydrogen.Element.Compound.TreeView.Types/index.js";
import * as Hydrogen_Schema_Graph_Viewport_LOD from "../Hydrogen.Schema.Graph.Viewport.LOD/index.js";
import * as Hydrogen_Schema_Graph_Viewport_State from "../Hydrogen.Schema.Graph.Viewport.State/index.js";
import * as Hydrogen_Schema_Graph_Viewport_Types from "../Hydrogen.Schema.Graph.Viewport.Types/index.js";
var elem = /* #__PURE__ */ Data_Array.elem(Hydrogen_Element_Compound_TreeView_Types.eqNodeId);
var div1 = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var eq1 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Graph_Viewport_LOD.eqLevelOfDetail);
var eq2 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Graph_Viewport_LOD.eqCullResult);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var notEq = /* #__PURE__ */ Data_Eq.notEq(Hydrogen_Element_Compound_TreeView_Types.eqNodeId);
var Pan = /* #__PURE__ */ (function () {
    function Pan(value0, value1) {
        this.value0 = value0;
        this.value1 = value1;
    };
    Pan.create = function (value0) {
        return function (value1) {
            return new Pan(value0, value1);
        };
    };
    return Pan;
})();
var Zoom = /* #__PURE__ */ (function () {
    function Zoom(value0) {
        this.value0 = value0;
    };
    Zoom.create = function (value0) {
        return new Zoom(value0);
    };
    return Zoom;
})();
var ZoomToLevel = /* #__PURE__ */ (function () {
    function ZoomToLevel(value0) {
        this.value0 = value0;
    };
    ZoomToLevel.create = function (value0) {
        return new ZoomToLevel(value0);
    };
    return ZoomToLevel;
})();
var FitAll = /* #__PURE__ */ (function () {
    function FitAll() {

    };
    FitAll.value = new FitAll();
    return FitAll;
})();
var CenterOn = /* #__PURE__ */ (function () {
    function CenterOn(value0) {
        this.value0 = value0;
    };
    CenterOn.create = function (value0) {
        return new CenterOn(value0);
    };
    return CenterOn;
})();
var ResetView = /* #__PURE__ */ (function () {
    function ResetView() {

    };
    ResetView.value = new ResetView();
    return ResetView;
})();
var zoomViewport = function (zoomingIn) {
    return function (vp) {
        var newZoom = (function () {
            if (zoomingIn) {
                return Hydrogen_Schema_Graph_Viewport_Types.zoomIn(vp.state.zoom);
            };
            return Hydrogen_Schema_Graph_Viewport_Types.zoomOut(vp.state.zoom);
        })();
        return {
            overscan: vp.overscan,
            chunkSize: vp.chunkSize,
            maxVisibleNodes: vp.maxVisibleNodes,
            state: Hydrogen_Schema_Graph_Viewport_State.applyZoom(newZoom)(vp.state)
        };
    };
};
var virtualNodeCount = function (vt) {
    return vt.totalNodes;
};
var viewportZoom = function (vp) {
    return vp.state.zoom;
};
var viewportScreenSize = function (vp) {
    return {
        width: vp.state.screenWidth,
        height: vp.state.screenHeight
    };
};
var viewportPan = function (vp) {
    return vp.state.pan;
};
var treeViewport = function (screenWidth) {
    return function (screenHeight) {
        var defaultZoom = Hydrogen_Schema_Graph_Viewport_Types.viewportZoom(1.0);
        return {
            state: Hydrogen_Schema_Graph_Viewport_State.viewportState(defaultZoom)(Hydrogen_Schema_Graph_Viewport_Types.origin)(screenWidth)(screenHeight),
            overscan: 200.0,
            chunkSize: 100,
            maxVisibleNodes: 1000
        };
    };
};
var shouldRenderNode = function (nid) {
    return function (vt) {
        return elem(nid)(vt.visibleNodeIds);
    };
};
var setViewportZoom = function (zoom) {
    return function (vp) {
        return {
            overscan: vp.overscan,
            chunkSize: vp.chunkSize,
            maxVisibleNodes: vp.maxVisibleNodes,
            state: {
                pan: vp.state.pan,
                screenWidth: vp.state.screenWidth,
                screenHeight: vp.state.screenHeight,
                zoom: zoom
            }
        };
    };
};
var setViewportPan = function (pan) {
    return function (vp) {
        return {
            overscan: vp.overscan,
            chunkSize: vp.chunkSize,
            maxVisibleNodes: vp.maxVisibleNodes,
            state: {
                zoom: vp.state.zoom,
                screenWidth: vp.state.screenWidth,
                screenHeight: vp.state.screenHeight,
                pan: pan
            }
        };
    };
};
var zoomToPoint = function (targetX) {
    return function (targetY) {
        return function (zoomingIn) {
            return function (vp) {
                var zoomed = zoomViewport(zoomingIn)(vp);
                var oldZoom = Hydrogen_Schema_Graph_Viewport_Types.zoomLevel(vp.state.zoom);
                var newZoom = Hydrogen_Schema_Graph_Viewport_Types.zoomLevel(zoomed.state.zoom);
                var zoomRatio = oldZoom / newZoom;
                var newPanY = targetY - (targetY - vp.state.pan.y) * zoomRatio;
                var newPanX = targetX - (targetX - vp.state.pan.x) * zoomRatio;
                return setViewportPan(Hydrogen_Schema_Graph_Viewport_Types.viewportPosition(newPanX)(newPanY))(zoomed);
            };
        };
    };
};
var renderWindow = function (vt) {
    return vt.visibleNodeIds;
};
var renderChunks = function (chunkSize) {
    return function (vt) {
        var totalNodes = Data_Array.length(vt.visibleNodeIds);
        var totalChunks = div1((totalNodes + chunkSize | 0) - 1 | 0)(chunkSize);
        var makeChunk = function (idx) {
            var start = idx * chunkSize | 0;
            var nodes = Data_Array.slice(start)(start + chunkSize | 0)(vt.visibleNodeIds);
            var isLast = idx === (totalChunks - 1 | 0);
            return {
                nodes: nodes,
                chunkIndex: idx,
                totalChunks: totalChunks,
                isLast: isLast
            };
        };
        return map(makeChunk)(Data_Array.range(0)(totalChunks - 1 | 0));
    };
};
var panViewport = function (dx) {
    return function (dy) {
        return function (vp) {
            return {
                overscan: vp.overscan,
                chunkSize: vp.chunkSize,
                maxVisibleNodes: vp.maxVisibleNodes,
                state: Hydrogen_Schema_Graph_Viewport_State.applyPan(dx)(dy)(vp.state)
            };
        };
    };
};
var nodeLOD = function (nid) {
    return function (layout) {
        return function (vp) {
            var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
            if (v instanceof Data_Maybe.Nothing) {
                return Hydrogen_Schema_Graph_Viewport_LOD.LOD_Hidden.value;
            };
            if (v instanceof Data_Maybe.Just) {
                var nodeSize = max(v.value0.width)(v.value0.height);
                return Hydrogen_Schema_Graph_Viewport_LOD.lodForZoom(vp.state.zoom)(nodeSize);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 471, column 3 - line 477, column 49): " + [ v.constructor.name ]);
        };
    };
};
var shouldShowIcon = function (nid) {
    return function (layout) {
        return function (vp) {
            var lod = nodeLOD(nid)(layout)(vp);
            return eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Full.value) || eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Simplified.value);
        };
    };
};
var shouldShowLabel = function (nid) {
    return function (layout) {
        return function (vp) {
            return Hydrogen_Schema_Graph_Viewport_LOD.shouldRenderLabel(nodeLOD(nid)(layout)(vp));
        };
    };
};
var simplifiedNode = function (nid) {
    return function (layout) {
        return function (vp) {
            var lod = nodeLOD(nid)(layout)(vp);
            return {
                showLabel: eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Full.value) || eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Simplified.value),
                showIcon: eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Full.value) || eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Simplified.value),
                showDot: eq1(lod)(Hydrogen_Schema_Graph_Viewport_LOD.LOD_Dot.value)
            };
        };
    };
};
var loadRequest = function (nodeId) {
    return function (priority) {
        return {
            nodeId: nodeId,
            priority: priority
        };
    };
};
var pendingLoads = function (vt) {
    return Data_Array.mapWithIndex(function (idx) {
        return function (nid) {
            return loadRequest(nid)(idx);
        };
    })(vt.pendingLoads);
};
var isRenderComplete = function (renderedChunks) {
    return function (chunkSize) {
        return function (vt) {
            var totalNodes = Data_Array.length(vt.visibleNodeIds);
            var totalChunks = div1((totalNodes + chunkSize | 0) - 1 | 0)(chunkSize);
            return renderedChunks >= totalChunks;
        };
    };
};
var initialViewport = treeViewport;
var getVisibleBounds = function (vp) {
    return Hydrogen_Schema_Graph_Viewport_Types.expandBounds(vp.overscan)(Hydrogen_Schema_Graph_Viewport_State.viewportBoundsAt(vp.state));
};
var isNodeVisible = function (nid) {
    return function (layout) {
        return function (vp) {
            var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
            if (v instanceof Data_Maybe.Nothing) {
                return false;
            };
            if (v instanceof Data_Maybe.Just) {
                var bounds = getVisibleBounds(vp);
                var cullResult = Hydrogen_Schema_Graph_Viewport_LOD.cullNode(bounds)(v.value0.x)(v.value0.y)(v.value0.width)(v.value0.height);
                return eq2(cullResult)(Hydrogen_Schema_Graph_Viewport_LOD.Visible.value) || eq2(cullResult)(Hydrogen_Schema_Graph_Viewport_LOD.PartiallyVisible.value);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 271, column 3 - line 278, column 64): " + [ v.constructor.name ]);
        };
    };
};
var nodeVisibility = function (nid) {
    return function (layout) {
        return function (vp) {
            var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
            if (v instanceof Data_Maybe.Nothing) {
                return Hydrogen_Schema_Graph_Viewport_LOD.Culled.value;
            };
            if (v instanceof Data_Maybe.Just) {
                var bounds = getVisibleBounds(vp);
                return Hydrogen_Schema_Graph_Viewport_LOD.cullNode(bounds)(v.value0.x)(v.value0.y)(v.value0.width)(v.value0.height);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 283, column 3 - line 289, column 64): " + [ v.constructor.name ]);
        };
    };
};
var fitToContent = function (layout) {
    return function (vp) {
        var content = Hydrogen_Element_Compound_TreeView_Layout_Core.contentBounds(layout);
        return {
            overscan: vp.overscan,
            chunkSize: vp.chunkSize,
            maxVisibleNodes: vp.maxVisibleNodes,
            state: Hydrogen_Schema_Graph_Viewport_State.fitContent(content)(vp.state)
        };
    };
};
var collectIfVisible = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (bounds) {
                return function (acc) {
                    return function (node) {
                        var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(node);
                        var isVisible = (function () {
                            var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
                            if (v instanceof Data_Maybe.Nothing) {
                                return false;
                            };
                            if (v instanceof Data_Maybe.Just) {
                                var result = Hydrogen_Schema_Graph_Viewport_LOD.cullNode(bounds)(v.value0.x)(v.value0.y)(v.value0.width)(v.value0.height);
                                return eq2(result)(Hydrogen_Schema_Graph_Viewport_LOD.Visible.value) || eq2(result)(Hydrogen_Schema_Graph_Viewport_LOD.PartiallyVisible.value);
                            };
                            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 316, column 17 - line 320, column 59): " + [ v.constructor.name ]);
                        })();
                        var newAcc = (function () {
                            if (isVisible) {
                                return Data_Array.snoc(acc)(node);
                            };
                            return acc;
                        })();
                        var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                        var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(node);
                        var $42 = isExp && Data_Array.length(children) > 0;
                        if ($42) {
                            var childNodes = Data_Array.mapMaybe(function (cid) {
                                return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                            })(children);
                            return foldl(collectIfVisible(tree)(expanded)(layout)(bounds))(newAcc)(childNodes);
                        };
                        return newAcc;
                    };
                };
            };
        };
    };
};
var collectVisibleNodes = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (bounds) {
                var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
                return foldl(collectIfVisible(tree)(expanded)(layout)(bounds))([  ])(roots);
            };
        };
    };
};
var visibleNodes = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (vp) {
                var bounds = getVisibleBounds(vp);
                var allNodes = collectVisibleNodes(tree)(expanded)(layout)(bounds);
                var limited = Data_Array.take(vp.maxVisibleNodes)(allNodes);
                return limited;
            };
        };
    };
};
var visibleNodeIds = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (vp) {
                return map(Hydrogen_Element_Compound_TreeView_Node.nodeId)(visibleNodes(tree)(expanded)(layout)(vp));
            };
        };
    };
};
var virtualizedTree = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (vp) {
                var visible = visibleNodeIds(tree)(expanded)(layout)(vp);
                return {
                    visibleNodeIds: visible,
                    totalNodes: Data_Array.length(visible),
                    renderedNodes: 0,
                    pendingLoads: [  ]
                };
            };
        };
    };
};
var chunkNodes = function (chunkIndex) {
    return function (chunkSize) {
        return function (vt) {
            var start = chunkIndex * chunkSize | 0;
            return Data_Array.slice(start)(start + chunkSize | 0)(vt.visibleNodeIds);
        };
    };
};
var nextChunk = function (currentChunk) {
    return function (chunkSize) {
        return function (vt) {
            var nextIdx = currentChunk + 1 | 0;
            var totalNodes = Data_Array.length(vt.visibleNodeIds);
            var totalChunks = div1((totalNodes + chunkSize | 0) - 1 | 0)(chunkSize);
            var $43 = nextIdx >= totalChunks;
            if ($43) {
                return Data_Maybe.Nothing.value;
            };
            return new Data_Maybe.Just({
                nodes: chunkNodes(nextIdx)(chunkSize)(vt),
                chunkIndex: nextIdx,
                totalChunks: totalChunks,
                isLast: nextIdx === (totalChunks - 1 | 0)
            });
        };
    };
};
var centerOnNode = function (nid) {
    return function (layout) {
        return function (vp) {
            var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
            if (v instanceof Data_Maybe.Nothing) {
                return vp;
            };
            if (v instanceof Data_Maybe.Just) {
                var z = Hydrogen_Schema_Graph_Viewport_Types.zoomLevel(vp.state.zoom);
                var newPanY = v.value0.y - vp.state.screenHeight / z / 2.0;
                var newPanX = v.value0.x - vp.state.screenWidth / z / 2.0;
                return setViewportPan(Hydrogen_Schema_Graph_Viewport_Types.viewportPosition(newPanX)(newPanY))(vp);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 531, column 3 - line 542, column 68): " + [ v.constructor.name ]);
        };
    };
};
var handleViewportEvent = function (event) {
    return function (layout) {
        return function (vp) {
            if (event instanceof Pan) {
                return panViewport(event.value0)(event.value1)(vp);
            };
            if (event instanceof Zoom) {
                return zoomViewport(event.value0)(vp);
            };
            if (event instanceof ZoomToLevel) {
                return setViewportZoom(Hydrogen_Schema_Graph_Viewport_Types.viewportZoom(event.value0))(vp);
            };
            if (event instanceof FitAll) {
                return fitToContent(layout)(vp);
            };
            if (event instanceof CenterOn) {
                return centerOnNode(event.value0)(layout)(vp);
            };
            if (event instanceof ResetView) {
                return initialViewport(vp.state.screenWidth)(vp.state.screenHeight);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Viewport (line 519, column 3 - line 526, column 76): " + [ event.constructor.name ]);
        };
    };
};
var acknowledgeLoad = function (nid) {
    return function (vt) {
        return {
            visibleNodeIds: vt.visibleNodeIds,
            totalNodes: vt.totalNodes,
            renderedNodes: vt.renderedNodes,
            pendingLoads: Data_Array.filter(function (id) {
                return notEq(id)(nid);
            })(vt.pendingLoads)
        };
    };
};
export {
    treeViewport,
    initialViewport,
    viewportZoom,
    viewportPan,
    viewportScreenSize,
    setViewportZoom,
    setViewportPan,
    panViewport,
    zoomViewport,
    zoomToPoint,
    fitToContent,
    visibleNodes,
    visibleNodeIds,
    isNodeVisible,
    nodeVisibility,
    virtualizedTree,
    virtualNodeCount,
    renderWindow,
    shouldRenderNode,
    renderChunks,
    chunkNodes,
    nextChunk,
    isRenderComplete,
    loadRequest,
    pendingLoads,
    acknowledgeLoad,
    nodeLOD,
    shouldShowLabel,
    shouldShowIcon,
    simplifiedNode,
    Pan,
    Zoom,
    ZoomToLevel,
    FitAll,
    CenterOn,
    ResetView,
    handleViewportEvent
};
//# sourceMappingURL=index.js.map
