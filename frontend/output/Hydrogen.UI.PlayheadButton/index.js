// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Render_Element from "../Hydrogen.Render.Element/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var Compact = /* #__PURE__ */ (function () {
    function Compact() {

    };
    Compact.value = new Compact();
    return Compact;
})();
var Full = /* #__PURE__ */ (function () {
    function Full() {

    };
    Full.value = new Full();
    return Full;
})();
var Seconds = /* #__PURE__ */ (function () {
    function Seconds() {

    };
    Seconds.value = new Seconds();
    return Seconds;
})();
var Default = /* #__PURE__ */ (function () {
    function Default() {

    };
    Default.value = new Default();
    return Default;
})();
var Minimal = /* #__PURE__ */ (function () {
    function Minimal() {

    };
    Minimal.value = new Minimal();
    return Minimal;
})();
var Filled = /* #__PURE__ */ (function () {
    function Filled() {

    };
    Filled.value = new Filled();
    return Filled;
})();
var Outlined = /* #__PURE__ */ (function () {
    function Outlined() {

    };
    Outlined.value = new Outlined();
    return Outlined;
})();
var Ghost = /* #__PURE__ */ (function () {
    function Ghost() {

    };
    Ghost.value = new Ghost();
    return Ghost;
})();
var Xs = /* #__PURE__ */ (function () {
    function Xs() {

    };
    Xs.value = new Xs();
    return Xs;
})();
var Sm = /* #__PURE__ */ (function () {
    function Sm() {

    };
    Sm.value = new Sm();
    return Sm;
})();
var Md = /* #__PURE__ */ (function () {
    function Md() {

    };
    Md.value = new Md();
    return Md;
})();
var Lg = /* #__PURE__ */ (function () {
    function Lg() {

    };
    Lg.value = new Lg();
    return Lg;
})();
var Xl = /* #__PURE__ */ (function () {
    function Xl() {

    };
    Xl.value = new Xl();
    return Xl;
})();
var Pixel = /* #__PURE__ */ (function () {
    function Pixel(value0) {
        this.value0 = value0;
    };
    Pixel.create = function (value0) {
        return new Pixel(value0);
    };
    return Pixel;
})();
var Playing = /* #__PURE__ */ (function () {
    function Playing() {

    };
    Playing.value = new Playing();
    return Playing;
})();
var Paused = /* #__PURE__ */ (function () {
    function Paused() {

    };
    Paused.value = new Paused();
    return Paused;
})();
var Stopped = /* #__PURE__ */ (function () {
    function Stopped() {

    };
    Stopped.value = new Stopped();
    return Stopped;
})();
var Buffering = /* #__PURE__ */ (function () {
    function Buffering() {

    };
    Buffering.value = new Buffering();
    return Buffering;
})();
var Seeking = /* #__PURE__ */ (function () {
    function Seeking() {

    };
    Seeking.value = new Seeking();
    return Seeking;
})();
var Loading = /* #__PURE__ */ (function () {
    function Loading() {

    };
    Loading.value = new Loading();
    return Loading;
})();
var volumeMuted = function (vm) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            volumeMuted: vm
        };
    };
};
var volume = function (v) {
    return function (config) {
        var clamp01 = function (n) {
            var $51 = n < 0.0;
            if ($51) {
                return 0.0;
            };
            var $52 = n > 1.0;
            if ($52) {
                return 1.0;
            };
            return n;
        };
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            volume: clamp01(v)
        };
    };
};
var variantClasses = function (v) {
    if (v instanceof Default) {
        return "bg-gray-800/90 text-white hover:bg-gray-700";
    };
    if (v instanceof Minimal) {
        return "bg-transparent text-gray-300 hover:text-white";
    };
    if (v instanceof Filled) {
        return "bg-white text-gray-900 hover:bg-gray-100";
    };
    if (v instanceof Outlined) {
        return "bg-transparent border-2 border-white/30 text-white hover:border-white";
    };
    if (v instanceof Ghost) {
        return "bg-transparent text-gray-400 hover:bg-white/10 hover:text-white";
    };
    throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 332, column 18 - line 337, column 77): " + [ v.constructor.name ]);
};
var variant = function (v) {
    return function (config) {
        return {
            size: config.size,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            variant: v
        };
    };
};
var trackHeight = function (th) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            trackHeight: new Data_Maybe.Just(th)
        };
    };
};
var title = function (t) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            title: new Data_Maybe.Just(t)
        };
    };
};
var timeFormat = function (tf) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            timeFormat: tf
        };
    };
};
var thumbSize = function (ts) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            thumbSize: ts
        };
    };
};
var tabIndex = function (ti) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            tabIndex: new Data_Maybe.Just(ti)
        };
    };
};
var sizePixels = function (sp) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: new Data_Maybe.Just(sp)
        };
    };
};
var sizeClasses = function (v) {
    if (v instanceof Xs) {
        return "w-6 h-6 text-xs";
    };
    if (v instanceof Sm) {
        return "w-8 h-8 text-sm";
    };
    if (v instanceof Md) {
        return "w-10 h-10 text-base";
    };
    if (v instanceof Lg) {
        return "w-12 h-12 text-lg";
    };
    if (v instanceof Xl) {
        return "w-16 h-16 text-xl";
    };
    if (v instanceof Pixel) {
        return "w-" + (show(v.value0) + (" h-" + show(v.value0)));
    };
    throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 323, column 15 - line 329, column 47): " + [ v.constructor.name ]);
};
var size = function (s) {
    return function (config) {
        return {
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            size: s
        };
    };
};
var showTime = function (st) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            showTime: st
        };
    };
};
var showProgress = function (sp) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            showProgress: sp
        };
    };
};
var pulse = function (p) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            sizePixels: config.sizePixels,
            pulse: p
        };
    };
};
var progressColor = function (pc) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            progressColor: new Data_Maybe.Just(pc)
        };
    };
};
var progress = function (p) {
    return function (config) {
        var clamp01 = function (n) {
            var $56 = n < 0.0;
            if ($56) {
                return 0.0;
            };
            var $57 = n > 1.0;
            if ($57) {
                return 1.0;
            };
            return n;
        };
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            progress: clamp01(p)
        };
    };
};
var playbackState = function (ps) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            playbackState: ps
        };
    };
};
var onClick = function (msg) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            onClick: new Data_Maybe.Just(msg)
        };
    };
};
var loading = function (l) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            loading: l
        };
    };
};
var id_ = function (i) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            id_: new Data_Maybe.Just(i)
        };
    };
};
var glow = function (g) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            glow: g
        };
    };
};
var formatTime = function (seconds) {
    return show1(seconds);
};
var eqTimeFormat = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Compact && y instanceof Compact) {
                return true;
            };
            if (x instanceof Full && y instanceof Full) {
                return true;
            };
            if (x instanceof Seconds && y instanceof Seconds) {
                return true;
            };
            return false;
        };
    }
};
var eqPlayheadVariant = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Default && y instanceof Default) {
                return true;
            };
            if (x instanceof Minimal && y instanceof Minimal) {
                return true;
            };
            if (x instanceof Filled && y instanceof Filled) {
                return true;
            };
            if (x instanceof Outlined && y instanceof Outlined) {
                return true;
            };
            if (x instanceof Ghost && y instanceof Ghost) {
                return true;
            };
            return false;
        };
    }
};
var eqPlayheadSize = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Xs && y instanceof Xs) {
                return true;
            };
            if (x instanceof Sm && y instanceof Sm) {
                return true;
            };
            if (x instanceof Md && y instanceof Md) {
                return true;
            };
            if (x instanceof Lg && y instanceof Lg) {
                return true;
            };
            if (x instanceof Xl && y instanceof Xl) {
                return true;
            };
            if (x instanceof Pixel && y instanceof Pixel) {
                return x.value0 === y.value0;
            };
            return false;
        };
    }
};
var eqPlaybackState = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Playing && y instanceof Playing) {
                return true;
            };
            if (x instanceof Paused && y instanceof Paused) {
                return true;
            };
            if (x instanceof Stopped && y instanceof Stopped) {
                return true;
            };
            if (x instanceof Buffering && y instanceof Buffering) {
                return true;
            };
            if (x instanceof Seeking && y instanceof Seeking) {
                return true;
            };
            if (x instanceof Loading && y instanceof Loading) {
                return true;
            };
            return false;
        };
    }
};
var duration = function (d) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            duration: new Data_Maybe.Just(d)
        };
    };
};
var disabled = function (d) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            disabled: d
        };
    };
};
var defaultConfig = /* #__PURE__ */ (function () {
    return {
        size: Md.value,
        variant: Default.value,
        disabled: false,
        loading: false,
        compact: false,
        className: "",
        id_: Data_Maybe.Nothing.value,
        onClick: Data_Maybe.Nothing.value,
        tabIndex: Data_Maybe.Nothing.value,
        ariaLabel: Data_Maybe.Nothing.value,
        title: Data_Maybe.Nothing.value,
        playbackState: Paused.value,
        progress: 0.0,
        buffered: Data_Maybe.Nothing.value,
        currentTime: Data_Maybe.Nothing.value,
        duration: Data_Maybe.Nothing.value,
        showTime: false,
        timeFormat: Compact.value,
        showTimeRemaining: false,
        isScrubber: false,
        thumbSize: Md.value,
        trackHeight: Data_Maybe.Nothing.value,
        progressColor: Data_Maybe.Nothing.value,
        showThumb: true,
        volume: 1.0,
        volumeMuted: false,
        showProgress: false,
        glow: false,
        pulse: false,
        sizePixels: Data_Maybe.Nothing.value
    };
})();
var scrubber = function (mods) {
    var config = Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })({
        ariaLabel: defaultConfig.ariaLabel,
        buffered: defaultConfig.buffered,
        className: defaultConfig.className,
        compact: defaultConfig.compact,
        currentTime: defaultConfig.currentTime,
        disabled: defaultConfig.disabled,
        duration: defaultConfig.duration,
        glow: defaultConfig.glow,
        id_: defaultConfig.id_,
        loading: defaultConfig.loading,
        onClick: defaultConfig.onClick,
        playbackState: defaultConfig.playbackState,
        progress: defaultConfig.progress,
        progressColor: defaultConfig.progressColor,
        pulse: defaultConfig.pulse,
        showProgress: defaultConfig.showProgress,
        showThumb: defaultConfig.showThumb,
        showTime: defaultConfig.showTime,
        showTimeRemaining: defaultConfig.showTimeRemaining,
        size: defaultConfig.size,
        sizePixels: defaultConfig.sizePixels,
        tabIndex: defaultConfig.tabIndex,
        thumbSize: defaultConfig.thumbSize,
        timeFormat: defaultConfig.timeFormat,
        title: defaultConfig.title,
        trackHeight: defaultConfig.trackHeight,
        variant: defaultConfig.variant,
        volume: defaultConfig.volume,
        volumeMuted: defaultConfig.volumeMuted,
        isScrubber: true
    })(mods);
    var progressBg = Data_Maybe.fromMaybe("bg-blue-500")(config.progressColor);
    var progressWidth = show1(config.progress * 100.0) + "%";
    var trackH = Data_Maybe.fromMaybe("h-1")(config.trackHeight);
    var bufferedWidth = (function () {
        if (config.buffered instanceof Data_Maybe.Just) {
            return show1(config.buffered.value0 * 100.0) + "%";
        };
        if (config.buffered instanceof Data_Maybe.Nothing) {
            return progressWidth;
        };
        throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 430, column 21 - line 432, column 31): " + [ config.buffered.constructor.name ]);
    })();
    return Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("w-full " + (trackH + " cursor-pointer relative")), Hydrogen_Render_Element.role("slider") ])([ Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("bg-white/20 w-full h-full rounded-full") ])([  ]), Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("bg-white/30 absolute h-full top-0 left-0 rounded-full"), Hydrogen_Render_Element.style("width")(bufferedWidth) ])([  ]), Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_(progressBg + " absolute h-full top-0 left-0 rounded-full"), Hydrogen_Render_Element.style("width")(progressWidth) ])([  ]) ]);
};
var timeDisplay = function (mods) {
    var config = Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods);
    var durationStr = (function () {
        if (config.duration instanceof Data_Maybe.Just) {
            return " / " + formatTime(config.duration.value0);
        };
        if (config.duration instanceof Data_Maybe.Nothing) {
            return "";
        };
        throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 458, column 19 - line 460, column 20): " + [ config.duration.constructor.name ]);
    })();
    var timeStr = (function () {
        if (config.currentTime instanceof Data_Maybe.Just) {
            return formatTime(config.currentTime.value0);
        };
        if (config.currentTime instanceof Data_Maybe.Nothing) {
            return "0:00";
        };
        throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 455, column 15 - line 457, column 24): " + [ config.currentTime.constructor.name ]);
    })();
    return Hydrogen_Render_Element.span_([ Hydrogen_Render_Element.class_("font-mono text-sm text-white/90") ])([ Hydrogen_Render_Element.text(timeStr + durationStr) ]);
};
var volumeSlider = function (mods) {
    var config = Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods);
    var icon = (function () {
        var $74 = config.volumeMuted || config.volume === 0.0;
        if ($74) {
            return "\ud83d\udd07";
        };
        return "\ud83d\udd0a";
    })();
    var progressWidth = show1(config.volume * 100.0) + "%";
    var size1 = sizeClasses(config.size);
    return Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("flex items-center gap-2") ])([ Hydrogen_Render_Element.button_([ Hydrogen_Render_Element.class_(size1) ])([ Hydrogen_Render_Element.text(icon) ]), Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("w-20 h-1 bg-white/30 rounded-full relative") ])([ Hydrogen_Render_Element.div_([ Hydrogen_Render_Element.class_("bg-white h-full rounded-full absolute left-0 top-0"), Hydrogen_Render_Element.style("width")(progressWidth) ])([  ]) ]) ]);
};
var currentTime = function (t) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            currentTime: new Data_Maybe.Just(t)
        };
    };
};
var compact = function (c) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            compact: c
        };
    };
};
var className = function (c) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            className: config.className + (" " + c)
        };
    };
};
var buildMediaButton = function (config) {
    return function (iconChar) {
        var variant1 = variantClasses(config.variant);
        var size1 = sizeClasses(config.size);
        var sc = (function () {
            if (config.disabled) {
                return "";
            };
            return variant1;
        })();
        var pc = (function () {
            if (config.pulse) {
                return "animate-pulse";
            };
            return "";
        })();
        var loadingClass = (function () {
            if (config.loading) {
                return "animate-pulse";
            };
            return "";
        })();
        var hc = (function () {
            if (config.disabled) {
                return "";
            };
            return "hover:scale-105";
        })();
        var gc = (function () {
            if (config.glow) {
                return "shadow-lg shadow-blue-500/50";
            };
            return "";
        })();
        var customSize = Data_Maybe.fromMaybe("")(config.sizePixels);
        var ac = (function () {
            if (config.disabled) {
                return "";
            };
            return "active:scale-95";
        })();
        var allClasses = "inline-flex items-center justify-center rounded-full transition-all duration-150 focus:outline-none disabled:pointer-events-none disabled:opacity-50" + (" " + (sc + (" " + (ac + (" " + (hc + (" " + (gc + (" " + (pc + (" " + (loadingClass + (" " + (size1 + (" " + (config.className + (function () {
            var $81 = customSize !== "";
            if ($81) {
                return " " + customSize;
            };
            return "";
        })()))))))))))))))));
        var attrs = append1([ Hydrogen_Render_Element.class_(allClasses), Hydrogen_Render_Element.type_("button"), Hydrogen_Render_Element.disabled(config.disabled) ])(append1((function () {
            if (config.id_ instanceof Data_Maybe.Just) {
                return [ Hydrogen_Render_Element.id_(config.id_.value0) ];
            };
            if (config.id_ instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 498, column 10 - line 500, column 24): " + [ config.id_.constructor.name ]);
        })())(append1((function () {
            if (config.tabIndex instanceof Data_Maybe.Just) {
                return [ Hydrogen_Render_Element.tabIndex(config.tabIndex.value0) ];
            };
            if (config.tabIndex instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 501, column 10 - line 503, column 24): " + [ config.tabIndex.constructor.name ]);
        })())(append1((function () {
            if (config.title instanceof Data_Maybe.Just) {
                return [ Hydrogen_Render_Element.title(config.title.value0) ];
            };
            if (config.title instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 504, column 10 - line 506, column 24): " + [ config.title.constructor.name ]);
        })())(append1((function () {
            if (config.ariaLabel instanceof Data_Maybe.Just) {
                return [ Hydrogen_Render_Element.ariaLabel(config.ariaLabel.value0) ];
            };
            if (config.ariaLabel instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 507, column 10 - line 509, column 24): " + [ config.ariaLabel.constructor.name ]);
        })())((function () {
            if (config.onClick instanceof Data_Maybe.Just) {
                return [ Hydrogen_Render_Element.onClick(config.onClick.value0) ];
            };
            if (config.onClick instanceof Data_Maybe.Nothing) {
                return [  ];
            };
            throw new Error("Failed pattern match at Hydrogen.UI.PlayheadButton (line 510, column 10 - line 512, column 24): " + [ config.onClick.constructor.name ]);
        })())))));
        return Hydrogen_Render_Element.button_(attrs)([ Hydrogen_Render_Element.text(iconChar) ]);
    };
};
var closedCaptions = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udcdd");
};
var exitFullscreen = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u26f6");
};
var fastForward = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23e9");
};
var fullscreen = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u26f6");
};
var live = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udd34");
};
var mediaButton = function (icon) {
    return function (mods) {
        return buildMediaButton(Data_Array.foldl(function (c) {
            return function (f) {
                return f(c);
            };
        })(defaultConfig)(mods))(icon);
    };
};
var mute = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udd07");
};
var nextTrack = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23ed");
};
var pause = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23f8");
};
var pictureInPicture = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udcfa");
};
var play = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u25b6");
};
var previousTrack = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23ee");
};
var record = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23fa");
};
var rewind = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23ea");
};
var settings = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u2699");
};
var skipBackward = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23ee");
};
var skipForward = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23ed");
};
var stop = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\u23f9");
};
var unmute = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udd0a");
};
var volumeDown = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udd09");
};
var volumeUp = function (mods) {
    return buildMediaButton(Data_Array.foldl(function (c) {
        return function (f) {
            return f(c);
        };
    })(defaultConfig)(mods))("\ud83d\udd0a");
};
var buffered = function (b) {
    return function (config) {
        var clamp01 = function (n) {
            var $92 = n < 0.0;
            if ($92) {
                return 0.0;
            };
            var $93 = n > 1.0;
            if ($93) {
                return 1.0;
            };
            return n;
        };
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            ariaLabel: config.ariaLabel,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            buffered: new Data_Maybe.Just(clamp01(b))
        };
    };
};
var ariaLabel = function (al) {
    return function (config) {
        return {
            size: config.size,
            variant: config.variant,
            disabled: config.disabled,
            loading: config.loading,
            compact: config.compact,
            className: config.className,
            id_: config.id_,
            onClick: config.onClick,
            tabIndex: config.tabIndex,
            title: config.title,
            playbackState: config.playbackState,
            progress: config.progress,
            buffered: config.buffered,
            currentTime: config.currentTime,
            duration: config.duration,
            showTime: config.showTime,
            timeFormat: config.timeFormat,
            showTimeRemaining: config.showTimeRemaining,
            isScrubber: config.isScrubber,
            thumbSize: config.thumbSize,
            trackHeight: config.trackHeight,
            progressColor: config.progressColor,
            showThumb: config.showThumb,
            volume: config.volume,
            volumeMuted: config.volumeMuted,
            showProgress: config.showProgress,
            glow: config.glow,
            pulse: config.pulse,
            sizePixels: config.sizePixels,
            ariaLabel: new Data_Maybe.Just(al)
        };
    };
};
export {
    play,
    pause,
    stop,
    skipForward,
    skipBackward,
    fastForward,
    rewind,
    nextTrack,
    previousTrack,
    mute,
    unmute,
    volumeUp,
    volumeDown,
    volumeSlider,
    timeDisplay,
    fullscreen,
    exitFullscreen,
    pictureInPicture,
    closedCaptions,
    settings,
    record,
    live,
    mediaButton,
    defaultConfig,
    size,
    variant,
    disabled,
    loading,
    compact,
    className,
    id_,
    onClick,
    tabIndex,
    ariaLabel,
    title,
    playbackState,
    Playing,
    Paused,
    Stopped,
    Buffering,
    Seeking,
    Loading,
    Compact,
    Full,
    Seconds,
    progress,
    buffered,
    currentTime,
    duration,
    showTime,
    timeFormat,
    scrubber,
    thumbSize,
    trackHeight,
    progressColor,
    volume,
    volumeMuted,
    showProgress,
    glow,
    pulse,
    sizePixels,
    Xs,
    Sm,
    Md,
    Lg,
    Xl,
    Pixel,
    Default,
    Minimal,
    Filled,
    Outlined,
    Ghost,
    eqPlayheadSize,
    eqPlayheadVariant,
    eqPlaybackState,
    eqTimeFormat
};
//# sourceMappingURL=index.js.map
