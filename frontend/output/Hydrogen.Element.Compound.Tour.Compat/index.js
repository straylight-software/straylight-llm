// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Hydrogen_Element_Compound_Tour_Types from "../Hydrogen.Element.Compound.Tour.Types/index.js";
var withScrollMargin = function (m) {
    return function (s) {
        return {
            config: {
                target: s.config.target,
                title: s.config.title,
                content: s.config.content,
                placement: s.config.placement,
                offset: s.config.offset,
                showArrow: s.config.showArrow,
                highlightPadding: s.config.highlightPadding,
                scrollMargin: m
            }
        };
    };
};
var withPlacement = function (p) {
    return function (s) {
        return {
            config: {
                target: s.config.target,
                title: s.config.title,
                content: s.config.content,
                offset: s.config.offset,
                showArrow: s.config.showArrow,
                highlightPadding: s.config.highlightPadding,
                scrollMargin: s.config.scrollMargin,
                placement: p
            }
        };
    };
};
var withOffset = function (o) {
    return function (s) {
        return {
            config: {
                target: s.config.target,
                title: s.config.title,
                content: s.config.content,
                placement: s.config.placement,
                showArrow: s.config.showArrow,
                highlightPadding: s.config.highlightPadding,
                scrollMargin: s.config.scrollMargin,
                offset: o
            }
        };
    };
};
var withHighlightPadding = function (p) {
    return function (s) {
        return {
            config: {
                target: s.config.target,
                title: s.config.title,
                content: s.config.content,
                placement: s.config.placement,
                offset: s.config.offset,
                showArrow: s.config.showArrow,
                scrollMargin: s.config.scrollMargin,
                highlightPadding: p
            }
        };
    };
};
var withArrow = function (a) {
    return function (s) {
        return {
            config: {
                target: s.config.target,
                title: s.config.title,
                content: s.config.content,
                placement: s.config.placement,
                offset: s.config.offset,
                highlightPadding: s.config.highlightPadding,
                scrollMargin: s.config.scrollMargin,
                showArrow: a
            }
        };
    };
};
var tooltipClassName = function (c) {
    return function (props) {
        return {
            placement: props.placement,
            offset: props.offset,
            showArrow: props.showArrow,
            className: props.className + (" " + c)
        };
    };
};
var steps = function (s) {
    return function (props) {
        return {
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            steps: s
        };
    };
};
var stepCount = Data_Array.length;
var step = function (cfg) {
    return {
        config: {
            target: cfg.target,
            title: cfg.title,
            content: cfg.content,
            placement: Hydrogen_Element_Compound_Tour_Types.PlacementBottom.value,
            offset: 8,
            showArrow: true,
            highlightPadding: 4,
            scrollMargin: 20
        }
    };
};
var showProgress = function (s) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            showProgress: s
        };
    };
};
var showOverlay = function (s) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            showOverlay: s
        };
    };
};
var scrollBehavior = function (s) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            scrollBehavior: s
        };
    };
};
var placement = function (p) {
    return function (props) {
        return {
            offset: props.offset,
            showArrow: props.showArrow,
            className: props.className,
            placement: p
        };
    };
};
var persistKey = function (k) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            persistKey: new Data_Maybe.Just(k)
        };
    };
};
var overlayOpacity = function (o) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            overlayOpacity: o
        };
    };
};
var onStepChange = function (handler) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            className: props.className,
            onStepChange: new Data_Maybe.Just(handler)
        };
    };
};
var onStep = function (handler) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            onStep: new Data_Maybe.Just(handler)
        };
    };
};
var onStart = function (handler) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            onStart: new Data_Maybe.Just(handler)
        };
    };
};
var onSkip = function (handler) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onStepChange: props.onStepChange,
            className: props.className,
            onSkip: new Data_Maybe.Just(handler)
        };
    };
};
var onComplete = function (handler) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            onComplete: new Data_Maybe.Just(handler)
        };
    };
};
var offset = function (o) {
    return function (props) {
        return {
            placement: props.placement,
            showArrow: props.showArrow,
            className: props.className,
            offset: o
        };
    };
};
var isLastStep = function (idx) {
    return function (stps) {
        return idx >= (Data_Array.length(stps) - 1 | 0);
    };
};
var isFirstStep = function (idx) {
    return idx === 0;
};
var isActive = function (a) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            isActive: a
        };
    };
};
var getStepAt = function (idx) {
    return function (arr) {
        return Data_Array.index(arr)(idx);
    };
};
var defaultTooltipProps = /* #__PURE__ */ (function () {
    return {
        placement: Hydrogen_Element_Compound_Tour_Types.PlacementBottom.value,
        offset: 8,
        showArrow: true,
        className: ""
    };
})();
var defaultProps = /* #__PURE__ */ (function () {
    return {
        steps: [  ],
        currentStep: 0,
        isActive: true,
        showProgress: true,
        showOverlay: true,
        overlayOpacity: 0.5,
        allowClickThrough: false,
        scrollBehavior: Hydrogen_Element_Compound_Tour_Types.ScrollSmooth.value,
        persistKey: Data_Maybe.Nothing.value,
        onStart: Data_Maybe.Nothing.value,
        onStep: Data_Maybe.Nothing.value,
        onComplete: Data_Maybe.Nothing.value,
        onSkip: Data_Maybe.Nothing.value,
        onStepChange: Data_Maybe.Nothing.value,
        className: ""
    };
})();
var currentStep = function (s) {
    return function (props) {
        return {
            steps: props.steps,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            currentStep: s
        };
    };
};
var className = function (c) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            allowClickThrough: props.allowClickThrough,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className + (" " + c)
        };
    };
};
var allowClickThrough = function (a) {
    return function (props) {
        return {
            steps: props.steps,
            currentStep: props.currentStep,
            isActive: props.isActive,
            showProgress: props.showProgress,
            showOverlay: props.showOverlay,
            overlayOpacity: props.overlayOpacity,
            scrollBehavior: props.scrollBehavior,
            persistKey: props.persistKey,
            onStart: props.onStart,
            onStep: props.onStep,
            onComplete: props.onComplete,
            onSkip: props.onSkip,
            onStepChange: props.onStepChange,
            className: props.className,
            allowClickThrough: a
        };
    };
};
export {
    defaultProps,
    defaultTooltipProps,
    step,
    withPlacement,
    withOffset,
    withArrow,
    withHighlightPadding,
    withScrollMargin,
    steps,
    currentStep,
    isActive,
    showProgress,
    showOverlay,
    overlayOpacity,
    allowClickThrough,
    scrollBehavior,
    persistKey,
    onStart,
    onStep,
    onComplete,
    onSkip,
    onStepChange,
    className,
    placement,
    offset,
    tooltipClassName,
    getStepAt,
    stepCount,
    isFirstStep,
    isLastStep
};
//# sourceMappingURL=index.js.map
