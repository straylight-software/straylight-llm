// Generated by purs version 0.15.15
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
var PixelEngine = /* #__PURE__ */ (function () {
    function PixelEngine() {

    };
    PixelEngine.value = new PixelEngine();
    return PixelEngine;
})();
var BristleEngine = /* #__PURE__ */ (function () {
    function BristleEngine() {

    };
    BristleEngine.value = new BristleEngine();
    return BristleEngine;
})();
var ParticleEngine = /* #__PURE__ */ (function () {
    function ParticleEngine() {

    };
    ParticleEngine.value = new ParticleEngine();
    return ParticleEngine;
})();
var VectorEngine = /* #__PURE__ */ (function () {
    function VectorEngine() {

    };
    VectorEngine.value = new VectorEngine();
    return VectorEngine;
})();
var SmudgeEngine = /* #__PURE__ */ (function () {
    function SmudgeEngine() {

    };
    SmudgeEngine.value = new SmudgeEngine();
    return SmudgeEngine;
})();
var CloneEngine = /* #__PURE__ */ (function () {
    function CloneEngine() {

    };
    CloneEngine.value = new CloneEngine();
    return CloneEngine;
})();
var SymmetryEngine = /* #__PURE__ */ (function () {
    function SymmetryEngine() {

    };
    SymmetryEngine.value = new SymmetryEngine();
    return SymmetryEngine;
})();
var PatternEngine = /* #__PURE__ */ (function () {
    function PatternEngine() {

    };
    PatternEngine.value = new PatternEngine();
    return PatternEngine;
})();
var showBrushEngine = {
    show: function (v) {
        if (v instanceof PixelEngine) {
            return "(BrushEngine PixelEngine)";
        };
        if (v instanceof BristleEngine) {
            return "(BrushEngine BristleEngine)";
        };
        if (v instanceof ParticleEngine) {
            return "(BrushEngine ParticleEngine)";
        };
        if (v instanceof VectorEngine) {
            return "(BrushEngine VectorEngine)";
        };
        if (v instanceof SmudgeEngine) {
            return "(BrushEngine SmudgeEngine)";
        };
        if (v instanceof CloneEngine) {
            return "(BrushEngine CloneEngine)";
        };
        if (v instanceof SymmetryEngine) {
            return "(BrushEngine SymmetryEngine)";
        };
        if (v instanceof PatternEngine) {
            return "(BrushEngine PatternEngine)";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Engine.Types (line 92, column 1 - line 100, column 53): " + [ v.constructor.name ]);
    }
};
var show = /* #__PURE__ */ Data_Show.show(showBrushEngine);
var eqBrushEngine = {
    eq: function (x) {
        return function (y) {
            if (x instanceof PixelEngine && y instanceof PixelEngine) {
                return true;
            };
            if (x instanceof BristleEngine && y instanceof BristleEngine) {
                return true;
            };
            if (x instanceof ParticleEngine && y instanceof ParticleEngine) {
                return true;
            };
            if (x instanceof VectorEngine && y instanceof VectorEngine) {
                return true;
            };
            if (x instanceof SmudgeEngine && y instanceof SmudgeEngine) {
                return true;
            };
            if (x instanceof CloneEngine && y instanceof CloneEngine) {
                return true;
            };
            if (x instanceof SymmetryEngine && y instanceof SymmetryEngine) {
                return true;
            };
            if (x instanceof PatternEngine && y instanceof PatternEngine) {
                return true;
            };
            return false;
        };
    }
};
var ordBrushEngine = {
    compare: function (x) {
        return function (y) {
            if (x instanceof PixelEngine && y instanceof PixelEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof PixelEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof PixelEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof BristleEngine && y instanceof BristleEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof BristleEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof BristleEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof ParticleEngine && y instanceof ParticleEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof ParticleEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof ParticleEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VectorEngine && y instanceof VectorEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VectorEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VectorEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof SmudgeEngine && y instanceof SmudgeEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof SmudgeEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof SmudgeEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof CloneEngine && y instanceof CloneEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof CloneEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof CloneEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof SymmetryEngine && y instanceof SymmetryEngine) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof SymmetryEngine) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof SymmetryEngine) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof PatternEngine && y instanceof PatternEngine) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Engine.Types (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqBrushEngine;
    }
};
var brushEngineId = function (v) {
    if (v instanceof PixelEngine) {
        return "pixel";
    };
    if (v instanceof BristleEngine) {
        return "bristle";
    };
    if (v instanceof ParticleEngine) {
        return "particle";
    };
    if (v instanceof VectorEngine) {
        return "vector";
    };
    if (v instanceof SmudgeEngine) {
        return "smudge";
    };
    if (v instanceof CloneEngine) {
        return "clone";
    };
    if (v instanceof SymmetryEngine) {
        return "symmetry";
    };
    if (v instanceof PatternEngine) {
        return "pattern";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Engine.Types (line 124, column 1 - line 124, column 39): " + [ v.constructor.name ]);
};
var brushEngineDescription = function (v) {
    if (v instanceof PixelEngine) {
        return "Raster stamp-based rendering. Fast general-purpose painting.";
    };
    if (v instanceof BristleEngine) {
        return "Individual bristle simulation. Natural media feel with ink depletion.";
    };
    if (v instanceof ParticleEngine) {
        return "Particle system spray. Airbrush effects and scatter patterns.";
    };
    if (v instanceof VectorEngine) {
        return "Resolution-independent paths. Clean illustration lines.";
    };
    if (v instanceof SmudgeEngine) {
        return "Pixel-pushing blending. Smooth color transitions.";
    };
    if (v instanceof CloneEngine) {
        return "Source-sampling brush. Clone, heal, and pattern stamp.";
    };
    if (v instanceof SymmetryEngine) {
        return "Mirrored drawing. Radial and bilateral symmetry modes.";
    };
    if (v instanceof PatternEngine) {
        return "Repeating patterns. Decorative borders and textures.";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Engine.Types (line 137, column 1 - line 137, column 48): " + [ v.constructor.name ]);
};
var brushEngineDebugInfo = function (engine) {
    return show(engine) + (" [id: " + (brushEngineId(engine) + "]"));
};
var allBrushEngines = /* #__PURE__ */ (function () {
    return [ PixelEngine.value, BristleEngine.value, ParticleEngine.value, VectorEngine.value, SmudgeEngine.value, CloneEngine.value, SymmetryEngine.value, PatternEngine.value ];
})();
export {
    PixelEngine,
    BristleEngine,
    ParticleEngine,
    VectorEngine,
    SmudgeEngine,
    CloneEngine,
    SymmetryEngine,
    PatternEngine,
    allBrushEngines,
    brushEngineId,
    brushEngineDescription,
    brushEngineDebugInfo,
    eqBrushEngine,
    ordBrushEngine,
    showBrushEngine
};
//# sourceMappingURL=index.js.map
