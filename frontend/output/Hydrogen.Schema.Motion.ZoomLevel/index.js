// Generated by purs version 0.15.15
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Bounded from "../Hydrogen.Schema.Bounded/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var ZoomLevel = function (x) {
    return x;
};
var zoomStep = 1.2;
var zoomFit = 1.0;
var zoom50 = 0.5;
var zoom400 = 4.0;
var zoom25 = 0.25;
var zoom200 = 2.0;
var zoom100 = 1.0;
var toPercentage = function (v) {
    return v * 100.0;
};
var toNumber = function (v) {
    return v;
};
var showZoomLevel = {
    show: function (v) {
        return show(v * 100.0) + "%";
    }
};
var pixelsPerFrame = function (v) {
    return function (basePixelsPerFrame) {
        return v * basePixelsPerFrame;
    };
};
var visibleFrames = function (zoom) {
    return function (viewportWidth) {
        return function (basePixelsPerFrame) {
            var ppf = pixelsPerFrame(zoom)(basePixelsPerFrame);
            return viewportWidth / ppf;
        };
    };
};
var minZoom = 1.0e-2;
var maxZoom = 100.0;
var framesPerPixel = function (v) {
    return function (basePixelsPerFrame) {
        return 1.0 / (v * basePixelsPerFrame);
    };
};
var eqZoomLevel = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordZoomLevel = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqZoomLevel;
    }
};
var $$default = 1.0;
var clamp = function (v) {
    return max(1.0e-2)(min(100.0)(v));
};
var zoomLevel = function (n) {
    return clamp(n);
};
var fitToWidth = function (v) {
    return function (viewportWidth) {
        return function (basePixelsPerFrame) {
            var requiredZoom = viewportWidth / (v * basePixelsPerFrame);
            return zoomLevel(requiredZoom);
        };
    };
};
var fromPercentage = function (pct) {
    return zoomLevel(pct / 100.0);
};
var zoomTo = function (pct) {
    return function (v) {
        return fromPercentage(pct);
    };
};
var setZoom = function (n) {
    return function (v) {
        return zoomLevel(n);
    };
};
var zoomIn = function (v) {
    return zoomLevel(v * zoomStep);
};
var zoomOut = function (v) {
    return zoomLevel(v / zoomStep);
};
var bounds = /* #__PURE__ */ Hydrogen_Schema_Bounded.numberBounds(1.0e-2)(100.0)("zoomLevel")("Timeline zoom scale (0.01-100.0)");
export {
    ZoomLevel,
    zoomLevel,
    fromPercentage,
    $$default as default,
    fitToWidth,
    toNumber,
    toPercentage,
    zoomIn,
    zoomOut,
    zoomTo,
    setZoom,
    pixelsPerFrame,
    framesPerPixel,
    visibleFrames,
    zoomFit,
    zoom25,
    zoom50,
    zoom100,
    zoom200,
    zoom400,
    minZoom,
    maxZoom,
    clamp,
    bounds,
    eqZoomLevel,
    ordZoomLevel,
    showZoomLevel
};
//# sourceMappingURL=index.js.map
