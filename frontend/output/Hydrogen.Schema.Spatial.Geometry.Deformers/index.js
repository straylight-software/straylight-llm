// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Hydrogen_Math_Core_Arithmetic from "../Hydrogen.Math.Core.Arithmetic/index.js";
import * as Hydrogen_Math_Core_Constants from "../Hydrogen.Math.Core.Constants/index.js";
import * as Hydrogen_Math_Core_Trigonometry from "../Hydrogen.Math.Core.Trigonometry/index.js";
var div = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var AxisX = /* #__PURE__ */ (function () {
    function AxisX() {

    };
    AxisX.value = new AxisX();
    return AxisX;
})();
var AxisY = /* #__PURE__ */ (function () {
    function AxisY() {

    };
    AxisY.value = new AxisY();
    return AxisY;
})();
var AxisZ = /* #__PURE__ */ (function () {
    function AxisZ() {

    };
    AxisZ.value = new AxisZ();
    return AxisZ;
})();
var DefBend = /* #__PURE__ */ (function () {
    function DefBend(value0) {
        this.value0 = value0;
    };
    DefBend.create = function (value0) {
        return new DefBend(value0);
    };
    return DefBend;
})();
var DefTwist = /* #__PURE__ */ (function () {
    function DefTwist(value0) {
        this.value0 = value0;
    };
    DefTwist.create = function (value0) {
        return new DefTwist(value0);
    };
    return DefTwist;
})();
var DefTaper = /* #__PURE__ */ (function () {
    function DefTaper(value0) {
        this.value0 = value0;
    };
    DefTaper.create = function (value0) {
        return new DefTaper(value0);
    };
    return DefTaper;
})();
var DefBulge = /* #__PURE__ */ (function () {
    function DefBulge(value0) {
        this.value0 = value0;
    };
    DefBulge.create = function (value0) {
        return new DefBulge(value0);
    };
    return DefBulge;
})();
var DefNoise = /* #__PURE__ */ (function () {
    function DefNoise(value0) {
        this.value0 = value0;
    };
    DefNoise.create = function (value0) {
        return new DefNoise(value0);
    };
    return DefNoise;
})();
var DefSpherify = /* #__PURE__ */ (function () {
    function DefSpherify(value0) {
        this.value0 = value0;
    };
    DefSpherify.create = function (value0) {
        return new DefSpherify(value0);
    };
    return DefSpherify;
})();
var unsafeIndex = function (arr) {
    return function (i) {
        var v = Data_Array.index(arr)(i);
        if (v instanceof Data_Maybe.Just) {
            return v.value0;
        };
        if (v instanceof Data_Maybe.Nothing) {
            return 0.0;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 663, column 21 - line 665, column 17): " + [ v.constructor.name ]);
    };
};
var transformVerticesIndexed = function (positions) {
    return function (f) {
        var vertexCount = div(Data_Array.length(positions))(3);
        var indices = Data_Array.range(0)(vertexCount - 1 | 0);
        return Data_Array.concatMap(function (i) {
            var z = unsafeIndex(positions)((i * 3 | 0) + 2 | 0);
            var y = unsafeIndex(positions)((i * 3 | 0) + 1 | 0);
            var x = unsafeIndex(positions)(i * 3 | 0);
            var result = f(i)(x)(y)(z);
            return [ result.x, result.y, result.z ];
        })(indices);
    };
};
var transformVertices = function (positions) {
    return function (f) {
        var vertexCount = div(Data_Array.length(positions))(3);
        var indices = Data_Array.range(0)(vertexCount - 1 | 0);
        return Data_Array.concatMap(function (i) {
            var z = unsafeIndex(positions)((i * 3 | 0) + 2 | 0);
            var y = unsafeIndex(positions)((i * 3 | 0) + 1 | 0);
            var x = unsafeIndex(positions)(i * 3 | 0);
            var result = f(x)(y)(z);
            return [ result.x, result.y, result.z ];
        })(indices);
    };
};
var spherify = function (cfg) {
    return function (v) {
        var deformedPositions = transformVertices(v.positions)(function (x) {
            return function (y) {
                return function (z) {
                    var dz = z - cfg.center.z;
                    var dy = y - cfg.center.y;
                    var dx = x - cfg.center.x;
                    var dist = Hydrogen_Math_Core_Arithmetic.sqrt(dx * dx + dy * dy + dz * dz);
                    var scale = (function () {
                        var $74 = dist > 1.0e-4;
                        if ($74) {
                            return cfg.radius / dist;
                        };
                        return 1.0;
                    })();
                    var targetX = cfg.center.x + dx * scale;
                    var targetY = cfg.center.y + dy * scale;
                    var targetZ = cfg.center.z + dz * scale;
                    return {
                        x: x + (targetX - x) * cfg.strength,
                        y: y + (targetY - y) * cfg.strength,
                        z: z + (targetZ - z) * cfg.strength
                    };
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var showDeformer = {
    show: function (v) {
        if (v instanceof DefBend) {
            return "Deformer:Bend";
        };
        if (v instanceof DefTwist) {
            return "Deformer:Twist";
        };
        if (v instanceof DefTaper) {
            return "Deformer:Taper";
        };
        if (v instanceof DefBulge) {
            return "Deformer:Bulge";
        };
        if (v instanceof DefNoise) {
            return "Deformer:Noise";
        };
        if (v instanceof DefSpherify) {
            return "Deformer:Spherify";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 519, column 1 - line 525, column 45): " + [ v.constructor.name ]);
    }
};
var showDeformAxis = {
    show: function (v) {
        if (v instanceof AxisX) {
            return "X";
        };
        if (v instanceof AxisY) {
            return "Y";
        };
        if (v instanceof AxisZ) {
            return "Z";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 122, column 1 - line 125, column 19): " + [ v.constructor.name ]);
    }
};
var reconstructFromAxis = function (v) {
    return function (v1) {
        return function (v2) {
            return function (v3) {
                if (v instanceof AxisX) {
                    return {
                        x: v1,
                        y: v2,
                        z: v3
                    };
                };
                if (v instanceof AxisY) {
                    return {
                        x: v2,
                        y: v1,
                        z: v3
                    };
                };
                if (v instanceof AxisZ) {
                    return {
                        x: v2,
                        y: v3,
                        z: v1
                    };
                };
                throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 611, column 1 - line 611, column 109): " + [ v.constructor.name, v1.constructor.name, v2.constructor.name, v3.constructor.name ]);
            };
        };
    };
};
var normalizeNormalArray = function (normals) {
    var vertexCount = div(Data_Array.length(normals))(3);
    return Data_Array.concatMap(function (i) {
        var nz = unsafeIndex(normals)((i * 3 | 0) + 2 | 0);
        var ny = unsafeIndex(normals)((i * 3 | 0) + 1 | 0);
        var nx = unsafeIndex(normals)(i * 3 | 0);
        var len = Hydrogen_Math_Core_Arithmetic.sqrt(nx * nx + ny * ny + nz * nz);
        var $87 = len > 1.0e-4;
        if ($87) {
            return [ nx / len, ny / len, nz / len ];
        };
        return [ 0.0, 1.0, 0.0 ];
    })(Data_Array.range(0)(vertexCount - 1 | 0));
};
var mod = function (a) {
    return function (b) {
        return a - (div(a)(b) * b | 0) | 0;
    };
};
var hashVertex = function (x) {
    return function (y) {
        return function (z) {
            return function (seed) {
                var iz = Data_Int.floor(z * 1000.0);
                var iy = Data_Int.floor(y * 1000.0);
                var ix = Data_Int.floor(x * 1000.0);
                var combined = ((ix + (iy * 31 | 0) | 0) + (iz * 997 | 0) | 0) + (seed * 7919 | 0) | 0;
                var h1 = (combined * 1103515245 | 0) + 12345 | 0;
                var h2 = Data_Int.toNumber(mod(h1)(1000000));
                return h2 / 1000000.0;
            };
        };
    };
};
var fbmNoise = function (base) {
    return function (octaves) {
        var go = function ($copy_acc) {
            return function ($copy_amp) {
                return function ($copy_freq) {
                    return function ($copy_i) {
                        var $tco_var_acc = $copy_acc;
                        var $tco_var_amp = $copy_amp;
                        var $tco_var_freq = $copy_freq;
                        var $tco_done = false;
                        var $tco_result;
                        function $tco_loop(acc, amp, freq, i) {
                            if (i >= octaves) {
                                $tco_done = true;
                                return acc;
                            };
                            if (Data_Boolean.otherwise) {
                                var noiseVal = Hydrogen_Math_Core_Trigonometry.sin(base * freq * 12.9898 + freq * 78.233) * 43758.5453;
                                var frac = noiseVal - Data_Int.toNumber(Data_Int.floor(noiseVal));
                                var contribution = (frac * 2.0 - 1.0) * amp;
                                $tco_var_acc = acc + contribution;
                                $tco_var_amp = amp * 0.5;
                                $tco_var_freq = freq * 2.0;
                                $copy_i = i + 1 | 0;
                                return;
                            };
                            throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 648, column 5 - line 657, column 69): " + [ acc.constructor.name, amp.constructor.name, freq.constructor.name, i.constructor.name ]);
                        };
                        while (!$tco_done) {
                            $tco_result = $tco_loop($tco_var_acc, $tco_var_amp, $tco_var_freq, $copy_i);
                        };
                        return $tco_result;
                    };
                };
            };
        };
        return go(0.0)(1.0)(1.0)(0);
    };
};
var eqDeformAxis = {
    eq: function (x) {
        return function (y) {
            if (x instanceof AxisX && y instanceof AxisX) {
                return true;
            };
            if (x instanceof AxisY && y instanceof AxisY) {
                return true;
            };
            if (x instanceof AxisZ && y instanceof AxisZ) {
                return true;
            };
            return false;
        };
    }
};
var eq2 = /* #__PURE__ */ Data_Eq.eq(eqDeformAxis);
var eqDeformer = {
    eq: function (x) {
        return function (y) {
            if (x instanceof DefBend && y instanceof DefBend) {
                return x.value0.angle === y.value0.angle && eq2(x.value0.axis)(y.value0.axis) && x.value0.length === y.value0.length && x.value0.origin === y.value0.origin && x.value0.strength === y.value0.strength;
            };
            if (x instanceof DefTwist && y instanceof DefTwist) {
                return x.value0.angle === y.value0.angle && eq2(x.value0.axis)(y.value0.axis) && x.value0.length === y.value0.length && x.value0.origin === y.value0.origin && x.value0.strength === y.value0.strength;
            };
            if (x instanceof DefTaper && y instanceof DefTaper) {
                return x.value0.amount === y.value0.amount && eq2(x.value0.axis)(y.value0.axis) && x.value0.curvature === y.value0.curvature && x.value0.length === y.value0.length && x.value0.origin === y.value0.origin && x.value0.strength === y.value0.strength;
            };
            if (x instanceof DefBulge && y instanceof DefBulge) {
                return x.value0.amount === y.value0.amount && eq2(x.value0.axis)(y.value0.axis) && x.value0.falloff === y.value0.falloff && x.value0.origin === y.value0.origin && x.value0.radius === y.value0.radius && x.value0.strength === y.value0.strength;
            };
            if (x instanceof DefNoise && y instanceof DefNoise) {
                return x.value0.amplitude === y.value0.amplitude && x.value0.frequency === y.value0.frequency && x.value0.octaves === y.value0.octaves && x.value0.seed === y.value0.seed && x.value0.strength === y.value0.strength;
            };
            if (x instanceof DefSpherify && y instanceof DefSpherify) {
                return x.value0.center.x === y.value0.center.x && x.value0.center.y === y.value0.center.y && x.value0.center.z === y.value0.center.z && x.value0.radius === y.value0.radius && x.value0.strength === y.value0.strength;
            };
            return false;
        };
    }
};
var ordDeformAxis = {
    compare: function (x) {
        return function (y) {
            if (x instanceof AxisX && y instanceof AxisX) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof AxisX) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof AxisX) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof AxisY && y instanceof AxisY) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof AxisY) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof AxisY) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof AxisZ && y instanceof AxisZ) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqDeformAxis;
    }
};
var defaultTwistConfig = /* #__PURE__ */ (function () {
    return {
        angle: Hydrogen_Math_Core_Constants.pi / 2.0,
        axis: AxisY.value,
        origin: 0.0,
        length: 1.0,
        strength: 1.0
    };
})();
var defaultTaperConfig = /* #__PURE__ */ (function () {
    return {
        amount: 0.5,
        axis: AxisY.value,
        origin: 0.0,
        length: 1.0,
        curvature: 0.0,
        strength: 1.0
    };
})();
var defaultSpherifyConfig = {
    radius: 0.5,
    center: {
        x: 0.0,
        y: 0.0,
        z: 0.0
    },
    strength: 1.0
};
var defaultNoiseConfig = {
    amplitude: 0.1,
    frequency: 1.0,
    octaves: 3,
    seed: 0,
    strength: 1.0
};
var defaultBulgeConfig = /* #__PURE__ */ (function () {
    return {
        amount: 0.2,
        axis: AxisY.value,
        origin: 0.0,
        radius: 1.0,
        falloff: 0.5,
        strength: 1.0
    };
})();
var defaultBendConfig = /* #__PURE__ */ (function () {
    return {
        angle: Hydrogen_Math_Core_Constants.pi / 4.0,
        axis: AxisY.value,
        origin: 0.0,
        length: 0.0,
        strength: 1.0
    };
})();
var clampInt = function (minVal) {
    return function (maxVal) {
        return function (x) {
            if (x < minVal) {
                return minVal;
            };
            if (x > maxVal) {
                return maxVal;
            };
            if (Data_Boolean.otherwise) {
                return x;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 624, column 1 - line 624, column 37): " + [ minVal.constructor.name, maxVal.constructor.name, x.constructor.name ]);
        };
    };
};
var noise = function (cfg) {
    return function (v) {
        var octaves = clampInt(1)(8)(cfg.octaves);
        var deformedPositions = transformVerticesIndexed(v.positions)(function (i) {
            return function (x) {
                return function (y) {
                    return function (z) {
                        var hash = hashVertex(x * cfg.frequency)(y * cfg.frequency)(z * cfg.frequency)(cfg.seed);
                        var noiseVal = fbmNoise(hash)(octaves);
                        var dist = Hydrogen_Math_Core_Arithmetic.sqrt(x * x + y * y + z * z);
                        var disp = noiseVal * cfg.amplitude * cfg.strength;
                        var dirZ = (function () {
                            var $116 = dist > 1.0e-4;
                            if ($116) {
                                return z / dist;
                            };
                            return 0.0;
                        })();
                        var dirY = (function () {
                            var $117 = dist > 1.0e-4;
                            if ($117) {
                                return y / dist;
                            };
                            return 1.0;
                        })();
                        var dirX = (function () {
                            var $118 = dist > 1.0e-4;
                            if ($118) {
                                return x / dist;
                            };
                            return 0.0;
                        })();
                        return {
                            x: x + dirX * disp,
                            y: y + dirY * disp,
                            z: z + dirZ * disp
                        };
                    };
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var clamp01 = function (x) {
    if (x < 0.0) {
        return 0.0;
    };
    if (x > 1.0) {
        return 1.0;
    };
    if (Data_Boolean.otherwise) {
        return x;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 617, column 1 - line 617, column 28): " + [ x.constructor.name ]);
};
var calculateVertexNormals = function (positions) {
    return map(function (i) {
        var vi = div(i)(3);
        var pos = unsafeIndex(positions)(i);
        var dist = Hydrogen_Math_Core_Arithmetic.sqrt(unsafeIndex(positions)(vi * 3 | 0) * unsafeIndex(positions)(vi * 3 | 0) + unsafeIndex(positions)((vi * 3 | 0) + 1 | 0) * unsafeIndex(positions)((vi * 3 | 0) + 1 | 0) + unsafeIndex(positions)((vi * 3 | 0) + 2 | 0) * unsafeIndex(positions)((vi * 3 | 0) + 2 | 0));
        var $120 = dist > 1.0e-4;
        if ($120) {
            return pos / dist;
        };
        return 0.0;
    })(Data_Array.range(0)(Data_Array.length(positions) - 1 | 0));
};
var axisComponents = function (v) {
    return function (v1) {
        return function (v2) {
            return function (v3) {
                if (v instanceof AxisX) {
                    return {
                        along: v1,
                        perp1: v2,
                        perp2: v3
                    };
                };
                if (v instanceof AxisY) {
                    return {
                        along: v2,
                        perp1: v1,
                        perp2: v3
                    };
                };
                if (v instanceof AxisZ) {
                    return {
                        along: v3,
                        perp1: v1,
                        perp2: v2
                    };
                };
                throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 605, column 1 - line 605, column 116): " + [ v.constructor.name, v1.constructor.name, v2.constructor.name, v3.constructor.name ]);
            };
        };
    };
};
var bend = function (cfg) {
    return function (v) {
        var deformedPositions = transformVertices(v.positions)(function (x) {
            return function (y) {
                return function (z) {
                    var v1 = axisComponents(cfg.axis)(x)(y)(z);
                    var t = (function () {
                        var $129 = cfg.length > 0.0 && cfg.length !== 0.0;
                        if ($129) {
                            return clamp01((v1.along - cfg.origin) / cfg.length);
                        };
                        var $130 = v1.along >= cfg.origin;
                        if ($130) {
                            return 1.0;
                        };
                        return 0.0;
                    })();
                    var bendAngle = cfg.angle * t * cfg.strength;
                    var cosA = Hydrogen_Math_Core_Trigonometry.cos(bendAngle);
                    var sinA = Hydrogen_Math_Core_Trigonometry.sin(bendAngle);
                    var newAlong = cfg.origin + v1.perp1 * sinA + (v1.along - cfg.origin) * cosA;
                    var newPerp1 = v1.perp1 * cosA - (v1.along - cfg.origin) * sinA;
                    return reconstructFromAxis(cfg.axis)(newAlong)(newPerp1)(v1.perp2);
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var bulge = function (cfg) {
    return function (v) {
        var deformedPositions = transformVertices(v.positions)(function (x) {
            return function (y) {
                return function (z) {
                    var v1 = axisComponents(cfg.axis)(x)(y)(z);
                    var radialDist = Hydrogen_Math_Core_Arithmetic.sqrt(v1.perp1 * v1.perp1 + v1.perp2 * v1.perp2);
                    var distAlong = Hydrogen_Math_Core_Arithmetic.abs(v1.along - cfg.origin);
                    var falloff = (function () {
                        var $138 = cfg.radius > 0.0 && cfg.radius !== 0.0;
                        if ($138) {
                            return 1.0 - clamp01(distAlong / cfg.radius);
                        };
                        return 1.0;
                    })();
                    var falloffCurved = Hydrogen_Math_Core_Arithmetic.pow(falloff)(1.0 + cfg.falloff);
                    var scale = 1.0 + cfg.amount * falloffCurved * cfg.strength;
                    var newPerp1 = v1.perp1 * scale;
                    var newPerp2 = v1.perp2 * scale;
                    return reconstructFromAxis(cfg.axis)(v1.along)(newPerp1)(newPerp2);
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var taper = function (cfg) {
    return function (v) {
        var deformedPositions = transformVertices(v.positions)(function (x) {
            return function (y) {
                return function (z) {
                    var v1 = axisComponents(cfg.axis)(x)(y)(z);
                    var t = (function () {
                        var $146 = cfg.length > 0.0 && cfg.length !== 0.0;
                        if ($146) {
                            return clamp01((v1.along - cfg.origin) / cfg.length);
                        };
                        var $147 = v1.along >= cfg.origin;
                        if ($147) {
                            return 1.0;
                        };
                        return 0.0;
                    })();
                    var tCurved = (function () {
                        var $148 = cfg.curvature > 0.0;
                        if ($148) {
                            return Hydrogen_Math_Core_Arithmetic.pow(t)(1.0 + cfg.curvature);
                        };
                        return t;
                    })();
                    var scale = 1.0 - cfg.amount * tCurved * cfg.strength;
                    var newPerp2 = v1.perp2 * scale;
                    var newPerp1 = v1.perp1 * scale;
                    return reconstructFromAxis(cfg.axis)(v1.along)(newPerp1)(newPerp2);
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var twist = function (cfg) {
    return function (v) {
        var deformedPositions = transformVertices(v.positions)(function (x) {
            return function (y) {
                return function (z) {
                    var v1 = axisComponents(cfg.axis)(x)(y)(z);
                    var t = (function () {
                        var $156 = cfg.length > 0.0 && cfg.length !== 0.0;
                        if ($156) {
                            return (v1.along - cfg.origin) / cfg.length;
                        };
                        return v1.along - cfg.origin;
                    })();
                    var twistAngle = cfg.angle * t * cfg.strength;
                    var sinA = Hydrogen_Math_Core_Trigonometry.sin(twistAngle);
                    var cosA = Hydrogen_Math_Core_Trigonometry.cos(twistAngle);
                    var newPerp1 = v1.perp1 * cosA - v1.perp2 * sinA;
                    var newPerp2 = v1.perp1 * sinA + v1.perp2 * cosA;
                    return reconstructFromAxis(cfg.axis)(v1.along)(newPerp1)(newPerp2);
                };
            };
        });
        return {
            normals: v.normals,
            uvs: v.uvs,
            tangents: v.tangents,
            colors: v.colors,
            indices: v.indices,
            positions: deformedPositions
        };
    };
};
var applyDeformer = function (v) {
    if (v instanceof DefBend) {
        return bend(v.value0);
    };
    if (v instanceof DefTwist) {
        return twist(v.value0);
    };
    if (v instanceof DefTaper) {
        return taper(v.value0);
    };
    if (v instanceof DefBulge) {
        return bulge(v.value0);
    };
    if (v instanceof DefNoise) {
        return noise(v.value0);
    };
    if (v instanceof DefSpherify) {
        return spherify(v.value0);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 528, column 1 - line 528, column 50): " + [ v.constructor.name ]);
};
var applyDeformers = function (deformers) {
    return function (mesh) {
        return foldl(function (m) {
            return function (d) {
                return applyDeformer(d)(m);
            };
        })(mesh)(deformers);
    };
};
var accumulateFaceNormals = function (positions) {
    return function (indices) {
        return function (normals) {
            return map(function (i) {
                var vi = div(i)(3);
                var pos = unsafeIndex(positions)(i);
                var dist = Hydrogen_Math_Core_Arithmetic.sqrt(unsafeIndex(positions)(vi * 3 | 0) * unsafeIndex(positions)(vi * 3 | 0) + unsafeIndex(positions)((vi * 3 | 0) + 1 | 0) * unsafeIndex(positions)((vi * 3 | 0) + 1 | 0) + unsafeIndex(positions)((vi * 3 | 0) + 2 | 0) * unsafeIndex(positions)((vi * 3 | 0) + 2 | 0));
                var component = mod(i)(3);
                var $167 = dist > 1.0e-4;
                if ($167) {
                    return pos / dist;
                };
                return 0.0;
            })(Data_Array.range(0)(Data_Array.length(positions) - 1 | 0));
        };
    };
};
var recalculateNormals = function (v) {
    var vertexCount = div(Data_Array.length(v.positions))(3);
    var zeroNormals = Data_Array.replicate(vertexCount * 3 | 0)(0.0);
    var normals = (function () {
        if (v.indices instanceof Data_Maybe.Just) {
            return accumulateFaceNormals(v.positions)(v.indices.value0)(zeroNormals);
        };
        if (v.indices instanceof Data_Maybe.Nothing) {
            return calculateVertexNormals(v.positions);
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Spatial.Geometry.Deformers (line 558, column 15 - line 560, column 50): " + [ v.indices.constructor.name ]);
    })();
    var normalizedNormals = normalizeNormalArray(normals);
    return {
        positions: v.positions,
        uvs: v.uvs,
        tangents: v.tangents,
        colors: v.colors,
        indices: v.indices,
        normals: new Data_Maybe.Just(normalizedNormals)
    };
};
export {
    defaultBendConfig,
    defaultTwistConfig,
    defaultTaperConfig,
    defaultBulgeConfig,
    defaultNoiseConfig,
    defaultSpherifyConfig,
    AxisX,
    AxisY,
    AxisZ,
    bend,
    twist,
    taper,
    bulge,
    noise,
    spherify,
    DefBend,
    DefTwist,
    DefTaper,
    DefBulge,
    DefNoise,
    DefSpherify,
    applyDeformer,
    applyDeformers,
    recalculateNormals,
    eqDeformAxis,
    ordDeformAxis,
    showDeformAxis,
    eqDeformer,
    showDeformer
};
//# sourceMappingURL=index.js.map
