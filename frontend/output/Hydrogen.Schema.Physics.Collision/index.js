// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var div1 = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var Point2D = function (x) {
    return x;
};
var OBB = function (x) {
    return x;
};
var NoContact = /* #__PURE__ */ (function () {
    function NoContact() {

    };
    NoContact.value = new NoContact();
    return NoContact;
})();
var Contact = /* #__PURE__ */ (function () {
    function Contact(value0) {
        this.value0 = value0;
    };
    Contact.create = function (value0) {
        return new Contact(value0);
    };
    return Contact;
})();
var NotColliding = /* #__PURE__ */ (function () {
    function NotColliding() {

    };
    NotColliding.value = new NotColliding();
    return NotColliding;
})();
var Colliding = /* #__PURE__ */ (function () {
    function Colliding() {

    };
    Colliding.value = new Colliding();
    return Colliding;
})();
var Separating = /* #__PURE__ */ (function () {
    function Separating() {

    };
    Separating.value = new Separating();
    return Separating;
})();
var Resting = /* #__PURE__ */ (function () {
    function Resting() {

    };
    Resting.value = new Resting();
    return Resting;
})();
var Sliding = /* #__PURE__ */ (function () {
    function Sliding() {

    };
    Sliding.value = new Sliding();
    return Sliding;
})();
var Rolling = /* #__PURE__ */ (function () {
    function Rolling() {

    };
    Rolling.value = new Rolling();
    return Rolling;
})();
var ResponseNone = /* #__PURE__ */ (function () {
    function ResponseNone() {

    };
    ResponseNone.value = new ResponseNone();
    return ResponseNone;
})();
var ResponseBounce = /* #__PURE__ */ (function () {
    function ResponseBounce(value0) {
        this.value0 = value0;
    };
    ResponseBounce.create = function (value0) {
        return new ResponseBounce(value0);
    };
    return ResponseBounce;
})();
var ResponseSlide = /* #__PURE__ */ (function () {
    function ResponseSlide(value0) {
        this.value0 = value0;
    };
    ResponseSlide.create = function (value0) {
        return new ResponseSlide(value0);
    };
    return ResponseSlide;
})();
var ResponseStick = /* #__PURE__ */ (function () {
    function ResponseStick() {

    };
    ResponseStick.value = new ResponseStick();
    return ResponseStick;
})();
var ResponseCustom = /* #__PURE__ */ (function () {
    function ResponseCustom(value0) {
        this.value0 = value0;
    };
    ResponseCustom.create = function (value0) {
        return new ResponseCustom(value0);
    };
    return ResponseCustom;
})();
var CollisionMask = function (x) {
    return x;
};
var CollisionLayer = function (x) {
    return x;
};
var BoundingCircle = function (x) {
    return x;
};
var BoundingCapsule = function (x) {
    return x;
};
var AABB = function (x) {
    return x;
};
var showPoint2D = {
    show: function (v) {
        return "Point2D(" + (show(v.x) + (", " + (show(v.y) + ")")));
    }
};
var showOBB = {
    show: function (v) {
        return "OBB(" + (show(v.halfWidth) + ("x" + (show(v.halfHeight) + (" @" + (show(v.rotation) + ")")))));
    }
};
var showContact = {
    show: function (v) {
        if (v instanceof NoContact) {
            return "NoContact";
        };
        if (v instanceof Contact) {
            return "Contact(depth=" + (show(v.value0.depth) + ")");
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 483, column 1 - line 485, column 61): " + [ v.constructor.name ]);
    }
};
var showCollisionState = {
    show: function (v) {
        if (v instanceof NotColliding) {
            return "NotColliding";
        };
        if (v instanceof Colliding) {
            return "Colliding";
        };
        if (v instanceof Separating) {
            return "Separating";
        };
        if (v instanceof Resting) {
            return "Resting";
        };
        if (v instanceof Sliding) {
            return "Sliding";
        };
        if (v instanceof Rolling) {
            return "Rolling";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 882, column 1 - line 888, column 27): " + [ v.constructor.name ]);
    }
};
var showCollisionResponse = {
    show: function (v) {
        if (v instanceof ResponseNone) {
            return "ResponseNone";
        };
        if (v instanceof ResponseBounce) {
            return "ResponseBounce(e=" + (show(v.value0.restitution) + ")");
        };
        if (v instanceof ResponseSlide) {
            return "ResponseSlide(\u03bc=" + (show(v.value0.friction) + ")");
        };
        if (v instanceof ResponseStick) {
            return "ResponseStick";
        };
        if (v instanceof ResponseCustom) {
            return "ResponseCustom(" + (show1(v.value0.id) + ")");
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 739, column 1 - line 744, column 66): " + [ v.constructor.name ]);
    }
};
var showCollisionMask = {
    show: function (v) {
        return "CollisionMask(" + (show1(v) + ")");
    }
};
var showCollisionLayer = {
    show: function (v) {
        return "CollisionLayer(" + (show1(v) + ")");
    }
};
var showBoundingCircle = {
    show: function (v) {
        return "BoundingCircle(r=" + (show(v.radius) + ")");
    }
};
var showBoundingCapsule = {
    show: function (v) {
        return "BoundingCapsule(r=" + (show(v.radius) + ")");
    }
};
var showAABB = {
    show: function (v) {
        return "AABB(" + (show(v.minX) + ("," + (show(v.minY) + (" -> " + (show(v.maxX) + ("," + (show(v.maxY) + ")")))))));
    }
};
var responseStick = /* #__PURE__ */ (function () {
    return ResponseStick.value;
})();
var responseNone = /* #__PURE__ */ (function () {
    return ResponseNone.value;
})();
var resolveOverlap = function (v) {
    if (v instanceof NoContact) {
        return {
            dx: 0.0,
            dy: 0.0
        };
    };
    if (v instanceof Contact) {
        return {
            dx: v.value0.normal.x * v.value0.depth,
            dy: v.value0.normal.y * v.value0.depth
        };
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 769, column 1 - line 769, column 60): " + [ v.constructor.name ]);
};
var point2D = function (x) {
    return function (y) {
        return {
            x: x,
            y: y
        };
    };
};
var origin2D = {
    x: 0.0,
    y: 0.0
};
var offsetPoint = function (v) {
    return function (dx) {
        return function (dy) {
            return {
                x: v.x + dx,
                y: v.y + dy
            };
        };
    };
};
var obbCorners = function (v) {
    var sinR = Data_Number.sin(v.rotation);
    var cosR = Data_Number.cos(v.rotation);
    var rotatePoint = function (lx) {
        return function (ly) {
            return {
                x: (v.center.x + lx * cosR) - ly * sinR,
                y: v.center.y + lx * sinR + ly * cosR
            };
        };
    };
    return {
        tl: rotatePoint(-v.halfWidth)(-v.halfHeight),
        tr: rotatePoint(v.halfWidth)(-v.halfHeight),
        bl: rotatePoint(-v.halfWidth)(v.halfHeight),
        br: rotatePoint(v.halfWidth)(v.halfHeight)
    };
};
var normalizePoint = function (v) {
    var len = Data_Number.sqrt(v.x * v.x + v.y * v.y);
    var $197 = len < 1.0e-4;
    if ($197) {
        return {
            x: 1.0,
            y: 0.0
        };
    };
    return {
        x: v.x / len,
        y: v.y / len
    };
};
var noLayers = 0;
var noContact = /* #__PURE__ */ (function () {
    return NoContact.value;
})();
var negatePoint = function (v) {
    return {
        x: -v.x,
        y: -v.y
    };
};
var modInt = function (a) {
    return function (b) {
        return a - (div1(a)(b) * b | 0) | 0;
    };
};
var minNum = function (a) {
    return function (b) {
        var $199 = a < b;
        if ($199) {
            return a;
        };
        return b;
    };
};
var midpoint = function (v) {
    return function (v1) {
        return {
            x: (v.x + v1.x) / 2.0,
            y: (v.y + v1.y) / 2.0
        };
    };
};
var maxNum = function (a) {
    return function (b) {
        var $202 = a > b;
        if ($202) {
            return a;
        };
        return b;
    };
};
var layerCollides = function (v) {
    return function (v1) {
        return v > 0 && (v1 > 0 && (v <= v1 || modInt(v1)(v) === 0));
    };
};
var isSeparating = function (v) {
    if (v instanceof Separating) {
        return true;
    };
    return false;
};
var isResting = function (v) {
    if (v instanceof Resting) {
        return true;
    };
    return false;
};
var isColliding = function (v) {
    if (v instanceof Colliding) {
        return true;
    };
    if (v instanceof Resting) {
        return true;
    };
    if (v instanceof Sliding) {
        return true;
    };
    if (v instanceof Rolling) {
        return true;
    };
    return false;
};
var hasContact = function (v) {
    if (v instanceof NoContact) {
        return false;
    };
    if (v instanceof Contact) {
        return true;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 500, column 1 - line 500, column 33): " + [ v.constructor.name ]);
};
var flipContact = function (v) {
    if (v instanceof NoContact) {
        return NoContact.value;
    };
    if (v instanceof Contact) {
        return new Contact({
            point: v.value0.point,
            normal: negatePoint(v.value0.normal),
            depth: v.value0.depth
        });
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 520, column 1 - line 520, column 34): " + [ v.constructor.name ]);
};
var eqPoint2D = {
    eq: function (x) {
        return function (y) {
            return x.x === y.x && x.y === y.y;
        };
    }
};
var eq2 = /* #__PURE__ */ Data_Eq.eq(eqPoint2D);
var ordPoint2D = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.x)(y.x);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare(x.y)(y.y);
        };
    },
    Eq0: function () {
        return eqPoint2D;
    }
};
var compare2 = /* #__PURE__ */ Data_Ord.compare(ordPoint2D);
var eqOBB = {
    eq: function (x) {
        return function (y) {
            return eq2(x.center)(y.center) && x.halfHeight === y.halfHeight && x.halfWidth === y.halfWidth && x.rotation === y.rotation;
        };
    }
};
var ordOBB = {
    compare: function (x) {
        return function (y) {
            var v = compare2(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare(x.halfHeight)(y.halfHeight);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare(x.halfWidth)(y.halfWidth);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare(x.rotation)(y.rotation);
        };
    },
    Eq0: function () {
        return eqOBB;
    }
};
var eqContact = {
    eq: function (x) {
        return function (y) {
            if (x instanceof NoContact && y instanceof NoContact) {
                return true;
            };
            if (x instanceof Contact && y instanceof Contact) {
                return x.value0.depth === y.value0.depth && eq2(x.value0.normal)(y.value0.normal) && eq2(x.value0.point)(y.value0.point);
            };
            return false;
        };
    }
};
var eqCollisionState = {
    eq: function (x) {
        return function (y) {
            if (x instanceof NotColliding && y instanceof NotColliding) {
                return true;
            };
            if (x instanceof Colliding && y instanceof Colliding) {
                return true;
            };
            if (x instanceof Separating && y instanceof Separating) {
                return true;
            };
            if (x instanceof Resting && y instanceof Resting) {
                return true;
            };
            if (x instanceof Sliding && y instanceof Sliding) {
                return true;
            };
            if (x instanceof Rolling && y instanceof Rolling) {
                return true;
            };
            return false;
        };
    }
};
var ordCollisionState = {
    compare: function (x) {
        return function (y) {
            if (x instanceof NotColliding && y instanceof NotColliding) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof NotColliding) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof NotColliding) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Colliding && y instanceof Colliding) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Colliding) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Colliding) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Separating && y instanceof Separating) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Separating) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Separating) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Resting && y instanceof Resting) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Resting) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Resting) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Sliding && y instanceof Sliding) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Sliding) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Sliding) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Rolling && y instanceof Rolling) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqCollisionState;
    }
};
var eqCollisionResponse = {
    eq: function (x) {
        return function (y) {
            if (x instanceof ResponseNone && y instanceof ResponseNone) {
                return true;
            };
            if (x instanceof ResponseBounce && y instanceof ResponseBounce) {
                return x.value0.restitution === y.value0.restitution;
            };
            if (x instanceof ResponseSlide && y instanceof ResponseSlide) {
                return x.value0.friction === y.value0.friction;
            };
            if (x instanceof ResponseStick && y instanceof ResponseStick) {
                return true;
            };
            if (x instanceof ResponseCustom && y instanceof ResponseCustom) {
                return x.value0.id === y.value0.id;
            };
            return false;
        };
    }
};
var eqCollisionMask = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordCollisionMask = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqCollisionMask;
    }
};
var eqCollisionLayer = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordCollisionLayer = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqCollisionLayer;
    }
};
var eqBoundingCircle = {
    eq: function (x) {
        return function (y) {
            return eq2(x.center)(y.center) && x.radius === y.radius;
        };
    }
};
var ordBoundingCircle = {
    compare: function (x) {
        return function (y) {
            var v = compare2(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare(x.radius)(y.radius);
        };
    },
    Eq0: function () {
        return eqBoundingCircle;
    }
};
var eqBoundingCapsule = {
    eq: function (x) {
        return function (y) {
            return eq2(x.pointA)(y.pointA) && eq2(x.pointB)(y.pointB) && x.radius === y.radius;
        };
    }
};
var ordBoundingCapsule = {
    compare: function (x) {
        return function (y) {
            var v = compare2(x.pointA)(y.pointA);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare2(x.pointB)(y.pointB);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare(x.radius)(y.radius);
        };
    },
    Eq0: function () {
        return eqBoundingCapsule;
    }
};
var eqAABB = {
    eq: function (x) {
        return function (y) {
            return x.maxX === y.maxX && x.maxY === y.maxY && x.minX === y.minX && x.minY === y.minY;
        };
    }
};
var ordAABB = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.maxX)(y.maxX);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare(x.maxY)(y.maxY);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v2 = compare(x.minX)(y.minX);
            if (v2 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v2 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare(x.minY)(y.minY);
        };
    },
    Eq0: function () {
        return eqAABB;
    }
};
var distanceSquared = function (v) {
    return function (v1) {
        var dy = v1.y - v.y;
        var dx = v1.x - v.x;
        return dx * dx + dy * dy;
    };
};
var distance = function (v) {
    return function (v1) {
        var dy = v1.y - v.y;
        var dx = v1.x - v.x;
        return Data_Number.sqrt(dx * dx + dy * dy);
    };
};
var contactPoint = function (v) {
    if (v instanceof NoContact) {
        return origin2D;
    };
    if (v instanceof Contact) {
        return v.value0.point;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 505, column 1 - line 505, column 35): " + [ v.constructor.name ]);
};
var contactNormal = function (v) {
    if (v instanceof NoContact) {
        return origin2D;
    };
    if (v instanceof Contact) {
        return v.value0.normal;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 510, column 1 - line 510, column 36): " + [ v.constructor.name ]);
};
var contactDepth = function (v) {
    if (v instanceof NoContact) {
        return 0.0;
    };
    if (v instanceof Contact) {
        return v.value0.depth;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 515, column 1 - line 515, column 34): " + [ v.constructor.name ]);
};
var clampUnit = function (maxVal) {
    return function (n) {
        if (n < 0.0) {
            return 0.0;
        };
        if (n > maxVal) {
            return maxVal;
        };
        if (Data_Boolean.otherwise) {
            return n;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 939, column 1 - line 939, column 40): " + [ maxVal.constructor.name, n.constructor.name ]);
    };
};
var pointToSegmentDistance = function (v) {
    return function (v1) {
        return function (v2) {
            var apy = v.y - v1.y;
            var apx = v.x - v1.x;
            var aby = v2.y - v1.y;
            var abx = v2.x - v1.x;
            var abSquared = abx * abx + aby * aby;
            var t = (function () {
                var $281 = abSquared < 1.0e-4;
                if ($281) {
                    return 0.0;
                };
                return clampUnit(1.0)((apx * abx + apy * aby) / abSquared);
            })();
            var closestX = v1.x + t * abx;
            var dx = v.x - closestX;
            var closestY = v1.y + t * aby;
            var dy = v.y - closestY;
            return Data_Number.sqrt(dx * dx + dy * dy);
        };
    };
};
var segmentSegmentDistance = function (a1) {
    return function (a2) {
        return function (b1) {
            return function (b2) {
                var d4 = pointToSegmentDistance(b2)(a1)(a2);
                var d3 = pointToSegmentDistance(b1)(a1)(a2);
                var d2 = pointToSegmentDistance(a2)(b1)(b2);
                var d1 = pointToSegmentDistance(a1)(b1)(b2);
                return minNum(minNum(d1)(d2))(minNum(d3)(d4));
            };
        };
    };
};
var responseBounce = function (e) {
    return new ResponseBounce({
        restitution: clampUnit(1.0)(e)
    });
};
var responseSlide = function (f) {
    return new ResponseSlide({
        friction: clampUnit(2.0)(f)
    });
};
var clampPositive = function (maxVal) {
    return function (n) {
        if (n < 0.0) {
            return 0.0;
        };
        if (n > maxVal) {
            return maxVal;
        };
        if (Data_Boolean.otherwise) {
            return n;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 932, column 1 - line 932, column 44): " + [ maxVal.constructor.name, n.constructor.name ]);
    };
};
var obb = function (center) {
    return function (halfWidth) {
        return function (halfHeight) {
            return function (rotation) {
                return {
                    center: center,
                    halfWidth: clampPositive(100000.0)(halfWidth),
                    halfHeight: clampPositive(100000.0)(halfHeight),
                    rotation: rotation
                };
            };
        };
    };
};
var clampNum = function (lo) {
    return function (hi) {
        return function (x) {
            if (x < lo) {
                return lo;
            };
            if (x > hi) {
                return hi;
            };
            if (Data_Boolean.otherwise) {
                return x;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 925, column 1 - line 925, column 49): " + [ lo.constructor.name, hi.constructor.name, x.constructor.name ]);
        };
    };
};
var clampDepth = function (d) {
    if (d < 0.0) {
        return 0.0;
    };
    if (d > 10000.0) {
        return 10000.0;
    };
    if (Data_Boolean.otherwise) {
        return d;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 946, column 1 - line 946, column 31): " + [ d.constructor.name ]);
};
var contact = function (point) {
    return function (normal) {
        return function (depth) {
            return new Contact({
                point: point,
                normal: normalizePoint(normal),
                depth: clampDepth(depth)
            });
        };
    };
};
var circleVsPoint = function (v) {
    return function (point) {
        var dist = distance(v.center)(point);
        var $290 = dist < v.radius;
        if ($290) {
            var normal = (function () {
                var $293 = dist < 1.0e-4;
                if ($293) {
                    return point2D(1.0)(0.0);
                };
                return point2D((point.x - v.center.x) / dist)((point.y - v.center.y) / dist);
            })();
            var depth = v.radius - dist;
            return new Contact({
                point: point,
                normal: normal,
                depth: depth
            });
        };
        return NoContact.value;
    };
};
var circleVsCircle = function (v) {
    return function (v1) {
        var radiusSum = v.radius + v1.radius;
        var dist = distance(v.center)(v1.center);
        var $296 = dist < radiusSum;
        if ($296) {
            var normal = (function () {
                var $299 = dist < 1.0e-4;
                if ($299) {
                    return point2D(1.0)(0.0);
                };
                return point2D((v1.center.x - v.center.x) / dist)((v1.center.y - v.center.y) / dist);
            })();
            var depth = radiusSum - dist;
            var contactPt = point2D(v.center.x + normal.x * v.radius)(v.center.y + normal.y * v.radius);
            return new Contact({
                point: contactPt,
                normal: normal,
                depth: depth
            });
        };
        return NoContact.value;
    };
};
var circleMerge = function (v) {
    return function (v1) {
        var d = distance(v.center)(v1.center);
        var newRadius = (d + v.radius + v1.radius) / 2.0;
        var t = (function () {
            var $303 = d < 1.0e-4;
            if ($303) {
                return 0.5;
            };
            return (newRadius - v.radius) / d;
        })();
        var newCenter = {
            x: v.center.x + t * (v1.center.x - v.center.x),
            y: v.center.y + t * (v1.center.y - v.center.y)
        };
        return {
            center: newCenter,
            radius: clampPositive(100000.0)(newRadius)
        };
    };
};
var circleExpand = function (delta) {
    return function (v) {
        return {
            center: v.center,
            radius: clampPositive(100000.0)(v.radius + delta)
        };
    };
};
var circleContains = function (v) {
    return function (p) {
        return distanceSquared(v.center)(p) <= v.radius * v.radius;
    };
};
var capsuleVsPoint = function (v) {
    return function (point) {
        var dist = pointToSegmentDistance(point)(v.pointA)(v.pointB);
        var $312 = dist < v.radius;
        if ($312) {
            var depth = v.radius - dist;
            var apy = point.y - v.pointA.y;
            var apx = point.x - v.pointA.x;
            var aby = v.pointB.y - v.pointA.y;
            var abx = v.pointB.x - v.pointA.x;
            var abSquared = abx * abx + aby * aby;
            var t = (function () {
                var $316 = abSquared < 1.0e-4;
                if ($316) {
                    return 0.0;
                };
                return clampUnit(1.0)((apx * abx + apy * aby) / abSquared);
            })();
            var closestX = v.pointA.x + t * abx;
            var dx = point.x - closestX;
            var closestY = v.pointA.y + t * aby;
            var dy = point.y - closestY;
            var normal = (function () {
                var $317 = dist < 1.0e-4;
                if ($317) {
                    return point2D(1.0)(0.0);
                };
                return point2D(dx / dist)(dy / dist);
            })();
            return new Contact({
                point: point,
                normal: normal,
                depth: depth
            });
        };
        return NoContact.value;
    };
};
var capsuleVsCapsule = function (v) {
    return function (v1) {
        var radiusSum = v.radius + v1.radius;
        var dist = segmentSegmentDistance(v.pointA)(v.pointB)(v1.pointA)(v1.pointB);
        var $320 = dist < radiusSum;
        if ($320) {
            var midB = midpoint(v1.pointA)(v1.pointB);
            var midA = midpoint(v.pointA)(v.pointB);
            var d = distance(midA)(midB);
            var normal = (function () {
                var $323 = d < 1.0e-4;
                if ($323) {
                    return point2D(1.0)(0.0);
                };
                return point2D((midB.x - midA.x) / d)((midB.y - midA.y) / d);
            })();
            var depth = radiusSum - dist;
            var contactPt = point2D(midA.x + normal.x * v.radius)(midA.y + normal.y * v.radius);
            return new Contact({
                point: contactPt,
                normal: normal,
                depth: depth
            });
        };
        return NoContact.value;
    };
};
var capsuleContains = function (v) {
    return function (p) {
        var dist = pointToSegmentDistance(p)(v.pointA)(v.pointB);
        return dist <= v.radius;
    };
};
var calculateSlide = function (v) {
    return function (v1) {
        return function (v2) {
            if (v1 instanceof NoContact) {
                return v;
            };
            if (v1 instanceof Contact) {
                var vn = v.vx * v1.value0.normal.x + v.vy * v1.value0.normal.y;
                var $331 = vn >= 0.0;
                if ($331) {
                    return v;
                };
                var tvy = v.vy - vn * v1.value0.normal.y;
                var tvx = v.vx - vn * v1.value0.normal.x;
                var frictionFactor = maxNum(0.0)(1.0 - v2);
                return {
                    vx: tvx * frictionFactor,
                    vy: tvy * frictionFactor
                };
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 801, column 1 - line 805, column 36): " + [ v.constructor.name, v1.constructor.name, v2.constructor.name ]);
        };
    };
};
var calculateBounce = function (v) {
    return function (v1) {
        return function (v2) {
            if (v1 instanceof NoContact) {
                return v;
            };
            if (v1 instanceof Contact) {
                var vn = v.vx * v1.value0.normal.x + v.vy * v1.value0.normal.y;
                var $337 = vn >= 0.0;
                if ($337) {
                    return v;
                };
                var factor = (1.0 + v2) * vn;
                return {
                    vx: v.vx - factor * v1.value0.normal.x,
                    vy: v.vy - factor * v1.value0.normal.y
                };
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Collision (line 778, column 1 - line 782, column 36): " + [ v.constructor.name, v1.constructor.name, v2.constructor.name ]);
        };
    };
};
var boundingCircle = function (center) {
    return function (radius) {
        return {
            center: center,
            radius: clampPositive(100000.0)(radius)
        };
    };
};
var boundingCapsule = function (pointA) {
    return function (pointB) {
        return function (radius) {
            return {
                pointA: pointA,
                pointB: pointB,
                radius: clampPositive(100000.0)(radius)
            };
        };
    };
};
var allLayers = 2147483647;
var absNum = function (n) {
    var $339 = n < 0.0;
    if ($339) {
        return -n;
    };
    return n;
};
var obbContains = function (v) {
    return function (v1) {
        var sinR = Data_Number.sin(-v.rotation);
        var dy = v1.y - v.center.y;
        var dx = v1.x - v.center.x;
        var cosR = Data_Number.cos(-v.rotation);
        var localX = dx * cosR - dy * sinR;
        var localY = dx * sinR + dy * cosR;
        return absNum(localX) <= v.halfWidth && absNum(localY) <= v.halfHeight;
    };
};
var aabbVsCircle = function (v) {
    return function (v1) {
        var closestY = clampNum(v.minY)(v.maxY)(v1.center.y);
        var closestX = clampNum(v.minX)(v.maxX)(v1.center.x);
        var closest = point2D(closestX)(closestY);
        var dist = distance(v1.center)(closest);
        var $346 = dist < v1.radius;
        if ($346) {
            var normal = (function () {
                var $347 = dist < 1.0e-4;
                if ($347) {
                    return point2D(1.0)(0.0);
                };
                return point2D((v1.center.x - closestX) / dist)((v1.center.y - closestY) / dist);
            })();
            var depth = v1.radius - dist;
            return new Contact({
                point: closest,
                normal: normal,
                depth: depth
            });
        };
        return NoContact.value;
    };
};
var circleVsAABB = function (circle) {
    return function (box) {
        return flipContact(aabbVsCircle(box)(circle));
    };
};
var aabbVsAABB = function (v) {
    return function (v1) {
        var $350 = v.maxX < v1.minX || (v1.maxX < v.minX || (v.maxY < v1.minY || v1.maxY < v.minY));
        if ($350) {
            return NoContact.value;
        };
        var overlapY = minNum(v.maxY - v1.minY)(v1.maxY - v.minY);
        var overlapX = minNum(v.maxX - v1.minX)(v1.maxX - v.minX);
        var centerBY = (v1.minY + v1.maxY) / 2.0;
        var centerBX = (v1.minX + v1.maxX) / 2.0;
        var centerAY = (v.minY + v.maxY) / 2.0;
        var centerAX = (v.minX + v.maxX) / 2.0;
        var $351 = overlapX < overlapY;
        if ($351) {
            var normalX = (function () {
                var $352 = centerBX > centerAX;
                if ($352) {
                    return 1.0;
                };
                return -1.0;
            })();
            var contactY = (maxNum(v.minY)(v1.minY) + minNum(v.maxY)(v1.maxY)) / 2.0;
            var contactX = (function () {
                var $353 = normalX > 0.0;
                if ($353) {
                    return v.maxX;
                };
                return v.minX;
            })();
            return new Contact({
                point: point2D(contactX)(contactY),
                normal: point2D(normalX)(0.0),
                depth: overlapX
            });
        };
        var normalY = (function () {
            var $354 = centerBY > centerAY;
            if ($354) {
                return 1.0;
            };
            return -1.0;
        })();
        var contactY = (function () {
            var $355 = normalY > 0.0;
            if ($355) {
                return v.maxY;
            };
            return v.minY;
        })();
        var contactX = (maxNum(v.minX)(v1.minX) + minNum(v.maxX)(v1.maxX)) / 2.0;
        return new Contact({
            point: point2D(contactX)(contactY),
            normal: point2D(0.0)(normalY),
            depth: overlapY
        });
    };
};
var aabbSize = function (v) {
    return {
        width: v.maxX - v.minX,
        height: v.maxY - v.minY
    };
};
var aabbMerge = function (v) {
    return function (v1) {
        return {
            minX: minNum(v.minX)(v1.minX),
            minY: minNum(v.minY)(v1.minY),
            maxX: maxNum(v.maxX)(v1.maxX),
            maxY: maxNum(v.maxY)(v1.maxY)
        };
    };
};
var aabbHalfSize = function (v) {
    return {
        hx: (v.maxX - v.minX) / 2.0,
        hy: (v.maxY - v.minY) / 2.0
    };
};
var aabbExpand = function (margin) {
    return function (v) {
        return {
            minX: v.minX - margin,
            minY: v.minY - margin,
            maxX: v.maxX + margin,
            maxY: v.maxY + margin
        };
    };
};
var aabbContains = function (v) {
    return function (v1) {
        return v1.x >= v.minX && (v1.x <= v.maxX && (v1.y >= v.minY && v1.y <= v.maxY));
    };
};
var aabbVsPoint = function (box) {
    return function (point) {
        var $364 = aabbContains(box)(point);
        if ($364) {
            var distTop = point.y - box.minY;
            var distRight = box.maxX - point.x;
            var distLeft = point.x - box.minX;
            var distBottom = box.maxY - point.y;
            var minDist = minNum(minNum(distLeft)(distRight))(minNum(distTop)(distBottom));
            var $367 = minDist <= distLeft && distLeft <= distRight;
            if ($367) {
                return new Contact({
                    point: point,
                    normal: point2D(-1.0)(0.0),
                    depth: distLeft
                });
            };
            var $368 = minDist <= distRight;
            if ($368) {
                return new Contact({
                    point: point,
                    normal: point2D(1.0)(0.0),
                    depth: distRight
                });
            };
            var $369 = minDist <= distTop;
            if ($369) {
                return new Contact({
                    point: point,
                    normal: point2D(0.0)(-1.0),
                    depth: distTop
                });
            };
            return new Contact({
                point: point,
                normal: point2D(0.0)(1.0),
                depth: distBottom
            });
        };
        return NoContact.value;
    };
};
var aabbCenter = function (v) {
    return {
        x: (v.minX + v.maxX) / 2.0,
        y: (v.minY + v.maxY) / 2.0
    };
};
var aabb = function (x1) {
    return function (y1) {
        return function (x2) {
            return function (y2) {
                return {
                    minX: minNum(x1)(x2),
                    minY: minNum(y1)(y2),
                    maxX: maxNum(x1)(x2),
                    maxY: maxNum(y1)(y2)
                };
            };
        };
    };
};
var aabbFromPoints = function (v) {
    return function (v1) {
        return aabb(v.x)(v.y)(v1.x)(v1.y);
    };
};
export {
    Point2D,
    point2D,
    origin2D,
    distance,
    distanceSquared,
    midpoint,
    offsetPoint,
    AABB,
    aabb,
    aabbFromPoints,
    aabbCenter,
    aabbSize,
    aabbHalfSize,
    aabbContains,
    aabbExpand,
    aabbMerge,
    BoundingCircle,
    boundingCircle,
    circleContains,
    circleExpand,
    circleMerge,
    BoundingCapsule,
    boundingCapsule,
    capsuleContains,
    OBB,
    obb,
    obbCorners,
    obbContains,
    NoContact,
    Contact,
    contact,
    noContact,
    hasContact,
    contactPoint,
    contactNormal,
    contactDepth,
    flipContact,
    aabbVsAABB,
    aabbVsPoint,
    aabbVsCircle,
    circleVsCircle,
    circleVsPoint,
    circleVsAABB,
    capsuleVsPoint,
    capsuleVsCapsule,
    ResponseNone,
    ResponseBounce,
    ResponseSlide,
    ResponseStick,
    ResponseCustom,
    responseNone,
    responseBounce,
    responseSlide,
    responseStick,
    resolveOverlap,
    calculateBounce,
    calculateSlide,
    CollisionLayer,
    CollisionMask,
    layerCollides,
    allLayers,
    noLayers,
    NotColliding,
    Colliding,
    Separating,
    Resting,
    Sliding,
    Rolling,
    isColliding,
    isSeparating,
    isResting,
    eqPoint2D,
    ordPoint2D,
    showPoint2D,
    eqAABB,
    ordAABB,
    showAABB,
    eqBoundingCircle,
    ordBoundingCircle,
    showBoundingCircle,
    eqBoundingCapsule,
    ordBoundingCapsule,
    showBoundingCapsule,
    eqOBB,
    ordOBB,
    showOBB,
    eqContact,
    showContact,
    eqCollisionResponse,
    showCollisionResponse,
    eqCollisionLayer,
    ordCollisionLayer,
    showCollisionLayer,
    eqCollisionMask,
    ordCollisionMask,
    showCollisionMask,
    eqCollisionState,
    ordCollisionState,
    showCollisionState
};
//# sourceMappingURL=index.js.map
