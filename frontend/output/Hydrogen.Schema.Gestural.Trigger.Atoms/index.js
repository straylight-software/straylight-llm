// Generated by purs version 0.15.15
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var min1 = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordInt);
var TriggerWindow = function (x) {
    return x;
};
var TriggerDelay = function (x) {
    return x;
};
var TriggerCount = function (x) {
    return x;
};
var TriggerCooldown = function (x) {
    return x;
};
var ProximityRadius = function (x) {
    return x;
};
var unwrapTriggerWindow = function (v) {
    return v;
};
var unwrapTriggerDelay = function (v) {
    return v;
};
var unwrapTriggerCount = function (v) {
    return v;
};
var unwrapTriggerCooldown = function (v) {
    return v;
};
var unwrapProximityRadius = function (v) {
    return v;
};
var tripleTrigger = 3;
var tightRadius = 50.0;
var standardWindow = 2000.0;
var standardCooldown = 1000.0;
var singleTrigger = 1;
var showTriggerWindow = {
    show: function (v) {
        return "TriggerWindow(" + (show(v) + "ms)");
    }
};
var showTriggerDelay = {
    show: function (v) {
        return "TriggerDelay(" + (show(v) + "ms)");
    }
};
var showTriggerCount = {
    show: function (v) {
        return "TriggerCount(" + (show1(v) + ")");
    }
};
var showTriggerCooldown = {
    show: function (v) {
        return "TriggerCooldown(" + (show(v) + "ms)");
    }
};
var showProximityRadius = {
    show: function (v) {
        return "ProximityRadius(" + (show(v) + "px)");
    }
};
var shortDelay = 100.0;
var shortCooldown = 500.0;
var quickWindow = 500.0;
var noDelay = 0.0;
var noCooldown = 0.0;
var nearRadius = 100.0;
var mkTriggerCooldown = function (ms) {
    return max(0.0)(ms);
};
var minTriggerWindow = 0.0;
var minTriggerDelay = 0.0;
var minTriggerCount = 1;
var minProximityRadius = 0.0;
var mediumDelay = 300.0;
var maxTriggerWindow = 10000.0;
var maxTriggerDelay = 30000.0;
var maxTriggerCount = 100;
var maxProximityRadius = 500.0;
var longDelay = 500.0;
var farRadius = 200.0;
var extendedWindow = 5000.0;
var eqTriggerWindow = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTriggerWindow = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqTriggerWindow;
    }
};
var eqTriggerDelay = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTriggerDelay = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqTriggerDelay;
    }
};
var eqTriggerCount = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTriggerCount = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqTriggerCount;
    }
};
var eqTriggerCooldown = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTriggerCooldown = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqTriggerCooldown;
    }
};
var eqProximityRadius = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordProximityRadius = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqProximityRadius;
    }
};
var doubleTrigger = 2;
var clampNumber = function (lo) {
    return function (hi) {
        return function (x) {
            return max(lo)(min(hi)(x));
        };
    };
};
var mkProximityRadius = function (px) {
    return clampNumber(minProximityRadius)(maxProximityRadius)(px);
};
var mkTriggerDelay = function (ms) {
    return clampNumber(minTriggerDelay)(maxTriggerDelay)(ms);
};
var mkTriggerWindow = function (ms) {
    return clampNumber(minTriggerWindow)(maxTriggerWindow)(ms);
};
var clampInt = function (lo) {
    return function (hi) {
        return function (x) {
            return max1(lo)(min1(hi)(x));
        };
    };
};
var mkTriggerCount = function (n) {
    return clampInt(minTriggerCount)(maxTriggerCount)(n);
};
export {
    mkTriggerDelay,
    unwrapTriggerDelay,
    minTriggerDelay,
    maxTriggerDelay,
    noDelay,
    shortDelay,
    mediumDelay,
    longDelay,
    mkTriggerCount,
    unwrapTriggerCount,
    minTriggerCount,
    maxTriggerCount,
    singleTrigger,
    doubleTrigger,
    tripleTrigger,
    mkTriggerCooldown,
    unwrapTriggerCooldown,
    noCooldown,
    shortCooldown,
    standardCooldown,
    mkTriggerWindow,
    unwrapTriggerWindow,
    minTriggerWindow,
    maxTriggerWindow,
    quickWindow,
    standardWindow,
    extendedWindow,
    mkProximityRadius,
    unwrapProximityRadius,
    minProximityRadius,
    maxProximityRadius,
    tightRadius,
    nearRadius,
    farRadius,
    eqTriggerDelay,
    ordTriggerDelay,
    showTriggerDelay,
    eqTriggerCount,
    ordTriggerCount,
    showTriggerCount,
    eqTriggerCooldown,
    ordTriggerCooldown,
    showTriggerCooldown,
    eqTriggerWindow,
    ordTriggerWindow,
    showTriggerWindow,
    eqProximityRadius,
    ordProximityRadius,
    showProximityRadius
};
//# sourceMappingURL=index.js.map
