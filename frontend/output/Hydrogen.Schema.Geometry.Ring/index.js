// Generated by purs version 0.15.15
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Geometry_Angle from "../Hydrogen.Schema.Geometry.Angle/index.js";
import * as Hydrogen_Schema_Geometry_Circle from "../Hydrogen.Schema.Geometry.Circle/index.js";
import * as Hydrogen_Schema_Geometry_Point from "../Hydrogen.Schema.Geometry.Point/index.js";
var show = /* #__PURE__ */ Data_Show.show(/* #__PURE__ */ Data_Show.showRecord()()(/* #__PURE__ */ Data_Show.showRecordFieldsCons({
    reflectSymbol: function () {
        return "center";
    }
})(/* #__PURE__ */ Data_Show.showRecordFieldsCons({
    reflectSymbol: function () {
        return "innerRadius";
    }
})(/* #__PURE__ */ Data_Show.showRecordFieldsConsNil({
    reflectSymbol: function () {
        return "outerRadius";
    }
})(Data_Show.showNumber))(Data_Show.showNumber))(Hydrogen_Schema_Geometry_Point.showPoint2D)));
var show1 = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var toRadians = /* #__PURE__ */ Hydrogen_Schema_Geometry_Angle.toRadians(Hydrogen_Schema_Geometry_Angle.angularUnitDegrees);
var eq1 = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Geometry_Point.eqPoint2D);
var compare = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Geometry_Point.ordPoint2D);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var Ring = function (x) {
    return x;
};
var unitRing = {
    center: Hydrogen_Schema_Geometry_Point.origin2D,
    innerRadius: 0.5,
    outerRadius: 1.0
};
var translateRing = function (dx) {
    return function (dy) {
        return function (v) {
            return {
                innerRadius: v.innerRadius,
                outerRadius: v.outerRadius,
                center: Hydrogen_Schema_Geometry_Point.translate2D(dx)(dy)(v.center)
            };
        };
    };
};
var showRingInstance = {
    show: function (v) {
        return "(Ring " + (show(v) + ")");
    }
};
var showRing = function (v) {
    return "Ring { center: (" + (show1(v.center.x) + (", " + (show1(v.center.y) + ("), inner: " + (show1(v.innerRadius) + (", outer: " + (show1(v.outerRadius) + " }")))))));
};
var setWidth = function (width) {
    return function (v) {
        if (width > 0.0 && width < v.outerRadius) {
            return new Data_Maybe.Just({
                center: v.center,
                outerRadius: v.outerRadius,
                innerRadius: v.outerRadius - width
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var setOuterRadius = function (newOuter) {
    return function (v) {
        if (newOuter > v.innerRadius) {
            return new Data_Maybe.Just({
                center: v.center,
                innerRadius: v.innerRadius,
                outerRadius: newOuter
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var setInnerRadius = function (newInner) {
    return function (v) {
        if (newInner > 0.0 && newInner < v.outerRadius) {
            return new Data_Maybe.Just({
                center: v.center,
                outerRadius: v.outerRadius,
                innerRadius: newInner
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var scaleRing = function (factor) {
    return function (v) {
        if (factor > 0.0) {
            return new Data_Maybe.Just({
                center: v.center,
                innerRadius: v.innerRadius * factor,
                outerRadius: v.outerRadius * factor
            });
        };
        return Data_Maybe.Nothing.value;
    };
};
var ringWidth = function (v) {
    return v.outerRadius - v.innerRadius;
};
var sameWidth = function (r1) {
    return function (r2) {
        return ringWidth(r1) === ringWidth(r2);
    };
};
var ringOuterRadius = function (v) {
    return v.outerRadius;
};
var ringInnerRadius = function (v) {
    return v.innerRadius;
};
var ringCenter = function (v) {
    return v.center;
};
var ringArea = function (v) {
    return Data_Number.pi * (v.outerRadius * v.outerRadius - v.innerRadius * v.innerRadius);
};
var radiusRatio = function (v) {
    return v.innerRadius / v.outerRadius;
};
var pointAtAngle = function (angle) {
    return function (v) {
        var rad = Hydrogen_Schema_Geometry_Angle.unwrapRadians(toRadians(angle));
        var avgRadius = (v.innerRadius + v.outerRadius) / 2.0;
        var x = v.center.x + avgRadius * Data_Number.cos(rad);
        var y = v.center.y + avgRadius * Data_Number.sin(rad);
        return Hydrogen_Schema_Geometry_Point.point2D(x)(y);
    };
};
var outerPointAtAngle = function (angle) {
    return function (v) {
        var rad = Hydrogen_Schema_Geometry_Angle.unwrapRadians(toRadians(angle));
        var y = v.center.y + v.outerRadius * Data_Number.sin(rad);
        var x = v.center.x + v.outerRadius * Data_Number.cos(rad);
        return Hydrogen_Schema_Geometry_Point.point2D(x)(y);
    };
};
var outerCircumference = function (v) {
    return 2.0 * Data_Number.pi * v.outerRadius;
};
var outerCircle = function (v) {
    return Hydrogen_Schema_Geometry_Circle.mkCircleUnsafe(v.center)(v.outerRadius);
};
var nearestPointOnRing = function (v) {
    return function (v1) {
        var dy = v.y - v1.center.y;
        var dx = v.x - v1.center.x;
        var d = Data_Number.sqrt(dx * dx + dy * dy);
        var $104 = d === 0.0;
        if ($104) {
            return Hydrogen_Schema_Geometry_Point.point2D(v1.center.x + v1.innerRadius)(v1.center.y);
        };
        var targetRadius = (function () {
            var $105 = d < (v1.innerRadius + v1.outerRadius) / 2.0;
            if ($105) {
                return v1.innerRadius;
            };
            return v1.outerRadius;
        })();
        var ny = dy / d;
        var nx = dx / d;
        return Hydrogen_Schema_Geometry_Point.point2D(v1.center.x + nx * targetRadius)(v1.center.y + ny * targetRadius);
    };
};
var mkRingUnsafe = function (center) {
    return function (inner) {
        return function (outer) {
            return {
                center: center,
                innerRadius: inner,
                outerRadius: outer
            };
        };
    };
};
var mkRing = function (center) {
    return function (inner) {
        return function (outer) {
            var $106 = inner > 0.0 && (outer > 0.0 && outer > inner);
            if ($106) {
                return new Data_Maybe.Just({
                    center: center,
                    innerRadius: inner,
                    outerRadius: outer
                });
            };
            return Data_Maybe.Nothing.value;
        };
    };
};
var ringFromCircle = function (outerC) {
    return function (inner) {
        var outer = Hydrogen_Schema_Geometry_Circle.circleRadius(outerC);
        var center = Hydrogen_Schema_Geometry_Circle.circleCenter(outerC);
        return mkRing(center)(inner)(outer);
    };
};
var ringFromWidth = function (center) {
    return function (outer) {
        return function (width) {
            var inner = outer - width;
            return mkRing(center)(inner)(outer);
        };
    };
};
var isWide = function (ring) {
    return radiusRatio(ring) < 0.3;
};
var isNarrow = function (ring) {
    return radiusRatio(ring) > 0.8;
};
var innerPointAtAngle = function (angle) {
    return function (v) {
        var rad = Hydrogen_Schema_Geometry_Angle.unwrapRadians(toRadians(angle));
        var y = v.center.y + v.innerRadius * Data_Number.sin(rad);
        var x = v.center.x + v.innerRadius * Data_Number.cos(rad);
        return Hydrogen_Schema_Geometry_Point.point2D(x)(y);
    };
};
var innerCircumference = function (v) {
    return 2.0 * Data_Number.pi * v.innerRadius;
};
var innerCircle = function (v) {
    return Hydrogen_Schema_Geometry_Circle.mkCircleUnsafe(v.center)(v.innerRadius);
};
var toCircles = function (ring) {
    return {
        inner: innerCircle(ring),
        outer: outerCircle(ring)
    };
};
var eqRing = {
    eq: function (x) {
        return function (y) {
            return eq1(x.center)(y.center) && x.innerRadius === y.innerRadius && x.outerRadius === y.outerRadius;
        };
    }
};
var ordRing = {
    compare: function (x) {
        return function (y) {
            var v = compare(x.center)(y.center);
            if (v instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            var v1 = compare1(x.innerRadius)(y.innerRadius);
            if (v1 instanceof Data_Ordering.LT) {
                return Data_Ordering.LT.value;
            };
            if (v1 instanceof Data_Ordering.GT) {
                return Data_Ordering.GT.value;
            };
            return compare1(x.outerRadius)(y.outerRadius);
        };
    },
    Eq0: function () {
        return eqRing;
    }
};
var distanceToRing = function (p) {
    return function (v) {
        var d = Hydrogen_Schema_Geometry_Point.distance2D(p)(v.center);
        var $120 = d < v.innerRadius;
        if ($120) {
            return v.innerRadius - d;
        };
        var $121 = d > v.outerRadius;
        if ($121) {
            return d - v.outerRadius;
        };
        return 0.0;
    };
};
var containsAngle = function (v) {
    return function (v1) {
        return true;
    };
};
var contains = function (p) {
    return function (v) {
        var d = Hydrogen_Schema_Geometry_Point.distance2D(p)(v.center);
        return d >= v.innerRadius && d <= v.outerRadius;
    };
};
var compareByWidth = function (r1) {
    return function (r2) {
        return compare1(ringWidth(r1))(ringWidth(r2));
    };
};
var compareByOuterRadius = function (r1) {
    return function (r2) {
        return compare1(ringOuterRadius(r1))(ringOuterRadius(r2));
    };
};
var averageCircumference = function (v) {
    var avgRadius = (v.innerRadius + v.outerRadius) / 2.0;
    return 2.0 * Data_Number.pi * avgRadius;
};
export {
    Ring,
    mkRing,
    mkRingUnsafe,
    ringCenter,
    ringInnerRadius,
    ringOuterRadius,
    ringArea,
    ringWidth,
    innerCircumference,
    outerCircumference,
    averageCircumference,
    radiusRatio,
    isNarrow,
    isWide,
    contains,
    containsAngle,
    distanceToRing,
    nearestPointOnRing,
    pointAtAngle,
    innerPointAtAngle,
    outerPointAtAngle,
    innerCircle,
    outerCircle,
    toCircles,
    translateRing,
    scaleRing,
    setInnerRadius,
    setOuterRadius,
    setWidth,
    unitRing,
    ringFromWidth,
    ringFromCircle,
    compareByOuterRadius,
    compareByWidth,
    sameWidth,
    showRing,
    eqRing,
    ordRing,
    showRingInstance
};
//# sourceMappingURL=index.js.map
