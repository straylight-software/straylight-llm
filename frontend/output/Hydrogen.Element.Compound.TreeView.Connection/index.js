// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Monoid from "../Data.Monoid/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Element_Compound_TreeView_Layout_Core from "../Hydrogen.Element.Compound.TreeView.Layout.Core/index.js";
import * as Hydrogen_Element_Compound_TreeView_Node from "../Hydrogen.Element.Compound.TreeView.Node/index.js";
import * as Hydrogen_Element_Compound_TreeView_State from "../Hydrogen.Element.Compound.TreeView.State/index.js";
import * as Hydrogen_Render_Element_Attributes from "../Hydrogen.Render.Element.Attributes/index.js";
import * as Hydrogen_Render_Element_Constructors from "../Hydrogen.Render.Element.Constructors/index.js";
import * as Hydrogen_Schema_Graph_Connection from "../Hydrogen.Schema.Graph.Connection/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var intercalate = /* #__PURE__ */ Data_Array.intercalate(Data_Monoid.monoidString);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var withShowConnections = function (b) {
    return function (p) {
        return {
            style: p.style,
            animateOnExpand: p.animateOnExpand,
            showConnections: b
        };
    };
};
var withConnectionStyle = function (s) {
    return function (p) {
        return {
            showConnections: p.showConnections,
            animateOnExpand: p.animateOnExpand,
            style: s
        };
    };
};
var straightPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        return "M " + (show(from.x) + (" " + (show(y1) + (" L " + (show(to.x) + (" " + show(y2)))))));
    };
};
var stepPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        return "M " + (show(from.x) + (" " + (show(y1) + (" H " + (show(to.x) + (" V " + show(y2)))))));
    };
};
var squareMarker = function (x) {
    return function (y) {
        return function (color) {
            return function (size) {
                var halfSize = size / 2.0;
                return Hydrogen_Render_Element_Constructors.element("rect")([ Hydrogen_Render_Element_Attributes.attr("x")(show(x - halfSize)), Hydrogen_Render_Element_Attributes.attr("y")(show(y - halfSize)), Hydrogen_Render_Element_Attributes.attr("width")(show(size)), Hydrogen_Render_Element_Attributes.attr("height")(show(size)), Hydrogen_Render_Element_Attributes.attr("fill")(color), Hydrogen_Render_Element_Attributes.attr("stroke")(color), Hydrogen_Render_Element_Attributes.attr("stroke-width")("1") ])([  ]);
            };
        };
    };
};
var orthogonalPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        var midY = (y1 + y2) / 2.0;
        return "M " + (show(from.x) + (" " + (show(y1) + (" V " + (show(midY) + (" H " + (show(to.x) + (" V " + show(y2)))))))));
    };
};
var dotMarker = function (x) {
    return function (y) {
        return function (color) {
            return function (size) {
                return function (filled) {
                    var radius = size / 2.0;
                    var fill = (function () {
                        if (filled) {
                            return color;
                        };
                        return "none";
                    })();
                    return Hydrogen_Render_Element_Constructors.element("circle")([ Hydrogen_Render_Element_Attributes.attr("cx")(show(x)), Hydrogen_Render_Element_Attributes.attr("cy")(show(y)), Hydrogen_Render_Element_Attributes.attr("r")(show(radius)), Hydrogen_Render_Element_Attributes.attr("fill")(fill), Hydrogen_Render_Element_Attributes.attr("stroke")(color), Hydrogen_Render_Element_Attributes.attr("stroke-width")("1") ])([  ]);
                };
            };
        };
    };
};
var diamondMarker = function (x) {
    return function (y) {
        return function (color) {
            return function (size) {
                var halfSize = size / 2.0;
                var points = show(x) + ("," + (show(y - halfSize) + (" " + (show(x + halfSize) + ("," + (show(y) + (" " + (show(x) + ("," + (show(y + halfSize) + (" " + (show(x - halfSize) + ("," + show(y))))))))))))));
                return Hydrogen_Render_Element_Constructors.element("polygon")([ Hydrogen_Render_Element_Attributes.attr("points")(points), Hydrogen_Render_Element_Attributes.attr("fill")(color), Hydrogen_Render_Element_Attributes.attr("stroke")(color), Hydrogen_Render_Element_Attributes.attr("stroke-width")("1") ])([  ]);
            };
        };
    };
};
var diagonalPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        var stepY = (y2 - y1) / 3.0;
        return "M " + (show(from.x) + (" " + (show(y1) + (" L " + (show(from.x) + (" " + (show(y1 + stepY) + (" L " + (show(to.x) + (" " + (show(y2 - stepY) + (" L " + (show(to.x) + (" " + show(y2)))))))))))))));
    };
};
var defaultConnectionProps = {
    style: Hydrogen_Schema_Graph_Connection.defaultConnectionStyle,
    showConnections: true,
    animateOnExpand: false
};
var curvedPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        var midY = (y1 + y2) / 2.0;
        return "M " + (show(from.x) + (" " + (show(y1) + (" C " + (show(from.x) + (" " + (show(midY) + (", " + (show(to.x) + (" " + (show(midY) + (", " + (show(to.x) + (" " + show(y2)))))))))))))));
    };
};
var connectionPoint = function (x) {
    return function (y) {
        return function (side) {
            return {
                x: x,
                y: y,
                side: side
            };
        };
    };
};
var nodeConnectionPoints = function (pos) {
    return {
        top: connectionPoint(pos.x)(pos.y - pos.height / 2.0)("top"),
        bottom: connectionPoint(pos.x)(pos.y + pos.height / 2.0)("bottom"),
        left: connectionPoint(pos.x - pos.width / 2.0)(pos.y)("left"),
        right: connectionPoint(pos.x + pos.width / 2.0)(pos.y)("right"),
        center: connectionPoint(pos.x)(pos.y)("center")
    };
};
var collectNodeConnections = function (tree) {
    return function (expanded) {
        return function (layout) {
            return function (acc) {
                return function (node) {
                    var nid = Hydrogen_Element_Compound_TreeView_Node.nodeId(node);
                    var isExp = Hydrogen_Element_Compound_TreeView_State.isExpanded(nid)(expanded);
                    var children = Hydrogen_Element_Compound_TreeView_Node.nodeChildren(node);
                    var $19 = isExp && Data_Array.length(children) > 0;
                    if ($19) {
                        var v = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(nid)(layout);
                        if (v instanceof Data_Maybe.Nothing) {
                            return acc;
                        };
                        if (v instanceof Data_Maybe.Just) {
                            var childPairs = Data_Array.mapMaybe(function (cid) {
                                var v1 = Hydrogen_Element_Compound_TreeView_Layout_Core.nodePosition(cid)(layout);
                                if (v1 instanceof Data_Maybe.Nothing) {
                                    return Data_Maybe.Nothing.value;
                                };
                                if (v1 instanceof Data_Maybe.Just) {
                                    return new Data_Maybe.Just({
                                        parentPos: v.value0,
                                        childPos: v1.value0,
                                        parentId: nid,
                                        childId: cid
                                    });
                                };
                                throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Connection (line 256, column 19 - line 263, column 24): " + [ v1.constructor.name ]);
                            })(children);
                            var childNodes = Data_Array.mapMaybe(function (cid) {
                                return Hydrogen_Element_Compound_TreeView_Node.getNode(cid)(tree);
                            })(children);
                            var accWithPairs = append1(acc)(childPairs);
                            return foldl(collectNodeConnections(tree)(expanded)(layout))(accWithPairs)(childNodes);
                        };
                        throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Connection (line 249, column 9 - line 272, column 90): " + [ v.constructor.name ]);
                    };
                    return acc;
                };
            };
        };
    };
};
var collectConnections = function (tree) {
    return function (expanded) {
        return function (layout) {
            var roots = Hydrogen_Element_Compound_TreeView_Node.rootNodes(tree);
            return foldl(collectNodeConnections(tree)(expanded)(layout))([  ])(roots);
        };
    };
};
var barMarker = function (x) {
    return function (y) {
        return function (color) {
            return function (size) {
                var halfSize = size / 2.0;
                return Hydrogen_Render_Element_Constructors.element("line")([ Hydrogen_Render_Element_Attributes.attr("x1")(show(x - halfSize)), Hydrogen_Render_Element_Attributes.attr("y1")(show(y)), Hydrogen_Render_Element_Attributes.attr("x2")(show(x + halfSize)), Hydrogen_Render_Element_Attributes.attr("y2")(show(y)), Hydrogen_Render_Element_Attributes.attr("stroke")(color), Hydrogen_Render_Element_Attributes.attr("stroke-width")("2") ])([  ]);
            };
        };
    };
};
var arrowMarker = function (x) {
    return function (y) {
        return function (color) {
            return function (size) {
                return function (filled) {
                    var halfSize = size / 2.0;
                    var points = show(x - halfSize) + ("," + (show(y - size) + (" " + (show(x) + ("," + (show(y) + (" " + (show(x + halfSize) + ("," + show(y - size))))))))));
                    var fill = (function () {
                        if (filled) {
                            return color;
                        };
                        return "none";
                    })();
                    return Hydrogen_Render_Element_Constructors.element("polygon")([ Hydrogen_Render_Element_Attributes.attr("points")(points), Hydrogen_Render_Element_Attributes.attr("fill")(fill), Hydrogen_Render_Element_Attributes.attr("stroke")(color), Hydrogen_Render_Element_Attributes.attr("stroke-width")("1") ])([  ]);
                };
            };
        };
    };
};
var renderTerminal = function (terminal) {
    return function (x) {
        return function (y) {
            return function (color) {
                return function (size) {
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalNone) {
                        return Hydrogen_Render_Element_Constructors.empty;
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalArrow) {
                        return arrowMarker(x)(y)(color)(size)(false);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalArrowFilled) {
                        return arrowMarker(x)(y)(color)(size)(true);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalArrowOpen) {
                        return arrowMarker(x)(y)(color)(size)(false);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalDot) {
                        return dotMarker(x)(y)(color)(size)(false);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalDotFilled) {
                        return dotMarker(x)(y)(color)(size)(true);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalDiamond) {
                        return diamondMarker(x)(y)(color)(size);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalSquare) {
                        return squareMarker(x)(y)(color)(size);
                    };
                    if (terminal instanceof Hydrogen_Schema_Graph_Connection.TerminalBar) {
                        return barMarker(x)(y)(color)(size);
                    };
                    throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Connection (line 451, column 3 - line 460, column 51): " + [ terminal.constructor.name ]);
                };
            };
        };
    };
};
var arcPath = function (from) {
    return function (to) {
        var y2 = to.y - to.height / 2.0;
        var y1 = from.y + from.height / 2.0;
        var dy = y2 - y1;
        var dx = to.x - from.x;
        var r = (dx * dx + dy * dy) / (4.0 * dy);
        return "M " + (show(from.x) + (" " + (show(y1) + (" A " + (show(r) + (" " + (show(r) + (" 0 0 1 " + (show(to.x) + (" " + show(y2)))))))))));
    };
};
var connectionPath = function (routing) {
    return function (parentPos) {
        return function (childPos) {
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingStraight) {
                return straightPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingCurved) {
                return curvedPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingOrthogonal) {
                return orthogonalPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingStep) {
                return stepPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingDiagonal) {
                return diagonalPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingArc) {
                return arcPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingSpline) {
                return curvedPath(parentPos)(childPos);
            };
            if (routing instanceof Hydrogen_Schema_Graph_Connection.RoutingBundle) {
                return curvedPath(parentPos)(childPos);
            };
            throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Connection (line 332, column 3 - line 340, column 58): " + [ routing.constructor.name ]);
        };
    };
};
var renderConnection = function (style) {
    return function (parentPos) {
        return function (childPos) {
            var strokeDashArray = (function () {
                if (style.strokePattern instanceof Hydrogen_Schema_Graph_Connection.StrokeSolid) {
                    return [  ];
                };
                if (style.strokePattern instanceof Hydrogen_Schema_Graph_Connection.StrokeDashed) {
                    return [ Hydrogen_Render_Element_Attributes.attr("stroke-dasharray")(show(style.strokePattern.value0) + (" " + show(style.strokePattern.value1))) ];
                };
                if (style.strokePattern instanceof Hydrogen_Schema_Graph_Connection.StrokeDotted) {
                    return [ Hydrogen_Render_Element_Attributes.attr("stroke-dasharray")("2 4") ];
                };
                if (style.strokePattern instanceof Hydrogen_Schema_Graph_Connection.StrokeDashDot) {
                    return [ Hydrogen_Render_Element_Attributes.attr("stroke-dasharray")("8 4 2 4") ];
                };
                if (style.strokePattern instanceof Hydrogen_Schema_Graph_Connection.StrokeCustom) {
                    return [ Hydrogen_Render_Element_Attributes.attr("stroke-dasharray")(intercalate(" ")(map(show)(style.strokePattern.value0))) ];
                };
                throw new Error("Failed pattern match at Hydrogen.Element.Compound.TreeView.Connection (line 296, column 23 - line 305, column 77): " + [ style.strokePattern.constructor.name ]);
            })();
            var path = connectionPath(style.routing)(parentPos)(childPos);
            var opacityAttr = (function () {
                var $31 = style.opacity < 1.0;
                if ($31) {
                    return [ Hydrogen_Render_Element_Attributes.attr("opacity")(show(style.opacity)) ];
                };
                return [  ];
            })();
            return Hydrogen_Render_Element_Constructors.element("g")([ Hydrogen_Render_Element_Attributes.class_("tree-connection") ])([ Hydrogen_Render_Element_Constructors.element("path")(append1([ Hydrogen_Render_Element_Attributes.attr("d")(path), Hydrogen_Render_Element_Attributes.attr("fill")("none"), Hydrogen_Render_Element_Attributes.attr("stroke")(style.strokeColor), Hydrogen_Render_Element_Attributes.attr("stroke-width")(show(style.strokeWidth)), Hydrogen_Render_Element_Attributes.attr("stroke-linecap")("round"), Hydrogen_Render_Element_Attributes.attr("stroke-linejoin")("round") ])(append1(strokeDashArray)(opacityAttr)))([  ]), renderTerminal(style.endTerminal)(childPos.x)(childPos.y)(style.strokeColor)(style.terminalSize) ]);
        };
    };
};
var renderConnectionPair = function (props) {
    return function (pair) {
        return renderConnection(props.style)(pair.parentPos)(pair.childPos);
    };
};
var renderConnections = function (props) {
    return function (tree) {
        return function (expanded) {
            return function (layout) {
                var $32 = !props.showConnections;
                if ($32) {
                    return Hydrogen_Render_Element_Constructors.empty;
                };
                var connections = collectConnections(tree)(expanded)(layout);
                return Hydrogen_Render_Element_Constructors.element("svg")([ Hydrogen_Render_Element_Attributes.class_("tree-connections"), Hydrogen_Render_Element_Attributes.attr("width")("100%"), Hydrogen_Render_Element_Attributes.attr("height")("100%"), Hydrogen_Render_Element_Attributes.style("position")("absolute"), Hydrogen_Render_Element_Attributes.style("top")("0"), Hydrogen_Render_Element_Attributes.style("left")("0"), Hydrogen_Render_Element_Attributes.style("pointer-events")("none"), Hydrogen_Render_Element_Attributes.style("overflow")("visible") ])(map(renderConnectionPair(props))(connections));
            };
        };
    };
};
var anchorPoint = function (side) {
    return function (pos) {
        var points = nodeConnectionPoints(pos);
        if (side === "top") {
            return points.top;
        };
        if (side === "bottom") {
            return points.bottom;
        };
        if (side === "left") {
            return points.left;
        };
        if (side === "right") {
            return points.right;
        };
        return points.center;
    };
};
export {
    renderConnections,
    renderConnection,
    connectionPath,
    straightPath,
    curvedPath,
    orthogonalPath,
    stepPath,
    connectionPoint,
    nodeConnectionPoints,
    anchorPoint,
    renderTerminal,
    arrowMarker,
    dotMarker,
    defaultConnectionProps,
    withConnectionStyle,
    withShowConnections
};
//# sourceMappingURL=index.js.map
