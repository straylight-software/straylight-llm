// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Hydrogen_Schema_Attestation_UUID5 from "../Hydrogen.Schema.Attestation.UUID5/index.js";
import * as Hydrogen_Schema_Attestation_UUID5_Namespaces from "../Hydrogen.Schema.Attestation.UUID5.Namespaces/index.js";
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var DistancePDConstraint = /* #__PURE__ */ (function () {
    function DistancePDConstraint() {

    };
    DistancePDConstraint.value = new DistancePDConstraint();
    return DistancePDConstraint;
})();
var BendingPDConstraint = /* #__PURE__ */ (function () {
    function BendingPDConstraint() {

    };
    BendingPDConstraint.value = new BendingPDConstraint();
    return BendingPDConstraint;
})();
var StretchPDConstraint = /* #__PURE__ */ (function () {
    function StretchPDConstraint() {

    };
    StretchPDConstraint.value = new StretchPDConstraint();
    return StretchPDConstraint;
})();
var AttachmentPDConstraint = /* #__PURE__ */ (function () {
    function AttachmentPDConstraint() {

    };
    AttachmentPDConstraint.value = new AttachmentPDConstraint();
    return AttachmentPDConstraint;
})();
var VolumePDConstraint = /* #__PURE__ */ (function () {
    function VolumePDConstraint() {

    };
    VolumePDConstraint.value = new VolumePDConstraint();
    return VolumePDConstraint;
})();
var CollisionPDConstraint = /* #__PURE__ */ (function () {
    function CollisionPDConstraint() {

    };
    CollisionPDConstraint.value = new CollisionPDConstraint();
    return CollisionPDConstraint;
})();
var velocityZ = function (v) {
    return v.vz;
};
var velocityY = function (v) {
    return v.vy;
};
var velocityX = function (v) {
    return v.vx;
};
var subtract = function (p1) {
    return function (p2) {
        return {
            x: p1.x - p2.x,
            y: p1.y - p2.y,
            z: p1.z - p2.z
        };
    };
};
var stateTime = function (s) {
    return s.time;
};
var stateParticles = function (s) {
    return s.particles;
};
var stateConstraints = function (s) {
    return s.constraints;
};
var solverTimestep = function (c) {
    return c.timestep;
};
var solverIterations = function (c) {
    return c.iterations;
};
var solverDamping = function (c) {
    return c.damping;
};
var showPDConstraintType = {
    show: function (v) {
        if (v instanceof DistancePDConstraint) {
            return "distance";
        };
        if (v instanceof BendingPDConstraint) {
            return "bending";
        };
        if (v instanceof StretchPDConstraint) {
            return "stretch";
        };
        if (v instanceof AttachmentPDConstraint) {
            return "attachment";
        };
        if (v instanceof VolumePDConstraint) {
            return "volume";
        };
        if (v instanceof CollisionPDConstraint) {
            return "collision";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Projective.Core (line 350, column 1 - line 356, column 43): " + [ v.constructor.name ]);
    }
};
var scale = function (s) {
    return function (p) {
        return {
            x: s * p.x,
            y: s * p.y,
            z: s * p.z
        };
    };
};
var projectionTarget = function (p) {
    return p.targetPositions;
};
var projectionEnergy = function (p) {
    return p.energy;
};
var projectionConstraintId = function (p) {
    return p.constraintId;
};
var predictPosition = function (h) {
    return function (p) {
        return {
            x: p.position.x + h * p.velocity.vx,
            y: p.position.y + h * p.velocity.vy,
            z: p.position.z + h * p.velocity.vz
        };
    };
};
var positionZ = function (p) {
    return p.z;
};
var positionY = function (p) {
    return p.y;
};
var positionX = function (p) {
    return p.x;
};
var pdConstraintStiffness = function (v) {
    if (v instanceof DistancePDConstraint) {
        return 1.0;
    };
    if (v instanceof BendingPDConstraint) {
        return 0.1;
    };
    if (v instanceof StretchPDConstraint) {
        return 1.0;
    };
    if (v instanceof AttachmentPDConstraint) {
        return 10.0;
    };
    if (v instanceof VolumePDConstraint) {
        return 1.0;
    };
    if (v instanceof CollisionPDConstraint) {
        return 1.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Projective.Core (line 370, column 1 - line 370, column 52): " + [ v.constructor.name ]);
};
var particleVelocity = function (p) {
    return p.velocity;
};
var particlePredicted = function (p) {
    return p.predicted;
};
var particlePosition = function (p) {
    return p.position;
};
var particleMass = function (p) {
    return p.mass;
};
var particleId = function (p) {
    return p.id;
};
var nsProjective = /* #__PURE__ */ Hydrogen_Schema_Attestation_UUID5.uuid5(Hydrogen_Schema_Attestation_UUID5_Namespaces.nsElement)("physics.projective");
var nsPDConstraint = /* #__PURE__ */ Hydrogen_Schema_Attestation_UUID5.uuid5(nsProjective)("constraint");
var nsDomain = /* #__PURE__ */ Hydrogen_Schema_Attestation_UUID5.uuid5(nsProjective)("domain");
var normalize = function (p) {
    var len = Data_Number.sqrt(p.x * p.x + p.y * p.y + p.z * p.z);
    var invLen = (function () {
        var $27 = len > 1.0e-4;
        if ($27) {
            return 1.0 / len;
        };
        return 0.0;
    })();
    return {
        x: p.x * invLen,
        y: p.y * invLen,
        z: p.z * invLen
    };
};
var mkVelocity = function (vvx) {
    return function (vvy) {
        return function (vvz) {
            return {
                vx: vvx,
                vy: vvy,
                vz: vvz
            };
        };
    };
};
var updateVelocities = function (h) {
    return function (damping) {
        return function (oldParticles) {
            return function (newParticles) {
                return Data_Array.zipWith(function (old) {
                    return function ($$new) {
                        var newVel = mkVelocity((($$new.predicted.x - old.position.x) / h) * damping)((($$new.predicted.y - old.position.y) / h) * damping)((($$new.predicted.z - old.position.z) / h) * damping);
                        return {
                            predicted: $$new.predicted,
                            id: $$new.id,
                            mass: $$new.mass,
                            position: $$new.predicted,
                            velocity: newVel
                        };
                    };
                })(oldParticles)(newParticles);
            };
        };
    };
};
var mkSystemState = function (parts) {
    return function (constrs) {
        return {
            particles: parts,
            constraints: constrs,
            time: 0.0
        };
    };
};
var mkSolverConfig = function (iters) {
    return function (h) {
        return function (damp) {
            return {
                iterations: max(1)(iters),
                timestep: max1(1.0e-4)(h),
                damping: max1(0.0)(min(1.0)(damp))
            };
        };
    };
};
var realtimeSolverConfig = /* #__PURE__ */ mkSolverConfig(5)(1.6e-2)(0.98);
var mkProjection = function (cid) {
    return function (targets) {
        return function (e) {
            return {
                constraintId: cid,
                targetPositions: targets,
                energy: e
            };
        };
    };
};
var mkPosition = function (px) {
    return function (py) {
        return function (pz) {
            return {
                x: px,
                y: py,
                z: pz
            };
        };
    };
};
var solveForParticle = function (particleIdx) {
    return function (particle) {
        return function (h2) {
            return function (numParticles) {
                return function (projections) {
                    var projectionWeight = Data_Int.toNumber(Data_Array.length(projections)) / Data_Int.toNumber(max(1)(numParticles));
                    var regularization = 1.0 + projectionWeight;
                    var massWeight = particle.mass.value / h2;
                    var totalWeight = massWeight + regularization;
                    var idxScale = (function () {
                        var $28 = particleIdx >= 0;
                        if ($28) {
                            return 1.0;
                        };
                        return 0.0;
                    })();
                    var weightedX = (massWeight * particle.predicted.x + regularization * particle.predicted.x) * idxScale;
                    var weightedY = (massWeight * particle.predicted.y + regularization * particle.predicted.y) * idxScale;
                    var weightedZ = (massWeight * particle.predicted.z + regularization * particle.predicted.z) * idxScale;
                    return mkPosition(weightedX / totalWeight)(weightedY / totalWeight)(weightedZ / totalWeight);
                };
            };
        };
    };
};
var mkParticle = function (pid) {
    return function (pos) {
        return function (vel) {
            return function (m) {
                return {
                    id: pid,
                    position: pos,
                    velocity: vel,
                    mass: m,
                    predicted: pos
                };
            };
        };
    };
};
var mkPDConstraint = function (cid) {
    return function (ctype) {
        return function (parts) {
            return function (w) {
                return function (rest) {
                    return {
                        id: cid,
                        constraintType: ctype,
                        particles: parts,
                        weight: w,
                        restValue: rest
                    };
                };
            };
        };
    };
};
var mkMass = function (m) {
    return {
        value: max1(1.0e-3)(m)
    };
};
var massValue = function (m) {
    return m.value;
};
var kineticEnergy = function (state) {
    return Data_Array.foldl(function (acc) {
        return function (p) {
            var vSq = p.velocity.vx * p.velocity.vx + p.velocity.vy * p.velocity.vy + p.velocity.vz * p.velocity.vz;
            return acc + 0.5 * p.mass.value * vSq;
        };
    })(0.0)(state.particles);
};
var inverseMass = function (m) {
    return 1.0 / m.value;
};
var highQualitySolverConfig = /* #__PURE__ */ mkSolverConfig(30)(1.6e-2)(0.995);
var globalSolve = function (config) {
    return function (particles) {
        return function (projections) {
            var numParticles = Data_Array.length(particles);
            var h2 = config.timestep * config.timestep;
            return Data_Array.mapWithIndex(function (idx) {
                return function (p) {
                    return solveForParticle(idx)(p)(h2)(numParticles)(projections);
                };
            })(particles);
        };
    };
};
var getParticleByIndex = function (particles) {
    return function (idx) {
        var defaultParticle = mkParticle(0)(mkPosition(0.0)(0.0)(0.0))(mkVelocity(0.0)(0.0)(0.0))(mkMass(1.0));
        return Data_Maybe.fromMaybe(defaultParticle)(Data_Array.index(particles)(idx));
    };
};
var projectAttachment = function (particles) {
    return function (constraint) {
        var pIdx = Data_Maybe.fromMaybe(0)(Data_Array.index(constraint.particles)(0));
        var p = getParticleByIndex(particles)(pIdx);
        return mkProjection(constraint.id)([ p.position ])(0.0);
    };
};
var projectDistance = function (particles) {
    return function (constraint) {
        var p1Idx = Data_Maybe.fromMaybe(0)(Data_Array.index(constraint.particles)(0));
        var p1 = getParticleByIndex(particles)(p1Idx);
        var p2Idx = Data_Maybe.fromMaybe(1)(Data_Array.index(constraint.particles)(1));
        var p2 = getParticleByIndex(particles)(p2Idx);
        var dz = p2.predicted.z - p1.predicted.z;
        var dy = p2.predicted.y - p1.predicted.y;
        var dx = p2.predicted.x - p1.predicted.x;
        var dist = Data_Number.sqrt(dx * dx + dy * dy + dz * dz);
        var energy = constraint.weight * (dist - constraint.restValue) * (dist - constraint.restValue);
        var invDist = (function () {
            var $29 = dist > 1.0e-4;
            if ($29) {
                return 1.0 / dist;
            };
            return 0.0;
        })();
        var nx = dx * invDist;
        var ny = dy * invDist;
        var nz = dz * invDist;
        var correction = (dist - constraint.restValue) * 0.5;
        var target1 = mkPosition(p1.predicted.x + nx * correction)(p1.predicted.y + ny * correction)(p1.predicted.z + nz * correction);
        var target2 = mkPosition(p2.predicted.x - nx * correction)(p2.predicted.y - ny * correction)(p2.predicted.z - nz * correction);
        return mkProjection(constraint.id)([ target1, target2 ])(energy);
    };
};
var projectConstraint = function (particles) {
    return function (constraint) {
        if (constraint.constraintType instanceof DistancePDConstraint) {
            return projectDistance(particles)(constraint);
        };
        if (constraint.constraintType instanceof AttachmentPDConstraint) {
            return projectAttachment(particles)(constraint);
        };
        return projectDistance(particles)(constraint);
    };
};
var projectAllConstraints = function (particles) {
    return function (constraints) {
        return map(projectConstraint(particles))(constraints);
    };
};
var iterateSolver = function ($copy_v) {
    return function ($copy_v1) {
        return function ($copy_v2) {
            return function ($copy_v3) {
                var $tco_var_v = $copy_v;
                var $tco_var_v1 = $copy_v1;
                var $tco_var_v2 = $copy_v2;
                var $tco_done = false;
                var $tco_result;
                function $tco_loop(v, v1, v2, v3) {
                    if (v === 0) {
                        $tco_done = true;
                        return v2;
                    };
                    var projections = projectAllConstraints(v2)(v3);
                    var newPositions = globalSolve(v1)(v2)(projections);
                    var updatedParticles = Data_Array.zipWith(function (p) {
                        return function (pos) {
                            return {
                                id: p.id,
                                mass: p.mass,
                                position: p.position,
                                velocity: p.velocity,
                                predicted: pos
                            };
                        };
                    })(v2)(newPositions);
                    $tco_var_v = v - 1 | 0;
                    $tco_var_v1 = v1;
                    $tco_var_v2 = updatedParticles;
                    $copy_v3 = v3;
                    return;
                };
                while (!$tco_done) {
                    $tco_result = $tco_loop($tco_var_v, $tco_var_v1, $tco_var_v2, $copy_v3);
                };
                return $tco_result;
            };
        };
    };
};
var integrateStep = function (config) {
    return function (state) {
        var predictedParticles = map(function (p) {
            return {
                id: p.id,
                mass: p.mass,
                position: p.position,
                velocity: p.velocity,
                predicted: predictPosition(config.timestep)(p)
            };
        })(state.particles);
        var finalParticles = iterateSolver(config.iterations)(config)(predictedParticles)(state.constraints);
        var updatedParticles = updateVelocities(config.timestep)(config.damping)(state.particles)(finalParticles);
        return {
            constraints: state.constraints,
            particles: updatedParticles,
            time: state.time + config.timestep
        };
    };
};
var eqPDConstraintType = {
    eq: function (x) {
        return function (y) {
            if (x instanceof DistancePDConstraint && y instanceof DistancePDConstraint) {
                return true;
            };
            if (x instanceof BendingPDConstraint && y instanceof BendingPDConstraint) {
                return true;
            };
            if (x instanceof StretchPDConstraint && y instanceof StretchPDConstraint) {
                return true;
            };
            if (x instanceof AttachmentPDConstraint && y instanceof AttachmentPDConstraint) {
                return true;
            };
            if (x instanceof VolumePDConstraint && y instanceof VolumePDConstraint) {
                return true;
            };
            if (x instanceof CollisionPDConstraint && y instanceof CollisionPDConstraint) {
                return true;
            };
            return false;
        };
    }
};
var ordPDConstraintType = {
    compare: function (x) {
        return function (y) {
            if (x instanceof DistancePDConstraint && y instanceof DistancePDConstraint) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof DistancePDConstraint) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof DistancePDConstraint) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof BendingPDConstraint && y instanceof BendingPDConstraint) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof BendingPDConstraint) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof BendingPDConstraint) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof StretchPDConstraint && y instanceof StretchPDConstraint) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof StretchPDConstraint) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof StretchPDConstraint) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof AttachmentPDConstraint && y instanceof AttachmentPDConstraint) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof AttachmentPDConstraint) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof AttachmentPDConstraint) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VolumePDConstraint && y instanceof VolumePDConstraint) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VolumePDConstraint) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VolumePDConstraint) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof CollisionPDConstraint && y instanceof CollisionPDConstraint) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Physics.Projective.Core (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqPDConstraintType;
    }
};
var dot = function (p1) {
    return function (p2) {
        return p1.x * p2.x + p1.y * p2.y + p1.z * p2.z;
    };
};
var distanceSq = function (p1) {
    return function (p2) {
        var dz = p2.z - p1.z;
        var dy = p2.y - p1.y;
        var dx = p2.x - p1.x;
        return dx * dx + dy * dy + dz * dz;
    };
};
var distanceBetween = function (p1) {
    return function (p2) {
        return Data_Number.sqrt(distanceSq(p1)(p2));
    };
};
var defaultSolverConfig = /* #__PURE__ */ mkSolverConfig(10)(1.6e-2)(0.99);
var cross = function (p1) {
    return function (p2) {
        return {
            x: p1.y * p2.z - p1.z * p2.y,
            y: p1.z * p2.x - p1.x * p2.z,
            z: p1.x * p2.y - p1.y * p2.x
        };
    };
};
var constraintWeight = function (c) {
    return c.weight;
};
var constraintType = function (c) {
    return c.constraintType;
};
var constraintRestValue = function (c) {
    return c.restValue;
};
var constraintParticles = function (c) {
    return c.particles;
};
var constraintId = function (c) {
    return c.id;
};
var constraintEnergy = function (particles) {
    return function (constraint) {
        var proj = projectConstraint(particles)(constraint);
        return proj.energy;
    };
};
var potentialEnergy = function (state) {
    return Data_Array.foldl(function (acc) {
        return function (c) {
            return acc + constraintEnergy(state.particles)(c);
        };
    })(0.0)(state.constraints);
};
var totalEnergy = function (state) {
    return kineticEnergy(state) + potentialEnergy(state);
};
var computeRHS = function (h2) {
    return function (particles) {
        return function (projections) {
            var numProjections = Data_Int.toNumber(Data_Array.length(projections));
            var projectionScale = (function () {
                var $39 = numProjections > 0.0;
                if ($39) {
                    return 1.0 / numProjections;
                };
                return 1.0;
            })();
            return map(function (p) {
                return mkPosition((p.mass.value / h2) * p.predicted.x * projectionScale)((p.mass.value / h2) * p.predicted.y * projectionScale)((p.mass.value / h2) * p.predicted.z * projectionScale);
            })(particles);
        };
    };
};
var allPDConstraintTypes = /* #__PURE__ */ (function () {
    return [ DistancePDConstraint.value, BendingPDConstraint.value, StretchPDConstraint.value, AttachmentPDConstraint.value, VolumePDConstraint.value, CollisionPDConstraint.value ];
})();
var add = function (p1) {
    return function (p2) {
        return {
            x: p1.x + p2.x,
            y: p1.y + p2.y,
            z: p1.z + p2.z
        };
    };
};
export {
    nsProjective,
    nsPDConstraint,
    nsDomain,
    mkPosition,
    positionX,
    positionY,
    positionZ,
    mkVelocity,
    velocityX,
    velocityY,
    velocityZ,
    mkMass,
    massValue,
    inverseMass,
    mkParticle,
    particleId,
    particlePosition,
    particleVelocity,
    particleMass,
    particlePredicted,
    predictPosition,
    DistancePDConstraint,
    BendingPDConstraint,
    StretchPDConstraint,
    AttachmentPDConstraint,
    VolumePDConstraint,
    CollisionPDConstraint,
    allPDConstraintTypes,
    pdConstraintStiffness,
    mkPDConstraint,
    constraintId,
    constraintType,
    constraintParticles,
    constraintWeight,
    constraintRestValue,
    mkProjection,
    projectionConstraintId,
    projectionTarget,
    projectionEnergy,
    mkSolverConfig,
    solverIterations,
    solverTimestep,
    solverDamping,
    defaultSolverConfig,
    highQualitySolverConfig,
    realtimeSolverConfig,
    mkSystemState,
    stateParticles,
    stateConstraints,
    stateTime,
    projectConstraint,
    projectAllConstraints,
    globalSolve,
    computeRHS,
    integrateStep,
    updateVelocities,
    constraintEnergy,
    totalEnergy,
    kineticEnergy,
    potentialEnergy,
    distanceBetween,
    distanceSq,
    normalize,
    dot,
    cross,
    scale,
    add,
    subtract,
    eqPDConstraintType,
    ordPDConstraintType,
    showPDConstraintType
};
//# sourceMappingURL=index.js.map
