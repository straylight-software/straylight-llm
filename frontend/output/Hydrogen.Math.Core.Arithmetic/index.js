// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Hydrogen_Math_Core_Constants from "../Hydrogen.Math.Core.Constants/index.js";
import * as Hydrogen_Math_Core_Exponential from "../Hydrogen.Math.Core.Exponential/index.js";
var sign = function (x) {
    if (x > 0.0) {
        return 1.0;
    };
    if (x < 0.0) {
        return -1.0;
    };
    if (Data_Boolean.otherwise) {
        return 0.0;
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 55, column 1 - line 55, column 25): " + [ x.constructor.name ]);
};
var min = function (a) {
    return function (b) {
        var $16 = a <= b;
        if ($16) {
            return a;
        };
        return b;
    };
};
var minArray = function (arr) {
    return Data_Array.foldl(min)(Hydrogen_Math_Core_Constants.infinity)(arr);
};
var max = function (a) {
    return function (b) {
        var $17 = a >= b;
        if ($17) {
            return a;
        };
        return b;
    };
};
var maxArray = function (arr) {
    return Data_Array.foldl(max)(Hydrogen_Math_Core_Constants.negativeInfinity)(arr);
};
var initialGuess = function (x) {
    var $18 = x >= 1.0;
    if ($18) {
        return x / 2.0;
    };
    return x * 2.0;
};
var fround = function (x) {
    return x;
};
var floor = function (x) {
    var i = Data_Int.floor(x);
    return Data_Int.toNumber(i);
};
var isOdd = function (x) {
    var half = x / 2.0;
    return floor(half) !== half;
};
var round = function (x) {
    var fl = floor(x);
    var diff = x - fl;
    var $19 = diff >= 0.5;
    if ($19) {
        return fl + 1.0;
    };
    return fl;
};
var clamp = function (minVal) {
    return function (maxVal) {
        return function (x) {
            if (!Hydrogen_Math_Core_Constants["isFinite"](x)) {
                return minVal;
            };
            if (Data_Boolean.otherwise) {
                return min(maxVal)(max(minVal)(x));
            };
            throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 130, column 1 - line 130, column 46): " + [ minVal.constructor.name, maxVal.constructor.name, x.constructor.name ]);
        };
    };
};
var ceil = function (x) {
    var fl = floor(x);
    var $23 = x === fl;
    if ($23) {
        return fl;
    };
    return fl + 1.0;
};
var trunc = function (x) {
    var $24 = x >= 0.0;
    if ($24) {
        return floor(x);
    };
    return ceil(x);
};
var pow = function (base) {
    return function (exponent) {
        if (exponent === 0.0) {
            return 1.0;
        };
        if (base === 0.0) {
            var $27 = exponent > 0.0;
            if ($27) {
                return 0.0;
            };
            return Hydrogen_Math_Core_Constants.infinity;
        };
        if (base === 1.0) {
            return 1.0;
        };
        if (exponent === 1.0) {
            return base;
        };
        if (base < 0.0 && !Hydrogen_Math_Core_Constants.isInteger(exponent)) {
            return 0.0 / 0.0;
        };
        if (base < 0.0) {
            var result = Hydrogen_Math_Core_Exponential.exp(exponent * Hydrogen_Math_Core_Exponential.log(-base));
            var $28 = isOdd(trunc(exponent));
            if ($28) {
                return -result;
            };
            return result;
        };
        if (Data_Boolean.otherwise) {
            return Hydrogen_Math_Core_Exponential.exp(exponent * Hydrogen_Math_Core_Exponential.log(base));
        };
        throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 215, column 1 - line 215, column 34): " + [ base.constructor.name, exponent.constructor.name ]);
    };
};
var abs = function (x) {
    var $29 = x < 0.0;
    if ($29) {
        return -x;
    };
    return x;
};
var newtonCbrt = function ($copy_x) {
    return function ($copy_guess) {
        return function ($copy_iter) {
            var $tco_var_x = $copy_x;
            var $tco_var_guess = $copy_guess;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(x, guess, iter) {
                if (iter >= 100) {
                    $tco_done = true;
                    return guess;
                };
                if (Data_Boolean.otherwise) {
                    var next = (2.0 * guess + x / (guess * guess)) / 3.0;
                    var $33 = abs(next - guess) < 1.0e-15 * abs(guess);
                    if ($33) {
                        $tco_done = true;
                        return next;
                    };
                    $tco_var_x = x;
                    $tco_var_guess = next;
                    $copy_iter = iter + 1 | 0;
                    return;
                };
                throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 180, column 1 - line 180, column 48): " + [ x.constructor.name, guess.constructor.name, iter.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_x, $tco_var_guess, $copy_iter);
            };
            return $tco_result;
        };
    };
};
var cbrtPositive = function (x) {
    return newtonCbrt(x)(x / 3.0)(0);
};
var cbrt = function (x) {
    if (x === 0.0) {
        return 0.0;
    };
    if (x < 0.0) {
        return -cbrtPositive(-x);
    };
    if (Data_Boolean.otherwise) {
        return cbrtPositive(x);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 169, column 1 - line 169, column 25): " + [ x.constructor.name ]);
};
var newtonSqrt = function ($copy_x) {
    return function ($copy_guess) {
        return function ($copy_iter) {
            var $tco_var_x = $copy_x;
            var $tco_var_guess = $copy_guess;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(x, guess, iter) {
                if (iter >= 100) {
                    $tco_done = true;
                    return guess;
                };
                if (Data_Boolean.otherwise) {
                    var next = (guess + x / guess) / 2.0;
                    var $38 = abs(next - guess) < 1.0e-15 * abs(guess);
                    if ($38) {
                        $tco_done = true;
                        return next;
                    };
                    $tco_var_x = x;
                    $tco_var_guess = next;
                    $copy_iter = iter + 1 | 0;
                    return;
                };
                throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 159, column 1 - line 159, column 48): " + [ x.constructor.name, guess.constructor.name, iter.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_x, $tco_var_guess, $copy_iter);
            };
            return $tco_result;
        };
    };
};
var sqrt = function (x) {
    if (x < 0.0) {
        return 0.0 / 0.0;
    };
    if (x === 0.0) {
        return 0.0;
    };
    if (x === Hydrogen_Math_Core_Constants.infinity) {
        return Hydrogen_Math_Core_Constants.infinity;
    };
    if (Data_Boolean.otherwise) {
        return newtonSqrt(x)(initialGuess(x))(0);
    };
    throw new Error("Failed pattern match at Hydrogen.Math.Core.Arithmetic (line 143, column 1 - line 143, column 25): " + [ x.constructor.name ]);
};
var hypot = function (x) {
    return function (y) {
        var ay = abs(y);
        var ax = abs(x);
        var maxVal = max(ax)(ay);
        var $40 = maxVal === 0.0;
        if ($40) {
            return 0.0;
        };
        return maxVal * sqrt((ax / maxVal) * (ax / maxVal) + (ay / maxVal) * (ay / maxVal));
    };
};
var hypot3 = function (x) {
    return function (y) {
        return function (z) {
            var az = abs(z);
            var ay = abs(y);
            var ax = abs(x);
            var maxVal = max(max(ax)(ay))(az);
            var $41 = maxVal === 0.0;
            if ($41) {
                return 0.0;
            };
            return maxVal * sqrt((ax / maxVal) * (ax / maxVal) + (ay / maxVal) * (ay / maxVal) + (az / maxVal) * (az / maxVal));
        };
    };
};
export {
    floor,
    ceil,
    round,
    trunc,
    fround,
    abs,
    sign,
    min,
    max,
    minArray,
    maxArray,
    clamp,
    pow,
    sqrt,
    cbrt,
    hypot,
    hypot3
};
//# sourceMappingURL=index.js.map
