// Generated by purs version 0.15.15
import * as Data_Ordering from "../Data.Ordering/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var VelocityLinear = /* #__PURE__ */ (function () {
    function VelocityLinear() {

    };
    VelocityLinear.value = new VelocityLinear();
    return VelocityLinear;
})();
var VelocitySoft = /* #__PURE__ */ (function () {
    function VelocitySoft() {

    };
    VelocitySoft.value = new VelocitySoft();
    return VelocitySoft;
})();
var VelocityFirm = /* #__PURE__ */ (function () {
    function VelocityFirm() {

    };
    VelocityFirm.value = new VelocityFirm();
    return VelocityFirm;
})();
var VelocitySCurve = /* #__PURE__ */ (function () {
    function VelocitySCurve() {

    };
    VelocitySCurve.value = new VelocitySCurve();
    return VelocitySCurve;
})();
var VelocityLogarithmic = /* #__PURE__ */ (function () {
    function VelocityLogarithmic() {

    };
    VelocityLogarithmic.value = new VelocityLogarithmic();
    return VelocityLogarithmic;
})();
var VelocityExponential = /* #__PURE__ */ (function () {
    function VelocityExponential() {

    };
    VelocityExponential.value = new VelocityExponential();
    return VelocityExponential;
})();
var velocityCurveMaxSensitivity = function (v) {
    if (v instanceof VelocityLinear) {
        return 1.0;
    };
    if (v instanceof VelocitySoft) {
        return 8.0;
    };
    if (v instanceof VelocityFirm) {
        return 2.0;
    };
    if (v instanceof VelocitySCurve) {
        return 1.5;
    };
    if (v instanceof VelocityLogarithmic) {
        return 3.91;
    };
    if (v instanceof VelocityExponential) {
        return 1.58;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Velocity.Types (line 138, column 1 - line 138, column 55): " + [ v.constructor.name ]);
};
var velocityCurveFormula = function (v) {
    if (v instanceof VelocityLinear) {
        return "y = x";
    };
    if (v instanceof VelocitySoft) {
        return "y = x^0.5";
    };
    if (v instanceof VelocityFirm) {
        return "y = x^2";
    };
    if (v instanceof VelocitySCurve) {
        return "y = 3x^2 - 2x^3";
    };
    if (v instanceof VelocityLogarithmic) {
        return "y = log(1+9x)/log(10)";
    };
    if (v instanceof VelocityExponential) {
        return "y = (e^x - 1)/(e - 1)";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Velocity.Types (line 114, column 1 - line 114, column 48): " + [ v.constructor.name ]);
};
var velocityCurveDescription = function (v) {
    if (v instanceof VelocityLinear) {
        return "Direct 1:1 mapping, technical drawing";
    };
    if (v instanceof VelocitySoft) {
        return "Sensitive to slow movements, sketching";
    };
    if (v instanceof VelocityFirm) {
        return "Requires fast movement, bold strokes";
    };
    if (v instanceof VelocitySCurve) {
        return "Smooth middle range, natural feel";
    };
    if (v instanceof VelocityLogarithmic) {
        return "Quick ramp then plateau, responsive start";
    };
    if (v instanceof VelocityExponential) {
        return "Slow start rapid end, building momentum";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Velocity.Types (line 99, column 1 - line 99, column 52): " + [ v.constructor.name ]);
};
var showVelocityCurve = {
    show: function (v) {
        if (v instanceof VelocityLinear) {
            return "(VelocityCurve Linear)";
        };
        if (v instanceof VelocitySoft) {
            return "(VelocityCurve Soft)";
        };
        if (v instanceof VelocityFirm) {
            return "(VelocityCurve Firm)";
        };
        if (v instanceof VelocitySCurve) {
            return "(VelocityCurve SCurve)";
        };
        if (v instanceof VelocityLogarithmic) {
            return "(VelocityCurve Logarithmic)";
        };
        if (v instanceof VelocityExponential) {
            return "(VelocityCurve Exponential)";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Velocity.Types (line 71, column 1 - line 77, column 59): " + [ v.constructor.name ]);
    }
};
var show1 = /* #__PURE__ */ Data_Show.show(showVelocityCurve);
var velocityCurveDebugInfo = function (curve) {
    return show1(curve) + (" formula:" + (velocityCurveFormula(curve) + (" maxSensitivity:" + show(velocityCurveMaxSensitivity(curve)))));
};
var eqVelocityCurve = {
    eq: function (x) {
        return function (y) {
            if (x instanceof VelocityLinear && y instanceof VelocityLinear) {
                return true;
            };
            if (x instanceof VelocitySoft && y instanceof VelocitySoft) {
                return true;
            };
            if (x instanceof VelocityFirm && y instanceof VelocityFirm) {
                return true;
            };
            if (x instanceof VelocitySCurve && y instanceof VelocitySCurve) {
                return true;
            };
            if (x instanceof VelocityLogarithmic && y instanceof VelocityLogarithmic) {
                return true;
            };
            if (x instanceof VelocityExponential && y instanceof VelocityExponential) {
                return true;
            };
            return false;
        };
    }
};
var ordVelocityCurve = {
    compare: function (x) {
        return function (y) {
            if (x instanceof VelocityLinear && y instanceof VelocityLinear) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VelocityLinear) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VelocityLinear) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VelocitySoft && y instanceof VelocitySoft) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VelocitySoft) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VelocitySoft) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VelocityFirm && y instanceof VelocityFirm) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VelocityFirm) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VelocityFirm) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VelocitySCurve && y instanceof VelocitySCurve) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VelocitySCurve) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VelocitySCurve) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VelocityLogarithmic && y instanceof VelocityLogarithmic) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof VelocityLogarithmic) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof VelocityLogarithmic) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof VelocityExponential && y instanceof VelocityExponential) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Velocity.Types (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqVelocityCurve;
    }
};
var allVelocityCurves = /* #__PURE__ */ (function () {
    return [ VelocityLinear.value, VelocitySoft.value, VelocityFirm.value, VelocitySCurve.value, VelocityLogarithmic.value, VelocityExponential.value ];
})();
export {
    VelocityLinear,
    VelocitySCurve,
    VelocitySoft,
    VelocityFirm,
    VelocityExponential,
    VelocityLogarithmic,
    allVelocityCurves,
    velocityCurveDescription,
    velocityCurveFormula,
    velocityCurveMaxSensitivity,
    velocityCurveDebugInfo,
    eqVelocityCurve,
    ordVelocityCurve,
    showVelocityCurve
};
//# sourceMappingURL=index.js.map
