// Generated by purs version 0.15.15
import * as Data_Ordering from "../Data.Ordering/index.js";
var AbsolutePixels = /* #__PURE__ */ (function () {
    function AbsolutePixels() {

    };
    AbsolutePixels.value = new AbsolutePixels();
    return AbsolutePixels;
})();
var PercentOfDiameter = /* #__PURE__ */ (function () {
    function PercentOfDiameter() {

    };
    PercentOfDiameter.value = new PercentOfDiameter();
    return PercentOfDiameter;
})();
var Adaptive = /* #__PURE__ */ (function () {
    function Adaptive() {

    };
    Adaptive.value = new Adaptive();
    return Adaptive;
})();
var AutoDensity = /* #__PURE__ */ (function () {
    function AutoDensity() {

    };
    AutoDensity.value = new AutoDensity();
    return AutoDensity;
})();
var Linear = /* #__PURE__ */ (function () {
    function Linear() {

    };
    Linear.value = new Linear();
    return Linear;
})();
var Catmull = /* #__PURE__ */ (function () {
    function Catmull() {

    };
    Catmull.value = new Catmull();
    return Catmull;
})();
var Bezier = /* #__PURE__ */ (function () {
    function Bezier() {

    };
    Bezier.value = new Bezier();
    return Bezier;
})();
var BSpline = /* #__PURE__ */ (function () {
    function BSpline() {

    };
    BSpline.value = new BSpline();
    return BSpline;
})();
var Hermite = /* #__PURE__ */ (function () {
    function Hermite() {

    };
    Hermite.value = new Hermite();
    return Hermite;
})();
var spacingModeToId = function (v) {
    if (v instanceof AbsolutePixels) {
        return "pixels";
    };
    if (v instanceof PercentOfDiameter) {
        return "percent";
    };
    if (v instanceof Adaptive) {
        return "adaptive";
    };
    if (v instanceof AutoDensity) {
        return "auto";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 225, column 1 - line 225, column 41): " + [ v.constructor.name ]);
};
var spacingModeDescription = function (v) {
    if (v instanceof AbsolutePixels) {
        return "Fixed pixel distance between dabs (e.g., 5px)";
    };
    if (v instanceof PercentOfDiameter) {
        return "Distance as percentage of brush size (e.g., 25%)";
    };
    if (v instanceof Adaptive) {
        return "Spacing adjusts based on stroke velocity";
    };
    if (v instanceof AutoDensity) {
        return "Algorithm determines optimal dab density";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 175, column 1 - line 175, column 48): " + [ v.constructor.name ]);
};
var spacingModeDebugInfo = function (mode) {
    return "SpacingMode: " + (spacingModeToId(mode) + (" \u2014 " + spacingModeDescription(mode)));
};
var showSpacingMode = {
    show: function (v) {
        if (v instanceof AbsolutePixels) {
            return "(SpacingMode AbsolutePixels)";
        };
        if (v instanceof PercentOfDiameter) {
            return "(SpacingMode PercentOfDiameter)";
        };
        if (v instanceof Adaptive) {
            return "(SpacingMode Adaptive)";
        };
        if (v instanceof AutoDensity) {
            return "(SpacingMode AutoDensity)";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 159, column 1 - line 163, column 49): " + [ v.constructor.name ]);
    }
};
var showInterpolationMethod = {
    show: function (v) {
        if (v instanceof Linear) {
            return "(InterpolationMethod Linear)";
        };
        if (v instanceof Catmull) {
            return "(InterpolationMethod Catmull)";
        };
        if (v instanceof Bezier) {
            return "(InterpolationMethod Bezier)";
        };
        if (v instanceof BSpline) {
            return "(InterpolationMethod BSpline)";
        };
        if (v instanceof Hermite) {
            return "(InterpolationMethod Hermite)";
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 101, column 1 - line 106, column 49): " + [ v.constructor.name ]);
    }
};
var sameSpacingModeKind = function (a) {
    return function (b) {
        return spacingModeToId(a) === spacingModeToId(b);
    };
};
var isSplineMethod = function (v) {
    if (v instanceof Catmull) {
        return true;
    };
    if (v instanceof BSpline) {
        return true;
    };
    if (v instanceof Hermite) {
        return true;
    };
    return false;
};
var isSmoothMethod = function (v) {
    if (v instanceof Linear) {
        return false;
    };
    return true;
};
var isRelativeSpacing = function (v) {
    if (v instanceof PercentOfDiameter) {
        return true;
    };
    return false;
};
var isDynamicSpacing = function (v) {
    if (v instanceof Adaptive) {
        return true;
    };
    if (v instanceof AutoDensity) {
        return true;
    };
    return false;
};
var interpolationMethodToId = function (v) {
    if (v instanceof Linear) {
        return "linear";
    };
    if (v instanceof Catmull) {
        return "catmull";
    };
    if (v instanceof Bezier) {
        return "bezier";
    };
    if (v instanceof BSpline) {
        return "b-spline";
    };
    if (v instanceof Hermite) {
        return "hermite";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 207, column 1 - line 207, column 57): " + [ v.constructor.name ]);
};
var sameInterpolationMethodKind = function (a) {
    return function (b) {
        return interpolationMethodToId(a) === interpolationMethodToId(b);
    };
};
var interpolationMethodDescription = function (v) {
    if (v instanceof Linear) {
        return "Straight lines between points, fastest but angular";
    };
    if (v instanceof Catmull) {
        return "Catmull-Rom spline, smooth curves passing through points";
    };
    if (v instanceof Bezier) {
        return "Cubic Bezier curves with precise control";
    };
    if (v instanceof BSpline) {
        return "B-spline, smoothest curves but may not pass through points";
    };
    if (v instanceof Hermite) {
        return "Hermite spline using velocity for smooth tangents";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 119, column 1 - line 119, column 64): " + [ v.constructor.name ]);
};
var interpolationMethodDebugInfo = function (method) {
    return "InterpolationMethod: " + (interpolationMethodToId(method) + (" \u2014 " + interpolationMethodDescription(method)));
};
var eqSpacingMode = {
    eq: function (x) {
        return function (y) {
            if (x instanceof AbsolutePixels && y instanceof AbsolutePixels) {
                return true;
            };
            if (x instanceof PercentOfDiameter && y instanceof PercentOfDiameter) {
                return true;
            };
            if (x instanceof Adaptive && y instanceof Adaptive) {
                return true;
            };
            if (x instanceof AutoDensity && y instanceof AutoDensity) {
                return true;
            };
            return false;
        };
    }
};
var ordSpacingMode = {
    compare: function (x) {
        return function (y) {
            if (x instanceof AbsolutePixels && y instanceof AbsolutePixels) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof AbsolutePixels) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof AbsolutePixels) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof PercentOfDiameter && y instanceof PercentOfDiameter) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof PercentOfDiameter) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof PercentOfDiameter) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Adaptive && y instanceof Adaptive) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Adaptive) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Adaptive) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof AutoDensity && y instanceof AutoDensity) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqSpacingMode;
    }
};
var eqInterpolationMethod = {
    eq: function (x) {
        return function (y) {
            if (x instanceof Linear && y instanceof Linear) {
                return true;
            };
            if (x instanceof Catmull && y instanceof Catmull) {
                return true;
            };
            if (x instanceof Bezier && y instanceof Bezier) {
                return true;
            };
            if (x instanceof BSpline && y instanceof BSpline) {
                return true;
            };
            if (x instanceof Hermite && y instanceof Hermite) {
                return true;
            };
            return false;
        };
    }
};
var ordInterpolationMethod = {
    compare: function (x) {
        return function (y) {
            if (x instanceof Linear && y instanceof Linear) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Linear) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Linear) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Catmull && y instanceof Catmull) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Catmull) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Catmull) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Bezier && y instanceof Bezier) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof Bezier) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof Bezier) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof BSpline && y instanceof BSpline) {
                return Data_Ordering.EQ.value;
            };
            if (x instanceof BSpline) {
                return Data_Ordering.LT.value;
            };
            if (y instanceof BSpline) {
                return Data_Ordering.GT.value;
            };
            if (x instanceof Hermite && y instanceof Hermite) {
                return Data_Ordering.EQ.value;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Brush.Interpolation.Types (line 0, column 0 - line 0, column 0): " + [ x.constructor.name, y.constructor.name ]);
        };
    },
    Eq0: function () {
        return eqInterpolationMethod;
    }
};
var allSpacingModes = /* #__PURE__ */ (function () {
    return [ AbsolutePixels.value, PercentOfDiameter.value, Adaptive.value, AutoDensity.value ];
})();
var allInterpolationMethods = /* #__PURE__ */ (function () {
    return [ Linear.value, Catmull.value, Bezier.value, BSpline.value, Hermite.value ];
})();
export {
    Linear,
    Catmull,
    Bezier,
    BSpline,
    Hermite,
    allInterpolationMethods,
    interpolationMethodDescription,
    isSmoothMethod,
    isSplineMethod,
    AbsolutePixels,
    PercentOfDiameter,
    Adaptive,
    AutoDensity,
    allSpacingModes,
    spacingModeDescription,
    isRelativeSpacing,
    isDynamicSpacing,
    interpolationMethodDebugInfo,
    interpolationMethodToId,
    sameInterpolationMethodKind,
    spacingModeDebugInfo,
    spacingModeToId,
    sameSpacingModeKind,
    eqInterpolationMethod,
    ordInterpolationMethod,
    showInterpolationMethod,
    eqSpacingMode,
    ordSpacingMode,
    showSpacingMode
};
//# sourceMappingURL=index.js.map
