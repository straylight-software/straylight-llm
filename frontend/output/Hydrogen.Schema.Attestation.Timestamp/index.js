// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Bounded from "../Hydrogen.Schema.Bounded/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var mod = /* #__PURE__ */ Data_EuclideanRing.mod(Data_EuclideanRing.euclideanRingInt);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var Timestamp = function (x) {
    return x;
};
var unsafeTimestamp = Timestamp;
var toSeconds = function (v) {
    return v / 1000.0;
};
var toNumber = function (v) {
    return v;
};
var toMillis = toNumber;
var timestampBounds = /* #__PURE__ */ (function () {
    return Hydrogen_Schema_Bounded.numberBounds(-1.0e13)(2.53402300799999e14)(Hydrogen_Schema_Bounded.Clamps.value)("Timestamp")("UTC timestamp in ms (-300000 BCE to year 9999)");
})();
var timestamp = function (ms) {
    if (ms < 0.0) {
        return 0.0;
    };
    if (ms > 2.534023008e14) {
        return 2.534023008e14;
    };
    if (Data_Boolean.otherwise) {
        return Data_Number.floor(ms);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 95, column 1 - line 95, column 33): " + [ ms.constructor.name ]);
};
var padZero4 = function (n) {
    if (n < 10) {
        return "000" + show(n);
    };
    if (n < 100) {
        return "00" + show(n);
    };
    if (n < 1000) {
        return "0" + show(n);
    };
    if (Data_Boolean.otherwise) {
        return show(n);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 279, column 1 - line 279, column 26): " + [ n.constructor.name ]);
};
var padZero3 = function (n) {
    if (n < 10) {
        return "00" + show(n);
    };
    if (n < 100) {
        return "0" + show(n);
    };
    if (Data_Boolean.otherwise) {
        return show(n);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 272, column 1 - line 272, column 26): " + [ n.constructor.name ]);
};
var padZero2 = function (n) {
    if (n < 10) {
        return "0" + show(n);
    };
    if (Data_Boolean.otherwise) {
        return show(n);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 266, column 1 - line 266, column 26): " + [ n.constructor.name ]);
};
var isLeapYear = function (year) {
    return mod(year)(4) === 0 && (mod(year)(100) !== 0 || mod(year)(400) === 0);
};
var fromSeconds = function (s) {
    return timestamp(s * 1000.0);
};
var eqTimestamp = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordTimestamp = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqTimestamp;
    }
};
var epoch = 0.0;
var diff = function (v) {
    return function (v1) {
        return v - v1;
    };
};
var daysInYear = function (year) {
    var $46 = isLeapYear(year);
    if ($46) {
        return 366;
    };
    return 365;
};
var findYear = function ($copy_year) {
    return function ($copy_days) {
        var $tco_var_year = $copy_year;
        var $tco_done = false;
        var $tco_result;
        function $tco_loop(year, days) {
            if (days < daysInYear(year)) {
                $tco_done = true;
                return {
                    year: year,
                    remaining: days
                };
            };
            if (Data_Boolean.otherwise) {
                $tco_var_year = year + 1 | 0;
                $copy_days = days - daysInYear(year) | 0;
                return;
            };
            throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 223, column 1 - line 223, column 60): " + [ year.constructor.name, days.constructor.name ]);
        };
        while (!$tco_done) {
            $tco_result = $tco_loop($tco_var_year, $copy_days);
        };
        return $tco_result;
    };
};
var daysInMonth = function (year) {
    return function (month) {
        if (month === 1) {
            return 31;
        };
        if (month === 2) {
            var $51 = isLeapYear(year);
            if ($51) {
                return 29;
            };
            return 28;
        };
        if (month === 3) {
            return 31;
        };
        if (month === 4) {
            return 30;
        };
        if (month === 5) {
            return 31;
        };
        if (month === 6) {
            return 30;
        };
        if (month === 7) {
            return 31;
        };
        if (month === 8) {
            return 31;
        };
        if (month === 9) {
            return 30;
        };
        if (month === 10) {
            return 31;
        };
        if (month === 11) {
            return 30;
        };
        if (Data_Boolean.otherwise) {
            return 31;
        };
        throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 246, column 1 - line 246, column 33): " + [ year.constructor.name, month.constructor.name ]);
    };
};
var findMonth = function ($copy_year) {
    return function ($copy_month) {
        return function ($copy_days) {
            var $tco_var_year = $copy_year;
            var $tco_var_month = $copy_month;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(year, month, days) {
                if (month >= 13) {
                    $tco_done = true;
                    return {
                        month: 12,
                        remaining: days
                    };
                };
                if (days < daysInMonth(year)(month)) {
                    $tco_done = true;
                    return {
                        month: month,
                        remaining: days
                    };
                };
                if (Data_Boolean.otherwise) {
                    $tco_var_year = year;
                    $tco_var_month = month + 1 | 0;
                    $copy_days = days - daysInMonth(year)(month) | 0;
                    return;
                };
                throw new Error("Failed pattern match at Hydrogen.Schema.Attestation.Timestamp (line 229, column 1 - line 229, column 69): " + [ year.constructor.name, month.constructor.name, days.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_year, $tco_var_month, $copy_days);
            };
            return $tco_result;
        };
    };
};
var daysToYMD = function (totalDays) {
    var yearResult = findYear(1970)(totalDays);
    var monthResult = findMonth(yearResult.year)(1)(yearResult.remaining);
    return {
        year: yearResult.year,
        month: monthResult.month,
        day: monthResult.remaining + 1 | 0
    };
};
var toISO8601 = function (v) {
    var totalSecs = Data_Number.floor(v / 1000.0);
    var totalDays = Data_Number.floor(totalSecs / 86400.0);
    var remainingSecs = totalSecs - totalDays * 86400.0;
    var millis = Data_Int.floor(v - totalSecs * 1000.0);
    var hours = Data_Int.floor(remainingSecs / 3600.0);
    var remainingAfterHours = remainingSecs - Data_Int.toNumber(hours) * 3600.0;
    var minutes = Data_Int.floor(remainingAfterHours / 60.0);
    var seconds = Data_Int.floor(remainingAfterHours - Data_Int.toNumber(minutes) * 60.0);
    var dateComponents = daysToYMD(Data_Int.floor(totalDays));
    return padZero4(dateComponents.year) + ("-" + (padZero2(dateComponents.month) + ("-" + (padZero2(dateComponents.day) + ("T" + (padZero2(hours) + (":" + (padZero2(minutes) + (":" + (padZero2(seconds) + ("." + (padZero3(millis) + "Z"))))))))))));
};
var showTimestamp = {
    show: function (ts) {
        return toISO8601(ts);
    }
};
var addMillis = function (delta) {
    return function (v) {
        return timestamp(v + delta);
    };
};
var addSeconds = function (delta) {
    return function (ts) {
        return addMillis(delta * 1000.0)(ts);
    };
};
var addMinutes = function (delta) {
    return function (ts) {
        return addSeconds(delta * 60.0)(ts);
    };
};
var addHours = function (delta) {
    return function (ts) {
        return addMinutes(delta * 60.0)(ts);
    };
};
var addDays = function (delta) {
    return function (ts) {
        return addHours(delta * 24.0)(ts);
    };
};
export {
    timestamp,
    unsafeTimestamp,
    toNumber,
    toMillis,
    toSeconds,
    toISO8601,
    fromSeconds,
    epoch,
    addMillis,
    addSeconds,
    addMinutes,
    addHours,
    addDays,
    diff,
    timestampBounds,
    eqTimestamp,
    ordTimestamp,
    showTimestamp
};
//# sourceMappingURL=index.js.map
