// Generated by purs version 0.15.15
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Scheduling_Event from "../Hydrogen.Schema.Scheduling.Event/index.js";
import * as Hydrogen_Schema_Temporal_TimeOfDay from "../Hydrogen.Schema.Temporal.TimeOfDay/index.js";
import * as Hydrogen_Schema_Temporal_Timezone from "../Hydrogen.Schema.Temporal.Timezone/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var show1 = /* #__PURE__ */ Data_Show.show(Hydrogen_Schema_Scheduling_Event.showEventId);
var append1 = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var visibilityToICalString = function (v) {
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Public) {
        return "PUBLIC";
    };
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Private) {
        return "PRIVATE";
    };
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Confidential) {
        return "CONFIDENTIAL";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Scheduling.EventMod (line 182, column 1 - line 182, column 47): " + [ v.constructor.name ]);
};
var statusToICalString = function (v) {
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Confirmed) {
        return "CONFIRMED";
    };
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Tentative) {
        return "TENTATIVE";
    };
    if (v instanceof Hydrogen_Schema_Scheduling_Event.Cancelled) {
        return "CANCELLED";
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Scheduling.EventMod (line 176, column 1 - line 176, column 44): " + [ v.constructor.name ]);
};
var setRecurrence = function (v) {
    return function (rec) {
        return {
            id: v.id,
            title: v.title,
            description: v.description,
            location: v.location,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            attendeeIds: v.attendeeIds,
            reminders: v.reminders,
            status: v.status,
            visibility: v.visibility,
            conferenceLink: v.conferenceLink,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            recurrence: new Data_Maybe.Just(rec)
        };
    };
};
var setLocation = function (v) {
    return function (loc) {
        return {
            id: v.id,
            title: v.title,
            description: v.description,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            attendeeIds: v.attendeeIds,
            recurrence: v.recurrence,
            reminders: v.reminders,
            status: v.status,
            visibility: v.visibility,
            conferenceLink: v.conferenceLink,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            location: new Data_Maybe.Just(loc)
        };
    };
};
var setDescription = function (v) {
    return function (desc) {
        return {
            id: v.id,
            title: v.title,
            location: v.location,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            attendeeIds: v.attendeeIds,
            recurrence: v.recurrence,
            reminders: v.reminders,
            status: v.status,
            visibility: v.visibility,
            conferenceLink: v.conferenceLink,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            description: new Data_Maybe.Just(desc)
        };
    };
};
var setConferenceLink = function (v) {
    return function (link) {
        return {
            id: v.id,
            title: v.title,
            description: v.description,
            location: v.location,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            attendeeIds: v.attendeeIds,
            recurrence: v.recurrence,
            reminders: v.reminders,
            status: v.status,
            visibility: v.visibility,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            conferenceLink: new Data_Maybe.Just(link)
        };
    };
};
var padZero4 = function (n) {
    if (n < 10) {
        return "000" + show(n);
    };
    if (n < 100) {
        return "00" + show(n);
    };
    if (n < 1000) {
        return "0" + show(n);
    };
    if (Data_Boolean.otherwise) {
        return show(n);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Scheduling.EventMod (line 198, column 1 - line 198, column 26): " + [ n.constructor.name ]);
};
var padZero = function (n) {
    if (n < 10) {
        return "0" + show(n);
    };
    if (Data_Boolean.otherwise) {
        return show(n);
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Scheduling.EventMod (line 192, column 1 - line 192, column 25): " + [ n.constructor.name ]);
};
var getTimezoneOffset = function (e) {
    return Hydrogen_Schema_Temporal_Timezone.getStandardOffset(Hydrogen_Schema_Scheduling_Event.getTimezone(e));
};
var getStartOffsetMinutes = function (e) {
    var tzOffset = Hydrogen_Schema_Temporal_Timezone.totalOffsetMinutes(Hydrogen_Schema_Temporal_Timezone.getStandardOffset(Hydrogen_Schema_Scheduling_Event.getTimezone(e)));
    var start = Hydrogen_Schema_Scheduling_Event.getStartDateTime(e);
    var rec = Hydrogen_Schema_Temporal_TimeOfDay.toRecord(start.time);
    return ((rec.hour * 60 | 0) + rec.minute | 0) + tzOffset | 0;
};
var getEndOffsetMinutes = function (e) {
    var tzOffset = Hydrogen_Schema_Temporal_Timezone.totalOffsetMinutes(Hydrogen_Schema_Temporal_Timezone.getStandardOffset(Hydrogen_Schema_Scheduling_Event.getTimezone(e)));
    var end = Hydrogen_Schema_Scheduling_Event.getEndDateTime(e);
    var rec = Hydrogen_Schema_Temporal_TimeOfDay.toRecord(end.time);
    return ((rec.hour * 60 | 0) + rec.minute | 0) + tzOffset | 0;
};
var formatTimeICal = function (t) {
    var rec = Hydrogen_Schema_Temporal_TimeOfDay.toRecord(t);
    return padZero(rec.hour) + (padZero(rec.minute) + padZero(rec.second));
};
var formatDateTimeShort = function (dt) {
    return show(dt.month) + ("/" + (show(dt.day) + ("/" + (show(dt.year) + (" " + Hydrogen_Schema_Temporal_TimeOfDay.format12H(dt.time))))));
};
var toDisplayString = function (e) {
    return Hydrogen_Schema_Scheduling_Event.getTitle(e) + (" (" + (formatDateTimeShort(Hydrogen_Schema_Scheduling_Event.getStartDateTime(e)) + ")"));
};
var formatDateTimeICal = function (dt) {
    return padZero4(dt.year) + (padZero(dt.month) + (padZero(dt.day) + ("T" + formatTimeICal(dt.time))));
};
var toICalString = function (e) {
    return "BEGIN:VEVENT\x0d\x0a" + ("UID:" + (show1(Hydrogen_Schema_Scheduling_Event.getId(e)) + ("\x0d\x0a" + ("SUMMARY:" + (Hydrogen_Schema_Scheduling_Event.getTitle(e) + ("\x0d\x0a" + ("DTSTART:" + (formatDateTimeICal(Hydrogen_Schema_Scheduling_Event.getStartDateTime(e)) + ("\x0d\x0a" + ("DTEND:" + (formatDateTimeICal(Hydrogen_Schema_Scheduling_Event.getEndDateTime(e)) + ("\x0d\x0a" + ("STATUS:" + (statusToICalString(Hydrogen_Schema_Scheduling_Event.getStatus(e)) + ("\x0d\x0a" + ("CLASS:" + (visibilityToICalString(Hydrogen_Schema_Scheduling_Event.getVisibility(e)) + ("\x0d\x0a" + "END:VEVENT\x0d\x0a"))))))))))))))))));
};
var cancel = function (v) {
    return {
        id: v.id,
        title: v.title,
        description: v.description,
        location: v.location,
        startDateTime: v.startDateTime,
        endDateTime: v.endDateTime,
        timezone: v.timezone,
        allDay: v.allDay,
        organizerId: v.organizerId,
        attendeeIds: v.attendeeIds,
        recurrence: v.recurrence,
        reminders: v.reminders,
        visibility: v.visibility,
        conferenceLink: v.conferenceLink,
        createdAt: v.createdAt,
        updatedAt: v.updatedAt,
        status: Hydrogen_Schema_Scheduling_Event.Cancelled.value
    };
};
var addReminder = function (v) {
    return function (rem) {
        return {
            id: v.id,
            title: v.title,
            description: v.description,
            location: v.location,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            attendeeIds: v.attendeeIds,
            recurrence: v.recurrence,
            status: v.status,
            visibility: v.visibility,
            conferenceLink: v.conferenceLink,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            reminders: append1(v.reminders)([ rem ])
        };
    };
};
var addAttendee = function (v) {
    return function (attendeeId) {
        return {
            id: v.id,
            title: v.title,
            description: v.description,
            location: v.location,
            startDateTime: v.startDateTime,
            endDateTime: v.endDateTime,
            timezone: v.timezone,
            allDay: v.allDay,
            organizerId: v.organizerId,
            recurrence: v.recurrence,
            reminders: v.reminders,
            status: v.status,
            visibility: v.visibility,
            conferenceLink: v.conferenceLink,
            createdAt: v.createdAt,
            updatedAt: v.updatedAt,
            attendeeIds: append1(v.attendeeIds)([ attendeeId ])
        };
    };
};
export {
    setLocation,
    setDescription,
    setConferenceLink,
    addAttendee,
    addReminder,
    setRecurrence,
    cancel,
    getStartOffsetMinutes,
    getEndOffsetMinutes,
    getTimezoneOffset,
    toDisplayString,
    toICalString,
    formatDateTimeShort,
    formatDateTimeICal,
    statusToICalString,
    visibilityToICalString
};
//# sourceMappingURL=index.js.map
