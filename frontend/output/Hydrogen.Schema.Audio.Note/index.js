// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Schema_Audio_Frequency_Types from "../Hydrogen.Schema.Audio.Frequency.Types/index.js";
import * as Hydrogen_Schema_Audio_MIDI from "../Hydrogen.Schema.Audio.MIDI/index.js";
import * as Hydrogen_Schema_Audio_Tick from "../Hydrogen.Schema.Audio.Tick/index.js";
var compare = /* #__PURE__ */ Data_Ord.compare(Hydrogen_Schema_Audio_Tick.ordTickPosition);
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showInt);
var eq = /* #__PURE__ */ Data_Eq.eq(Hydrogen_Schema_Audio_Tick.eqTickPosition);
var compare1 = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordInt);
var VelocityRange = function (x) {
    return x;
};
var NoteProbability = function (x) {
    return x;
};
var velocityRange = function (n) {
    if (n < 0) {
        return 0;
    };
    if (n > 64) {
        return 64;
    };
    if (Data_Boolean.otherwise) {
        return n;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Audio.Note (line 167, column 1 - line 167, column 38): " + [ n.constructor.name ]);
};
var unwrapVelocityRange = function (v) {
    return v;
};
var unwrapNoteProbability = function (v) {
    return v;
};
var transposeNote = function (semitones) {
    return function (n) {
        return {
            velocity: n.velocity,
            start: n.start,
            duration: n.duration,
            channel: n.channel,
            pitch: Hydrogen_Schema_Audio_Frequency_Types.midiNote(Hydrogen_Schema_Audio_Frequency_Types.unwrapMidiNote(n.pitch) + semitones | 0)
        };
    };
};
var stretchNote = function (newDuration) {
    return function (n) {
        return {
            pitch: n.pitch,
            velocity: n.velocity,
            start: n.start,
            channel: n.channel,
            duration: newDuration
        };
    };
};
var sortByStart = /* #__PURE__ */ (function () {
    var compareByStart = function (a) {
        return function (b) {
            return compare(a.start)(b.start);
        };
    };
    return Data_Array.sortBy(compareByStart);
})();
var showVelocityRange = {
    show: function (v) {
        return "\xb1" + show(v);
    }
};
var showNoteProbability = {
    show: function (v) {
        return show(v) + "%";
    }
};
var setNoteVelocity = function (vel) {
    return function (n) {
        return {
            pitch: n.pitch,
            start: n.start,
            duration: n.duration,
            channel: n.channel,
            velocity: vel
        };
    };
};
var scaleNoteVelocity = function (factor) {
    return function (n) {
        var rawVel = Hydrogen_Schema_Audio_MIDI.unwrapVelocity(n.velocity);
        var scaled = Data_Int.round(Data_Int.toNumber(rawVel) * factor);
        return {
            pitch: n.pitch,
            start: n.start,
            duration: n.duration,
            channel: n.channel,
            velocity: Hydrogen_Schema_Audio_MIDI.velocity(scaled)
        };
    };
};
var noteWithChannel = function (pitchVal) {
    return function (vel) {
        return function (startTick) {
            return function (durationTicks) {
                return function (chan) {
                    return {
                        pitch: Hydrogen_Schema_Audio_Frequency_Types.midiNote(pitchVal),
                        velocity: Hydrogen_Schema_Audio_MIDI.velocity(vel),
                        start: Hydrogen_Schema_Audio_Tick.tickPosition(startTick),
                        duration: Hydrogen_Schema_Audio_Tick.tickDuration(durationTicks),
                        channel: Hydrogen_Schema_Audio_MIDI.channel(chan)
                    };
                };
            };
        };
    };
};
var noteVelocityRaw = function (n) {
    return Hydrogen_Schema_Audio_MIDI.unwrapVelocity(n.velocity);
};
var noteVelocity = function (n) {
    return n.velocity;
};
var noteStart = function (n) {
    return n.start;
};
var noteProbability = function (n) {
    if (n < 0) {
        return 0;
    };
    if (n > 100) {
        return 100;
    };
    if (Data_Boolean.otherwise) {
        return n;
    };
    throw new Error("Failed pattern match at Hydrogen.Schema.Audio.Note (line 147, column 1 - line 147, column 42): " + [ n.constructor.name ]);
};
var notePitch = function (n) {
    return n.pitch;
};
var noteEnd = function (n) {
    return Hydrogen_Schema_Audio_Tick.tickPosition(Hydrogen_Schema_Audio_Tick.unwrapTickPosition(n.start) + Hydrogen_Schema_Audio_Tick.unwrapTickDuration(n.duration) | 0);
};
var noteOverlaps = function (a) {
    return function (b) {
        var bStart = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(b.start);
        var bEnd = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(noteEnd(b));
        var aStart = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(a.start);
        var aEnd = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(noteEnd(a));
        return aStart < bEnd && bStart < aEnd;
    };
};
var notesInRange = function (rangeStart) {
    return function (rangeEnd) {
        var inRange = function (n) {
            var rStart = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(rangeStart);
            var rEnd = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(rangeEnd);
            var nStart = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(n.start);
            var nEnd = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(noteEnd(n));
            return nStart < rEnd && nEnd > rStart;
        };
        return Data_Array.filter(inRange);
    };
};
var noteDuration = function (n) {
    return n.duration;
};
var noteContains = function (pos) {
    return function (n) {
        var start = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(n.start);
        var p = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(pos);
        var end = Hydrogen_Schema_Audio_Tick.unwrapTickPosition(noteEnd(n));
        return p >= start && p < end;
    };
};
var noteChannel = function (n) {
    return n.channel;
};
var noteAtPosition = function (pos) {
    return function (n) {
        return eq(n.start)(pos);
    };
};
var note = function (pitchVal) {
    return function (vel) {
        return function (startTick) {
            return function (durationTicks) {
                return {
                    pitch: Hydrogen_Schema_Audio_Frequency_Types.midiNote(pitchVal),
                    velocity: Hydrogen_Schema_Audio_MIDI.velocity(vel),
                    start: Hydrogen_Schema_Audio_Tick.tickPosition(startTick),
                    duration: Hydrogen_Schema_Audio_Tick.tickDuration(durationTicks),
                    channel: Hydrogen_Schema_Audio_MIDI.channel(1)
                };
            };
        };
    };
};
var moveNote = function (newStart) {
    return function (n) {
        return {
            pitch: n.pitch,
            velocity: n.velocity,
            duration: n.duration,
            channel: n.channel,
            start: newStart
        };
    };
};
var eqVelocityRange = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordVelocityRange = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqVelocityRange;
    }
};
var eqNoteProbability = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordNoteProbability = {
    compare: function (x) {
        return function (y) {
            return compare1(x)(y);
        };
    },
    Eq0: function () {
        return eqNoteProbability;
    }
};
var adjustNoteVelocity = function (offset) {
    return function (n) {
        var rawVel = Hydrogen_Schema_Audio_MIDI.unwrapVelocity(n.velocity);
        return {
            pitch: n.pitch,
            start: n.start,
            duration: n.duration,
            channel: n.channel,
            velocity: Hydrogen_Schema_Audio_MIDI.velocity(rawVel + offset | 0)
        };
    };
};
export {
    note,
    noteWithChannel,
    notePitch,
    noteVelocity,
    noteStart,
    noteDuration,
    noteChannel,
    noteEnd,
    NoteProbability,
    noteProbability,
    unwrapNoteProbability,
    VelocityRange,
    velocityRange,
    unwrapVelocityRange,
    transposeNote,
    moveNote,
    stretchNote,
    setNoteVelocity,
    scaleNoteVelocity,
    adjustNoteVelocity,
    noteVelocityRaw,
    noteOverlaps,
    noteContains,
    noteAtPosition,
    sortByStart,
    notesInRange,
    eqNoteProbability,
    ordNoteProbability,
    showNoteProbability,
    eqVelocityRange,
    ordVelocityRange,
    showVelocityRange
};
//# sourceMappingURL=index.js.map
