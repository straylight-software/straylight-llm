// Generated by purs version 0.15.15
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Show from "../Data.Show/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var compare = /* #__PURE__ */ Data_Ord.compare(Data_Ord.ordNumber);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var WetnessValue = function (x) {
    return x;
};
var wetnessRaw = function (v) {
    return v;
};
var wetnessMapWidth = function (m) {
    return m.width;
};
var wetnessMapHeight = function (m) {
    return m.height;
};
var wetnessEqual = function (v) {
    return function (v1) {
        return v === v1;
    };
};
var showWetnessValue = {
    show: function (v) {
        return "Wetness(" + (show(v) + ")");
    }
};
var setWetnessAt = function (m) {
    return function (x) {
        return function (y) {
            return function (val) {
                var $53 = x >= 0 && (x < m.width && (y >= 0 && y < m.height));
                if ($53) {
                    var idx = (y * m.width | 0) + x | 0;
                    var newValues = Data_Maybe.fromMaybe(m.values)(Data_Array.updateAt(idx)(val)(m.values));
                    return {
                        width: m.width,
                        height: m.height,
                        values: newValues
                    };
                };
                return m;
            };
        };
    };
};
var mkWetnessValue = function (w) {
    return max(0.0)(min(1.0)(w));
};
var rewet = function (v) {
    return function (amount) {
        return mkWetnessValue(v + max(0.0)(amount));
    };
};
var mkDryingConfig = function (rate) {
    return function (humidity) {
        return function (temp) {
            return {
                rate: max(0.0)(rate),
                humidity: max(0.0)(min(1.0)(humidity)),
                temperature: max(0.0)(temp)
            };
        };
    };
};
var slowDrying = /* #__PURE__ */ mkDryingConfig(2.0e-2)(0.8)(0.7);
var isWetnessAt = function (v) {
    return function (level) {
        return v === level;
    };
};
var isWet = function (v) {
    return v > 0.5;
};
var totalWetArea = function (m) {
    return Data_Array.length(Data_Array.filter(isWet)(m.values));
};
var wetAreaFraction = function (m) {
    var wetCount = Data_Array.length(Data_Array.filter(isWet)(m.values));
    var total = Data_Array.length(m.values);
    var $59 = total > 0;
    if ($59) {
        return Data_Int.toNumber(wetCount) / Data_Int.toNumber(total);
    };
    return 0.0;
};
var isDry = function (v) {
    return v < 5.0e-2;
};
var isDamp = function (v) {
    return v >= 5.0e-2 && v <= 0.5;
};
var fullyWet = 1.0;
var fullyDry = 0.0;
var getWetnessAt = function (m) {
    return function (x) {
        return function (y) {
            var $62 = x >= 0 && (x < m.width && (y >= 0 && y < m.height));
            if ($62) {
                return Data_Maybe.fromMaybe(fullyDry)(Data_Array.index(m.values)((y * m.width | 0) + x | 0));
            };
            return fullyDry;
        };
    };
};
var mkWetnessMap = function (w) {
    return function (h) {
        var safeW = max1(1)(w);
        var safeH = max1(1)(h);
        var total = safeW * safeH | 0;
        return {
            width: safeW,
            height: safeH,
            values: Data_Array.replicate(total)(fullyDry)
        };
    };
};
var fastDrying = /* #__PURE__ */ mkDryingConfig(0.1)(0.2)(1.5);
var eqWetnessValue = {
    eq: function (x) {
        return function (y) {
            return x === y;
        };
    }
};
var ordWetnessValue = {
    compare: function (x) {
        return function (y) {
            return compare(x)(y);
        };
    },
    Eq0: function () {
        return eqWetnessValue;
    }
};
var dryingRate = function (config) {
    var humidityFactor = 1.0 - config.humidity * 0.9;
    return config.rate * humidityFactor * config.temperature;
};
var dryingTime = function (config) {
    return function (v) {
        return function (v1) {
            var effectiveRate = dryingRate(config);
            var delta = v - v1;
            var $70 = effectiveRate > 0.0 && delta > 0.0;
            if ($70) {
                return delta / effectiveRate;
            };
            return 0.0;
        };
    };
};
var estimateFullDryTime = function (config) {
    return function (wetness) {
        return dryingTime(config)(wetness)(fullyDry);
    };
};
var dryAreaFraction = function (m) {
    var total = Data_Array.length(m.values);
    var dryCount = Data_Array.length(Data_Array.filter(isDry)(m.values));
    var $71 = total > 0;
    if ($71) {
        return Data_Int.toNumber(dryCount) / Data_Int.toNumber(total);
    };
    return 1.0;
};
var displayWetnessValue = function (w) {
    if (isDry(w)) {
        return "dry";
    };
    if (isDamp(w)) {
        return "damp";
    };
    if (isWet(w)) {
        return "wet";
    };
    return show(wetnessRaw(w));
};
var displayDryingConfig = function (config) {
    return "drying rate: " + (show(dryingRate(config)) + ("/s, humidity: " + (show(config.humidity * 100.0) + "%")));
};
var defaultDrying = /* #__PURE__ */ mkDryingConfig(5.0e-2)(0.5)(1.0);
var blendWetness = function (v) {
    return function (v1) {
        return function (factor) {
            var t = max(0.0)(min(1.0)(factor));
            var inv = 1.0 - t;
            return mkWetnessValue(v * inv + v1 * t);
        };
    };
};
var averageWetness = function (m) {
    var total = Data_Array.foldl(function (acc) {
        return function (v) {
            return acc + v;
        };
    })(0.0)(m.values);
    var count = Data_Int.toNumber(Data_Array.length(m.values));
    var $77 = count > 0.0;
    if ($77) {
        return total / count;
    };
    return 0.0;
};
var applyDrying = function (m) {
    return function (config) {
        return function (deltaTime) {
            var effectiveRate = dryingRate(config);
            var dryAmount = effectiveRate * deltaTime;
            var dryCell = function (v) {
                return mkWetnessValue(v - dryAmount);
            };
            var newValues = map(dryCell)(m.values);
            return {
                width: m.width,
                height: m.height,
                values: newValues
            };
        };
    };
};
var ambientHumidity = function (config) {
    return config.humidity;
};
var addWetnessAt = function (m) {
    return function (x) {
        return function (y) {
            return function (delta) {
                var v = getWetnessAt(m)(x)(y);
                var newWetness = mkWetnessValue(v + delta);
                return setWetnessAt(m)(x)(y)(newWetness);
            };
        };
    };
};
var absorbWetness = function (v) {
    return function (absorptionRate) {
        var absorbed = v * max(0.0)(min(1.0)(absorptionRate));
        return mkWetnessValue(v - absorbed);
    };
};
export {
    mkWetnessValue,
    wetnessRaw,
    fullyDry,
    fullyWet,
    isDry,
    isWet,
    isDamp,
    mkWetnessMap,
    wetnessMapWidth,
    wetnessMapHeight,
    getWetnessAt,
    setWetnessAt,
    addWetnessAt,
    mkDryingConfig,
    dryingRate,
    ambientHumidity,
    defaultDrying,
    fastDrying,
    slowDrying,
    applyDrying,
    dryingTime,
    estimateFullDryTime,
    blendWetness,
    rewet,
    absorbWetness,
    averageWetness,
    totalWetArea,
    dryAreaFraction,
    wetAreaFraction,
    displayWetnessValue,
    displayDryingConfig,
    wetnessEqual,
    isWetnessAt,
    eqWetnessValue,
    ordWetnessValue,
    showWetnessValue
};
//# sourceMappingURL=index.js.map
