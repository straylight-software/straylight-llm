// Generated by purs version 0.15.15
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Semiring from "../Data.Semiring/index.js";
import * as Data_Show from "../Data.Show/index.js";
import * as Hydrogen_Math_Core_Constants from "../Hydrogen.Math.Core.Constants/index.js";
import * as Hydrogen_Math_Core_Trigonometry from "../Hydrogen.Math.Core.Trigonometry/index.js";
import * as Hydrogen_Schema_Dimension_Vector_Vec3 from "../Hydrogen.Schema.Dimension.Vector.Vec3/index.js";
var show = /* #__PURE__ */ Data_Show.show(Data_Show.showNumber);
var show1 = /* #__PURE__ */ Data_Show.show(/* #__PURE__ */ Hydrogen_Schema_Dimension_Vector_Vec3.showVec3(Data_Show.showNumber));
var addVec3 = /* #__PURE__ */ Hydrogen_Schema_Dimension_Vector_Vec3.addVec3(Data_Semiring.semiringNumber);
var eq1 = /* #__PURE__ */ Data_Eq.eq(/* #__PURE__ */ Hydrogen_Schema_Dimension_Vector_Vec3.eqVec3(Data_Eq.eqNumber));
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var scaleVec3 = /* #__PURE__ */ Hydrogen_Schema_Dimension_Vector_Vec3.scaleVec3(Data_Semiring.semiringNumber);
var RotateDelta = /* #__PURE__ */ (function () {
    function RotateDelta(value0, value1) {
        this.value0 = value0;
        this.value1 = value1;
    };
    RotateDelta.create = function (value0) {
        return function (value1) {
            return new RotateDelta(value0, value1);
        };
    };
    return RotateDelta;
})();
var PanDelta = /* #__PURE__ */ (function () {
    function PanDelta(value0, value1) {
        this.value0 = value0;
        this.value1 = value1;
    };
    PanDelta.create = function (value0) {
        return function (value1) {
            return new PanDelta(value0, value1);
        };
    };
    return PanDelta;
})();
var ZoomDelta = /* #__PURE__ */ (function () {
    function ZoomDelta(value0) {
        this.value0 = value0;
    };
    ZoomDelta.create = function (value0) {
        return new ZoomDelta(value0);
    };
    return ZoomDelta;
})();
var SetTarget = /* #__PURE__ */ (function () {
    function SetTarget(value0) {
        this.value0 = value0;
    };
    SetTarget.create = function (value0) {
        return new SetTarget(value0);
    };
    return SetTarget;
})();
var ResetView = /* #__PURE__ */ (function () {
    function ResetView() {

    };
    ResetView.value = new ResetView();
    return ResetView;
})();
var zeroSphericalDelta = {
    radius: 1.0,
    phi: 0.0,
    theta: 0.0
};
var sphericalToCartesian = function (s) {
    var sinTheta = Hydrogen_Math_Core_Trigonometry.sin(s.theta);
    var sinPhi = Hydrogen_Math_Core_Trigonometry.sin(s.phi);
    var cosTheta = Hydrogen_Math_Core_Trigonometry.cos(s.theta);
    var cosPhi = Hydrogen_Math_Core_Trigonometry.cos(s.phi);
    return Hydrogen_Schema_Dimension_Vector_Vec3.vec3(s.radius * sinPhi * sinTheta)(s.radius * cosPhi)(s.radius * sinPhi * cosTheta);
};
var showOrbitControlsMsg = {
    show: function (v) {
        if (v instanceof RotateDelta) {
            return "RotateDelta(" + (show(v.value0) + (", " + (show(v.value1) + ")")));
        };
        if (v instanceof PanDelta) {
            return "PanDelta(" + (show(v.value0) + (", " + (show(v.value1) + ")")));
        };
        if (v instanceof ZoomDelta) {
            return "ZoomDelta(" + (show(v.value0) + ")");
        };
        if (v instanceof SetTarget) {
            return "SetTarget(" + (show1(v.value0) + ")");
        };
        if (v instanceof ResetView) {
            return "ResetView";
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Scene3D.Controls (line 125, column 1 - line 130, column 31): " + [ v.constructor.name ]);
    }
};
var orbitControlsToPosition = function (state) {
    return addVec3(state.target)(sphericalToCartesian(state.spherical));
};
var orbitControlsToCamera = function (state) {
    return {
        position: orbitControlsToPosition(state),
        target: state.target,
        up: Hydrogen_Schema_Dimension_Vector_Vec3.vec3(0.0)(1.0)(0.0)
    };
};
var eqOrbitControlsMsg = {
    eq: function (x) {
        return function (y) {
            if (x instanceof RotateDelta && y instanceof RotateDelta) {
                return x.value0 === y.value0 && x.value1 === y.value1;
            };
            if (x instanceof PanDelta && y instanceof PanDelta) {
                return x.value0 === y.value0 && x.value1 === y.value1;
            };
            if (x instanceof ZoomDelta && y instanceof ZoomDelta) {
                return x.value0 === y.value0;
            };
            if (x instanceof SetTarget && y instanceof SetTarget) {
                return eq1(x.value0)(y.value0);
            };
            if (x instanceof ResetView && y instanceof ResetView) {
                return true;
            };
            return false;
        };
    }
};
var defaultSpherical = /* #__PURE__ */ (function () {
    return {
        radius: 10.0,
        phi: Hydrogen_Math_Core_Constants.pi / 4.0,
        theta: Hydrogen_Math_Core_Constants.pi / 4.0
    };
})();
var defaultOrbitControls = /* #__PURE__ */ (function () {
    return {
        target: Hydrogen_Schema_Dimension_Vector_Vec3.vec3(0.0)(0.0)(0.0),
        spherical: defaultSpherical,
        sphericalDelta: zeroSphericalDelta,
        panOffset: Hydrogen_Schema_Dimension_Vector_Vec3.vec3(0.0)(0.0)(0.0),
        zoomScale: 1.0,
        enableDamping: true,
        dampingFactor: 5.0e-2,
        rotateSpeed: 1.0,
        panSpeed: 1.0,
        zoomSpeed: 1.0,
        minDistance: 0.1,
        maxDistance: 1000.0,
        minPhi: 1.0e-2,
        maxPhi: Hydrogen_Math_Core_Constants.pi - 1.0e-2
    };
})();
var updateOrbitControls = function (msg) {
    return function (state) {
        if (msg instanceof RotateDelta) {
            var thetaDelta = msg.value0 * state.rotateSpeed * 1.0e-2;
            var phiDelta = msg.value1 * state.rotateSpeed * 1.0e-2;
            return {
                target: state.target,
                spherical: state.spherical,
                panOffset: state.panOffset,
                zoomScale: state.zoomScale,
                enableDamping: state.enableDamping,
                dampingFactor: state.dampingFactor,
                rotateSpeed: state.rotateSpeed,
                panSpeed: state.panSpeed,
                zoomSpeed: state.zoomSpeed,
                minDistance: state.minDistance,
                maxDistance: state.maxDistance,
                minPhi: state.minPhi,
                maxPhi: state.maxPhi,
                sphericalDelta: {
                    radius: state.sphericalDelta.radius,
                    theta: state.sphericalDelta.theta - thetaDelta,
                    phi: state.sphericalDelta.phi - phiDelta
                }
            };
        };
        if (msg instanceof PanDelta) {
            var scale = state.spherical.radius * state.panSpeed * 2.0e-3;
            var panY = msg.value1 * scale;
            var panX = msg.value0 * scale;
            var offset = Hydrogen_Schema_Dimension_Vector_Vec3.vec3(0.0 - panX)(panY)(0.0);
            return {
                target: state.target,
                spherical: state.spherical,
                sphericalDelta: state.sphericalDelta,
                zoomScale: state.zoomScale,
                enableDamping: state.enableDamping,
                dampingFactor: state.dampingFactor,
                rotateSpeed: state.rotateSpeed,
                panSpeed: state.panSpeed,
                zoomSpeed: state.zoomSpeed,
                minDistance: state.minDistance,
                maxDistance: state.maxDistance,
                minPhi: state.minPhi,
                maxPhi: state.maxPhi,
                panOffset: addVec3(state.panOffset)(offset)
            };
        };
        if (msg instanceof ZoomDelta) {
            return {
                target: state.target,
                spherical: state.spherical,
                sphericalDelta: state.sphericalDelta,
                panOffset: state.panOffset,
                enableDamping: state.enableDamping,
                dampingFactor: state.dampingFactor,
                rotateSpeed: state.rotateSpeed,
                panSpeed: state.panSpeed,
                zoomSpeed: state.zoomSpeed,
                minDistance: state.minDistance,
                maxDistance: state.maxDistance,
                minPhi: state.minPhi,
                maxPhi: state.maxPhi,
                zoomScale: state.zoomScale * msg.value0
            };
        };
        if (msg instanceof SetTarget) {
            return {
                spherical: state.spherical,
                sphericalDelta: state.sphericalDelta,
                panOffset: state.panOffset,
                zoomScale: state.zoomScale,
                enableDamping: state.enableDamping,
                dampingFactor: state.dampingFactor,
                rotateSpeed: state.rotateSpeed,
                panSpeed: state.panSpeed,
                zoomSpeed: state.zoomSpeed,
                minDistance: state.minDistance,
                maxDistance: state.maxDistance,
                minPhi: state.minPhi,
                maxPhi: state.maxPhi,
                target: msg.value0
            };
        };
        if (msg instanceof ResetView) {
            return {
                spherical: defaultOrbitControls.spherical,
                sphericalDelta: defaultOrbitControls.sphericalDelta,
                panOffset: defaultOrbitControls.panOffset,
                zoomScale: defaultOrbitControls.zoomScale,
                enableDamping: defaultOrbitControls.enableDamping,
                dampingFactor: defaultOrbitControls.dampingFactor,
                rotateSpeed: defaultOrbitControls.rotateSpeed,
                panSpeed: defaultOrbitControls.panSpeed,
                zoomSpeed: defaultOrbitControls.zoomSpeed,
                minDistance: defaultOrbitControls.minDistance,
                maxDistance: defaultOrbitControls.maxDistance,
                minPhi: defaultOrbitControls.minPhi,
                maxPhi: defaultOrbitControls.maxPhi,
                target: state.target
            };
        };
        throw new Error("Failed pattern match at Hydrogen.GPU.Scene3D.Controls (line 183, column 33 - line 217, column 51): " + [ msg.constructor.name ]);
    };
};
var clampRadius = function (state) {
    return function (radius) {
        return max(state.minDistance)(min(state.maxDistance)(radius));
    };
};
var clampPhi = function (state) {
    return function (phi) {
        return max(state.minPhi)(min(state.maxPhi)(phi));
    };
};
var applyDamping = function (state) {
    var newRadius = clampRadius(state)(state.spherical.radius * state.zoomScale);
    var factor = (function () {
        if (state.enableDamping) {
            return state.dampingFactor;
        };
        return 1.0;
    })();
    var newPhi = clampPhi(state)(state.spherical.phi + state.sphericalDelta.phi * factor);
    var newTheta = state.spherical.theta + state.sphericalDelta.theta * factor;
    var remainingFactor = 1.0 - factor;
    return {
        enableDamping: state.enableDamping,
        dampingFactor: state.dampingFactor,
        rotateSpeed: state.rotateSpeed,
        panSpeed: state.panSpeed,
        zoomSpeed: state.zoomSpeed,
        minDistance: state.minDistance,
        maxDistance: state.maxDistance,
        minPhi: state.minPhi,
        maxPhi: state.maxPhi,
        spherical: {
            radius: newRadius,
            phi: newPhi,
            theta: newTheta
        },
        sphericalDelta: {
            radius: 1.0,
            phi: state.sphericalDelta.phi * remainingFactor,
            theta: state.sphericalDelta.theta * remainingFactor
        },
        zoomScale: 1.0 + (state.zoomScale - 1.0) * remainingFactor,
        target: addVec3(state.target)(scaleVec3(factor)(state.panOffset)),
        panOffset: scaleVec3(remainingFactor)(state.panOffset)
    };
};
export {
    defaultOrbitControls,
    RotateDelta,
    PanDelta,
    ZoomDelta,
    SetTarget,
    ResetView,
    updateOrbitControls,
    applyDamping,
    orbitControlsToCamera,
    orbitControlsToPosition,
    sphericalToCartesian,
    eqOrbitControlsMsg,
    showOrbitControlsMsg
};
//# sourceMappingURL=index.js.map
