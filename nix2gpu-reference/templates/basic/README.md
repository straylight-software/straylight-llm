# Basic `nix2gpu` template

This template includes the most basic possible template for creating a `nix2gpu` container.

It keeps the configuration minimal. You can still add services, `home-manager`, and other features directly through `nix2gpu` options without wiring extra inputs.
